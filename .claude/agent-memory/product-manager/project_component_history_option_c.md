---
name: component-history-option-c
description: F/CTO ratified Option C (2026-08-12) — NAV component-history capture-substrate V1.x, visualization+backfill V2; CoA question kept orthogonal
metadata:
  type: project
---

F/CTO ratified **Option C** of the incumbent-exceeds-V1 NAV component-history P-flag on 2026-08-12: capture-only component-checkpoint substrate in V1.x (append-only audit-class sibling beside `nav_daily` 054, NEVER columns on 054), subcomponent visualization + sheet-history backfill V2. Sheet identity: RE + Cash + Bonds + Equity + Alt − Liabilities = NAV. Never-list: no reopen of §2.1.2 scalar-only lock; Expenses history stays with the locked §2.3.4 story (its V1.3 backend issue is OPEN — never write its SELF ID into mergeable text, auto-close hazard).

**Why:** the irreplaceable asset is the daily observations — the sheet stops at drop-replace cutover; un-captured days are unrecoverable. Visualization can ship any time.

**How to apply:** Entry text FINALIZED in temp/pm-backlog-entries-component-history.md (Entries 1→§7.1, 2→§5.1, 3→§7 CoA candidate incl. Architect's schema-direction framing + the `012`-built/`048`-dropped FK precedent); routed to team-lead for the meta branch. Status correction (2026-08-12, post-ADR-review): the two PM scope-precision calls (no V1.x read helper; identity = documented property not invariant) are **formally OPEN at ADR-054 Decision 5**, recorded as PM recommendations only — the ratified P-flag text was silent on both, so they had passed no F/CTO gate; they close at ADR-054 ratify. Do NOT cite them as decided until DECISIONS.md shows ADR-054 ratified with answers. Granularity: per-account leaf is ADR-054 Decision 3 (Architect decision, not a lean), which structurally dissolves the CoA coupling (Decision 6 orthogonality). Terminal draft shape (PASS-verified 2026-08-12): D5 = never-list (ratified) + the two OPEN questions; D6 = orthogonality; ADR Status = Proposed, flips to Accepted at the doc-PR F/CTO review — that review is where the two questions close. Numbering deliberately skips over an unmerged ADR-053 on a held branch — the transient gap on main is correct, don't "fix" it; the ADR file's routing preamble must not merge, and the auto-close grep must be scoped to the ADR body. Until entries land in BACKLOG.md and Architect's ADR lands in DECISIONS.md, this ratify is recorded nowhere durable — verify against BACKLOG.md/DECISIONS.md live before acting; delete or trim this memory once landed. The CoA/hierarchical-accounts question is a SEPARATE Architect-owned flag: answered no later than the earlier of §7.3 GL-substrate sequencing or before any component-checkpoint schema commits to a category key (per-account-leaf capture dissolves that coupling). Ratifying Option C made no CoA commitment — keep them orthogonal.
