---
name: v15-preflight-recalibration
description: V1.5 (§2.6 Monthly report) pre-flight — sitting RAN 2026-09-04 (R1–R14 in docs/records/v15-preflight/sitting-log.md); PM's PRD amendment batch (R10 + SELF-364 fold) committed on meta/v15-prd-amend @5d7f7c5 awaiting F/CTO read + merge; what PM still owes.
metadata:
  type: project
---

**State (2026-09-04):** the V1.5 pre-flight sitting has run; every ruling is in `docs/records/v15-preflight/sitting-log.md` R1–R14 (read live). PM's PRD amendment PR landed on branch `meta/v15-prd-amend` @ `5d7f7c5` (one commit on `1417337`; team-lead opens the PR, F/CTO reads the diff before merge). It carries the R10 batch (A-1 … A-14 as drafted, Sec R-4 supersede sentences verbatim, App B (a)/(f) RESOLVED-AT-Lock markers, App C rows) AND SELF-364's §2.5.3 fold (ADR-067 D5 (f)/(g)/(i) + E19b rollover copy + a BACKLOG §5.5 year-end-settle booking). SELF-365's re-worded AC (R12 six-clause definition, skip = does not count) is at `temp/v15-amend/acs/SELF-365.md` for the liaison — gitignored, gone at cleanup; if the liaison never pasted it, re-derive from R12.

**Why the rulings matter to PM going forward:** R1 = the report is a FROZEN RENDERED PAYLOAD (φ-1 stands verbatim; my A-5 exception wording withdrawn) — the "Regenerate" affordance and the pre-finalize no-tax-ledger prompt are load-bearing product copy, not optional. R4 = supersede, never overwrite; "pending = draft; generated = the current final; a superseded version is never rendered in V1" is the ratified bridge. R9 = four uncarried surfaces folded (listing+notification → P5; copy-from-prior + $ReAlloc → P3; staleness banner → P8) and A10 opened for the on-demand write path. R11 = column `commentary_marketable_securities`; heading **Marketable Securities**. R12 = a skipped-commentary month does NOT count toward N=2; the authored-vs-skipped fact is a V1.5 AC on A1/P4. R13 = dispatch order; PRD PR precedes P9.

**House style learned (brief said "grep ADR-067" — PRD has zero hits):** PRD amendments are anchored with HTML provenance comments (`<!-- V1.5 sitting R10 / pm-findings A-n -->`) followed by an inline sentence; never restructure. SECURITY's derivative-surface posture is **§4.3 (axis vi) + the §4.4 SD-12 row**, not "§4.4 SD-10" as pm-findings A-12 said.

**How to apply / still owed:** (1) post-merge cross-check of the amended text against the consolidated ADR when Architect lands it in A1's PR (R14) — the Lock 12 widening, RT-21 rewrite and ARCH :208 narrowing may want PRD pointer touch-ups; (2) §7.32 item 6 reduces to header + citation once P6 lands; (3) at V1.5 close, the BACKLOG §5.6 revision-history item is now "a read surface over rows that already exist" — say so if it is promoted; (4) verify the ratify against the sitting log, never from this memory. Precedent file for shape: [[v14-preflight-recalibration]].
