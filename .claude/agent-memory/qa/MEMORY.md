# Memory index

- [Verify drafted tests against BOTH gates](feedback_verify_drafted_tests_against_both_gates.md) — `vitest run` alone missed a real 3-error PageData typecheck failure in commit-ready text I authored; `npm run check` is a separate, required gate.
- [Late annotation → next-touch rider, not its own commit](feedback_late_annotation_next_touch_rider.md) — a REF/CONSUMER label added after the fact necessarily postdates the sha it names; say so, or don't force the commit — ride the next real edit instead.

- [Test-only grants don't follow a FK retarget](feedback_test_only_grants_dont_follow_a_fk_retarget.md) — a service_role teeth-isolation grant block, distant from the fixture, still named the old table; pg_prove caught it, reading didn't. Grep the WHOLE file for the old table name, not just the INSERTs.
- [Run before deliver once the migration is committed](feedback_run_before_deliver_when_migration_is_committed.md) — the scratch-DB recipe is ~15 fast commands; it caught 3 real bugs (type cast, `overriding system value` order, a verb's role-restore gap) that reading missed.
- [Reconcile against a diff, not a worktree](feedback_reconcile_against_diff_not_worktree.md) — a "delivered, not committed" teammate fix can leave their own worktree file unchanged; read their .diff or you'll duplicate the fix.
- [Id-preservation leg needs synthetic construction on a fresh stack](feedback_id_preservation_leg_needs_synthetic_construction_on_fresh_stack.md) — no real rows moved yet on fresh CI; use `overriding system value` to prove the mechanism, not to observe a real move.
- [FK retarget flips a KNOWN-GAP leg's expectation](feedback_fk_retarget_flips_known_gap_expectation.md) — self200 (v1) lives_ok→throws_ok via id-not-in-target-table, not the mechanism its own comment predicted; trace consumer legs, not just INSERT-pattern breakage.
- [RT-26 fence matches tokens not intent](feedback_rt26_fence_matches_tokens_not_intent.md) — never spell out a fenced key's literal token in prose, even in a comment asserting its absence; describe it instead.
- **[Real PostgREST two-tenant Vitest pattern — RUN FOR REAL 2026-08-21, 5/5 green](reference_postgrest_two_tenant_vitest_pattern.md)** — SELF-238 AC9 merge gate; will recur for SELF-240. ⚠ scope "contains none of" to values not labels; fund investment-account cash to net zero; check EVERY fixture number (incl. %s) across tenants for collisions.
- [SELF-244 close-gate must cover the SELF-233 planning-target write path](project_self244_close_gate_write_path_inventory.md) — flagged 2026-08-17, no action until SELF-244 starts.
- [Plain-text response is invisible to teammates in team mode](feedback_plain_text_response_invisible_to_teammates_in_team_mode.md) — even when a teammate's phrasing reads like "answer inline," the answer is a SendMessage call, never a bare text turn.
- [Scratch-DB pgTAP harness gotchas](feedback_scratch_db_pgtap_harness_gotchas.md) — `docker exec -i`, pgtap must live in `public` schema, harness is permissive vs real prod ACLs, **pg_prove's `docker run -v` needs a real HOST path**, `\'` is not a valid quote-escape in a plain SQL string (recurred 2026-08-25), and psql never interpolates `:'var'` inside a `do $$ $$` body (2026-08-26).
- [A new fence can encode the old bug into an EXISTING file's fixture](feedback_new_fence_can_encode_old_bug_into_existing_fixtures.md) — a catalog-assertion sweep misses this; only a full pg_prove regression run surfaces it (SELF-248/092: 037's own fixture built the defect state as intended behavior).
- [Same-value write can't fail a lives_ok control](feedback_same_value_write_cant_fail_a_lives_ok_control.md) — retargeting a leg to whatever's already in the fixture can silently make a "guard is lifted" positive control toothless against a transition-scoped regression; verify the write value differs from what the row already holds (SELF-248/037 Sec round-2).
- [Verify a causal mechanism before stating it](feedback_verify_causal_mechanism_before_stating.md) — a true result + an unverified "why" is still a defect; architect caught mine.
- [Scope the grep to what the assertion checks](feedback_scope_the_grep_to_what_the_assertion_checks.md) — a "no token X anywhere" claim must be scoped to prosrc, never the whole file; the header's own prohibition prose contains the token.
- [Send full text, not a path pointer, to a non-authoring role](feedback_full_text_not_path_pointer_to_non_authoring_role.md) — paste for a first/small hand-off; switch to own-worktree + path + self-verified `git diff --no-index` once a file is large and under repeated revision.
- [Never write into a teammate's worktree](feedback_never_write_into_a_teammates_worktree.md) — not even a temporary, self-removed copy; it collided with Backend's live run mid-flight. Verify from their path or ask them to run it, never write into it.
- [CSV test fixtures need csv.writer, not f-strings](feedback_csv_test_fixtures_use_csv_writer.md) — an unquoted internal comma read as a 10x-class scaling defect in the code, not the fixture bug it was.
- **[⚠ Postgres roles are cluster-level, not per-DB](feedback_postgres_roles_are_cluster_level_not_per_db.md) — a real incident.** `ALTER ROLE pfin_etl` inside a "scratch DB" session overwrote the REAL shared credential's password cluster-wide, unrecoverably. Never arm a real role for a test; create+drop a throwaway one.
- [Run ruff before every Python handoff](feedback_run_ruff_before_python_handoff.md) — 28/28 green pytest said nothing about an unused import; the commit hook caught it because I never commit, so `ruff check` has to run on my side first.
- [Re-diff the source before a re-verification run](feedback_rediff_source_before_reverify.md) — diff your copy against the authoring teammate's live worktree BEFORE re-running, not after; architect confirmed this closed the "tested a copy that happens to match" gap unprompted.
- **[Pattern-match to a past incident ≠ verified root cause](feedback_pattern_match_to_past_incident_verify_current_state.md)** — pfin_etl NOLOGIN-but-password-set looked like my earlier incident's residue; it was F/CTO's deliberate reissue. Availability isn't likelihood — check current state, don't report a resemblance as a root cause. The sign-off-before-ALTER gate is what caught it.
- [A committed ad hoc seed contaminates every later run in the same scratch DB](feedback_scratch_db_perf_seed_must_be_rolled_back.md) — an uncommitted-rollback perf-measurement seed leaked into an unrelated migration's inversion-control fixture and looked exactly like a regression. Match "have" values against your own seed data before reporting one.
- [Vitest `toHaveLength` on a raw DOM-element array can crash, not fail](feedback_vitest_tohavelength_dom_array_crash.md) — Svelte 5 + jsdom pretty-printer chokes serializing an SVG element inside a plain array on the failing case only; assert `.length` (a number) instead of the array.
- [Fixture scaffolding that reuses the function body's own formula hides a shared bug](feedback_fixture_scaffolding_reusing_body_formula_hides_shared_bug.md) — legitimate for placing checkpoints, not sufficient for proving correctness; add independent-arithmetic legs and corrupt-the-control before calling it done. Architect caught this on 071's battery.
- [`git checkout stash@{0} -- <path>` clobbers intervening teammate commits on that path](feedback_stash_checkout_path_clobbers_intervening_changes.md) — it replaces the file wholesale with the stash's OWN base; re-diff the restored file against the teammate's live tree, not just your own prior version, before handing it off.
- [A handed-off diff can be stale even when its content is right](feedback_handed_off_diff_verify_base_not_just_content.md) — `git apply --check` PER FILE (a multi-file diff is atomic) + trace the base blob to a commit; reading content alone missed that backend's diff predated e7b8da9 + my own committed battery on 6/8 files.
- [SvelteKit reserves the `+` prefix in `src/routes/`](reference_sveltekit_plus_prefix_reserved_in_routes.md) — `+page.staleness.test.ts` broke `svelte-kit sync` silently (vitest alone doesn't catch it); name route-level tests topic-first, matching `nav-series.server.test.ts`.
- **[⚠ Worktree branch drift — RECURRED FOUR TIMES, new triggers each time](feedback_worktree_branch_drift_after_compaction.md)** — first: post-compaction, redid a full turn of landed work. Second (SELF-243): stale after a prior item merged, caught before wasting work. Third (SELF-248): branch already claimed by a sibling worktree → drafted+edited in the MAIN REPO's own `main` checkout instead, undetected until pg_prove showed the fix hadn't landed. Fourth (SELF-250): branch was CORRECT, but a Write-tool call for a new file defaulted to the main-repo path anyway — Write/Edit's `file_path` does not inherit Bash's `cd` state; caught fast this time via the file's silent absence from a pg_prove run. Check `git log -1` vs the shared branch's tip AND spell out the worktree-rooted path explicitly on every Write/Edit call creating a new file.
- [Held delivery must resend full content](feedback_held_delivery_resend_full_content.md) — "holding delivery" ≠ "already delivered a draft"; resuming after a hold, re-send full content, never a diff against an assumed prior state.
- [`svelte/server` render() for required-prop compile proofs](reference_svelte_server_render_for_required_prop_proofs.md) — `@testing-library/svelte`'s render() tolerates omitted required props (types gap); plus the `@ts-expect-error` literal-marker-must-appear-exactly-once gotcha.
- [RLS battery files keyed to original migration, not amending one](reference_rls_battery_file_keyed_to_original_migration.md) — `071_..._rls.sql` extended in place for `072`'s amendment; don't conclude a battery gap from "no file at this migration's number." `plan()` counts drift — read live, never cite from memory.
- [Snapshot NO-FK column still needs its source table retargeted](feedback_snapshot_no_fk_column_still_needs_source_table_retarget.md) — 031's history.sub_cat_id has no FK, but the LIVE row it snapshots from is fenced one hop upstream (023); "no FK on this column" ≠ "no FK in this data's lineage."
- **[⚠ pg_prove full-suite runs must scope to /tests, not /tests/rls — RECURRED ONCE](feedback_pg_prove_scope_full_tests_tree_not_rls_only.md)** — excludes 00_rls_inversion_self_test.sql (the harness's own teeth check) + 2 others; type `/tests` from this file, don't recall it. Also: `CREATE DATABASE...TEMPLATE` doesn't copy `pg_db_role_setting` (false-alarms 01_session_timezone T2) — [[feedback_scratch_db_pgtap_harness_gotchas]].
- **[⚠ count(distinct col) silently drops NULL — recurred TWICE in one file](feedback_count_distinct_silently_drops_null.md)** — SELF-330/086's own N1/N2 NULL-blindness self-test didn't immunize a sibling assertion two legs over; grep `count(distinct` as a class when a nullable key is in scope, not just the one site under review.
- **[Documenting a caution ≠ applying it](feedback_documenting_a_caution_is_not_applying_it.md)** — wrote DESIGN.md §16 (reused scratch DB masks id-order bugs), then ran the next suite-level claim against the exact reused DB it warns about, 3 hours later. Check the just-written caution against your own very next matching action.
- **[⚠ UTC-vs-local date drift — recurred THREE times before diagnosed](feedback_utc_vs_local_date_drift.md)** — dated three separate SELF-330 artifacts 2026-08-21 (repo convention is local, actually 2026-08-20); cross-check a real local timestamp before dating anything, and diagnose the mechanism on the SECOND recurrence, not the fourth.
- **[⚠ NOT NULL/no-DEFAULT column forces a whole-tree fixture sweep — RECURRED, 085→091](feedback_not_null_no_default_column_forces_whole_tree_sweep.md)** — 16 files/~50 statements at 091 (`is_tax_payment`); grep the WHOLE `tests/` tree for INSERTs into the changed table, not just its own file, and separately grep for hardcoded seed-row COUNTS the migration's new rows may have shifted.
- [`expect_cross_tenant_write_blocked` doesn't self-restore role — Sec caught it in my own file](feedback_write_blocked_helper_leaves_intruder_role.md) — a precondition placed right after it silently reads as the intruder tenant; the gotcha was already written in 084's own comment, I didn't check before reusing the helper.
- **[⚠ Two directories both look like the battery home — grep the wrong one, get a false "zero coverage" finding](feedback_repo_root_tests_vs_supabase_tests_two_directories.md)** — repo-root `/tests/rls/` holds only DESIGN.md; the real pgTAP tree is `supabase/tests/rls/`. Refuted a live SELF-249 finding. Cross-check `supabase/tests/` before reporting any coverage gap.
- [Magic-link + cookie-transplant login for live walks](reference_magic_link_cookie_login_for_live_walks.md) — team-lead-sanctioned (SELF-258): no password ever created/entered; incl. the vite-binds-IPv6-only gotcha (`localhost` works, `127.0.0.1` errors).
- [Rollup vs per-account jsonb: `cat` lives on the SECTION vs the ROW](feedback_rollup_vs_peraccount_jsonb_cat_key_location_differs.md) — copying a parity-leg shape between "sibling" jsonb functions silently NULLs one side; check each function's real `jsonb_build_object`, don't pattern-match the other leg.
- [pg_prove aggregate-run TAP artifact, mechanism unconfirmed](feedback_pg_prove_aggregate_run_tap_artifact_unconfirmed.md) — a file 100% green in isolation can show "planned N but ran M" only inside a full-tree run; discriminator is exit-status+Failed-list+isolated-rerun, not "it says ok". Don't overclaim the cause.
- **[⚠ `language sql` bodies have NO pg_depend edges — Sec caught this in my own 099 leg](feedback_language_sql_body_has_no_pg_depend_edges.md)** — a "not exists in pg_depend" leg is vacuously true for EVERY body (measured: fn_cashflow_items shows zero rows despite 4 relations). Use a `prosrc` regex positive-pin instead; inversion-verify by doctoring a scratch copy to actually call a second helper.
