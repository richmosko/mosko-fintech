# Memory index

- **[⚠ Any DML that reads a column needs SELECT on it — RETURNING, WHERE, subquery](feedback_returning_clause_needs_select_on_every_returned_column.md)** — the write verb's own grant doesn't cover it; use ROW_COUNT instead of RETURNING. SELF-353/107, SELF-345/111.

- [Verify drafted tests against BOTH gates](feedback_verify_drafted_tests_against_both_gates.md) — `vitest run` alone missed a real typecheck failure; `npm run check` is a separate required gate.
- [Late annotation → next-touch rider, not its own commit](feedback_late_annotation_next_touch_rider.md) — a REF/CONSUMER label added after the fact postdates the sha it names; ride the next real edit instead.

- [Test-only grants don't follow a FK retarget](feedback_test_only_grants_dont_follow_a_fk_retarget.md) — grep the WHOLE file for the old table name, not just the INSERTs; pg_prove caught it, reading didn't.
- [Run before deliver once the migration is committed](feedback_run_before_deliver_when_migration_is_committed.md) — the ~15-command scratch-DB recipe caught 3 real bugs that reading missed.
- [Reconcile against a diff, not a worktree](feedback_reconcile_against_diff_not_worktree.md) — a "delivered, not committed" teammate fix can leave their worktree file unchanged; read their .diff.
- [Id-preservation leg needs synthetic construction on a fresh stack](feedback_id_preservation_leg_needs_synthetic_construction_on_fresh_stack.md) — use `overriding system value` to prove the mechanism when no real rows have moved yet.
- [FK retarget flips a KNOWN-GAP leg's expectation](feedback_fk_retarget_flips_known_gap_expectation.md) — trace consumer legs, not just INSERT-pattern breakage.
- [RT-26 fence matches tokens not intent](feedback_rt26_fence_matches_tokens_not_intent.md) — never spell out a fenced key's literal token in prose, even asserting its absence.
- **[Real PostgREST two-tenant Vitest pattern](reference_postgrest_two_tenant_vitest_pattern.md)** — SELF-238 AC9 merge gate. ⚠ scope "contains none of" to values not labels; check every fixture number for cross-tenant collisions.
- [SELF-244 close-gate must cover the SELF-233 planning-target write path](project_self244_close_gate_write_path_inventory.md) — flagged 2026-08-17, no action until SELF-244 starts.
- **[SvelteKit `update()` resets the form unless `{reset:false}`](feedback_sveltekit_update_reset_defaults_on_success.md)** — success/failure branch asymmetry hides real UX defects; only a real submit in a real browser catches it.
- **[Seed re-provision guard vs user delete — a seam-bug class](feedback_seed_reprovision_vs_user_delete_interaction.md)** — an every-request `provision*` call silently un-deletes what the user just deleted; check this whenever DELETE ships against a table an `ensure*` load also populates.
- [vi.mock is file-scoped](reference_vi_mock_file_scoped_dom_test_technique.md) — extend a shared test stub locally, don't widen it.
- [`supabase migration up --local --include-all` for a live walk, never `db reset`](reference_migration_up_include_all_for_live_walk.md) — a scratch clone isn't reachable by the live app; apply additively to the shared stack DB instead.
- **[⚠ Shared cookie across tabs → a false "critical cross-tenant write" finding](feedback_shared_cookie_across_tabs_false_positive.md)** — cookies are domain-scoped, not per-tab; ALWAYS re-verify signed-in identity in the SAME script, immediately before any cross-tenant probe.
- [`cookieStore.set(...)` replaces the blocked `document.cookie` write](reference_cookiestore_set_for_magic_link_login.md) — for magic-link login; plus faster token-extraction via `curl -sD -`.
- [Control build at an earlier migration head via template rebuild](reference_control_build_at_earlier_head_via_template_rebuild.md) — move new migration+test out, rebuild template, clone, test, restore, rebuild again.
- [Call-shaped regex vs name-substring count](feedback_call_shaped_regex_vs_name_substring_count.md) — a bare substring count over-counts a prose mention of the callee in its own comment.
- [Default tax schedules auto-provision on first page load, not signup](project_default_tax_schedules_autoprovision_on_first_page_load.md) — the true bootstrap state is unreachable by any real logged-in user.
- [pg_stat_user_functions counting probe for "evaluated N times" claims](reference_pg_stat_user_functions_counting_probe.md) — session `track_functions='all'` + calls delta across a rolled-back txn + `pg_stat_force_next_flush()`.
- [`nav` is a top-level key, not nested under `buildups`](feedback_nav_is_top_level_not_under_buildups.md) — a wrong nesting assumption silently reads as NULL, not an error.
- [nav_daily's write-tenant binding fence](reference_nav_daily_write_tenant_binding_fence.md) — `app.nav_computed_for` GUC must be set to the row's users_id before EVERY insert, even as postgres.
- **[⚠ "Independently reconstructed" can share a blind spot with the very composer it checks](feedback_independent_reconstruction_shares_blindspot_with_shared_helper.md)** — both sides calling the SAME shared primitive move together under a strike; inversion-verify, don't assume independence from the pattern name.
- [`fn_create_manual_purchase` journaled-fixture recipe](reference_fn_create_manual_purchase_journaled_fixture.md) — `p_cost_basis` is TOTAL not per-unit; auto-mints day-1 eod_price; bump a LATER eod_price row separately for a gain.
- **[⚠ A research-only fork can write/commit/push/report on its own — SELF-269](feedback_fork_scope_creep_wrote_committed_pushed_reported.md)** — check `git status` on the target file before writing; a fork's completion report is unverified until re-derived, same as a teammate's.
- [Plain-text response is invisible to teammates in team mode](feedback_plain_text_response_invisible_to_teammates_in_team_mode.md) — even when a teammate's phrasing reads like "answer inline," the answer is a SendMessage call, never a bare text turn.
- [Scratch-DB pgTAP harness gotchas](feedback_scratch_db_pgtap_harness_gotchas.md) — `docker exec -i`, pgtap in `public` schema, harness permissive vs prod ACLs, pg_prove's `docker run -v` needs a real HOST path, `\'` isn't a valid escape, `:'var'` doesn't interpolate in `do $$ $$`, `pg_catalog.coalesce(...)` doesn't exist, multi-unknown-literal UDF calls may need casts on every arg.
- [A new fence can encode the old bug into an EXISTING file's fixture](feedback_new_fence_can_encode_old_bug_into_existing_fixtures.md) — a catalog-assertion sweep misses this; only a full pg_prove regression run surfaces it (SELF-248/092: 037's own fixture built the defect state as intended behavior).
- [Same-value write can't fail a lives_ok control](feedback_same_value_write_cant_fail_a_lives_ok_control.md) — retargeting a leg to whatever's already in the fixture can silently make a "guard is lifted" positive control toothless against a transition-scoped regression; verify the write value differs from what the row already holds (SELF-248/037 Sec round-2).
- [Verify a causal mechanism before stating it](feedback_verify_causal_mechanism_before_stating.md) — a true result + an unverified "why" is still a defect; architect caught mine.
- [Scope the grep to what the assertion checks](feedback_scope_the_grep_to_what_the_assertion_checks.md) — a "no token X anywhere" claim must be scoped to prosrc, never the whole file; the header's own prohibition prose contains the token.
- [Send full text, not a path pointer, to a non-authoring role](feedback_full_text_not_path_pointer_to_non_authoring_role.md) — paste for a first/small hand-off; switch to own-worktree + path + self-verified `git diff --no-index` once a file is large and under repeated revision.
- [Never write into a teammate's worktree](feedback_never_write_into_a_teammates_worktree.md) — not even a temporary, self-removed copy; it collided with Backend's live run mid-flight. Verify from their path or ask them to run it, never write into it.
- [CSV test fixtures need csv.writer, not f-strings](feedback_csv_test_fixtures_use_csv_writer.md) — an unquoted internal comma read as a 10x-class scaling defect in the code, not the fixture bug it was.
- **[⚠ Postgres roles are cluster-level, not per-DB](feedback_postgres_roles_are_cluster_level_not_per_db.md) — a real incident.** `ALTER ROLE pfin_etl` in a "scratch DB" session overwrote the REAL shared credential cluster-wide. Never arm a real role for a test; create+drop a throwaway one.
- [Run ruff before every Python handoff](feedback_run_ruff_before_python_handoff.md) — green pytest says nothing about an unused import; I never commit, so `ruff check` runs on my side first.
- [Re-diff the source before a re-verification run](feedback_rediff_source_before_reverify.md) — diff your copy against the authoring teammate's live worktree BEFORE re-running, not after.
- **[Pattern-match to a past incident ≠ verified root cause](feedback_pattern_match_to_past_incident_verify_current_state.md)** — pfin_etl's NOLOGIN-but-password-set looked like a prior incident's residue; was F/CTO's deliberate reissue. Check current state, don't report a resemblance as root cause.
- [A committed ad hoc seed contaminates every later run in the same scratch DB](feedback_scratch_db_perf_seed_must_be_rolled_back.md) — match "have" values against your own seed data before reporting a regression.
- [Vitest `toHaveLength` on a raw DOM-element array can crash, not fail](feedback_vitest_tohavelength_dom_array_crash.md) — jsdom chokes serializing an SVG element in an array on the failing case only; assert `.length` instead.
- [Fixture scaffolding that reuses the function body's own formula hides a shared bug](feedback_fixture_scaffolding_reusing_body_formula_hides_shared_bug.md) — add independent-arithmetic legs and corrupt-the-control before calling it done.
- [`git checkout stash@{0} -- <path>` clobbers intervening teammate commits](feedback_stash_checkout_path_clobbers_intervening_changes.md) — replaces the file with the stash's OWN base; re-diff against the teammate's live tree before handoff.
- [A handed-off diff can be stale even when its content is right](feedback_handed_off_diff_verify_base_not_just_content.md) — `git apply --check` PER FILE + trace the base blob to a commit; content-reading alone misses this.
- [SvelteKit reserves the `+` prefix in `src/routes/`](reference_sveltekit_plus_prefix_reserved_in_routes.md) — `+page.staleness.test.ts` broke `svelte-kit sync` silently (vitest alone doesn't catch it); name route-level tests topic-first, matching `nav-series.server.test.ts`.
- **[⚠ Worktree branch drift — RECURRED FIVE TIMES, new triggers each time](feedback_worktree_branch_drift_after_compaction.md)** — post-compaction stale branch; branch claimed by a sibling worktree (silently edited main's own checkout instead); Write/Edit's `file_path` doesn't inherit Bash's `cd`; a sibling's benign push mid-task carrying an undispatched new function. Check `git status --short --branch` (not just `git log -1`) before the final commit, and spell out worktree-rooted paths explicitly on every new-file Write/Edit.
- [Held delivery must resend full content](feedback_held_delivery_resend_full_content.md) — "holding delivery" ≠ "already delivered a draft"; resuming after a hold, re-send full content, never a diff against an assumed prior state.
- [`svelte/server` render() for required-prop compile proofs](reference_svelte_server_render_for_required_prop_proofs.md) — `@testing-library/svelte`'s render() tolerates omitted required props (types gap); plus the `@ts-expect-error` literal-marker-must-appear-exactly-once gotcha.
- [RLS battery files keyed to original migration, not amending one](reference_rls_battery_file_keyed_to_original_migration.md) — `071_..._rls.sql` extended in place for `072`'s amendment; don't conclude a battery gap from "no file at this migration's number." `plan()` counts drift — read live, never cite from memory.
- [Snapshot NO-FK column still needs its source table retargeted](feedback_snapshot_no_fk_column_still_needs_source_table_retarget.md) — 031's history.sub_cat_id has no FK, but the LIVE row it snapshots from is fenced one hop upstream (023); "no FK on this column" ≠ "no FK in this data's lineage."
- **[⚠ pg_prove full-suite runs must scope to /tests, not /tests/rls — RECURRED ONCE](feedback_pg_prove_scope_full_tests_tree_not_rls_only.md)** — excludes 00_rls_inversion_self_test.sql (the harness's own teeth check) + 2 others; type `/tests` from this file, don't recall it. Also: `CREATE DATABASE...TEMPLATE` doesn't copy `pg_db_role_setting` (false-alarms 01_session_timezone T2) — [[feedback_scratch_db_pgtap_harness_gotchas]].
- **[⚠ count(distinct col) silently drops NULL — recurred TWICE in one file](feedback_count_distinct_silently_drops_null.md)** — SELF-330/086's own N1/N2 NULL-blindness self-test didn't immunize a sibling assertion two legs over; grep `count(distinct` as a class when a nullable key is in scope, not just the one site under review.
- **[Documenting a caution ≠ applying it](feedback_documenting_a_caution_is_not_applying_it.md)** — wrote DESIGN.md §16 (reused scratch DB masks id-order bugs), then ran the next suite-level claim against the exact reused DB it warns about, 3 hours later. Check the just-written caution against your own very next matching action.
- **[⚠ UTC-vs-local date drift — recurred THREE times before diagnosed](feedback_utc_vs_local_date_drift.md)** — dated three separate SELF-330 artifacts 2026-08-21 (repo convention is local, actually 2026-08-20); cross-check a real local timestamp before dating anything, and diagnose the mechanism on the SECOND recurrence, not the fourth.
- **[⚠ NOT NULL/no-DEFAULT column forces a whole-tree fixture sweep — RECURRED](feedback_not_null_no_default_column_forces_whole_tree_sweep.md)** — grep the WHOLE `tests/` tree for INSERTs into the changed table, and separately for hardcoded seed-row COUNTS the new rows may have shifted.
- [`expect_cross_tenant_write_blocked` doesn't self-restore role](feedback_write_blocked_helper_leaves_intruder_role.md) — a precondition placed right after it silently reads as the intruder tenant.
- **[⚠ Two directories both look like the battery home](feedback_repo_root_tests_vs_supabase_tests_two_directories.md)** — repo-root `/tests/rls/` holds only DESIGN.md; the real pgTAP tree is `supabase/tests/rls/`. Cross-check before reporting any coverage gap.
- [Magic-link + cookie-transplant login for live walks](reference_magic_link_cookie_login_for_live_walks.md) — team-lead-sanctioned: no password ever created/entered; `localhost` works, `127.0.0.1` errors (IPv6-only vite bind).
- [Rollup vs per-account jsonb: `cat` lives on the SECTION vs the ROW](feedback_rollup_vs_peraccount_jsonb_cat_key_location_differs.md) — check each function's real `jsonb_build_object`, don't pattern-match a sibling leg.
- [pg_prove aggregate-run TAP artifact, mechanism CONFIRMED](feedback_pg_prove_aggregate_run_tap_artifact_unconfirmed.md) — drift = count of trailing savepoint-wrapped legs at a file's own tail, exact arithmetic, pre-documented in the file's own header.
- **[⚠ Fixture-clock trap RECURRED despite being NAMED in the dispatch](feedback_fixture_clock_trap_recurred_self257.md)** — `created_at` defaults to real wall-clock now(), not transaction_date. Pin `created_at` explicitly on every fixture row, always.
- [db-template-clone.sh — first real-use report](reference_db_template_clone_tool.md) — sub-second scratch clone; still need pgtap per clone + manual drop when done. Marker can be cross-branch stale — [[feedback_template_clone_marker_can_be_cross_branch_stale]].
- [Same-month aggregate has no sub_cat scope](feedback_same_month_aggregate_no_subcat_scope.md) — "different Sub-Cat" isn't isolation for a whole-household monthly total; only a different month is.
- **[⚠ `language sql` bodies have NO pg_depend edges](feedback_language_sql_body_has_no_pg_depend_edges.md)** — a "not exists in pg_depend" leg is vacuously true for every body; use a `prosrc` regex positive-pin, inversion-verify.
- [Teammate address is the roster's short name, not the agent-type string](feedback_teammate_address_is_role_slug_not_agent_type.md) — `ListAgents` first when unsure.
- [Disclose an added leg beyond what was required](feedback_disclose_added_legs_beyond_what_was_required.md) — a diff-only sign-off trusts everything inside it is either the ask or a disclosed extra.
- [Data-modifying CTE nested inside `is()` fails — must be top-level + `\gset`](feedback_data_modifying_cte_must_be_top_level_not_nested_in_is.md) — pg_prove caught it, bare reading wouldn't have. Cast the `\gset`-captured var (`::bigint`) or `is()` fails to resolve.
- **[⚠ `pfin_tmpl`'s staleness marker can be CROSS-BRANCH stale, not just old](feedback_template_clone_marker_can_be_cross_branch_stale.md)** — check the marker's head against your own tree before bypassing the fence; fall back to a by-hand sequential apply when it diverges.
- [A control leg cannot flip under its own named inversion](feedback_control_leg_cannot_flip_under_its_own_named_inversion.md) — a structurally-untouched control can't go RED under a spec that says it should; report the divergence, don't round up to "confirmed."
