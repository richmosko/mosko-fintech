{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "{ echo \"[Claude Code SessionStart hook — sourced from docs/handoff-prompts.md]\"; echo \"\"; echo \"Before responding to the user's first message in this session, complete the following re-orientation protocol; then address whatever the user actually asked.\"; echo \"\"; echo \"After your four-step orient summary, also check WORKFLOW.md's header line that begins with '**Current phase:**'. If it indicates the current phase is complete with a next phase ready to enter (e.g., 'Phase X — ... (complete). Next: Phase Y'), append this one-liner to your response: 'Phase transition pending — the workflow prompt for executing it lives in docs/handoff-prompts.md (## Phase-transition prompt). Invoke when ready.' If no transition is pending, omit the flag.\"; echo \"\"; awk '/^## Subsequent sessions/,/^---$/' docs/handoff-prompts.md | sed -n 's/^> //p'; } | jq -Rs '{hookSpecificOutput: {hookEventName: \"SessionStart\", additionalContext: .}}'",
            "statusMessage": "Loading session orientation"
          }
        ]
      }
    ]
  }
}
