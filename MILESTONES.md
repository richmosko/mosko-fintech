# mosko-fintech — MILESTONES

Compact state ledger for mosko-fintech. **Top section (above `## Roadmap`) is auto-loaded by the SessionStart hook** per [ADR-009](DECISIONS.md#adr-009) Decision 6 (compact-ledger auto-load model — `MILESTONES.md` is the auto-load anchor; everything else consult-on-demand). Detail below the cutoff is consult-on-demand.

**Conventions:**
- Milestones tracked at two scales per [ADR-009](DECISIONS.md#adr-009) Decision 7: **meta-process** (M0 / M1 / M2 / M3 = R / P / I+V / Deploy) and **product** (V1.0 / V1.1 / V1.final / V2-X).
- Sprint = Linear cycle (orthogonal pacing wrapper, not a hierarchy level).
- Feature = Linear Issue = one PR = one I↔V loop.
- Last updated: 2026-08-01 (v1.133 — **SELF-320 account.sub_cat_id PHYSICALLY DROPPED — migration 048 (PR #278, merge `c82fabd`); FIRST-EVER drop of a DDL-realized Decision-3 instance.** Schema follow-on to SELF-319's UI/app removal: migration `048` drops trigger `account_matched_sub_cat` → fn `fn_account_matched_sub_cat` (Decision-3 **#5** fence) → column `pfin.account.sub_cat_id`, then DROP+recreates `fn_create_manual_account` 7-arg→6-arg (identical body; posture intact — INVOKER, `search_path=''`, revoke-public/grant-authenticated, no service_role). App calls the 6-arg RPC. **Decision-3 #5 marked DROPPED (F/CTO-ratified):** label retired-in-place (NOT renumbered), new **DROPPED (built-then-removed)** status class distinct from DDL-deferred #3/#4 → **15 labeled / 12 DDL-realized**; consistent across ADR-011 D3 (header + #5 entry + dated DROP-resolution block) + supabase/CLAUDE.md 3(b) + the 048 header. **De-conflation:** only the account-level asset sub_cat dropped; the transaction-level ones (annotation #10, split #13, `transaction_type`, `fn_create_manual_trans` param) untouched (QA de-conflation guard). **Sec joint-review CLEAN** (reduces attack surface; posture/ledger correct). **QA:** `048` battery (plan 12, non-vacuous — column/trigger/fn GONE, 6-arg works, anon denied, #10/#13 regression-guard) + reconciled `012` (12→4, kept acct_setup discriminator) + `013` (12→9, kept create/atomicity/anon-denial — QA caught 013 also referenced the dropped surface); `supabase test db` **Files=47 / Tests=810 / PASS** (no `db reset` — F/CTO live data intact). **Governance: §10 3 · RT-26 4 · DEFINER 4 · Decision-3 15/12** (a drop — net-negative surface). Combined-tree api check 0/0 · 15/15 CI (live pgTAP). Role-separated: Architect (048 + D3 amendment) → Backend (6-arg call) → QA → Sec. Follow-up: RT-26 allowlist header-comment drift ("3"→"4") in rt26-allowlist.txt + api/CLAUDE.md (Sec N2/N3, pre-existing). — *prior v1.132 —* **SELF-319 dead account-level asset Sub-category REMOVED from the UI — Frontend+Backend (PR #275, merge `8292123`).** The account-level asset Sub-category (`pfin.account.sub_cat_id`; migration 012/ADR-025; editable via SELF-236) was conceptually wrong + **dead code** — assets/holdings within an account carry the allocation category/sub-category (per-asset `user_asset_category` #8, per-transaction annotations #10), the account itself does not; verified nothing reads `account.sub_cat_id` for allocation/NAV (not fn_compute_nav / fn_holdings / NAV / staleness). Removed the Sub-category section from accounts/[account_id] (display + SELF-236 reassign) + the picker from accounts/new; dropped the `reassignSubCat` action, `subCats` loaders, the account embed, and `sub_cat_id` from the create schema (client+server) + `reassignSubCatSchema`; `fn_create_manual_account` now passes `p_sub_cat_id: null`. **Per-transaction cashflow category UNTOUCHED** (Categorize/Split + entry-form picker via `account_trans_annotation`/`loadCashflowSubCats`). **DB column + `fn_account_matched_sub_cat` (012) trigger + `013` param stay DORMANT** (UI/app-surface reversal only; full column drop + Decision-3 #5 ledger removal = a future Architect ADR). Net −254 lines. svelte-check 0/0 (697) · vitest 474 · browser-verified. **Governance FLAT: §10 3 · RT-26 4 · DEFINER 4 · Decision-3 15/13** (no migration; #5 stays labeled+realized, now app-unreferenced/dormant). SELF-319 Done (manual — Linear auto-close still broken, 4th). — *prior v1.131 —* **SELF-318 SimpleFIN connect-UX fixes LANDED — Frontend (PR #273, merge `dec877a`).** Two connect-flow fixes from V1.1 local testing: (1) **removed the misleading "Institution name (optional)" field** from the SimpleFIN connect widget — one connection can span accounts at MANY institutions, so a one-label-per-connection field was wrong (`institution_name` now provider-derived/null; the flow/relay param stays, always `undefined`); (2) **per-account import/ignore selection** on the shared connect-attributes step — SimpleFIN grants ALL of a token's accounts with no share-selection step (unlike Plaid Link), so an "Import this account" toggle (default on) excludes unchecked accounts from the payload → never landed (`fn_land_linked_accounts` lands one row per element; absence = not imported); requires ≥1, ignored rows skip validation + hide fields, Save button + lede show the count. **Frontend-only** — the server already lands only payload accounts (SELF-199); no migration / no new RLS surface / **no Sec joint-review trigger**. svelte-check 0/0 · vitest 474 · build clean · SimpleFIN tests 40/40; institution-name removal browser-verified (import-selection live render best confirmed on the next connect — the attributes page fail-closes without a live handoff). **Governance FLAT: §10 3 · RT-26 4 · DEFINER 4 · Decision-3 15/13.** SELF-318 Done (manual — Linear auto-close still broken). — *prior v1.130 —* **SELF-317 "Sync now" user-initiated manual provider-sync trigger LANDED — full-stack (PR #271, merge `f495cb2`); ADR-037 amendment.** On-demand refresh instead of waiting for the @daily cron: browser `POST /api/sync` (per-connection `source_id` or sync-all) → new dedicated worker `POST /admission/manual-sync` on the existing RT-27 admission surface (reuses `WORKER_ADMISSION_SHARED_SECRET` + CA-1 private-bind — **no new secret**; api/src stays **off** RT-26). Reuses the provider-agnostic `poll.ts processSource` ingest path (no fork; not the Plaid-only webhook path). **A2** async (return-fast `202` + fire-and-forget in the always-on admission process; UI polls `040`/`043`) · **R1** 60s/source in-memory debounce (worker-side source of truth) + in-flight lock · migration **`047`** additively widens `linked_source_sync_audit.source` CHECK `+manual` (C6b; the orchestrator writes its own `source='manual'` audit row per triggered source — C6a, since `processSource` doesn't). Cross-tenant = **two independent DB-enforced locks**: session-derived `ownerUserId` (never body) → app RLS resolve→404 → worker enumeration under `withTenant()` (RLS, not service_role — Sec **C1** hardening) → `resolveCredential` double-bind (0 rows on a foreign source). **Sec joint-review CLEAN-WITH-CONDITIONS → re-review of built code CLEAN** (C1-C4/C6a all verified in code); **QA two-tenant battery** proved all 7 cross-tenant items non-vacuously (inversion controls) + **caught 4 real coverage gaps** in the initial unit tests. Frontend per-connection "Sync now" (60s disable + 3s poll, short-circuits when `last_successful_sync_at` advances). **Governance FLAT: §10 3 · RT-26 4 · DEFINER 4 · Decision-3 15/13** (route on existing RT-27; `047` no FK/DEFINER; api/src credential-less). Combined-tree API svelte-check 0/0 (697) + vitest 474 · worker typecheck + vitest 304 · **15/15 CI** (pgTAP battery + live-DB lane). Full role-separated loop: Architect (`047`+ADR) → Backend (worker+app) → Frontend → QA → Sec. SELF-317 Done. — *prior v1.129 —* **Accounts Hub landing + header nav + connections rename + field help-text + provider-label + poll env-fix LANDED — a V1.1 UI-gap bundle (PR #269, merge `255af7d`); SELF-316** (supersedes SELF-314/315, closed duplicate). Fixed the `/accounts` **404** (no index route → 5 breadcrumbs + the `accounts/connect/attributes`→`/accounts` redirect all dead-ended); added the Accounts Hub (grouped account list + connection-status chips + Connect/Add-manual CTAs + collapsible Inactive group; **per-account gross-value column DEFERRED** — needs a valuation migration; renders no NAV/gross total per PM-2); header primary nav (Net Worth · Accounts, active-state); Connections rename ("Connected accounts"→"Connections") + per-connection **provider label** (SimpleFIN/Plaid — system value, not user-editable); account-detail field help-text (Type/Scope/Tax-treatment + corrected Sub-category framing = whole-account asset class, single-asset only); `--env-file-if-exists=.env` on the provider-sync poll/admit/serve-admission scripts (local-dev-only). All-frontend + read-only RLS-scoped loader (no service_role) + dev-script — **no migration, no new RLS surface, no Sec joint-review triggers.** 14/14 CI green; svelte-check 0/0; browser-driven vs local Supabase. Ran the SimpleFIN poll (213 txns synced + verified). **Linear auto-close-on-merge did NOT fire** (SELF-316 advanced to In Progress on PR-open but not Done on merge → set manually; F/CTO integration-setting fix pending). **Sync-now** manual-sync trigger design ratified (Option A: `/api/sync`→worker `/admission/manual-sync`; A2 async + R1 debounce; ADR-037 amendment) → Sec joint-review in flight before build. Governance FLAT: §10 3 · Decision-3 15/13 · DEFINER 4 · RT-26 4. — *prior v1.128 —* **V1.0 MILESTONE COMPLETE — the first full V1 product milestone; rotation to V1.1 (Net worth full) per [ADR-017](DECISIONS.md#adr-017) D2.** All 4 V1.0 sub-milestones closed (Onboarding §2.4 13/13 · §2.1.1 NAV 2/2 · Platform foundation · Auth foundation); the last build feature was **v1.127 SELF-288** (Auth-5 account recovery — password reset + MFA-step-up recovery flow; Sec GREEN, QA both-legs-live). **SELF-184** (Coolify→Discord) parked in **Phase-7 deploy** (`V1.x Cross-cutting infra`). **V1.1 = Net worth full (§2.1.2–§2.1.7, SELF-217–229) → current;** NO §7 promotion (V1.1–V1.4 already in Linear per the carve-out). No phase transition — Phase 6 (Build Loop) continues into V1.1 build; **Phase 7 (Deploy — incl. provisioning a live VPS/Coolify) Not-started.** Also landed **`docs/local-dev.md`** (local run-order + ports + the `:5173`/`:3000` auth gotcha + Plaid/SimpleFIN `.env` key placement). Governance FLAT: §10 3 · Decision-3 15/13 · DEFINER 4 · RT-26 4. — *prior v1.126 —* **V1.0 §2.4 ONBOARDING SUB-milestone COMPLETE (13/13); NO V1.0→V1.1 rotation yet.** All 13 §2.4 issues Done (SELF-196–204 + 206–209; SELF-205 re-scoped to V1.6 at ADR-035). "V1.0" is 4 sub-milestones; **SELF-184** (Coolify→Discord) re-homed to **Phase-7 deploy** (→ `V1.x Cross-cutting infra`; needs the not-yet-provisioned V1 greenfield-VPS Coolify + F1 PII audit; F/CTO-owned) which closes Platform foundation, leaving **SELF-288** (account recovery) as the SOLE remaining V1.0 build straggler; the rotation lands when SELF-288 does. Closed by the final two features — **v1.124 SELF-209** (§2.4.5 RLS close-gate: SEAM-only `plan(36)` battery composing the ~19 per-migration two-tenant batteries + 4 net-new inversion-checked tests proving the whole §2.4 write surface tenant-isolated; Sec GREEN; no RLS hole) + **v1.125 SELF-208** (§2.4.4.c non-silent staleness markers: read-only INVOKER `fn_aggregation_has_stale_constituent` migration `046` + `StaleConstituentBadge` WCAG-disclosure on the NAV surface; Sec RT-13 GREEN; QA pgTAP `plan(17)`). **No BACKLOG §7 promotion pending** (V1.1 already populated in Linear per the going-forward carve-out; §7 promotion first triggers at V1.3-complete). This meta/doc PR lands the §2.4-close bookkeeping + the SELF-208 design-spec §4 "tag→disclosure" delta. Governance FLAT throughout: **§10 3 · Decision-3 15/13 · DEFINER 4 · RT-26 4.** — *prior v1.123 —* **SELF-206 §2.4.4.a Plaid webhook handler LANDED — full-stack (PR #261, merge `f2268ed`); ADR-037.** Route Z-INVOKER two-phase RPC (migration `045`: `fn_plaid_webhook_resolve` read-only + `fn_plaid_webhook_commit` atomic, both INVOKER) + ratified OWD-A A3 typed `sync_cursor` column. **RT-05 = asymmetric ES256/JWK** (capability-verify: the AC's static `PLAID_WEBHOOK_SECRET` HMAC doesn't exist in `plaid@27`); Option 1 — worker fetches the PUBLIC JWK, `api/src` verifies the ES256 JWT + `request_body_sha256` locally (`jose`), stays credential-less. **C-X2 = sync-first/gate-at-completion (Close-i)**, self-contained on Plaid retry + sync idempotency (no poll dependency — code-level Plaid poll exists via `poll.ts`/SELF-279; cadence = SELF-213). **Per-path idempotency:** transactions → NULL id + cursor dedup (content-hash false-collides on Plaid's same-second bodies — QA inversion-proved the over-dedup guard); state → per-delivery `provider_event_id` gate. Two new worker admission routes on the existing RT-27 listener (routes-on-RT-27). **Sec GREEN + 4 mods** (incl. Decision-3 #15 fold-in 14/12→15/13 + a latent-C-X2-regression docstring catch). **§10 3 · Decision-3 15/13 · DEFINER 4 · RT-26 4.** QA pgTAP `plan(35)` + 9-case RT-05 ES256 battery; api 386 · worker 280 · pgTAP 750. DevOps: `PLAID_WEBHOOK_SECRET` removed + `jose` dep-audit CI job. Doc-sync (this meta PR): ADR-037 AC1 + SECURITY §4.5 RT-05/RT-27 + ARCH §3.1 reconciled to ES256/JWK + Route Z. Third/final V1.0 §2.4 connection feature; next SELF-208 + SELF-209. — *prior v1.122 —* **SELF-207 connection-state view + reactive re-auth + Vault rotation LANDED — full-stack §2.4.4.b (PR #259, merge `051001e`).** Per-account connection-state view + persistent P4 re-auth banner + reactive re-auth (Plaid update-mode / **SimpleFIN in-place `vault.update_secret` rotation**, source_id-identity → `021` preserved) + **SimpleFIN unhealthy-detection** (poll 401/403 → `login_required`, idempotent; closes the reactive loop). Migrations `043` (connection-state INVOKER `_latest` view over the `040` owner-semantics view) + `044` (sync-audit stable `linked_source_id` decouple + `040` re-join — **Decision-3 #15**; plain-bigint-not-declared-FK for immutable table + nullable/lenient fence for deploy-safety). Six capability-verify/design catches handled in-flight (in-place-rotation supersession of the ratified create→swap→delete, source_id-identity vs 021-orphan, CSP ship-blocker, sync-history digest-orphan, no-detector). **Sec joint-review GREEN** (all 5 surfaces; flag = `vault.update_secret`-under-`service_role`, discharged via a real service_role smoke + QA's real-Vault E2E) · QA reauth app-path 7/7 + real-poll E2E 4/4 + `043` 11/11 + `044` 13/13 + pgTAP 715 · combined tree api 0/623+vitest 348 / worker typecheck+vitest 259. **§10 3 · Decision-3 14→15 / 12→13 · DEFINER 4.** Second §2.4 feature closed this session (after SELF-199). Follow-ups: SELF-312 (+reauth step-up parity), SELF-313 (user-delete vs immutable-audit), `044` NOT-NULL tighten, SELF-206 webhook `linked_source_id`. — *prior v1.121 —* **SELF-199 per-account attribute capture LANDED — full-stack §2.4.1.d (PR #257, merge `bdb6b2f`).** Post-connect attributes form (name/scope/tax-treatment/account-type per selected account) → `pfin.account` via `fn_land_linked_accounts` INVOKER RPC (migration `042`, F/CTO-ratified **A2**; `ON CONFLICT`/`021` reactivate-only; first TABLE-returning write RPC → `#variable_conflict use_column`, capability-verify-caught pre-build). Client-carries-refs seam + pre-RPC `requireStepUp` guard (resolves the aal1-owner-vs-attacker 403 ambiguity). **Sec joint-review GREEN · QA pgTAP plan(23) 23/23 + app-path dbit 4/4 (real TOTP) · §10 3 · Decision-3 14/12 · DEFINER 4 UNCHANGED** (INVOKER, no new fn). First Onboarding §2.4 issue closed toward V1.0. Finding #4 (MFA source-of-truth divergence — out-of-band-only, fail-closed, recovery-covered) deferred to follow-up **SELF-312** (`sec-joint-review`). — *prior v1.120 —* **ADR-037 provider-agnostic connection-lifecycle model-ratify gate LANDED — meta/doc PR.** Re-derives the remaining V1.0 §2.4 issues (SELF-199/206/207) off their June Plaid-transport ACs onto the landed `linked_source` substrate (`015`+`040`; SELF-206 was schema-impossible as written — `plaid_*` tables dropped at `015`). **F/CTO ratified Plaid PRIMARY / SimpleFIN SECONDARY (one Plaid-unsupported account) / provider-agnostic interface** — resolves [ADR-027](DECISIONS.md#adr-027) provider-selection gate i. Three one-way doors ratified: #3 SELF-206 in V1.0; OWD-B B1 (thin shared `ProviderSyncAdapter`); OWD-A A3 (derive `last_successful_sync_at` from sync-audit log per ADR-032; typed cursor column at migration `042`). **§10 3 · Decision-3 14/12 · DEFINER 4 flat** (team-lead cross-checked verbatim vs DECISIONS.md + supabase/CLAUDE.md; the `042` cursor column adds no §10 instance / no FK-shaped column / no DEFINER fn). Sec review-recommended for the model; joint-review-MANDATORY at build (`042` / SELF-206 / SELF-207 re-auth / SELF-209). Build sequence: `042` → SELF-199 → SELF-207 → SELF-206 → SELF-208 → SELF-209 close-gate. — *prior v1.119 —* **SELF-311 default taxonomy provisioning on signup LANDED — full-stack (PR #252, merge `9456fc5`); ADR-036.** B1 lazy authenticated upsert + A1 migration-embedded `pfin.taxonomy_default` (63-row) reference table; provisions both asset §2.2.1 + cashflow §2.3.1 domains at first access, fail-soft. Migration `041` (scoped `authenticated` INSERT-only policy, `with check users_id=auth.uid()`, 025 aal2 backstop Shape A). Sec GREEN (review-surface) · QA pgTAP `plan(16)` + paired `009` fix · **§10 3 · Decision-3 +0 · DEFINER 4 UNCHANGED.** Closes the SELF-200 open-signup taxonomy dead-end (BACKLOG §7.5 O1). — *prior v1.118 —* **SELF-200 pending-symbol classification LANDED — full-stack §2.4.1.e (PR #250, merge `907826e`).** Derived-view (absence-as-pending; no table, no migration); classify UPSERT into `user_asset_category` under caller-RLS (Decision-3 fences #8/#9). §10 3 / Decision-3 / DEFINER 4 UNCHANGED; QA pgTAP `plan(17)`. Filed BACKLOG §7.5 O1. — *prior v1.117 —* **ADR-035: SELF-205 per-transaction reconcile mechanism SUPERSEDED; re-scoped to V1.6 read-only statement-vs-GL tie-out (meta/doc PR).** `reconciled_flag`/B1/B2 retired (GL reconciled-by-construction + immutable audit-class); SELF-205 moved V1.0 → V1.6, dropped from the §2.4 close-gate. De-scope: §10 3 · Decision-3 · DEFINER 4 UNCHANGED; no Sec joint-review (triggers absent). F/CTO-ratified. — *prior v1.116 —* **SELF-204 manual↔provider duplicate detection + per-account sync-history LANDED — full-stack §2.4.3.c (PR #247, merge `b202784`).** Per ADR-034; detection-only. Migration `040`: hash-dedup idx UNIQUE→non-unique, `fn_create_manual_trans` +`p_import_hash`, `manual_provider_dup_candidate` + `linked_source_sync_history` views. Shared cross-tier `import_hash` (canonical worker TS + machine-copy + run-always CI drift fence). Sec GREEN ×5 · QA `plan(22)` · **§10 3 · Decision-3 14/12 · DEFINER 4 UNCHANGED.** — *prior v1.115 —* **ADR-034 SELF-204 model-ratify gate LANDED (PR #246, merge `f1cdded`).** Decision-only; dedup already provider↔provider, net-new manual↔provider handled detection-only; §10 3 / Decision-3 / DEFINER 4 unchanged. Sec GREEN + F/CTO ratified. — *prior v1.114 —* **SELF-203 manual stock-split entry LANDED — full-stack (PR #244, merge `686f412`).** §2.4.3.b, stock-split-only per ADR-033. Migration `039`: `account_trans_corp_action_shape` CHECK (book-neutral) + `fn_create_stock_split` position-level INVOKER RPC. QA pgTAP `plan(21)` · Sec GREEN. Position-level not per-lot (OWD-1→C; per-lot → BACKLOG §7.3 G2); single-account (OWD-2). — *prior v1.113 —* **SELF-203 re-scope follow-through (PR #243, merge `fa035cc`).** Doc-only per ADR-033: PRD §2.4.3 drops "other" (V2-reserved); BACKLOG §7.3 stages deferred transfer-in-kind (G1); `supabase/CLAUDE.md` Decision-3 count synced (→ 14 labeled / 12 DDL-realized). — *prior v1.112 —* **ADR-033 SELF-203 model-ratify gate LANDED (PR #242, merge `8834501`).** Decision-only: no new `transaction_type` — split→`corp_action`, "other"→V2-reserved, transfer-in-kind→deferred journal `group_type`; two paper-caught collisions (`acct_setup` mis-book; Σ=0 trial-balance break). §10 3 / Decision-3 / DEFINER 4 unchanged. Sec GREEN + F/CTO ratified. Also: fixed the Linear↔GitHub merged→Done auto-close bug (F/CTO integration-setting change, confirmed on #244). — *prior v1.111 —* **SELF-202 manual cash-transaction entry LANDED — the FIRST Onboarding §2.4 issue toward closing V1.0 (PR #240, merge `ff61a23`).** Full-stack §2.4.3.a, re-derived to the append-only model per **ADR-032** (skip ELIMINATED — a "skip" is a report-view filter, not stored data; transfers categorize as Transfer + Σ=0-offset in the GL). Migration `038`: un-dorm `029` split writes (3 wr_access-JOIN policies + grant, aal2-claused) + `fn_create_manual_trans` INVOKER RPC (atomic account_trans INSERT + conditional 023 annotation) + **closed a pre-existing aal2/C3 gap** on `account_trans_split`'s SELECT policy (SELF-304, folded in — 029 post-dated the 025 retrofit). Edit = `004` reverse-and-replace (immutable ledger); split = un-dormed 029 under the locked child-lifecycle rule (parent XOR children, Σ=parent enforced at COMMIT). Backend 5-action layer + Frontend entry (Money in/out toggle) / edit / split UI. **Full role-separated build loop:** Architect (038 + ADR-032) → Backend/Frontend → QA `plan(21)` + Sec GREEN ×2 (DDL + app-layer money-flow) → CI green → F/CTO merge. Backend capability-verify clean-apply smoke PASS; npm check 0 · vitest 226 · pgTAP battery green in CI. **§10 3 · DEFINER 4/3 · Decision-3 unchanged** (no new FK; #13 activated, #10 exercised; append-only, no new DB objects at app layer). SELF-202 + SELF-304 Done. **Milestone-rotation check 2026-07-26: NOT due** (GL = project, not a V1.x milestone; V1.0 not closed). **V1.0 reconciliation:** stale-opens SELF-210/191/182 flipped Done (Platform + §2.1.1 NAV effectively there); the real V1.0 gap is **Onboarding §2.4** (~8 issues + SELF-209 close-gate). GL follow-ups SELF-302/303 re-homed → V1.4 tax cluster; 5 SELF-202 fast-follows filed. **NEXT: continue Onboarding §2.4 (SELF-203/204/200/209 + co-root 199) to close V1.0.** — *prior v1.110 —* **SELF-298 cross-tenant fence-message softening LANDED — the GL-project non-blocking cleanup fast-follow (PR #237, merge `1007ec9`).** Softened the 4 aal2-gated matched-tenant fence RAISE messages (#10 @ `023` sub_cat · #12 @ `033` journal · `030` trade read · `034` basis_adjust read): an own-tenant aal1+totp caller hits the `025` aal2-gated RLS (NOT EXISTS) → the fence RAISEd a misleading "cross-tenant" message (fail-closed + safe, wrongly attributed); now list *not found / not visible under current AAL / cross-tenant*, ADR-011 D3 instance-# citations (#10/#12) retained. Switched the paired assertions on those messages from `throws_like('%msg%')` → `throws_ok(…,'P0001',null,…)` (SQLSTATE-match, distinct from RLS 42501 → preserves the 004 all-42501 false-green teeth); business-rule RAISEs kept message-precise `throws_like`. **Brief-drift catch:** the `023` battery F1/F2/F3 (#10) assertions were beyond the Linear AC but would have hard-RED'd CI on the reword — caught + flipped. Fences #5(`012`)/#8(`022`)/`029` untouched (not aal2-gated). Sec + QA joint-review **AMBER → GREEN** (both independently caught the lone `033:315` (3e) `throws_like`→`throws_ok` miss; fixed). **§10 3 · DEFINER 4 · Decision-3 14/12 UNCHANGED — zero ledger movement.** Local `supabase test db` **Files=34 Tests=575 PASS**; CI green (pgTAP battery + RT-22/26/27 + TBC greps). Message-only edits — no new migration; `001`–`037` live. SELF-298 Done. **Double-Entry GL project fully closed — no GL feature work remains. NEXT: resume the V1 FEATURE CLUSTERS (§2.1.2+ net-worth / §2.2 allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report), or a milestone-rotation check.** — *prior v1.109 —* **`037` GL-completion LANDED — the tenth-and-LAST Double-Entry GL migration; CLOSES the GL project core (SELF-301; PR #234, merge `72b88f9`).** Migration `037_gl_completion.sql` (single file, 4 components): (1) `fn_gl_entries` create-or-replace — the matched-**SELL realized-gain** branch (F/CTO-ratified **Option A**: one Realized-Gain line to equity; `matched_book` = FIFO acquisition + pro-rata position-level basis_adjust; **balanced-by-construction**; ST/LT + recapture character **derived in the tax layer**, not posted; unmatched sells keep `035`'s Suspense floor; corp_action/wash_sale unchanged). (2) `fn_journal_close_balance` INVOKER BEFORE UPDATE — **future-dated-leg reject (ALL group_types, top-of-close guard)** → transfer Σ(amount)=0 / transfer_in_kind per-security Σ(qty)=0 / **compound no-Suspense-residual gate** → write-once `close_book_value`+`closed_at`; reopen nulls. (3) `pfin.journal` ALTER — the 2 snapshot columns. (4) `fn_account_trans_annotation_freeze_closed` INVOKER — rejects attach/detach **AND reclassification** of a closed-journal leg (F/CTO-ratified stronger form; keeps the snapshot faithful; reopen unfreezes). Plus **F1** (Sec-gated): `journal_insert` WITH CHECK gains `and status='open'` (reject born-closed; `025` aal2 backstop reproduced verbatim — byte-verified) — `033`'s file untouched. **Compound close law REFINED** (both QA + Architect independently found the literal total-Σ **vacuous** — self-balancing contras; F/CTO ratified the no-Suspense-residual + all-legs-dated-through hardening). **F/CTO ratifications this migration:** gain-line A · Σ=0-close B (group_type-branched) · close-snapshot A + freeze guard · defer wash_sale + substantive corp_action · compound-close hardening · hoist future-dated reject to all group_types · stronger freeze (membership+classification). **§10 3 (RT-22/RT-26/RT-27) · DEFINER 4 (both triggers INVOKER) · Decision-3 14/12 UNCHANGED** (no new FK) — **the whole GL project closes at ZERO ledger movement**. Sec joint-review **AMBER → FULL GREEN** (F1 insert-closed hole + compound-vacuity hardening + the paired battery; aal2 backstop regression-guarded by `033`'s (6c) against the recreated policy). QA `plan(26)` inversion-proven (Finding-2 resolved — the compound branch is now a real fence); `035` battery fixture-fix paired (`plan(13)` unchanged). Backend clean-apply `001`→`037`: full suite **Files=34 Tests=575 PASS** (`037` 26/26 · `035` 13/13 · `033` 28/28). Migrations `001`–`037` live. **Double-Entry GL: M1 + M2.5 + M1-evt(A1/A2/B) + M2 + M3-basis + M4-GL-read + M4-GL-write(036/037) — 10 migrations, COMPLETE.** Deferred follow-ups (Suspense-parked, documented in CONTRACT): wash_sale P&L + substantive corp_action (spin-off/cash-in-lieu). — *prior v1.108 —* **M4-GL-write Slice 1 (`036` lot_match write-enablement) LANDED — the ninth Double-Entry GL migration + the M4-GL-write surface (SELF-300; PR #232, merge `69279a9`).** Migration `036_lot_match_write_enablement.sql` adds `grant insert on pfin.lot_match to authenticated` + the `lot_match_insert` WITH CHECK policy (sell-leg parent-chain ownership via `account_users.wr_access`) — the RLS write companion to the already-realized `032` **Decision-3 `#14`** fence `fn_lot_match_matched_tenant_security`. **Makes `#14` EXERCISABLE, not newly realized** (no new function, base table, DEFINER, or FK column; `032`'s landed file untouched — append-only discipline; the exercisable-vs-realized wording rides a refreshed `comment on table`). **F/CTO-ratified FIFO/specific-lot basis method** (the realized-gain one-way door — per-lot / lot_match-dependent) + **dependency-split**: `036` = the focused write surface; **`037`** = the GL-completion (fn_gl_entries SELL realized-gain/recapture + corp_action + wash_sale, co-landed with the `Σ=0`-at-close trigger + close-snapshot column). Matching *inference* (FIFO/specific-lot) = Backend/worker, NOT DB scope. **Defence-in-depth on INSERT:** ACL grant · `#14` BEFORE-INSERT tenant+security match (fires FIRST; owner-only account resolution fail-closes a foreign leg at `'cannot resolve'`) · `wr_access` WITH CHECK on the sell leg · `032` immutability triple-fence intact. **§10 3 (RT-22/RT-26/RT-27, authenticated-tier) · Decision-3 UNCHANGED (14 labeled / 12 realized — `#14` exercisable) · DEFINER 4.** Sec joint-review **AMBER → GREEN** (B1 authenticated-tier write-path battery + B2 `032`-assertion migration; the `#14`-fires-before-WITH-CHECK ordering subtlety blessed; the contained `#14` trigger-disable isolation technique blessed, `030`-precedent). QA `plan(21)` **inversion-proven**; `032` battery migrated `plan(21)→plan(19)` (B2 — stale grant-layer 4b/4c re-homed to `036`). Backend clean-apply `001`→`036`: full suite **Files=33 Tests=549 PASS** (`036` 21/21 + `032` 19/19), `#14` re-enabled post-suite. SELF-300 Done (retitled to the `036` scope); `037` filed (blocked-by SELF-300). Migrations `001`–`036` live. **NEXT: `037` — the LAST GL migration (fn_gl_entries GL-completion + `Σ=0`-close trigger + close-snapshot column).** — *prior v1.107 —* **M4-GL-read (`fn_gl_entries`) LANDED — the eighth Double-Entry GL migration + the M4-GL READ surface (SELF-297; PR #230, merge `7d001c3`).** Migration `035_fn_gl_entries.sql` adds `pfin.fn_gl_entries(p_as_of date) RETURNS TABLE(...)` — a **SECURITY INVOKER** GL/trial-balance read helper (Lock 11; clones the `fn_compute_nav`@`019` posture: `search_path=''`, `revoke public` + `grant execute to authenticated`, no service_role/anon). **Flat GL entry rows** (F/CTO-ratified Option A — aggregation stays a derivable wrapper). **Correct-by-construction balance:** every source fact emits book-value postings summing to exactly 0 (real leg + imputed/derived contra), so `SUM(amount_book)=0` holds **globally AND per-currency by construction**; misclassification only routes a contra to the derived **Suspense** line, never unbalances (11 imputation branches; Unrealized-Gains = `fn_compute_nav − book NAV` memo pair). **F/CTO-ratified NARROW (Option A):** `035` is read-only; the write/VETO-adjacent obligations that `032`/`033` CONTRACT blocks deferred to "the M4-GL PR" split to a follow-on **`036`/M4-GL-write (SELF-300, joint-review-mandatory)** — documented in the `035` CONTRACT block: SELL realized-gain/position-basis-removal (parks Dr Cash/Cr Suspense; **recapture** = `min(gain, accum_depreciation)` authored-ready but vacuous until lot_match writes), corp_action GL, wash_sale P&L, the `Σ=0`-at-close enforcement trigger (`033` B-ii), the lot_match INSERT grant + `wr_access` WITH CHECK policy (activates **Decision-3 #14**), and the close-snapshot column. **§10 3 · DEFINER allowlist UNCHANGED (INVOKER) · Decision-3 UNCHANGED** (no new FK-shaped column; #14 stays DORMANT until `036`). **Sec routine review GREEN** (read-only surface over money data; isolation fails-closed incl. INVOKER-under-INVOKER `fn_compute_nav` sub-call — no DEFINER RLS-bypass; zero write surface leaked). QA `plan(13)` two-tenant pgTAP **inversion-proven + non-vacuous** (cross-tenant read fails-closed · global + per-currency `Σ(amount_book)=0` · Suspense residual = −900 from the SELL · 4 source-branch positive controls). **Backend clean-apply `001`→`035` PG17: 13/13 PASS; full suite Files=32 Tests=530 PASS** — no `030`/`031`-style fixture collision, no regression. SELF-297 Done; **SELF-300 (`036`) filed, blocked-by SELF-297** (Sec **N1**: the #14 *fence* is already DDL-realized at `032` — `036` activates the write grant/policy; **N2**: `036` is joint-review-mandatory). Migrations `001`–`035` live. **NEXT: `036`/M4-GL-write (SELF-300) — the LAST GL migration.** — *prior v1.106 —* **M3-basis (basis_adjust row-shape constraints) LANDED — the seventh Double-Entry GL migration (SELF-296; PR #228, merge `02d5d90`).** Migration `034` adds the `account_trans_basis_adjust_shape` CHECK on `account_trans` (`basis_adjust ⟹ quantity=0 AND security_id NOT NULL AND cost_basis NOT NULL`; reason-independent, role-agnostic — fires under the `017` service_role ingest too) + the cross-table INVOKER trigger `fn_account_trans_annotation_basis_adjust_reason` on the `023` overlay (**R1** domain `metadata.reason ∈ {depreciation, return_of_capital, wash_sale}` · **R2** reason↔amount `return_of_capital ⟺ amount≠0` (anchored to the frozen `004` amount) · **R3** additive guard `reason ⟹ basis_adjust`; BEFORE INS/UPD, `search_path=''`, NULL-safe fail-closed; a structural clone of `030`'s Trade trigger) + a **reserved-metadata-keys registry** (`reason`=basis_adjust-only, `action`=corp_action-only) on the `023.metadata` comment. **F/CTO-ratified A-i** (`reason` on the mutable `023` overlay per Amendment 1 item 8 — the immutable `cost_basis`-delta + `amount` facts determine the economics, `reason` is the interpretive label) · **B-i** · **C** (`cost_basis` role-widens acquisition→signed delta; NO `017` constraint to relax — verified: only the NaN-fence, no `>=0`) · **D** (roll-forward / accumulated-depreciation / recapture DEFERRED to M4-GL) · **KEEP R3** (`metadata.reason` is a RESERVED key — reversibility-asymmetry: reserve while empty, relax later free; Architect + Sec both concurred). **§10 3 · DEFINER 4 (zero new — CHECK + INVOKER trigger) · Decision-3 14/12 UNCHANGED** (value/tenancy-neutral; no FK-shaped column). Sec joint-review **CONDITIONAL-GREEN → FINAL GREEN** (`004` immutability unweakened; R3 KEEP; the aal2-gated-INVOKER-read edge benign → **SELF-298**); QA `plan(19)` 19/19 **inversion-proven**. **Backend's full-suite directory-mode gate caught + resolved a `030`/`031` fixture collision** — `034` was clean in isolation but broke pre-existing fixtures using `metadata.reason` as a generic payload on standard rows + a bare-basis_adjust vocab row → reshaped `030(1a)` to a valid basis_adjust + switched `030(8a)`/`031(3c)` `reason`→`note`; full suite **517 PASS** (up from 499). Migrations `001`–`034` live. **NEXT in the GL sequence: M4-GL (`fn_gl_entries` — the book/market GL/trial-balance INVOKER read helper, where the deferred `Σ=0`-at-close [M2] + basis roll-forward/recapture [M3] + the write-path to journal groupings all land — the LAST GL migration).** — *prior v1.105 —* **M2 (journal_group→journal grouping parent + Decision-3 #12 leg fence) LANDED — the sixth Double-Entry GL migration (SELF-295; PR #223 migration + PR #224 paired battery, merges `6fbc67d` / `3e0d5b6`).** Migration `033` `pfin.journal` — write-live double-entry GROUPING parent (N `account_trans` legs tied `Σ=0` via a shared journal; grouping, NOT a stored journal-line table; header-comes-after-the-lines) + `journal_id` FK on the `023` overlay + the **Decision-3 #12** matched-tenant leg fence (`fn_account_trans_annotation_matched_journal`; BEFORE INSERT OR UPDATE, INVOKER, NULL-safe fail-closed, **hybrid-resolved** — leg tenant chain-resolved via `trans_id→account_trans→account.users_id`, journal read directly; UPDATE-path load-bearing). RLS 4 policies + `025` aal2 backstop AND-ed into all four; UPDATE WITH CHECK blocks cross-tenant close/reopen; service_role ungranted; anon zero-grant. **A2 (F/CTO-ratified): write-live shell; `Σ=0`-at-close DEFERRED to M4-GL** (the book-value imputation engine `fn_gl_entries` — market value plays NO role; F/CTO corrected the design's misleading "GL-projected value" wording) + **B-ii/C-i/D** + the **`journal_group`→`journal` table harmonization** (extends ADR-031 Amendment 1 item 8; PK/FK `journal_id`). **§10 3 · DEFINER 4 (zero new; #12 fence INVOKER) · Decision-3 +1 → #12** (family **14 labeled / 12 realized**; #12 realizes LAST, non-contiguous behind #13@`029` / #14@`032`). Sec joint-review **CONDITIONAL-GREEN** (#12 pinned; all binding conditions discharged; the misleading fence-message edge → **SELF-298** fast-follow); QA `plan(28)` 28/28 (the #12 fence **inversion-proven** — mutate to BEFORE INSERT only → only the UPDATE-attach assertion flips RED); Backend clean-apply `001`→`033` PG17; CI green. **Battery-omission caught + remediated:** the paired `033_journal_rls.sql` was authored untracked + omitted from #223 (team-lead push-verification miss) → landed as fast-follow **#224** (test-pairing restored, CI 28/28 against the real migration). **NEXT in the GL sequence: M3-basis (`basis_adjust` event + `reason` subtype) / M4-GL (`fn_gl_entries` read layer). Open doc-reconciliation: N1 (ADR-011 D3 body fold-in of #12/#13/#14) + N2 (ADR-031 "DEFINER stays 3"→4) — Architect pass, Sec eyeball.** Migrations `001`–`033` live. — *prior v1.104 —* **M1-evt Slice B (lot_match junction + Decision-3 #14 fence) LANDED — the fifth Double-Entry GL migration; CLOSES M1-evt (SELF-293 Done, all 3 slices) (PR #221, merge `ee40ac0`).** Migration `032` `pfin.lot_match` — append-only immutable many-to-many junction (`sell_trans_id`+`buy_trans_id`→account_trans; `quantity_matched`; `match_seq` per-sell re-match batch) recording which buy lots a sell closes (per-lot tax character). Model (b) many-to-many (a 1:1 FK can't express mixed ST/LT split sales). **Write-dormant** (029 pattern — fenced shell now; matching logic + INSERT grant at M4-GL). **Decision-3 #14 fence** (Sec-pinned, count=ONE per fence relationship): `sell-tenant=buy-tenant` + `sell.security_id=buy.security_id`, BEFORE INSERT, INVOKER, NULL-safe fail-closed. **(4-B) self-versioning** (`match_seq` batches = the lot-match reclass-history; `031` untouched → no new DEFINER; supersedes the A2 add-column expectation). **§10 3 · DEFINER 4 · Decision-3 +1 → #14** (family 13 labeled / 11 realized). Sec joint-review **GREEN** (migration + battery); QA `plan(21)` 21/21; CI green. **M1-evt (SELF-293) COMPLETE: A1 (030) + A2 (031) + B (032). NEXT in the GL sequence: M2 (journal_group, #12) / M3-basis / M4-GL.** Migrations `001`–`032` live. — *prior v1.103 —* **M1-evt Slice A2 (reclassification-history audit table) LANDED — the fourth Double-Entry GL migration + the project's FIRST SECURITY DEFINER allowlist growth (SELF-293, PR #219, merge `aec9e9a`).** F/CTO REOPENED reclass-history to V1 (reverses ADR-031 Amendment 1 §9; decouples the audit story from the C+ freeze bet). Migration `031` `pfin.account_trans_annotation_history` — ADR-011 D2 audit-class, append-only `004`/`005`-mirror immutable (trigger-based, blocks ALL roles incl. service_role), full-snapshot `version_seq` chain, captures `023` routing state (`sub_cat_id` no-FK snapshot + `metadata`; not `note`) on INSERT + routing-UPDATE. Sole write path = new `fn_reclass_history_insert` **SECURITY DEFINER** helper → **allowlist 3→4** (authored 3 + reserved general-audit-log slot; un-forgeable — authenticated no INSERT grant). Team-lead cross-check caught + corrected the Architect's initial "stays 3" over-round → honest +1 before F/CTO ratify (disposition A). **§10 3 (unchanged — DEFINER ledger separate, de-conflation) · Decision-3 +0.** Sec joint-review **GREEN** on BOTH veto surfaces (D2 audit-class + new DEFINER fn) + battery; QA `plan(18)` 18/18; CI green. ADR-031 Amendment 2 + ADR-011 D9 amendment; DEFINER "3"→"4" doc sweep folded in. **NEXT: Slice B (lot-match buy-reference FK #14) — F/CTO elected to build now (design-first in flight); last M1-evt piece.** Migrations `001`–`031` live. — *prior v1.102 —* **M1-evt Slice A1 (event-class vocab widen + Trade constraints) LANDED — the third Double-Entry GL migration (SELF-293, PR #217, merge `c06d78c`).** The import-critical core of M1-evt (3 slices: A1=this / A2=reclass-history [F/CTO reopened to V1] / B=lot-match #14 deferred). Migration `030`: widens `transaction_type` CHECK to the lean `{standard,acct_setup,basis_adjust,corp_action}` (additive; settle-before-import — incumbent has basis_adjust events) + `metadata jsonb` on `023` + ONE combined Trade-constraint trigger (Sec Condition A: consistency + sign-alignment, BEFORE INS/UPD, INVOKER, NULL-safe, **UPDATE-path load-bearing**). ADR-025 amendment + line 759/766 fixes (lot-match #13→#14). **Sec joint-review GREEN** (migration + battery); **§10 3 · DEFINER 3 · Decision-3 +0** (Trade fence INVOKER + D3-neutral; #10 untouched). QA `plan(21)` 21/21 (incl. qty=0 strict->0 boundary); 012 battery 12/12 vs widened schema; CI green. **NEXT: Slice A2 (reclass-history audit-class, ADR-011 D2 — design-first in flight), then Slice B (lot-match #14, deferred).** Migrations `001`–`030` live. — *prior v1.101 —* **M2.5 (account_trans_split receipt-split overlay) LANDED — the second Double-Entry GL migration (SELF-294, PR #215, merge `1c5cead`).** Migration `029` adds `pfin.account_trans_split`, a 1:many split-child table under the immutable `account_trans` parent, with **Σ(children)=parent.amount DB-enforced**. Built to 4 F/CTO-ratified decisions (walked one-by-one): **anchor α** (parent-chain tenancy, mirrors `023`), **Σ=parent** deferred CONSTRAINT TRIGGER + `security_invoker` reconciliation view (T-account Dr/Cr shape), **write-dormant** (`009`), **`sub_cat_id` nullable** + soft precedence. The GL track's **first Sec-joint-review-mandatory** migration — Sec **GREEN** (migration + battery); Decision-3 **+1 → #13** (Sec-pinned against live ADR-011 + ADR-031 D8; family → **12 labeled / 10 DDL-realized**; first *forward-reservation gap* — #13 realizes while #12/journal_group stays reserved-unrealized). **§10 3 · DEFINER 3** unchanged. QA `plan(17)` 17/17 two-tenant pgTAP; CI green. Bookkeeping folded in: reconciled stale D3 "7"/§10/DEFINER counts in `supabase/CLAUDE.md` 3(b) + the `spawn-sec-joint-review` skill. **NEXT: M1-evt (event-vocab + ADR-025 amendment, Sec-gated) or M2 (journal_group, where #12 DDL-realizes).** Migrations `001`–`029` live. — *prior v1.100 —* **M1 (Category-as-class CHECK) LANDED — the first migration of the Double-Entry GL track (SELF-292, PR #213, merge `1a117f9`).** The GL moves from design to build: migration `028_user_taxonomy_cashflow_class.sql` adds a domain-conditional CHECK constraining cashflow-domain `user_taxonomy.cat` to the ratified 5-class enum **Revenue/Expense/Transfer/Equity/Trade** (the top-level Cat *becomes* the enforced accounting class; no `flow_class`/`normal_balance` column). Pure DDL, **no backfill** (table V1-write-dormant + empty at migration time per `011`). Build loop role-separated: Architect authored + clean-apply-verified; QA paired the pgTAP battery (`009` `plan(14)→plan(23)`: accept 5 classes / reject non-enum `23514` / asset-domain free / two-tenant isolation) + swept and settled two stale committed fixtures (`009` `Income`→`Revenue`; `023` `Housing`/`Food`→`Expense`). **Not Sec-joint-review-mandatory** (attribute CHECK, no fence surface); QA sign-off was the gate. All CI green (pgTAP + Live-DB + §10 fences). Ledgers flat **§10 3 · DEFINER 3 · Decision-3 unchanged**. Bookkeeping folded in: reconciled the stale §10 "2-instance" count in `supabase/CLAUDE.md` + the `apply-migration` skill → canonical **3 (RT-22+RT-26+RT-27)** (RT-27 catalogued at SELF-212 but those docs predated it). Double-Entry GL Linear project stood up (SELF-292→297, full blocked-by chain M1→M1-evt→M2.5→M2→M3-basis→M4-GL); SELF-292 Done. **NEXT: the sequence continues — M1-evt (event-class vocab, Sec joint-review-mandatory + ADR-025 amendment) per ship-order, or M2.5 (split-child, lighter). Open (non-blocking): the F/CTO local `seed.sql` rewrite to the ratified class names (blocks local `db reset`; includes the OtherCF/AcctSetup dissolution mapping).** — prior v1.99 — **Settle-before-import conventions RATIFIED + ADR-031 Amendment 1 LANDED (PR #211, merge `68bd07c`).** The double-entry category/event conventions are now settled (resolving the ADR-031 Decision 9 one-way-door *before* any incumbent import): 5-class cashflow enum **Revenue/Expense/Transfer/Equity/Trade** (Income→Revenue; Distribution→`Equity::Distribution`; **Trade** added as a Cat with `BTO/STC/STO/BTC` open/close sub-cats, superseding the NULL-cat partition); **Transfer=tax-neutral** (`Cash`/`Asset`-in-kind); lean event vocab `{standard, acct_setup, basis_adjust, corp_action}` (buy/sell derived from `sign(quantity)`); `corp_action` = splits/swaps/ticker-renames (always a dated txn, **never a registry rename**); **shorts** = negative-quantity storage + derived `Securities Sold Short` liability; naming `journal_id`/`metadata`-jsonb; reclass-history deferred to V2. **Sec re-confirmed GREEN-with-conditions** (Trade constraints = INSERT+UPDATE overlay triggers; **the reclass-history deferral is COUPLED to the monthly-report snapshot shipping in V1 as a true immutable freeze — if `C+` slips to V2, reclass-history reopens**). Ledgers flat **§10 3 · DEFINER 3**. Design records: `temp/settle-before-import-conventions.md` + `temp/double-entry-design-v2.md`. **NEXT: either begin the double-entry migrations (M1 Category-as-class = smallest first step, un-gated) OR the V1 FEATURE CLUSTERS (§2.1.2+ net-worth, the active build track). NOTE: the monthly-report snapshot is now load-bearing (Sec Condition B) — must ship V1 as a real immutable freeze**; prior v1.98 — **ADR-031 double-entry GL design FORMALIZED (F/CTO-ratified 2026-07-23; PR #209, merge `bb76894`) + ADR-025 Amendment 1.** Event-sourced double-entry general ledger as a **derived layer** over the immutable `account_trans` ledger — no rewrite, no stored `journal_entry`/`journal_line` (classic Approach B REJECTED; the C→B "ladder" retired). Model (9 decisions): facts-vs-interpretation event-sourcing (immutability = audit of FACTS, not classification — the ADR-025 amendment); three buckets + manual A/R/A/P + hierarchical chart-of-accounts escape hatch (`M-hier`); Category-as-class (no `flow_class`/`normal_balance` column); dual **book/market** valuation + `basis_adjust` events + Unrealized-Gains equity line; imputed contras + `journal_group_id` grouping + virtual scratch/Suspense (`Σ=0`-at-close). Sec **conditional-GREEN (no veto, 2026-07-23)** — binding conditions on M1-evt/M2 (append-only reclassification history not jsonb; **per-tenant scratch/Suspense = standing VETO**; matched-tenant `journal_group` fence); two new Decision-3 instances forward-flagged (`journal_group_id` #12 + lot-match FK); **§10 stays 3 · DEFINER allowlist stays 3** (every fence INVOKER). NO migrations built — M1→M4-GL + M-hier deferred, each joint-review-mandatory; the category→class map / event vocab / grouping rule / basis semantics are a **settle-before-import** one-way-door. Design of record: `temp/double-entry-design-v2.md`. NEXT: the V1 FEATURE CLUSTERS (§2.1.2+ net-worth / §2.2 allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report); the double-entry GL is a formalized future V1.x track gated on settle-before-import**; prior v1.97 — **TOTP GA REACHED — Auth-3b generally available. The C2/C3 GA-gate docs LANDED (PR #207, merge `b85fd51`): SECURITY §4.5 RT-29 (aal2 DB step-up backstop + MB-1 guard + Decision-7 domain tighten, ADR-029) + RT-30 (recovery posture — `026`/`027` service_role-only default-deny stores + scrypt algo-of-record + 5/hr rate-limit + atomic gate/consume + downgrade-first + 4th RT-26 factory; ADR-030 Amdt 1 + ADR-016 D3); catalog 28→30 (29 active), RT-30 annotates "GA-gate … now satisfied", both NOT §10-catalogued → §10 stays 3. Then the last discoverability gap CLOSED: a Security nav link → `/settings/security` added to the authed header (`+layout.svelte`, between email + sign-out; plain `<a>`, tokens-only per ADR-013 P5, no server surface, touches no joint-review-mandatory surface → no Sec gate). A signed-in user can now FIND MFA setup → enroll TOTP → step up → DB-enforced aal2 → self-recover a lost phone. svelte-check 0 · vitest 213 · build clean; ledgers flat DEFINER 3 · §10 3 · Decision-3 unchanged; migrations `001`–`027` live. NEXT: the V1 FEATURE CLUSTERS (§2.1.2+ net-worth / §2.2 allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report)**; prior v1.96 — **Auth-3b COMPLETE end-to-end — Slice 2b LANDED: `027 mfa_recovery_attempt` rate-limit store (PR #204) + the self-service recovery app (PR #205, merge `5cb44be`): the **4th RT-26 surface** (`supabase-admin.ts` factory, ADR-016 Decision 3, allowlist 3→4 intra-instance §10-stays-3) + `/mfa/recover` (atomic rate-gate + constant-time compare + atomic consume + downgrade-first + admin deleteFactor + users_id-from-session) + Node built-in async `crypto.scrypt` hashing (**bcryptjs DROPPED**, zero runtime hashing dep; ADR-030 Amendment 1) + nodemailer notify (fail-soft, deploy-gated) + display-once codes UI; Sec hard-gate GREEN 9/9; svelte-check 0 · vitest 213 · smoke 10/10; migrations `001`–`027` live; ledgers flat DEFINER 3 · §10 3 · Decision-3 unchanged. A user can enroll TOTP → step up → DB-enforced aal2 → self-recover a lost phone. NEXT: C2 SECURITY posture entry + C3 checklist, before TOTP GA**; prior v1.95 — **Auth-3b Slice 2a LANDED — recovery-code store: migration `026` `pfin.mfa_recovery_code` (service_role-only, app-side bcrypt/argon2-hashed one-time backup codes; RLS + zero authenticated grant/policy → default-deny both layers) + the column-scoped `grant select(users_id),update(mfa_policy) on user_settings to service_role` (the guard-exempt recovery downgrade) + ADR-030 (service_role forced by construction; CV-R1 empirically BLOCKED — aal1 can't unenroll a verified factor) (SELF-291/PR #202, merge `1dbabfb`); Sec joint-review GREEN + default-deny pgTAP plan(13)/376 PASS; migrations `001`–`026` live; the 4th RT-26 surface + ADR-016 amendment deferred to Slice 2b; ledgers flat DEFINER 3 · §10 3 · Decision-3 unchanged**; prior v1.94 — **Auth-3b Slice 1 COMPLETE — app flow LANDED: TOTP enrollment (`/settings/security`) + step-up (`/mfa/step-up`) + the fail-closed `requireStepUp` guard in the `hooks.server.ts` chokepoint (N1/N2/N3 reconciliation; N2 keyed off GoTrue AAL) (SELF-291/PR #200, merge `f7b96c8`); Sec joint-review unconditional GREEN (C-APP1 nonce-only CSP confirmed → `{@html}` QR safe); svelte-check 0 · vitest 181 · 13/13 live smoke; opt-in TOTP now ENFORCED (`025`) AND usable end-to-end; ledgers flat DEFINER 3 · §10 3 · Decision-3 unchanged**; prior v1.93 — **Auth-3b Slice-1 DB enforcement LANDED: migration `025` — the per-user-conditional aal2 step-up backstop (14 tables / 31 policies, reads+writes) + the MB-1 downgrade guard (BEFORE UPDATE trigger, INVOKER, closing the aal1 self-downgrade of the backstop's own control variable `mfa_policy`, F/CTO Option A) + the `('none','totp')` domain tighten (`passkey` deferred to Auth-6, additive; clause+guard stay set-referencing) (SELF-291/PR #198, merge `fa8900c`); ADR-029 (Decisions 1–7) + Slice-2 recovery invariants locked; Sec joint-review GREEN (MB-1 cleared, C1 pgTAP battery signed off; full suite 363/23 PASS); migrations `001`–`025` live, ledgers DEFINER 3 · §10 3 · Decision-3 unchanged**; prior v1.92 — **Auth & Accounts foundation LANDED: Auth-1 SMTP scaffolding (SELF-290/PR #193) + Auth-2 signup/login/session/confirmation (SELF-285/PR #195) + Auth-3 MFA substrate `024` `pfin.user_settings`+`mfa_policy` (SELF-286/PR #196) — the app is now browser-reachable; F/CTO Option-B split → real DB-enforced TOTP is Auth-3b/SELF-291; email-2FA DROPPED (SELF-287)**, migrations `001`–`024` live, ledgers **DEFINER 3 · §10 3 · Decision-3 11**; prior v1.91 — **SELF-211 §2.1.1 headline Net Worth read-surface LANDED — first V1 feature surface on the complete `001`–`023` substrate; `pfin.fn_compute_nav` rendered as a single trustworthy number (Option A; composition → V1.1/SELF-225); SELF-210 reconciled to a verification issue**, PR #191 / merge `69f531a`; ledgers flat **DEFINER 3 · §10 3 · Decision-3 11**; **surfaced the no-`/login` platform gap → new Auth & Accounts project (SELF-285–290): SMTP→signup/login→TOTP MFA + per-user `mfa_policy`→email-2FA→recovery→passkeys V1.x**; prior v1.90 — **task #13 ADR-011 Decision-3 enumeration fold-in DONE — canonical #1–#11 in the body list, drift-prone dual-count retired**, PR #189 / SELF-284; §10 3 · Decision-3 = 11 labels / 9-of-11 DDL-realized; prior v1.89 — **migration `023` `pfin.account_trans_annotation` overlay LANDED — the 015–021 substrate is COMPLETE (migrations `001`–`023` live)**, PR #187 / SELF-283; Decision-3 +1 [#10], §10 3; prior v1.88 — **migration `022` `pfin.user_asset_category` allocation junction LANDED**, PR #185 / SELF-282; Decision-3 +2 [#8/#9], §10 3; prior v1.87 — **OQ-2 provider-agnostic connect seam + in-app SimpleFIN connect SHIPPED**, PR #183 / SELF-281; prior v1.86 — **#16 `safeGetSession` unvalidated-session hardening SHIPPED**, PR #181 / SELF-280; prior v1.85 — **#14 CA-2 recurring reachability probe SHIPPED**, PR #179 / SELF-279) — **the `## Pending (immediate)` CURRENT block below holds the authoritative resume state** (the **SELF-212 frontend-connect wave LANDED** [PR #175, merge `d8cbbac`, v1.83 — worker Option-C admission endpoint + credential-less api/src relay legs (SELF-197) + Plaid Link UI (SELF-198) + RT-27 private-bind fence + ADR-027 (hh)/ADR-028/ADR-008; **§10 count 2→3**; SELF-197/198 Done] + **#12 ADR-028 per-route CSP** [v1.84, PR #177 — nonce-mode CSP + PLAID_ENV-gated host + CSP-3 amendment]; worker PR #1 [Foundation + Plaid path, PR #165] + the **credential-admission slice** [`connect()`/`revoke()` + Vault + dev CLI, PR #167] + the **account-mapping slice** [`landAccounts` + `021` dedup index + `admit --map`, PR #169] + the **SimpleFINAdapter** [slice 3a, PR #171] + the **sync scheduler** [slice 3b, PR #173] LANDED; migrations `001`–`021` live; the provider-sync worker is functionally complete through the schedule loop (admit → map → sync) — **SELF-212** (production Plaid + browser→worker relay) is next). Prior detail retained for trace: (**Phase 6 Build Loop — authoring the `015`–`021` ingest/valuation substrate; `015` `linked_source` fold + `016` `pfin.asset` registry + `017` `account_trans` investment/fence/RPC + `018` provider snapshots + `019` valuation core LANDED (5 of 7 — PR #150/SELF-273/v1.72; PR #152/SELF-274/v1.73; PR #154/SELF-275/v1.74; PR #156/SELF-276/v1.75; PR #158/SELF-277/v1.76). NEXT: author `020` (allocation junction — novel-fence site 4). Migrations `001`–`019` live; ledgers DEFINER 3 · §10 2 · Decision-3 8.**) The full V1 holdings/valuation/allocation/net-worth data model was designed, ratified, and Sec-GREEN in v1.71 (all in `temp/015-*`); authoring is underway, 5 of 7 landed. **`015`:** R-14 `linked_source` fold + Decision-3 #6. **`016`:** `pfin.asset` universal registry (hybrid RLS; committed currency seed). **`017`:** `account_trans` investment cols + the novel `security_id` fence (Decision-3 #7, load-bearing under service_role) + `fn_ingest_transactions` RPC. **`018`:** provider snapshots (`holdings_checkpoint` activation + NEW `account_balance_checkpoint`; append-only + service_role INSERT; merge-block #8). **`019`:** valuation core — the **uniform roll-forward** model (F/CTO 2026-07-15, "won't rearchitect"; supersedes Rule A): `eod_price` (OWD-A grain + `+provider_implied` vocab) + `fn_holdings_as_of` (roll-forward: checkpoint anchor + Σ strictly-after) + `fn_compute_nav` (uniform `qty × eod_price`, D-first source-priority `manual > exact > provider_implied`) + the A1 `holdings_checkpoint.security_id` fence (**Decision-3 #11, count 7→8**) + the `018`-deferred cash latest-view (`security_invoker=true`); FMP OUT of V1 (a future higher-priority price source; provider-implied fills the floor). Remaining design to land in `020`–`021`: allocation junction (`020` `asset_id`+`sub_cat_id` = #8/#9) + annotation overlay (`021` `sub_cat_id` = #10); **Decision-3 8 → 11** (020/021 realize the reserved #8/#9/#10; #11 already realized at 019); `account.sub_cat_id` DORMANT. **2 migrations `020`–`021`** remain (each Sec joint-review + C6 two-tenant pgTAP battery). Migrations `001`–`019` live on main; ledgers on main **DEFINER 3 · §10 2 · Decision-3 8**. **NEXT: author `020`.** **Deferred follow-on: the provider-implied Worker deliverable** (symbol→`security_id` resolution + global-asset registration + `provider_implied` `eod_price` write — makes `019` functionally live end-to-end; Sec-gated: div-by-zero / tenant-binding / `ON CONFLICT` upsert). **DATA BLOCKERS (F/CTO): registry seed export + sheet column-map + txn sign-convention** (gate seed/backfill + import, not schema DDL). Full design record: `temp/015-ingest-substrate-design.md` (§16 ledger) + `temp/015-architect-design-spec.md` + `temp/015-sec-review.md` + `temp/015-adr-amendments-draft.md`. CHANGELOG.md v1.75 → v1.76. (Prior: `018` provider snapshots v1.75; `017` account_trans investment v1.74; `016` pfin.asset registry v1.73; `015` linked_source fold v1.72.)

## Project / Initiative

| Field | Value |
|---|---|
| Project name | mosko-fintech |
| Linear Initiative | _TBD — created at Phase 4 entry per ADR-009 Decision 7_ |
| Status | Active |
| Owner | F/CTO |
| Target ship date | _TBD — V1 sub-version sequencing defined as M1's "Populate product milestones" output_ |
| Health | On track |

## Current Phase

| Field | Value |
|---|---|
| **Phase** | **Phase 6 (Build Loop) — entered 2026-06-29 per [ADR-020](DECISIONS.md#adr-020) Phase 5 close + Phase 6 entry approval.** Phase 5 (Workshop Setup) ✅ Complete 2026-06-29 — all 7 exit criteria PASS + mosko extensions; **SELF-186 V1.0 first-implementation smoke-test PASSED** (validated the full Architect→Sec→DevOps-CI→branch-protection→F/CTO build loop; `001_pfin_foundation.sql` live on main, db-tests green); branch protection on main configured (was assumed-but-absent — caught at the exit walk); 9 steps across PRs #97–#116. **Meta-process M2 (Implement + Verify) Active.** Phase 4.5 (Agentic Flow Ramp) SKIPPED per [ADR-018](DECISIONS.md#adr-018). |
| **Active work surface** | **Phase 6 (Build Loop):** team-lead orchestrates per-issue; execution agents (Backend / Frontend / Architect / QA / Sec / DevOps) dispatched per Linear issue through the validated build loop (role-agent → QA + Sec joint-review → DevOps CI → branch-protected PR → F/CTO ratify); milestone-rotation per [ADR-017](DECISIONS.md#adr-017) D2. Consumes locked PRD + ARCH + SECURITY + DESIGN + Linear V1.0–V1.4 (89 issues) + BACKLOG §7 (V1.5 + V1.final). **Foundation live:** `001_pfin_foundation` (`pfin` schema + `fn_refresh_updated_at`); first base-table work = SELF-187+ (V1.0 — Platform foundation + Onboarding + Net worth §2.1.1). |
| **Entry-gate items** | (1) **Phase 4.5 disposition F/CTO-ratified SKIP** at Phase 4 close per [ADR-018](DECISIONS.md#adr-018) — Phase 4 6-Wave execution materially exercised the agentic loop. (2) **SELF-186 (B1 Apply migration `001_users_id_rename.sql`) ratified as Phase 5 close-gate V1.0 first-implementation-issue smoke-test** per [ADR-018](DECISIONS.md#adr-018). (3) Wave 1 A1-A5 Linear-issue verification (SELF-181-185 scaffold + auth + CI + Coolify + Discord) at Phase 5 Step 1 verification. |
| **Phase 5 progress** | **Steps 1–8 landed.** Step 1 (entry gates + team setup) ✅. Step 2 (build-time agent defs DevOps → Backend → Frontend → QA; PRs #97–#100) ✅. Step 3 (Architect/PM/Sec refinements; PRs #101–#103) ✅. Step 4 (CI test-fixture + per-Wave RLS battery; PRs #104 W1 / #105 W0 / #106 W2+W3) **substantially closed** — closes SD-15 + RT-15 implicit gaps. Step 5 (per-directory CLAUDE.md `supabase`/`workers`/`api`/`web`; PR #108) ✅ — Sec joint-review GREEN, §10 three-axis CLEAN; `api`/`web` forward-looking. Step 6 (skills library — 4 skills: `brief-drift-catch` CoS / `milestone-rotation` DevOps / `apply-migration` Architect / `spawn-sec-joint-review` PM; PR #110) ✅ — role-owned, each boundary-checked via `brief-drift-catch` (counts clean: D3=7, D4 ledger=2, DEFINER 3→2). Step 7 (Linear MCP verify + milestone-rotation rehearsal; PR #112) ✅ — `docs/linear-setup.md` lands; rotation gate dry-run validated (1/16-Done milestone → correctly blocks); write-path permission proof on SELF-269 (reverted clean); milestone model verified live (native project-milestones V1.0–V1.5 + V1.x + V1.final). Step 8 (pre-commit hooks + secrets management; PR #114) ✅ — `.husky/` (ruff + hadolint; JS deferred to Phase 6) + 3 per-surface `.env.example` + `secrets-manifest.yml` CI/production non-overlap fence (fail-closed); **Sec joint-review GREEN** (AMBER→GREEN, TBC present-tense→forward-discipline reword); §10 ledger untouched (Path B). Step 9 (exit-criteria walk + SELF-186 close-gate smoke-test; branch protection; PRs #116 + close PR) ✅ — 7/7 exit criteria PASS; SELF-186 PASSED (full build loop validated, db-tests green); branch protection on main configured (was absent — exit-walk catch); ADR-020 phase-gate. **Phase 5 ✅ COMPLETE 2026-06-29; Phase 6 (Build Loop) entered.** |
| **Authoritative state files** | [`docs/PRD/index.html`](docs/PRD/index.html) + [`docs/ARCH/index.html`](docs/ARCH/index.html) v1.0 + [`docs/SECURITY/index.html`](docs/SECURITY/index.html) (Wave 6 Gate E SD-10 + SD-12 storage-surface extensions per PR #95) + [`docs/DESIGN/`](docs/DESIGN/) + [`BACKLOG.md`](BACKLOG.md) §5 + §7 + [`DECISIONS.md`](DECISIONS.md) (40 ADRs through ADR-020 Phase 5 close + Phase 6 entry; ADR-011 Decision 10 greenfield-reconciliation amendment at v1.53) + [`supabase/migrations/`](supabase/migrations/) (`001`–`046` live; `046_fn_aggregation_has_stale_constituent` is the latest — SELF-208 read-only INVOKER staleness-detection primitive (`045` = SELF-206 webhook two-phase RPC pair + A3 `sync_cursor`)) + [`docs/linear-setup.md`](docs/linear-setup.md). Linear V1.0–V1.4 + BACKLOG §7 V1.5 + V1.final are the live work-tracking layer; rotation per [ADR-017](DECISIONS.md#adr-017) Decision 2. |
| **Team task trackers** | `phase-4-scoping` (torn down at Phase 4 close-PR merge per [ADR-003](DECISIONS.md#adr-003)). `phase-5-workshop-setup` (torn down 2026-06-25 after Step 4 substantial close). |
| **Outer category** | **I+V (Phase 6)** per [ADR-009](DECISIONS.md#adr-009) Decision 2 — mosko Phases 5–6 sit under template's Implement+Verify outer frame; M2 (Implement + Verify) Active. |
| **Started** | 2026-06-04 (Phase 5 entry); Phase 4 entry 2026-06-02 → close 2026-06-04 (3 calendar days; fastest phase-completion in mosko history per Phase 4 lessons-learned). |
| **Driver agents** | **Phase 5:** CoS (lead on workshop setup + build-time agent definitions + per-directory CLAUDE.md); DevOps (CI test-fixture + secrets-non-overlap + milestone-rotation skill; bootstrapped first per WORKFLOW.md); Architect (consult on schema-migration ordering + per-directory CLAUDE.md technical content); Sec joint-review at build-time agent posture review + secrets-manifest lock + RT-15 parity-fixture posture lock; PM consult on per-agent Linear permission scope; F/CTO sign-off on every agent definition + skills + secrets manifest. |
| **Gate criteria** | **Phase 5 exit: ✅ 7/7 PASS + extensions** (2026-06-29 per [ADR-020](DECISIONS.md#adr-020)) — task assignable end-to-end ✅ (SELF-186); CI on clean checkout ✅; branch protection on main ✅ (configured at close — was absent); 10 agent defs w/ Linear scope ✅; Phase 0.5 re-signed-off ✅; invoke-by-name ✅; agent+Linear-issue end-to-end ✅ (SELF-186); §10 discipline preserved ✅ (grain-count reconciliation carried); SD-15 + RT-15 closed ✅; rotation rehearsal ✅; SELF-186 smoke-test ✅ PASSED; lessons-learned (8 patterns) added ✅. **Phase 6 gate** (per WORKFLOW.md Phase 6 scaffold): V1 issues ship through the build loop; per-issue QA + Sec joint-review on V1-SHIP-BLOCK surfaces; milestone-rotation at milestone close. |
| **New memory entries this session** | None pending at Phase 4 close. Phase 4 lessons-learned subsection codifies 12 durable patterns in WORKFLOW.md; candidate memory codifications (brief-vs-canonical-ADR cross-check boundary class; 2-teammate independent verification) may surface at Phase 5+ application sites. |

## Active Feature

| Field | Value |
|---|---|
| Feature | _Phase 4 entered 2026-06-02 per joint Phase 2 + Phase 3 close. First Phase 4 work activates Linear MCP + creates "V1 launch" initiative. **No individual Linear-feature granularity until Linear MCP activates** (Phase 4 Step 1)._ |
| Linear issue | _N/A — Linear activates as part of Phase 4 Step 1_ |
| Branch | _N/A — Phase 4 entry via this joint-close PR on `meta/phase-2-3-close`. Subsequent Phase 4 work spawns per-feature branches: `feature/<N>-<slug>` per [ADR-009](DECISIONS.md#adr-009) Decision 9 once Linear issues exist; pre-Linear Phase 4 admin work spawns on `phase/plan-*` branches._ |
| Status | _Phase 1 closed 2026-05-26 (PRD v1.0); Phase 2 + Phase 3 closed jointly 2026-06-02 (design system v1.0 + ARCH v1.0); Phase 4 entered 2026-06-02. First downstream deliverable: Linear "V1 launch" initiative + project decomposition._ |

## Recent activity (last 5 entries)

Convention per [ADR-017](DECISIONS.md#adr-017): last 5 entries; 1-sentence each; detail in [CHANGELOG.md](CHANGELOG.md).

- **2026-07-31 (Phase 6 — V1.0 MILESTONE COMPLETE — rotation to V1.1 (Net worth full); meta/doc PR, v1.128)** — all 4 V1.0 sub-milestones closed (Onboarding §2.4 13/13 · §2.1.1 NAV 2/2 · Platform foundation · Auth foundation); **the first full V1 product milestone.** **V1.1 (Net worth full, SELF-217–229) → current** per [ADR-017](DECISIONS.md#adr-017) D2; NO §7 promotion (V1.1–V1.4 already in Linear); **SELF-184** (Coolify→Discord) parked in **Phase-7 deploy** (`V1.x Cross-cutting infra`). No phase transition — Phase 6 continues into V1.1 build; Phase 7 (Deploy, incl. a live VPS/Coolify) Not-started. Also landed **`docs/local-dev.md`**. Governance FLAT (§10 3 · D3 15/13 · DEFINER 4 · RT-26 4).
- **2026-07-31 (Phase 6 — SELF-288 Auth-5 account recovery LANDED — full-stack; PR #266, merge `ee71671`, v1.127)** — the **LAST V1.0 build feature.** Password-reset half (AC#1+#4; MFA recovery codes AC#2 already delivered at SELF-291) + break-glass docs (AC#3), riding GoTrue (no migration/service_role). Anti-enum byte-identical uniform response + per-IP/per-email rate-limit; **MFA step-up wired into reset (F/CTO Option A)** — app-layer-enforced aal2 (`/reset-password` de-exempted; boundary test = aal2-non-bypass lock); lost-authenticator → `/mfa/recover`. Sec GREEN (Option-A = strict security improvement); QA 16 always-on + 3 live-dbit, **both legs proven live** (no-step-up blocked / step-up completes). Combined 459 pass. **§10 3 · D3 15/13 · DEFINER 4 · RT-26 4.**
- **2026-07-31 (Phase 6 — V1.0 §2.4 ONBOARDING SUB-milestone COMPLETE (13/13); §2.4-close doc-sync, v1.126)** — all 13 §2.4 issues Done (SELF-196–204 + 206–209; SELF-205 re-scoped to V1.6 at ADR-035, hence the 204→206 gap). **NO V1.0→V1.1 rotation yet:** SELF-184 (Coolify→Discord, F1 PII gate) re-homed to **Phase-7 deploy** (→ `V1.x Cross-cutting infra`; needs the not-yet-provisioned V1 greenfield-VPS Coolify + F1 audit; F/CTO-owned) — closing Platform foundation — leaving **SELF-288** (account recovery) the sole remaining V1.0 build straggler; rotation lands when it does. No §7 promotion pending (V1.1 already populated). Lands the SELF-208 design-spec §4 "tag→disclosure" delta + the ledger update. Governance FLAT (§10 3 · D3 15/13 · DEFINER 4 · RT-26 4).
- **2026-07-31 (Phase 6 — SELF-208 §2.4.4.c non-silent staleness markers LANDED — full-stack; PR #264, merge `415ea44`, v1.125)** — read-only INVOKER `fn_aggregation_has_stale_constituent` (migration `046`, auth.uid()-scoped, no params; reads non-healthy active `linked_source` via the `043` view; per-item status for per-status affordance) + `StaleConstituentBadge` (canary hue, WCAG keyboard-disclosure, re-auth link vs informational) on the NAV surface + fail-soft loader. F/CTO-ratified `is_active=TRUE`. Sec RT-13 GREEN · QA pgTAP `plan(17)` + 14 vitest · api 400 · **§10 3 · D3 15/13 · DEFINER 4.** The last V1.0 §2.4 feature.
- **2026-07-31 (Phase 6 — SELF-209 §2.4.5 RLS policy lock + V1-SHIP-BLOCK close-gate LANDED; PR #263, merge `f8f72d7`, v1.124)** — **SEAM-only** `plan(36)` battery composing the ~19 per-migration two-tenant batteries + 4 net-new inversion-checked tests (DEFINER-allowlist posture / manual-entry edit-path immutability / `account_users` deny-by-default [AC2 deferred per SELF-190] / §2.4 cross-tenant sweep); **no RLS hole found**; 3 deferrals explicitly certified. Full suite 44 files/**786** PASS. Sec joint-review GREEN, no mods. **§10 3 · DEFINER 4 · Decision-3 15/13** — all +0. The V1.0 §2.4 close-gate.

## Pending (immediate)

> **CURRENT (2026-07-31):** **V1.0 MILESTONE COMPLETE — the first full V1 product milestone; rotated to V1.1 (Net worth full).** All 4 V1.0 sub-milestones closed (Onboarding §2.4 13/13 · §2.1.1 Current NAV 2/2 · Platform foundation · Auth foundation); the final build feature was **SELF-288** (Auth-5 account recovery — password reset + MFA-step-up recovery flow, v1.127; Sec GREEN, QA both-legs-live). **SELF-184** (Coolify→Discord) is parked in **Phase-7 deploy** (`V1.x Cross-cutting infra`) — it needs a live VPS/Coolify, not provisioned (Phase 7 Not-started). **CURRENT BUILD = V1.1 — Net worth full (§2.1.2–§2.1.7, SELF-217–229), already populated in Linear** (no §7 promotion due per the going-forward carve-out). No phase transition — Phase 6 (Build Loop) continues into V1.1 build. Local dev setup: see [`docs/local-dev.md`](docs/local-dev.md). — *prior v1.119 —* **SELF-311 default taxonomy provisioning on signup LANDED — full-stack (PR #252, merge `9456fc5`, v1.119; SELF-311 Done).** — *prior v1.119 —* **SELF-311 default taxonomy provisioning on signup LANDED — full-stack (PR #252, merge `9456fc5`, v1.119; SELF-311 Done).** Route Z-INVOKER two-phase INVOKER RPC (migration `045`) + OWD-A A3 `sync_cursor`; RT-05 = asymmetric ES256/JWK (Option 1 — worker fetches the PUBLIC JWK, `api/src` credential-less + verifies locally with `jose`); C-X2 sync-first/gate-at-completion (**Close-i**); per-path idempotency (transactions cursor-dedup / state `provider_event_id` gate); Sec GREEN + 4 mods (incl. Decision-3 #15 fold-in 14/12→15/13); **§10 3 · Decision-3 15/13 · DEFINER 4 · RT-26 4**; QA pgTAP `plan(35)` + 9-case RT-05 ES256 battery. **Next: SELF-208 (§2.4.4.c non-silent staleness markers) + SELF-209 (§2.4.5 RLS policy lock + V1-SHIP-BLOCK verification battery) — the last two V1.0 §2.4 Onboarding issues; then V1.0 §2.4 close-gate.** — *prior v1.119 —* **SELF-311 default taxonomy provisioning on signup LANDED — full-stack (PR #252, merge `9456fc5`, v1.119; SELF-311 Done).** ADR-036 (B1 lazy authenticated upsert + A1 `pfin.taxonomy_default` reference table + first-access-lazy): migration `041` (global 63-row taxonomy_default + scoped `authenticated` INSERT-only policy on user_taxonomy `with check users_id=auth.uid()`, 025 aal2 backstop Shape A) + Backend provision (existence-guarded, fail-soft, both domains) at login/signup + layout guard. Sec GREEN (review-surface) · QA pgTAP `plan(16)` + paired 009 fix · **§10 3 · Decision-3 +0 · DEFINER 4 UNCHANGED**. QA empirically corrected the aal2 rationale (WITH CHECK evaluates pre-`ON CONFLICT`; decision unchanged — safety = the existence-guard). **Closes the SELF-200 open-signup taxonomy dead-end (BACKLOG §7.5 O1).** **Next: remaining V1.0 §2.4 — SELF-199 (§2.4.1.d account-share/attribute capture) + the SELF-209 §2.4.5 RLS close-gate; SELF-206/207/208 gated on Plaid SELF-212.** — *prior v1.118 —* **SELF-200 pending-symbol classification LANDED — full-stack §2.4.1.e (PR #250, merge `907826e`, v1.118; SELF-200 Done).** Derived-view (no migration): "pending" = ever-transacted MINUS classified, computed at read time; header badge → `/portfolio/classify` picker (non-binding provider hint, AC3); classify UPSERT into `user_asset_category` (`022`) through Decision-3 fences #8/#9. Absence-as-pending, reconciled from the schema-impossible AC1 per migration `009` intent. Sec GREEN · QA pgTAP `plan(17)` + app 7/7 · svelte-check 0/0 · vitest 272 · pgTAP 653 · CI 12/12 · **§10 3 / Decision-3 / DEFINER 4 UNCHANGED** (no migration; reuses `022` fences). **Next: BACKLOG §7.5 O1 default-taxonomy-on-signup** (V1-SHIP-BLOCK, both domains, promoted into V1 per F/CTO 2026-07-28 open-signup ruling) — **Architect design pass is the immediate next step**; then remaining V1.0 §2.4 (SELF-199 + the SELF-209 §2.4.5 RLS close-gate; SELF-206/207/208 gated on Plaid SELF-212). — *prior v1.116 —* **SELF-204 manual↔provider duplicate detection + per-account sync-history LANDED — full-stack §2.4.3.c (PR #247, merge `b202784`, v1.116; SELF-204 Done).** Detection-only (SELF-204 detects / SELF-205 interprets); migration `040` (index relax + `p_import_hash` + `manual_provider_dup_candidate` detection view + owner-semantics per-connection `linked_source_sync_history` view) + shared cross-tier `import_hash` (canonical + generated copy + run-always REQUIRED `dedup-hash` drift fence; ADR-034 Amdts 1/2) + full-stack app. Sec GREEN ×5 · QA `plan(22)` · §10 3 / Decision-3 14/12 / DEFINER 4 UNCHANGED. **Next: remaining Onboarding §2.4** — SELF-199/200 (+ SELF-203/204 close-out) + the **SELF-209** §2.4.5 RLS close-gate. **SELF-205 REMOVED from the V1.0 §2.4 set (ADR-035, v1.117):** its per-transaction reconcile (`reconciled_flag`/B1/B2) is SUPERSEDED — reconciled-by-construction via the double-entry GL — and the residual external-agreement need is re-scoped to a read-only period statement-vs-GL tie-out under the new **V1.6 — Reconciliation / statement tie-out** milestone (retains ID; verified not a SELF-209 blocker). **Superseded prior (SELF-203, v1.114):** **SELF-203 manual stock-split entry LANDED — full-stack (§2.4.3.b; PR #244, merge `686f412`, v1.114; SELF-203 Done).** Position-level book-neutral `corp_action` entry (migration `039` + `fn_create_stock_split` INVOKER RPC + `plan(21)` battery + Backend + Frontend), single-account, source-of-truth-gated; OWD-1→C (per-lot deferred, §7.3 G2) + OWD-2. Preceded by the ADR-033 model-ratify gate + re-scope (#242/#243). Sec GREEN · QA 21/21 · CI green · §10 3 / DEFINER 4 / Decision-3 14/12 UNCHANGED. **Next: remaining Onboarding §2.4 issues** (SELF-204 Plaid-vs-manual dedup + sync-history audit; SELF-205 reconciled-flag; SELF-199/200). Generic 'other' non-cash event V2-reserved; transfer-in-kind → §7.3 G1 (later-V1.x GL-substrate). **Prior (2026-07-25):** **Phase 6 Build Loop active — 🎉 DOUBLE-ENTRY GL COMPLETE. `037` GL-completion LANDED (SELF-301 Done, PR #234, merge `72b88f9`) — the tenth-and-LAST GL migration. The GL project is DONE: M1 + M2.5 + M1-evt(A1/A2/B) + M2 + M3-basis + M4-GL-read(035) + M4-GL-write(036/037) = 10 migrations, `001`–`037` live.** `037` = fn_gl_entries matched-SELL realized-gain (Option A, one line to equity, character in tax layer, balanced-by-construction) + fn_journal_close_balance (future-dated reject all-types → compound no-Suspense-residual → write-once close snapshot) + membership/classification freeze guard on closed journals + F1 (`journal_insert status='open'`, aal2 preserved). Compound close law refined from the vacuous total-Σ (F/CTO hardening). §10 3 · DEFINER 4 · Decision-3 14/12 UNCHANGED — closes at zero ledger movement. Sec AMBER→FULL GREEN · QA `plan(26)` · Backend suite 575 PASS. **SELF-298 LANDED (PR #237, merge `1007ec9`, v1.110)** — the pre-existing non-blocking GL cleanup is CLOSED: softened the 4 aal2-gated matched-tenant fence RAISE messages (#10 `023` sub_cat · #12 `033` journal · `030` trade read · `034` basis_adjust read) so "cross-tenant" is no longer the sole asserted cause (now *not found / not visible under current AAL / cross-tenant*; ADR-011 D3 instance-# citations retained) + switched the paired assertions on those messages to `throws_ok(…,'P0001',null,…)` (message-precise `throws_like` kept for business-rule RAISEs). **Brief-drift catch:** the `023` battery F1/F2/F3 (#10) assertions were beyond the Linear AC but would have hard-RED'd CI — caught + flipped. Fences #5/#8/029 untouched (not aal2-gated). Sec + QA joint-review **AMBER → GREEN** (both caught the lone `033:315` (3e) `throws_like`→`throws_ok` miss; fixed). §10 3 · DEFINER 4 · Decision-3 14/12 UNCHANGED — zero ledger movement. Local `supabase test db` 575 PASS; CI green. **SELF-202 LANDED (PR #240, merge `ff61a23`, v1.111) — the FIRST Onboarding §2.4 issue toward closing V1.0 (manual cash entry, append-only per ADR-032; also closed the C3 aal2 gap SELF-304). **Milestone-rotation check 2026-07-26: NOT due** (GL = project not a V1.x milestone; V1.0 not closed). **V1.0 reconciliation:** Platform foundation + §2.1.1 NAV effectively done (stale-opens SELF-210/191/182 flipped Done); the real V1.0 gap is **Onboarding §2.4** — SELF-199/200/203/204/205/206/207/208 + **SELF-209 (§2.4.5 RLS close-gate)** + SELF-184 (Platform Discord). **⏭ IMMEDIATE NEXT: continue Onboarding §2.4 to close V1.0** — SELF-202 (taproot) unblocks SELF-203 (AcctSetup events) / SELF-204 (dedup) / SELF-200 (symbol classify) / SELF-209 (RLS close-gate leg); SELF-199 (post-connect attribute capture) is the parallel co-root. ⚠️ SELF-206/207/208 (§2.4.4 connection lifecycle) are gated on the Plaid production-tier call (SELF-212). GL follow-ups SELF-302/303 → V1.4 tax cluster; 5 SELF-202 fast-follows filed (SELF-305–309).** **GL deferred follow-ups** (Suspense-parked, own future migrations, NOT blocking): wash_sale P&L + substantive corp_action (spin-off/cash-in-lieu). — *prior:* `036` (SELF-300) = the `lot_match` INSERT grant + `wr_access` WITH CHECK policy — the RLS write companion to the already-realized `032` `#14` fence, made EXERCISABLE (not newly realized). F/CTO-ratified **FIFO/specific-lot** basis method (the realized-gain one-way door) + **dependency-split** (`036` write surface / `037` GL-completion); matching inference = Backend/worker. Sec joint-review AMBER→GREEN (B1 write-path battery + B2 `032`-migration; ordering-subtlety + `#14` trigger-disable isolation blessed) · QA `plan(21)` · `032` migrated `plan(21)→(19)` · Backend full suite Files=33 Tests=549 PASS. **§10 3 · Decision-3 14/12 UNCHANGED · DEFINER 4.** — *prior:* `035` (SELF-297) is `pfin.fn_gl_entries` — the SECURITY INVOKER GL/trial-balance read helper (Lock 11, clones `fn_compute_nav`@`019`): flat entry rows, correct-by-construction `SUM(amount_book)=0` (global + per-currency), Suspense-routed residual, `036`-deferred write obligations documented in its CONTRACT block. Sec routine GREEN · QA `plan(13)` inversion-proven · Backend clean-apply `001`→`035` 13/13 + full suite 530 PASS. — *prior:* M3-basis (SELF-296, PR #228, merge `02d5d90`) is `034_basis_adjust_constraints.sql` — the basis_adjust ROW-SHAPE constraints: a single-table CHECK `account_trans_basis_adjust_shape` on `account_trans` (`basis_adjust ⟹ quantity=0 AND security_id NOT NULL AND cost_basis NOT NULL`; reason-independent, role-agnostic — fires under the `017` service_role ingest) + a cross-table INVOKER trigger `fn_account_trans_annotation_basis_adjust_reason` on the `023` overlay (R1 domain `metadata.reason ∈ {depreciation, return_of_capital, wash_sale}` / R2 reason↔amount `return_of_capital ⟺ amount≠0` (anchored to the frozen `004` amount) / R3 additive guard `reason ⟹ basis_adjust`; BEFORE INS/UPD, `search_path=''`, NULL-safe; a `030`-Trade-trigger clone) + a **reserved-metadata-keys registry** (`reason`=basis_adjust-only [R1/R2/R3-enforced], `action`=corp_action-only [reserved, guard forward-flagged]) on the `023.metadata` comment. **F/CTO-ratified: A-i** (`reason` on the mutable `023` overlay per ADR-031 Amendment 1 item 8 — the immutable `cost_basis`-delta + `amount` facts determine the economics; `reason` is the interpretive label, its edit-audit = the `031` reclass-history) · **B-i** (CHECK + overlay trigger) · **C** (`cost_basis` role-widens acquisition→signed delta, negative for depreciation; NO `017` constraint to relax — verified: only the NaN-fence, no `>=0`, and the one-directional `qty_requires_security` CHECK doesn't collide) · **D** (roll-forward `effective = original + Σ deltas` / accumulated-depreciation `derive by reason='depreciation'` / recapture `min(gain, accum_depr)` DEFERRED to M4-GL) · **KEEP R3** (`metadata.reason` is a RESERVED basis_adjust-only key — the decisive reversibility-asymmetry: reserve-while-empty is free, drop-now-reserve-later needs scrubbing imprinted stray reasons; Architect + Sec both concurred — Sec: "the collision is evidence FOR the guard"). **§10 3 · DEFINER 4 (zero new — both CHECK / INVOKER-trigger; `pg_catalog`-verified 3 authored) · Decision-3 14/12 UNCHANGED** (value/tenancy-neutral — no FK-shaped column; `security_id` already fenced by `#7`). `004` immutability UNWEAKENED (the CHECK is INSERT-time DDL, not the UPDATE/DELETE/TRUNCATE triple-fence). Sec joint-review **CONDITIONAL-GREEN → FINAL GREEN** (all confirms + R3 KEEP + the aal2-gated-INVOKER-read edge benign/honest-message → folds into **SELF-298**); QA `plan(19)` 19/19 **inversion-proven** (2 surgical mutants). **Backend's full-suite directory-mode gate caught + resolved a `030`/`031` fixture collision** — `034` clean-applied + its own battery 19/19 in isolation, but the authoritative full-suite run FAILED: the CHECK rejected `030`'s bare-basis_adjust vocab-test row + R3 rejected two fixtures using `metadata.reason` as a generic jsonb payload on standard rows. Resolved (reshape `030(1a)` to a valid basis_adjust + switch `030(8a)`/`031(3c)` `reason`→`note` + the reserved-keys registry closing the discoverability gap); full suite **517 PASS** (up from 499 — the 18 previously-aborted assertions now run + pass). Backend clean-apply `001`→`034` PG17. Migrations `001`–`034` live. **NEXT in the GL sequence: M4-GL (SELF-297) — `fn_gl_entries`, the book/market GL/trial-balance INVOKER read helper: the LAST GL migration and the consumer of everything deferred (M2's `Σ=0`-at-close, M3-basis's roll-forward/accumulated-depreciation/recapture, the write-path to journal groupings, split reads, imputed contras, Suspense). Non-blocking follow-ups: SELF-298 (fence-message soften — now also `030`/`034` aal2-gated-INVOKER-read messages + the `throws_like`→`throws_ok 'P0001'` switch); the symmetric `metadata.action` guard at the corp_action migration (Sec cond 2 forward-flag, in the `034` header).** — *Prior v1.105 CURRENT retained for trace:* **Phase 6 Build Loop active — M2 LANDED (SELF-295 Done); the Double-Entry GL has 6 migrations in (M1 + M2.5 + M1-evt A1/A2/B + M2).** M2 (SELF-295, PR #223 migration `6fbc67d` + PR #224 paired battery `3e0d5b6`) is `033_journal.sql` — `pfin.journal`, the write-live double-entry GROUPING parent (N `account_trans` legs tied `Σ=0` via a shared journal; grouping, NOT a stored journal-line table; the Escrow/compound pattern, header-comes-after-the-lines) + `journal_id` FK on the `023` overlay + the **Decision-3 #12** matched-tenant leg fence (`fn_account_trans_annotation_matched_journal`; BEFORE INSERT OR UPDATE, INVOKER, NULL-safe fail-closed; **hybrid-resolved** — leg tenant chain-resolved via `trans_id→account_trans→account.users_id`, journal `users_id` read directly; UPDATE-path load-bearing). RLS 4 policies + the `025` aal2 backstop AND-ed into all four; UPDATE WITH CHECK blocks cross-tenant close/reopen + re-tenanting; service_role ungranted; anon zero-grant. **A2 (F/CTO-ratified): write-live shell; the `Σ=0`-at-close balance check DEFERRED to M4-GL** — it runs the book-value imputation engine `fn_gl_entries` (the realized-gain plug + imputed contras are DERIVED, not stored), so **market value plays NO role** in any balance (an F/CTO challenge corrected the design's misleading "GL-projected value" wording; `group_type` selects cash / per-security-qty / book-value-imputation conservation, all stored facts). **B-ii** (defer snapshot cols) **· C-i** (plain `status`; user closes; same-tenant reopen — the fence re-fires on the overlay leg-write, NOT on the journal UPDATE, which is gated by UPDATE WITH CHECK) **· D** (scratch/Suspense = runtime per-tenant `pfin.account` via `013`, NO DDL — a global singleton = the standing Sec VETO, not tripped) **· `journal_group`→`journal` table harmonization** (F/CTO-ratified; extends ADR-031 Amendment 1 item 8; PK/FK `journal_id`). **§10 3 · DEFINER 4 (zero new; #12 fence INVOKER; `fn_refresh_updated_at` reuse) · Decision-3 +1 → #12** (family **14 labeled / 12 DDL-realized**; #12 realizes LAST — non-contiguous behind #13@`029` / #14@`032`; only #3/#4 monthly_report remain V1.3+). Sec joint-review **CONDITIONAL-GREEN, no veto** (#12 pinned + `pg_catalog`-verified; fence airtight both directions incl. under service_role RLS-bypassed; per-tenant-scratch VETO not tripped; aal2 verbatim on all 4 policies; all binding conditions discharged; the misleading fence-message edge ruled fail-closed/safe → **SELF-298** fast-follow); QA `plan(28)` 28/28 (the #12 fence **inversion-proven** — mutating to BEFORE INSERT only flips ONLY the UPDATE-attach assertion RED); Backend clean-apply `001`→`033` under PG17 + directory-mode 498 tests PASS; CI green. **Battery-omission caught + remediated:** the paired `033_journal_rls.sql` was authored untracked in the working tree + omitted from #223 (team-lead push-verification miss) → landed as fast-follow **#224**, restoring same-PR test-pairing (CI 28/28 against the real migration; memory `feedback_verify_paired_artifacts_before_push` codified). Migrations `001`–`033` live. **NEXT in the GL sequence: M3-basis (`basis_adjust` event + `reason` subtype) or M4-GL (`fn_gl_entries` — the book-value/market GL/trial-balance INVOKER read helper, where the deferred `Σ=0`-at-close + the write-path to journal groupings land). Open doc-reconciliation (non-blocking; Architect pass + Sec eyeball): N1 — fold #12/#13/#14 into the ADR-011 D3 body enumeration (now lags 3 realized labels); N2 — fix stale ADR-031 D7/D8 "DEFINER stays 3" → 4; N4 — user-deletion runbook note (`journal.users_id` CASCADE vs `023.journal_id` NO ACTION). SELF-298 = the fence-message soften (#10 + #12 harmonize + battery throws_like→throws_ok P0001).** — *Prior v1.104 CURRENT retained for trace:* **Phase 6 Build Loop active — M1-evt COMPLETE (all 3 slices merged, SELF-293 Done); the Double-Entry GL has 5 migrations in (M1 + M2.5 + M1-evt A1/A2/B).** Slice B (SELF-293, PR #221, merge `ee40ac0`) is `032_lot_match.sql` — the append-only immutable many-to-many `pfin.lot_match` junction (`sell_trans_id`+`buy_trans_id`→account_trans; `quantity_matched`; `match_seq` per-sell re-match batch) recording which buy lots a sell closes (per-lot ST/LT tax character; model (b) many-to-many, since a 1:1 FK can't express mixed-character split sales). **Write-dormant** (029 pattern — fenced shell now; the matching/inference logic + INSERT grant land with M4-GL). **Decision-3 #14 fence** (Sec-pinned, count=ONE per fence relationship): `sell-tenant=buy-tenant` + `sell.security_id=buy.security_id`, BEFORE INSERT, INVOKER, NULL-safe fail-closed. **(4-B) self-versioning** — the `match_seq` batches ARE the lot-match reclass-history (ADR-011 D2); `031`'s DEFINER helper untouched → **no new DEFINER** (supersedes the A2 add-column expectation). **§10 3 · DEFINER 4 · Decision-3 +1 → #14** (family **13 labeled / 11 realized**; #12 journal_group still reserved-unrealized at M2); migrations `001`–`032` live. Sec joint-review **GREEN** (migration + battery); QA `plan(21)` 21/21; CI green. **M1-evt (SELF-293) is fully COMPLETE across A1 (030, event-class vocab + Trade constraints) / A2 (031, reclass-history audit table + DEFINER 3→4) / B (032, lot_match #14).** **NEXT in the GL sequence: M2 (`journal_group` + scratch/Suspense — where #12 DDL-realizes; Sec's standing per-tenant-scratch/Suspense VETO surface) or M3-basis / M4-GL (the read layer). Open: the ADR-011 D3 body fold-in (#12 + #13 + #14) — the canonical enumeration now lags three instances (task #8; Architect ADR pass).** — *Prior v1.103 CURRENT retained for trace:* **Phase 6 Build Loop active — M1-evt Slice A2 LANDED; the Double-Entry GL is building (4 migrations in: M1 + M2.5 + M1-evt A1 + A2).** A2 (SELF-293, PR #219, merge `aec9e9a`) is `031_reclass_history.sql` — the append-only **reclassification-history audit table** (`pfin.account_trans_annotation_history`): ADR-011 **D2 audit-class**, `004`/`005`-mirror immutable (trigger-based, blocks ALL roles incl. service_role), full-snapshot `version_seq` chain, captures the GL-routing-relevant `023` overlay state (`sub_cat_id` no-FK snapshot + `metadata`; not `note`) on INSERT + routing-UPDATE. Sole write path = the new `fn_reclass_history_insert` **SECURITY DEFINER** helper (un-forgeable — authenticated has no INSERT grant) → the project's **FIRST authored DEFINER allowlist growth, 3→4** (committed 4 = `fn_refresh_updated_at` + `fn_grant_creator_access` + `fn_reclass_history_insert` + the reserved general-audit-log slot [SELF-201 Task #7, still unauthored]; authored 3). **F/CTO REOPENED reclass-history to V1** (reverses ADR-031 Amendment 1 §9's V2-deferral; decouples the audit story from the C+ monthly-report freeze bet — heavier-but-better). A team-lead cross-check caught + corrected the Architect's initial "stays 3" over-round → the honest +1 before ratify (disposition A). **§10 3 (unchanged — DEFINER ledger separate, de-conflation SELF-187) · DEFINER 4 · Decision-3 +0** (family 12 labeled / 10 realized; #14 lot-match next at Slice B); migrations `001`–`031` live. Sec joint-review **GREEN** on BOTH veto surfaces (D2 audit-class + the new DEFINER fn) + battery; QA `plan(18)` 18/18; CI green. ADR-031 Amendment 2 + ADR-011 D9 amendment; the DEFINER "3"→"4" doc sweep folded into the v1.103 bookkeeping (supabase/CLAUDE.md + apply-migration/spawn-sec-joint-review skills + architect/security-reviewer/backend-engineer agent defs). **NEXT: Slice B (lot-match buy-reference FK #14) — F/CTO elected to build now (design-first in flight); the last M1-evt piece. Then M2 (journal_group, where #12 realizes) or M3-basis. Open: the ADR-011 D3 body fold-in (#12 + #13) ahead of Slice B pinning #14.** — *Prior v1.102 CURRENT retained for trace:* **Phase 6 Build Loop active — M1-evt Slice A1 LANDED; the Double-Entry GL is building (3 migrations in: M1 + M2.5 + M1-evt-A1).** M1-evt Slice A1 (SELF-293, PR #217, merge `c06d78c`) is the third GL migration on main: `030` widens `transaction_type` to the lean fact vocab `{standard,acct_setup,basis_adjust,corp_action}` (additive; settle-before-import — incumbent has basis_adjust events; GL handling deferred to M3-basis/M4-GL) + `metadata jsonb` on `023` + ONE combined **Trade-constraint trigger** (Sec Condition A: `security_id ⟺ cat='Trade'` + BTO/BTC⟹qty>0, STC/STO⟹qty<0; BEFORE INS/UPD, INVOKER, NULL-safe fail-closed, **UPDATE-path load-bearing**). M1-evt is **3 slices** (F/CTO ratify, walked one-by-one): **A1 = this** (import-critical core) · **A2 = reclass-history audit-class table** (F/CTO REOPENED it to V1 per Sec Condition B — reversing Amendment 1 §6's V2-deferral; the heavier-but-better long-term call, decoupling the audit story from the C+ monthly-report freeze bet; **design-first in flight**) · **B = lot-match buy-reference FK #14** (deferred to realized-gain/tax compute). Sec joint-review **GREEN** (migration + battery, all fail-closed edges + qty=0 boundary locked); ADR-025 amendment + line 759/766 fixes (lot-match #13→#14). **§10 3 · DEFINER 3 · Decision-3 +0 (family 12 labeled / 10 DDL-realized; #12 journal_group reserved, #14 lot-match next at Slice B)**; migrations `001`–`030` live. QA `plan(21)` 21/21; 012 battery 12/12 vs widened schema; CI green. **NEXT: Slice A2 (reclass-history, ADR-011 D2 audit-class — design-first; then author + Sec joint-review + QA), then Slice B (lot-match #14). Open: the ADR-011 D3 body fold-in (#12 + #13 provenance) ahead of Slice B pinning #14; a short ADR-031 amendment recording the reclass-history reopening.** — *Prior v1.101 CURRENT retained for trace:* **Phase 6 Build Loop active — M2.5 (account_trans_split receipt-split overlay) LANDED; the Double-Entry GL is building (2 of the M-sequence migrations in).** M2.5 (SELF-294, PR #215, merge `1c5cead`) is the second GL migration on main: `029_account_trans_split.sql` — a 1:many split-child table under the immutable `account_trans` parent, with **Σ(children)=parent.amount** DB-enforced by a deferred CONSTRAINT TRIGGER, plus a `security_invoker` reconciliation view (T-account Dr/Cr shape). Built to 4 F/CTO-ratified decisions (walked one-by-one): **anchor α** (parent-chain tenancy, mirrors `023`) · **Σ=parent** deferred trigger + recon view · **write-dormant** (`009` — writes deferred to the split-UI PR) · **`sub_cat_id` nullable** + **soft precedence** (M4-GL read rule). The GL track's **first Sec-joint-review-mandatory** migration — Sec **GREEN** on both the migration and the QA battery; Decision-3 **+1 → #13** (Sec-pinned against live ADR-011 [11 labeled/9 realized] + ADR-031 D8 [#12 reserved-by-name]; family → **12 labeled / 10 DDL-realized**; first *forward-reservation gap* — #13 realizes while #12/journal_group stays reserved-unrealized). **§10 3 · DEFINER 3 · Decision-3 12 labeled/10 realized (+ lot-match forward-flagged)**; migrations `001`–`029` live. QA `plan(17)` 17/17 two-tenant pgTAP; CI green. Bookkeeping folded in: reconciled stale D3/§10/DEFINER counts in `supabase/CLAUDE.md` 3(b) + the `spawn-sec-joint-review` skill. **NEXT: the GL sequence continues — M1-evt (event-class vocab refactor + ADR-025 amendment, the heavy Sec-gated step) or M2 (journal_group + scratch/Suspense, where #12 DDL-realizes). Open (non-blocking): the D3-body #13/#12 fold-in (at M2); the split-entry frontend + T-account rendering off the recon view.** — *Prior v1.100 CURRENT retained for trace:* **Phase 6 Build Loop active — M1 (Category-as-class CHECK) LANDED; the Double-Entry GL has moved from design to BUILD.** M1 (SELF-292, PR #213, merge `1a117f9`) is the first GL migration on main: `028_user_taxonomy_cashflow_class.sql` — a domain-conditional CHECK constraining cashflow-domain `user_taxonomy.cat` to the ratified 5-class enum `Revenue/Expense/Transfer/Equity/Trade` (the top-level Cat *becomes* the enforced accounting class; no `flow_class`/`normal_balance` column). Pure DDL, **no backfill** (table V1-write-dormant + empty at migration time per `011`). Build loop role-separated: Architect authored + clean-apply-verified; QA paired the pgTAP battery (`009` `plan(14)→plan(23)`: accept 5 classes / reject non-enum `23514` / asset-domain free / two-tenant isolation) + swept and settled two stale committed fixtures (`009` `Income`→`Revenue`; `023` `Housing`/`Food`→`Expense`). **Not Sec-joint-review-mandatory** (attribute CHECK, no fence surface); QA sign-off was the gate. All CI green (pgTAP + Live-DB + §10 fences). Ledgers flat **DEFINER 3 · §10 3 · Decision-3 11 (+2 forward-flagged)**; migrations `001`–`028` live. Bookkeeping folded in: reconciled the stale §10 "2-instance" count → canonical **3 (RT-22+RT-26+RT-27)** in `supabase/CLAUDE.md` + the `apply-migration` skill. Double-Entry GL Linear project stood up (SELF-292→297, full blocked-by chain M1→M1-evt→M2.5→M2→M3-basis→M4-GL); SELF-292 Done. **NEXT: the sequence continues — M1-evt (event-class vocab refactor, Sec joint-review-mandatory + ADR-025 amendment) per ship-order, or M2.5 (split-child, lighter). Open (non-blocking): the F/CTO local `seed.sql` rewrite to the ratified class names (blocks local `db reset`; includes the OtherCF/AcctSetup dissolution mapping). Load-bearing: the monthly-report snapshot must ship V1 as a real immutable freeze (Sec Amendment-1 Condition B).** — *Prior v1.99 CURRENT retained for trace:* **Phase 6 Build Loop active — the double-entry GL is DESIGNED + FORMALIZED end-to-end (ADR-031 + Amendment 1) and its settle-before-import conventions are RATIFIED; NEXT is a build choice: the double-entry migrations OR the V1 feature clusters.** ADR-031 Amendment 1 LANDED (PR #211, merge `68bd07c`): the ratified category/event conventions — 5-class enum `Revenue/Expense/Transfer/Equity/Trade` (Trade is a Cat with BTO/STC/STO/BTC sub-cats; `Transfer=tax-neutral`), lean event vocab `{standard, acct_setup, basis_adjust, corp_action}`, `corp_action` dated-txn (never registry rename), shorts = negative-qty + derived short-liability, `journal_id`/`metadata`-jsonb, reclass-history V2-deferred. Sec GREEN-with-conditions (Trade constraints = INSERT+UPDATE triggers; **reclass-history deferral coupled to the V1 monthly-report snapshot being a true immutable freeze — else reopens**). NO migrations built; M1→M4-GL (+M-hier) each joint-review-gated; design records in `temp/`. **NEXT: (a) start the double-entry migrations (M1 Category-as-class, smallest un-gated step) or (b) the V1 FEATURE CLUSTERS (§2.1.2+ full net-worth, active build track). Load-bearing: the monthly-report snapshot must ship V1 as a real immutable freeze (Sec Condition B).** Ledgers flat **DEFINER 3 · §10 3 · Decision-3 11 (+2 forward-flagged)**; migrations `001`–`027` live. — *Prior v1.98 CURRENT retained for trace:* **Phase 6 Build Loop active — the double-entry general-ledger DESIGN is FORMALIZED; NEXT is the V1 FEATURE CLUSTERS (build) + the settle-before-import conventions (design).** **ADR-031 + ADR-025 Amendment 1 LANDED** (F/CTO-ratified 2026-07-23; PR #209, merge `bb76894`): an event-sourced double-entry GL as a **derived layer** over the immutable `account_trans` ledger — no rewrite, no stored `journal_entry`/`journal_line` (classic Approach B rejected; the C→B "ladder" retired). Model (9 decisions): facts-vs-interpretation event-sourcing (immutability = audit of FACTS, not classification — the ADR-025 amendment); three buckets + manual A/R/A/P + hierarchical chart-of-accounts escape hatch (`M-hier`); Category-as-class (no `flow_class`/`normal_balance` column); dual **book/market** valuation + `basis_adjust` events + Unrealized-Gains equity line; imputed contras + `journal_group_id` grouping + per-tenant virtual scratch/Suspense (`Σ=0`-at-close). Sec **conditional-GREEN (no veto, 2026-07-23)** — binding conditions on M1-evt/M2: **per-tenant scratch/Suspense = standing VETO**, append-only reclassification history (not jsonb), matched-tenant `journal_group` leg fence = Decision-3 `#12` + a lot-match FK forward-flagged; **§10 stays 3 · DEFINER allowlist stays 3** (every fence INVOKER). **NO migrations built** — M1 (Category-as-class) → M1-evt → M2.5 (splits) → M2 (grouping) → M3-basis → M4-GL → C+ → M-hier deferred, each joint-review-mandatory; design of record `temp/double-entry-design-v2.md`. **The category→class map / event vocab / grouping rule / basis semantics are a settle-before-import one-way-door** — settle (F/CTO ratify + Sec where flagged) before the incumbent transaction export imports (backfill imprints them). **NEXT: the V1 FEATURE CLUSTERS** (§2.1.2+ net-worth / §2.2 allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report) remain the active build track; the double-entry GL is a formalized future V1.x track. Migrations `001`–`027` live; ledgers flat **DEFINER 3 · §10 3 · Decision-3 11 (+2 forward-flagged)**. — *Prior v1.97 CURRENT retained for trace:* **Phase 6 Build Loop active — TOTP GA REACHED; NEXT is the V1 FEATURE CLUSTERS.** The C2/C3 GA-gate docs LANDED (PR #207, merge `b85fd51` — SECURITY §4.5 **RT-29** aal2-backstop/MB-1-guard/Decision-7-tighten [ADR-029] + **RT-30** recovery posture [`026`/`027` service_role-only + scrypt algo-of-record + 5/hr rate-limit + 4th RT-26 factory; ADR-030 Amdt 1 + ADR-016 D3]; catalog 28→30, RT-30 annotates "GA-gate … now satisfied", both NOT §10-catalogued → **§10 stays 3**), and the last discoverability gap is CLOSED (this PR: a **Security** nav link → `/settings/security` in the authed header — `+layout.svelte`, plain `<a>`, tokens-only, no server surface, a link to an existing route so no Sec gate). **Opt-in TOTP MFA is now generally available end-to-end:** a signed-in user can FIND MFA setup → enroll TOTP → step up at login → the DB enforces aal2 on the direct API → self-recover a lost phone with a rate-limited scrypt-hashed backup code. `svelte-check` 0 · `vitest` 213 · build clean; ledgers flat **DEFINER 3 · §10 3 · Decision-3 unchanged**; migrations `001`–`027` live. **NEXT: the V1 FEATURE CLUSTERS** now unblocked by the complete auth front door — **§2.1.2+ full net-worth** / §2.2 asset-allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report read-surfaces. **Non-blocking follow-ups:** live-Mailpit notify e2e (DevOps: `config.toml` smtp_port + restart); Sec N-A (deleteFactor-permanent-fail UX) / N-B (scrypt-v2 version-field validation). — *Prior v1.96 CURRENT retained for trace:* **Phase 6 Build Loop active — Auth-3b COMPLETE end-to-end (real TOTP MFA + step-up + self-service recovery); NEXT is C2/C3 docs (before TOTP GA), then the V1 feature clusters.** The full arc merged this session: **Slice 1** DB backstop (`025`) + MB-1 guard + domain tighten (v1.93/PR #198) + app flow (enrollment/step-up/fail-closed guard, v1.94/PR #200); **Slice 2a** `026` recovery-code store + ADR-030 (v1.95/PR #202); **Slice 2b** `027` rate-limit store (PR #204) + the recovery app (v1.96/PR #205, merge `5cb44be`) — the **4th RT-26 surface** (`supabase-admin.ts` factory, first actual service_role-key use in api/src; ADR-016 Decision 3; allowlist 3→4 intra-instance, §10 stays 3) + `/mfa/recover` (atomic rate-gate + constant-time compare + atomic check-and-consume + downgrade-first + admin `deleteFactor` 3× + `users_id`-from-session) + Node built-in async `crypto.scrypt` (**bcryptjs DROPPED** → zero runtime hashing dep; ADR-030 Amendment 1) + nodemailer notify (fail-soft, deploy-gated) + display-once codes UI. **A user can now enroll TOTP → step up at login → the DB enforces aal2 on the direct API → self-recover a lost phone with a rate-limited scrypt-hashed backup code.** Every code PR passed the full role-separated gate + Sec joint-review; ledgers held flat throughout: **DEFINER 3 · §10 3 (RT-26 allowlist 3→4 intra-instance) · Decision-3 unchanged**; migrations `001`–`027` live. **NEXT: C2** SECURITY §4.5 RT-class posture entry (Sec-authored: aal2 backstop + MB-1 guard + Decision-7 domain-tighten + the recovery posture — 026/027 service_role-only + scrypt algo-of-record + rate-limit + 4th RT-26 surface) **+ C3** migration-checklist note — both **before TOTP GA**. Then the **V1 feature clusters** (§2.1.2+ full net-worth / §2.2 asset-allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report) now unblocked by the auth front door. **Non-blocking follow-ups:** live-Mailpit notify e2e (DevOps: `config.toml` smtp_port + restart); Sec N-A (deleteFactor-permanent-fail UX polish) / N-B (scrypt v2 version-field validation). — *Prior v1.95 CURRENT retained for trace:* **Phase 6 Build Loop active — Auth-3b Slice 2a LANDED (recovery-code store `026` + ADR-030, v1.95/PR #202); NEXT is Slice 2b (the `/mfa/recover` endpoint + UI + the ADR-016 amendment / 4th RT-26 surface).** Self-service TOTP recovery DB foundation: `pfin.mfa_recovery_code` (service_role-only, app-side bcrypt/argon2-hashed one-time codes; RLS + zero authenticated grant/policy → default-deny both layers, hashes off the direct API) + the column-scoped `grant select(users_id),update(mfa_policy) on user_settings to service_role` (the guard-exempt recovery downgrade). **ADR-030:** service_role forced by construction (3 forcings + **CV-R1 empirically BLOCKED** — aal1 can't unenroll a verified factor; conditional-lock #4 → service_role; recovery mints no aal2). Sec joint-review GREEN + default-deny pgTAP plan(13)/376 PASS; migrations `001`–`026` live; ledgers flat **DEFINER 3 · §10 3 · Decision-3 unchanged**. **NEXT: Slice 2b** — `/mfa/recover` (verify→consume→admin `deleteFactor`→downgrade→notify→re-enroll) + display-once issuance + recovery UI + **the ADR-016 amendment (4th RT-26 surface) + `rt26-allowlist.txt` update** (the accepted one-way-door — where the `026`-granted service_role capability becomes live). Sec 2b gates: atomic check-and-consume (single-stmt + rows-affected TOCTOU), hash algo/cost + constant-time compare, rate-limit (5/hr/user), notify-on-MFA-change (app-sent, deploy-gated on Auth-1 SMTP). Then **C2** SECURITY §4.5 RT-doc entry (Sec co-authored, before TOTP GA) + **C3** checklist note, then the remaining §2.1 net-worth surfaces (§2.1.2+) + §2.2/§2.3/§2.5/§2.6 clusters. Slice 2b (recovery) gates TOTP GA (lost-phone lockout without it; NOT urgent — no prod users; Auth-1 prod SMTP/wiring pending). — *Prior v1.94 CURRENT retained for trace:* **Phase 6 Build Loop active — Auth-3b Slice 1 COMPLETE (DB backstop v1.93/PR #198 + app flow v1.94/PR #200); NEXT is Slice 2 (recovery codes), design-first.** Opt-in TOTP is now BOTH enforced (the `025` aal2 backstop + MB-1 guard) AND usable end-to-end (app flow: `/settings/security` enroll → QR+verify → `mfa_policy='totp'` via an N1-safe UPDATE after a verified factor + aal2-gated disable; `/mfa/step-up` challenge→aal2; the fail-CLOSED `requireStepUp` guard sequenced in the `hooks.server.ts` chokepoint, N2 keyed off GoTrue AAL, GET-page-nav scope). Sec joint-review unconditional GREEN on both (C-APP1 nonce-only CSP confirmed → `{@html}` QR safe; MB-1 BLOCK cleared at the DB layer, app layer = DiD over it). svelte-check 0 · vitest 181 · pgTAP 363/23 · 13/13 live smoke; migrations `001`–`025` live; ledgers flat **DEFINER 3 · §10 3 · Decision-3 unchanged**. **NEXT: Slice 2 — recovery codes** (`026` recovery-code store + redemption + the **CV-R1** capability-verify [can GoTrue unenroll a verified factor at aal1?]; conditional-lock #4 → service_role fallback on the (2b) backup-code path if CV-R1 fails; ratified recovery invariants = aal2 self-service disable OR verified backup-code server-side, never bare aal1, + notify-on-every-MFA-change; joint-review-mandatory), likely **design-first**. Slice 2 gates TOTP GA (lost-phone lockout without it; NOT urgent — no prod users yet, Auth-1 prod SMTP/wiring still pending). Then the **C2** SECURITY §4.5 RT-doc entry (Sec co-authored) + **C3** checklist note, then the remaining §2.1 net-worth surfaces (§2.1.2+) + §2.2/§2.3/§2.5/§2.6 clusters. — *Prior v1.93 CURRENT retained for trace:* **Phase 6 Build Loop active — Auth-3b Slice-1 DB enforcement LANDED (v1.93, PR #198, merge `fa8900c`); NEXT is the Slice-1 app flow (enrollment + step-up UI).** **Auth-3b Slice-1 DB unit (migration `025`, SELF-291):** the per-user-conditional aal2 step-up backstop (14 tenant tables / 31 policies, reads+writes; COALESCE null-safe, NEVER blanket; tenant isolation preserved byte-faithfully and ANDed with — never replaced by — the aal clause; inline-not-helper per the InitPlan single-eval finding, allowlist stays 3 by construction) + the **MB-1 downgrade guard** (Sec-caught self-defeat: the backstop's own control variable `mfa_policy` was aal1-downgradable via the direct API; F/CTO **Option A** — a BEFORE UPDATE trigger, INVOKER, blocking the `authenticated` tier from lowering out of `{totp,passkey}` unless aal2; `service_role` recovery + `postgres` deliberately un-gated) + the **`('none','totp')` CHECK tighten** (F/CTO-caught: `passkey` unused for V1 enforcement + sole enabler of an inert lateral flip → deferred to Auth-6/SELF-289, additive re-add; lateral is now a schema `23514`; clause+guard stay set-referencing). ADR-029 (Decisions 1–7); Slice-2 recovery invariants locked (aal2 self-service disable OR verified backup-code server-side — never bare aal1; notify-on-every-MFA-change). Sec joint-review GREEN (MB-1 BLOCK cleared, C1 pgTAP battery signed off — non-vacuous blocks, isolation-⟂-MFA at both aal levels, false-green-proof; 363/23 PASS). Ledgers flat: **DEFINER 3 · §10 3 · Decision-3 unchanged**; migrations `001`–`025` live. **NEXT: the Slice-1 app flow** (Backend enrollment + fail-closed `requireStepUp` guard in `hooks.server.ts` + N2/N3 reconciliation; Frontend QR + step-up screens — separate PR, its own Sec joint-review gate; §7 ordering satisfied — enforcement landed first), then **C2/C3** doc follow-ups + **Slice 2** (recovery codes `026` + redemption + CV-R1), then the remaining §2.1 net-worth surfaces (§2.1.2+) + §2.2/§2.3/§2.5/§2.6 clusters. — *Prior v1.92 CURRENT retained for trace:* **Phase 6 Build Loop active — the AUTH FOUNDATION landed (the app is now browser-reachable); NEXT is real DB-enforced TOTP.** **Auth-1 + Auth-2 + Auth-3 SHIPPED (v1.92, PRs #193/#195/#196):** SMTP scaffolding (SELF-290 — provider-agnostic GoTrue SMTP, Resend default; operator Resend/DNS + prod wiring still pending) + signup/login/session/email-confirmation (SELF-285 — makes every protected page reachable; Sec-GREEN, `URL`-origin open-redirect fix landed) + the MFA substrate migration `024` `pfin.user_settings`+`mfa_policy` (SELF-286 — owner-only RLS, fail-soft provisioning, `[auth.mfa.totp]` enable; Sec-GREEN). **Auth model (F/CTO-ratified):** GoTrue authenticates / RLS authorizes; per-user OPT-IN MFA (default `none`); **Option-B split** → **real DB-enforced TOTP = Auth-3b (SELF-291)**, the Sec-gated fast-follow (the per-user-conditional `aal2` backstop is what makes TOTP a real guarantee — app-layer step-up alone protects nothing on the directly-reachable data API, C6); **email-2FA DROPPED** (SELF-287 — email OTP is `aal1`, can't mint `aal2`). Supported factors: **password / TOTP (V1.0 via Auth-3b) / passkey (V1.x)**; email retained for signup verification + password recovery only. Migrations `001`–`024` live; ledgers **DEFINER 3 · §10 3 · Decision-3 11**. **NEXT: Auth-3b (SELF-291) real TOTP** (the ~15-policy `aal2` backstop + enrollment/step-up + recovery codes; Sec N1/N2/N3 carry-forward) — or Auth-5 recovery — then the remaining §2.1 net-worth surfaces (§2.1.2+) + §2.2/§2.3/§2.5/§2.6 clusters. — *Prior v1.91 net-worth CURRENT detail retained for trace:* **SELF-211 §2.1.1 headline Net Worth LANDED (PR #191, merge `69f531a`, v1.91)** — the root route renders `pfin.fn_compute_nav` as a single trustworthy number (Option A; composition breakdown → V1.1 / SELF-225); consumes the existing INVOKER helper via the anon+RLS client (no new DB work); SELF-210 reconciled to a verification issue. Ledgers flat: **DEFINER 3 · §10 3 · Decision-3 11**. **This surfaced a platform gap: there is NO `/login` route / auth-session UI, so the net-worth surface (and every protected page — `accounts/*` and now `/`) is not browser-reachable yet.** → Stood up the **Auth & Accounts** project (SELF-285–290): **Auth-1** SMTP infra (provider-agnostic GoTrue SMTP, Resend default) → **Auth-2** signup/login/session + email confirmation → **Auth-3** TOTP MFA + per-user `mfa_policy` → **Auth-4** opt-in email-2FA (app-enforced) → **Auth-5** recovery → **Auth-6** passkeys (V1.x). Auth model ratified: **GoTrue authenticates / Postgres RLS authorizes; MFA is a per-user account-setup choice; tenant isolation (`users_id = auth.uid()`) is the universal invariant** (a user's MFA-strength choice bounds only their own impersonation risk). Product-scope: built as **general multi-user software** (family use eventual) → public signup + email confirmation is a real V1 flow. **NEXT: Auth-1 (SELF-290, SMTP) — the front door that makes shipped read-surfaces viewable — then continue the V1 feature clusters (§2.1.2+ full net-worth / §2.2 / §2.3 / §2.5 / §2.6).** — *Prior CURRENT state (provider-sync ingest + SELF-212 wave) retained below for trace:* **Phase 6 Build Loop active — the ADR-027 provider-sync ingest build + the SELF-212 frontend-connect wave (LANDED v1.83).** The provider-sync worker is functionally complete through the schedule loop (admit → map → sync), **and the browser→worker Plaid production-onboarding path shipped (PR #175, merge `d8cbbac`, v1.83):** worker Option-C admission endpoint (both legs) + credential-less api/src relay (SELF-197) + Plaid Link UI (SELF-198) + the RT-27 private-bind fence + ADR-027 (hh)/ADR-028/ADR-008 index amendment; **§10 catalogued-instance count 2→3** (RT-27 third = the network-exposure/config layer); SELF-197/198 Done, SELF-212 Outcome A. Substrate migrations `015`–`021` + the `fence-tbc-node` CI fence + **worker PR #1 + credential-admission (PR #167) + account-mapping (PR #169) + SimpleFINAdapter (slice 3a, PR #171) + the sync scheduler (slice 3b, PR #173) LANDED** — **migrations `001`–`021` live**. **Provider selection RESOLVED (ADR-027 gate i, v1.77): Plaid PRIMARY + SimpleFIN FALLBACK + import/manual; SnapTrade DROPPED, Teller INVALIDATED** (empirical record `temp/aggregator-strategy-memo.md`). **`fence-tbc-node` CI fence LANDED (PR #164, v1.78):** Node `TenantBoundClient` grep (LEG 1 raw-client / `@supabase/supabase-js` ban + LEG 2 `SUPABASE_SERVICE_ROLE_KEY`-absence tripwire) + FIX 1 (dropped the `--exclude`-basename false-negative in BOTH the Node and Python ETL fences, lockstep, + collision regressions + negative control); now load-bearing in CI (production-mode against `workers/provider-sync/src/`). **Worker PR #1 LANDED (task #12, PR #165, merge `95cde45`, v1.78):** the dual-mode `TenantBoundClient` — login role = **`authenticator`** (NOINHERIT PostgREST broker; the ratified **(b)** capability resolution, smoke-verified on the local stack, `temp/provider-sync-login-role-capability.md` T1–T5 + the ADR-019 login-role note; `withTenant`=`SET LOCAL ROLE authenticated`+jwt for the INVOKER `fn_ingest_transactions`, `withServiceRole`=`SET LOCAL ROLE service_role` for privileged snapshot/`eod_price`/global-asset writes) + the `ProviderAdapter` interface + 3 DTOs + PlaidAdapter (empirical invariants: `amount × −1`, `CUR:USD`→cash-leg, cusip-first, blank→NULL) + resolution (global auto-register) + mapper (Sec's 3 guards) + 34 unit tests. Consumes `015`–`020`, no new migration. **3× GREEN** (Sec GREEN-with-conditions [C1/C3/C4/C2 + SC-1 cleared; non-atomicity blessed] · Architect contract-consumption · QA test-posture). **Credential-admission slice LANDED (PR #167, v1.79):** worker-owns-exchange (F/CTO one-way-door; access_token never leaves the worker/Vault) — `connect()` (atomic `vault.create_secret` + `linked_source` INSERT under service_role; re-admission update-same-handle + the **SC3-C8** tenant-match guard; returns account refs only, NO `pfin.account` write) + `revoke()` (decrypt → `/item/remove` → tenant-scoped DELETE, abort-on-failure) + a sandbox-gated dev CLI; **ADR-027 (s)–(v)**; consumes `015`, no migration; 3× GREEN (Sec C3/C4/C5+atomicity + #1 blessed + SC3-C8 · Architect 1:1 · QA G1/G2); 53 tests. Deferred: SC3-C1 (`ownerUserId`-from-server-session ADR-lock) + SC3-C6 (app→worker handoff SC-review) bind at SELF-212. **Account-mapping slice LANDED (PR #169, merge `18b2e50`, v1.80):** `landAccounts` (withTenant/INVOKER; DB-stamped un-forgeable `users_id`; the `021` `ON CONFLICT` dedup arbiter + the N1 `resolveAccountIds` re-run merge + a fail-closed `ownerUserId===client.usersId` guard) + `PlaidAdapter.mapAccountType` + `admit --map` (connect→map; `connect()` stays refs-only); **migration `021`** partial UNIQUE dedup index on `account(linked_source_id, provider_account_id)` (index-only; allocation-junction renumbers `021`→`022`, annotation →`023`); ADR-027 (w)–(aa); 5-track GREEN (Sec joint-review no conditions · Architect 1:1 · Backend 71/71 · QA plan(11)/278 PASS · CI 10/10); ledgers flat (exercises #6, adds none). **SimpleFINAdapter slice 3a LANDED (PR #171, merge `df9e151`, v1.81):** the SimpleFIN adapter satisfying `ProviderAdapter` (setup-token→Access-URL Vault admission; `external_connection_id` = SHA-256 of the full normalized Access URL incl. userinfo — Sec-C4, after a design-caught + team-lead-re-caught auth-strip collision; R-7-native DTOs no-flip; **local-delete-only** revoke [no provider endpoint]; blank-symbol/cusip pre-validation); ADR-027 (bb)/(cc)/(dd) + SECURITY §4.2 C7 residual; Sec SC-3 design→final GREEN (C1–C5/C8 + **F1** `new URL()` leak-vector wrap) · Architect 1:1 · Backend 107/107 · QA **G3** admission-atomicity + **SC-4** dup-asset (**10 live-DB tests**, `RUN_DB_INTEGRATION=1` loud-skip in CI); no migration. Delivered the deferred **QA G3** + **SC-4** + blank-symbol/cusip pre-validation. **Sync scheduler slice 3b LANDED (PR #173, merge `a237101`, v1.82):** the Coolify-cron `poll` entrypoint driving the shipped `syncProviderData` across all active `linked_source` (Plaid + SimpleFIN); per-source `forTenant` failure-isolation; **exit non-zero only on fleet-fatal** (per-source audited-not-paged); `forEnumeration()` service_role null-tenant read-only client (withTenant fail-closed, off-RT-26); the Flag-2 V1-now case-normalize; DevOps Pattern A Dockerfile + `worker-ci.yml` (**worker unit tests + the live-DB integration lane [G2/G3/SC-4] now mechanical CI gates**). **SC-2 = FLAT** (deterministic `checkpoint_id desc` read already shipped @018/019; (b) rejected under `005` immutability); ADR-027 (ee)/(ff)/(gg); Gate GREEN (Architect 1:1 · Sec no conditions [forEnumeration posture 4/4 + Dockerfile off-RT-26 on record] · Backend 127/127 · QA G2 idempotency + SC-4 case-closure, 23 live-DB tests); ledger FLAT. **The provider-sync worker is now functionally complete through the schedule loop (admit → map → sync).** **SELF-212 wave DONE (v1.83, PR #175); #12 ADR-028 per-route CSP DONE (v1.84, PR #177)** — SC3-C1/C6 bound + Sec-CLEAN throughout; #12 shipped the global strict CSP base + per-route widen (structural CSP-1) + PLAID_ENV-tier-gated host (ADR-028 CSP-3 amended; env-tier not build-mode per F/CTO) + both Sec test follow-ups closed (Sec CSP-1 PASS; §10 untouched, RT-28 MEDIUM non-§10). **#14 CA-2 recurring reachability probe + Discord — DONE (v1.85, PR #179, SELF-279; hardens RT-27, ledger flat). #16 `safeGetSession` unvalidated-session hardening — DONE (v1.86, PR #181, SELF-280; ADR-015 chokepoint, ledger flat). OQ-2 provider-agnostic connect-seam + in-app SimpleFIN connect — DONE (v1.87, PR #183, SELF-281; new route on RT-27, ledger flat; RF-3 shipped the SimpleFIN revoke-at-Bridge warning).** **015–021 SUBSTRATE COMPLETE** (`023` annotation-overlay DONE v1.89 / PR #187 / SELF-283, D3 #10; `022` allocation-junction DONE v1.88 / PR #185 / SELF-282, #8/#9 — migrations `001`–`023` live). **Task #13 D3 enumeration fold-in DONE (v1.90, PR #189, SELF-284)** — canonical Decision-3 **#1–#11** folded into the ADR-011 body list (11 non-contiguous labels, 9/11 DDL-realized; #3/#4 `monthly_report` pending V1.3+; #11 early @019); the drift-prone dual-count retired, the canonical enumeration now authoritative. **NEXT (resume order): the V1 FEATURE CLUSTERS** — the complete `001`–`023` substrate now unblocks the user-facing read-surfaces: **§2.1 net-worth first** (§2.1.1 headline SHIPPED @ SELF-211 / v1.91 — see the CURRENT lead above; §2.1.2+ full net-worth remains; viewability gated on the Auth front door), then §2.2 asset-allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report. **C6 revoke-warning UX DEFERRED → SELF-207** (connection-state view — no in-app disconnect affordance to attach it to yet; the SimpleFIN side shipped at OQ-2/RF-3). Also open: **C2** `pfin_worker` V1.x hardening; the Architect **V1.x `upper(...)` expression-index** for structural case-insensitive global-asset dedup (Flag 2 structural leg, batched with the next asset-registry migration); the `Number(sourceId)` bigint-precision edge (non-blocking, single-user). then the V1 **feature clusters** the substrate now enables (§2.1 net-worth / §2.2 asset-allocation / §2.3 cash-flow / §2.5 taxes / §2.6 monthly-report read-surfaces). Ledgers on main: **DEFINER 3 · §10 3** (RT-27 catalogued at v1.83) **· Decision-3 8** · migrations `001`–`021` live. Also open: ADR-011 D3 enumeration fold-in (task #13). — *Historical detail (v1.71 design + the original 015–021 framing) below is retained for trace; the resume state above supersedes any stale "NEXT: author NNN" pointers in it.* **`015`** = `linked_source` fold (Decision-3 #6). **`016`** = `pfin.asset` registry (hybrid RLS). **`017`** = `account_trans` investment cols + `security_id` fence (Decision-3 #7, load-bearing under service_role) + `fn_ingest_transactions` RPC. **`018`** = provider snapshots (`holdings_checkpoint` activation + NEW `account_balance_checkpoint` cash analog; append-only + service_role INSERT; Sec merge-block #8). **`019`** = valuation core — the **uniform roll-forward** model (F/CTO 2026-07-15, "won't rearchitect"; supersedes Rule A + an FMP-free intermediate): `pfin.eod_price` (OWD-A grain + `+provider_implied` vocab + hybrid RLS + `014` NaN) + `fn_holdings_as_of` (roll-forward: checkpoint anchor + Σ strictly-after) + **`fn_compute_nav`** (Lock 11 INVOKER, uniform `qty × eod_price` all account types, D-first source-priority `manual > exact > provider_implied`) + the A1 `holdings_checkpoint.security_id` fence (**Decision-3 #11, count 7→8**, load-bearing under service_role) + the cash latest-view (`security_invoker=true`); FMP OUT of V1 (future higher-priority price source; provider-implied fills the floor). All five build loops clean — Sec GREEN · QA C6 (`plan(30)`/`plan(20)`/`plan(19)`/`plan(16)` + `019`'s 47-assertion battery) · Backend clean apply + `test db` green · CI 8/8. **NEXT: author `020`** (allocation junction — novel-fence site 4) then `021`(annotation overlay). Design highlights still to land: auto-map deferred V1.x (R-18); **Decision-3 8→11 [design remaining]** (`020` `asset_id`/`sub_cat_id` = #8/#9 + `021` `sub_cat_id` = #10; #11 realized at `019`). **DATA BLOCKERS (F/CTO): registry seed export + sheet column-map + txn sign-convention** (gate seed/backfill + import, not schema DDL). Migrations `001`–`019` live · DEFINER allowlist 3 · §10 catalogued 2 · **Decision-3 canonical 8** (on main). **Tracked follow-ups (non-blocking):** the **provider-implied Worker deliverable** (`019` follow-on — symbol→`security_id` resolution + global-asset registration + `provider_implied` `eod_price` materialization; **makes `019` functionally live end-to-end**; Sec-gated: div-by-zero / tenant-binding / `ON CONFLICT` upsert) + the ADR-011 Decision-3 enumeration fold-in (#6/#7/#11 into the canonical list) + the deferred provider-sync **worker-enablement** migration (SELF-275: grant `service_role` SELECT on `pfin.asset`+`pfin.account` for the ingest-fence subquery; PostgREST `statement_timeout`; import-path dedup shape) + the `pfin.asset` global-enrichment write path (SELF-197+; C6-gated) + Sec's SECURITY-doc generalization (§4.2/SD-03/RT-02/RT-05; doc-flow branch). **DONE clusters:** **SELF-277** `019` · **SELF-276** `018` · **SELF-275** `017` · **SELF-274** `016` · **SELF-273** `015`; **SELF-201**+**SELF-236** manual-entry Sub-Cat; **SELF-231** taxonomy `009`/`010`/`011`; **SELF-196** Plaid schema `007`/`008` (folded by `015`). **Provider selection (ADR-027 gate i) ✅ RESOLVED 2026-07-16 — Plaid PRIMARY + SimpleFIN FALLBACK + import/manual; SnapTrade DROPPED, Teller INVALIDATED.** Round-3 Plaid production probe (F/CTO's real accounts) cleared all four feature cells incl. **Fidelity end-to-end** (inv-txns 551 ≈ SnapTrade's 550, holdings reliable, + banks/cards; SnapTrade brokerage-only + holdings-410-on-tier → redundant). Plaid bills per-**Item** (not per-account) ~**$2.56/mo** (5 Items) or **$0** under the 10-Item Trial; card history is issuer-capped (backfill from the sheet). The provider-sync **worker build** (Plaid + SimpleFIN adapters behind `015 linked_source`) is now the next major ingest piece — **Architect design in flight** (OWD-1 adapter interface + OWD-2 ingest mapping + the `016`-deferred global-asset write-path migration). Full record: the [ADR-027](DECISIONS.md#adr-027) gate-(i) amendment + `temp/aggregator-strategy-memo.md`.
>
> **⚠ AGGREGATOR-STRATEGY PIVOT (2026-07-07, F/CTO-driven).** Empirical testing against F/CTO's real accounts (`temp/simplefin-test.mjs`) + research established that **no general aggregator reliably returns complete Fidelity *transactions*** — Fidelity locked consumer data to Akoya; Plaid=blocked/in-flux; SimpleFIN/MX=holdings+balances-but-not-transactions (only 1 of 5 Fidelity accounts returned txns, known 55-day activity absent, no error); even Akoya (Finicity/Yodlee) has per-account-type txn gaps. Fidelity **holdings + balances ARE reliable** across providers. **Direction (F/CTO): a provider-agnostic multi-aggregator ingest abstraction** — route each institution to its best provider behind one internal interface, with **CSV/OFX import + manual entry (SELF-201) as first-class providers** (the guaranteed-complete source for Fidelity txn gaps). Recommended cheap mix: **SnapTrade (free — sanctioned Fidelity *developer* API)** for brokerages + **SimpleFIN ($1.50/mo)/Plaid** for banks+cards + import/manual. Quiltt ($100–500/mo) + Yodlee/Finicity (enterprise) ruled out on cost. Full research + pricing table + sources: **`temp/aggregator-strategy-memo.md`**.
>
> **Track status under the pivot:** **SELF-197 (Plaid Link + token exchange) PAUSED / superseded-as-scoped** — the `007`/`008` Vault credential-storage pattern + the SELF-197 capability-verify (Vault write-path viable; designs at `temp/self-197-015-design.md`, `temp/self-197-vault-writepath-design.md`) still apply to whatever provider(s) win (store each provider's credential Vault-encrypted). **SELF-200 (auto-Unsorted symbol surfacing) BLOCKED** on the provider path (needs live investment sync). **SELF-212 (Plaid production sales call) likely moot.** **NEXT (open) — provider-strategy ADR ✅ [ADR-027](DECISIONS.md#adr-027) LANDED 2026-07-08: (1) empirically test SnapTrade (Fidelity dev-API Activities → transactions) + Plaid-PAYG (request Fidelity Investments access → `/investments/transactions/get`) → provider-selection decision; (2) Architect design the `015` `pfin.asset` ingest table + `linked_source` substrate (OWD-1 interface contract + OWD-2 normalized mapping — both left OPEN by ADR-027); (3) PM §2.4/§2.5 re-scope (Plaid Link → per-provider connect flows).** **Unblocked non-Plaid work available NOW:** §2.2 aggregation (SELF-237 per-Sub-Cat primitive, SELF-239 allocation table), §2.1 net-worth stories, or the SELF-201 deferred items below.
>
> **SELF-201 deferred (non-blocking):** audit-log infra (A2 — audit-log table + reserved 3rd DEFINER-allowlist insert-helper; wires the `013` forward-hook; joint-review-mandatory to author); UX copy pass (provisional enum labels); optional Visual-fidelity pass; 2 value-correctness flags → PM (liability Sub-Cat dropdown; negative-value policy). **SELF-231 seed:** `supabase/seed.sql` **built + smoke-verified** (gitignored, 65 rows: 36 asset tax-neutral + 29 cash-flow; `db reset` 001→014 + seed green on the local Docker stack). Committed template at `temp/self-231-seed-template.sql`. Carried follow-ups: **GDPR-erasure decision** (routine specified: enumerate Items → `/item/remove` each → delete user; cleans under service_role, no DEFINER); **`plaid_sync_audit` grant/policy change = Sec-joint-review-mandatory** (credential-adjacent deny-by-default surface); **FORCE-RLS defense-in-depth pass** (scoped; per-table postgres-run access assessed first); **C6 exposure-gating standing obligation** (pfin is now Data-API-exposed → every pfin table is internet-facing the moment it is granted → the §4.5 two-tenant RLS battery gates exposure on every migration); **SELF-212** Plaid production-tier call gates the Plaid runtime track; `role:qa`/`role:devops` Linear labels; W0b Coolify repoint (F/CTO-executed). (fence-tbc integrity gap RESOLVED at SELF-193/PR #129.) **Resolved 2026-06-29:** the `config.toml major_version=17` confirm-vs-prod gate is closed **by-decision** via [ADR-021](DECISIONS.md#adr-021) — V1 is **greenfield** (new VPS stood up from scratch; cax21/pfindash.com incumbent is reference-only, NOT a deploy dependency), and **PG 17 is the forward target by choice** (the incumbent measured 15.8 on 2026-06-29; moot under greenfield). From-scratch stand-up documented in [`docs/deployment-runbook.md`](docs/deployment-runbook.md) (skeleton, filled incrementally). **ARCH §5 Deployment Topology reframed** to greenfield/PG-17 (cax21 → reference-precedent sizing baseline), which also reconciled the **BLS-key** + **ETL discrete-`PFIN_DB_*`** ARCH↔code flags (Sec confirm-pass GREEN; §10 ledger untouched). **The dated blocks below are a resolved historical ledger (Phase 3 / Phase 4 era), retained for provenance — not current pending work.**

**PHASE 3 EXIT-GATE CLOSED** as of 2026-06-02. Both criteria APPROVED: row #7 Sec sign-off on auth/RLS/secrets (per Sec row #7 comprehensive audit) + row #8 F/CTO sign-off on ARCH v1.0. **Thirteen-PR Phase 3 ARCH streak (#65–#79, #81) closed clean.** §4 Tech Stack table fully populated (8/8 rows); §3 Data Flow + §3.1 + §3.2 (mermaid renders fixed at PR #81) + §4.1 + §4 Auth + §4 Observability + §5 Deployment Topology + §7 Integration Points + §7.1 Plaid endpoint posture + §6 CI/CD Pipeline + §6.1 Sec-test catalog mapping + §8 Trade-offs & Alternatives + §8.1-§8.6 sub-sections + §9 Open Questions + §9.1 + ADR-011 Decision 4 §10 attribution-discipline CHANGELOG annotation all locked. **§10 attribution discipline 14 consecutive CLEAN surfaces on FULL three-axis basis** (PR #65/#66/#67/#68/#69/#71/#72/#74 §8/§9/PR-A row #4 v2/PR-B annotation/PR-C sweep/row #7 audit/PR #81 mermaid). **Path B preference 6-application** + **section-hint convention 5-application** durable as of PR #80. **Team-lead Sec-Lock cross-check 7-application track record** (+ row #7 audit (b)1 SECURITY TOC/header citation verification). **Phase 4 entry gates only on Phase 2 close** per ADR-012 joint-close (Phase 2 Steps 1-10 completed 2026-05-29 per ADR-013/014/015 trio; ready-to-close).

**Memory codification batch — RESOLVED 2026-05-31 post-PR-72 merge** (F/CTO ratify on "1" → all 4 candidates codified one-at-a-time per `feedback_one_question_at_a_time`):
- ✅ **(a) Section-hint canonical-territory statement convention (v1.41) — UPDATED TO DURABLE.** Existing memory `feedback_section_hint_canonical_territory_statement` body updated to reflect 3-application track record (PR #69 introduction; PR #71 second app Sec-validated; PR #72 third app at 9 owners Sec-2 STRONG DURABILITY EVIDENCE); convention is now default shape for ARCH surfaces with adjacency lists ≥6 per Sec-2 (b)5 ratify.
- ✅ **(b) Post-ratify cross-check at v1 file pattern — NEW MEMORY `feedback_post_ratify_v1_cross_check` codified.** Mechanism: when v1 drafted against assumed-ratify, add explicit ratify-assumption flag at v1 top so post-ratify cross-check has hook to verify against; surgical-fix deltas in-place before SendMessage. Sec-2 commendation: "discipline-of-disciplines."
- ✅ **(c) Team-lead cross-check of Sec findings against Lock wording at ratify boundary — NEW MEMORY `feedback_team_lead_sec_ratify_lock_cross_check` codified.** Mechanism: when Sec findings cite Lock wording from DECISIONS.md, team-lead reads cited Lock verbatim BEFORE forwarding ratify-question to F/CTO; catches Sec misreads at earliest ratify boundary. Composes with verbatim-vs-paraphrase discipline.
- ✅ **(d) Async-mismatch-surfaces-at-boundary meta-mechanism — NEW MEMORY `feedback_async_mismatch_boundary_hooks` codified.** Architect's meta-pattern: every async-ordering mismatch surfaces at a boundary IF the discipline has a hook to verify against; build verification hooks into every dispatch + every sign-off. PR #72 exercised this 4 times across 4 distinct boundaries. The 4-hook stack (verbatim relay + §10 attribution + post-ratify v1 + team-lead Sec/Lock) covers 4 distinct boundaries with 1:1 mapping.

**Memory codification batch — RESOLVED 2026-05-31 post-PR-74 merge** (F/CTO ratify on multi-select → both candidates codified):
- ✅ **(a) Team-lead Sec-Lock cross-check discipline 2nd-application strong-validation.** Existing memory `feedback_team_lead_sec_ratify_lock_cross_check` body + description updated to reflect 2-application track record (PR #72 boundary-failure-that-codified + PR #74 strong-application validation). PR #74 was first SUCCESS-application after PR #72 codification: team-lead pre-relay cross-checked Sec's Decision 4 lines 352-355 citation verbatim BEFORE forwarding (a)1 finding to F/CTO; Sec's citation accurate, Architect's Path B fix landed CLEAN. Validates discipline composes mechanically when invoked + adds verification confidence when no drift exists.
- ✅ **(b) Path-B Decision-4-link-carries-the-lookup v2-fix shape — sub-bullet added to `feedback_decision_4_instance_ledger_cross_check`.** Pattern: when v1 paraphrases canonical content from a referenced source (e.g., Decision 4 four-layer composition), v2 fix can either (A) restore verbatim enumeration OR (B) drop the enumeration entirely + let the link carry the canonical lookup. Path B reduces drift surface to zero by removing the paraphrase opportunity. Path B preferred when §-surface section-hint convention frames REFERENCES-not-ABSORBS (per PR #74 §8.5 application); Path A preferred when §-surface ABSORBS-and-restates canonical content for reader convenience. Codified as deterministic choice criterion (not Architect taste).

**Memory codification batch — RESOLVED 2026-06-02 post-PR-76 + PR-A + PR-B + PR-C merge cycle (4 PRs)** (track-record extensions; no new memory codifications):
- ✅ **(a) Section-hint canonical-territory statement convention 5-application track record body update.** Memory `feedback_section_hint_canonical_territory_statement` body extended from 3-application track record (PR #69/#71/#72) to 5-application (add PR #74 §8 + PR #76 §9 at 9+ neighbors / 5-6 neighbors respectively). Convention remains durable per PR #72 + Sec-2 (b)5 ratify; track-record validation continues to extend.
- ✅ **(b) Path B preference 6-application track record body update.** Memory `feedback_decision_4_instance_ledger_cross_check` v2-fix-shapes section + description extended to reflect 6-application track record (PR #74 §8.5 codification + PR #76 §9 row #5 by-default + PR-A row #4 §3 surgical + PR-A row #4 §4 surgical + PR-B annotation by-construction + PR-C sweep by-construction). KEEP-at-canonical-anchor pattern (PR-A row #4 (b)3 disposition at §4.1) added as third disposition alongside Path A / Path B — KEEP when §-surface IS the canonical anchor for the content; numbered list IS substantive RT-positioning territory rather than REFERENCES-gloss.
- ✅ **(c) Team-lead Sec-Lock cross-check discipline 6-application track record body update.** Memory `feedback_team_lead_sec_ratify_lock_cross_check` body + description extended from 2-application (PR #72 + PR #74) to 6-application (add PR #76 §9.1 stale-snapshot disambiguation + PR-A row #4 SECURITY §4.2/§4.3/§4.6 citation refinement + PR-B annotation §4 line 365 verbatim cross-check + PR-C sweep line 897 verbatim cross-check). Discipline composes mechanically at 6th application; SUCCESS-cases now outnumber boundary-failure-cases 5:1.
- ✅ **(d) §10 attribution discipline 12-consecutive-CLEAN-surface streak body update.** Memory `feedback_decision_4_instance_ledger_cross_check` Convergence-outcome pattern extended from 3-PR convergence loop (#65/#66/#67) to 12 consecutive surfaces (#65/#66/#67/#68/#69/#71/#72/#74 §8/§9/PR-A row #4 v2/PR-B annotation/PR-C sweep). Streak captured durably in-repo at ADR-011 Decision 4 CHANGELOG annotation per PR-B.

**Memory codification batch — RESOLVED 2026-06-02 post-PR-81 + row #7 + row #8 Phase 3 exit-gate close** (F/CTO ratified continue-direct-work pivot to isolated test-harness; new memory + track-record extensions):
- ✅ **(a) NEW MEMORY `feedback_mermaid_sequencediagram_constraints` codified.** Captures vendored Mermaid sequenceDiagram constraint set discovered across PR #81's 6-round whack-a-mole saga: cascading-failure-across-blocks; NO line breaks of ANY type in messages; `"..."` wrap on special-char messages; `;` → `#59;` + `#` → `#35;` (Mermaid entity syntax NO `&` prefix; HTML `&#NN;` defeated by browser pre-decode); short messages render better; participant aliases stricter than messages. Codifies recovery discipline (isolated `docs/test-NAME.html` test-harness pattern; converts debug cycle ~5+ min/round → ~30 sec/round) + browser-render-verification-at-drafting-time discipline (PR #67 introduced defect latent; PR #81 surfaced + fixed 6 rounds later).
- ✅ **(b) Team-lead Sec-Lock cross-check discipline 7-application track record body update.** Memory `feedback_team_lead_sec_ratify_lock_cross_check` body + description extended from 6-application to 7-application (+ row #7 audit (b)1 SECURITY HTML TOC line 32/33 + §4.4 line 100 + §4.5 line 366 citation verification before forwarding to F/CTO). Discipline composes mechanically; SUCCESS-cases continue to outnumber boundary-failure-cases.
- ✅ **(c) §10 attribution discipline 14-consecutive-CLEAN-surface streak body update.** Memory `feedback_decision_4_instance_ledger_cross_check` Convergence-outcome pattern extended from 12 consecutive to 14 consecutive (+ row #7 audit 13th + PR #81 mermaid 14th by-construction — mermaid syntax fixes orthogonal to §10 attribution territory).

**Pending F/CTO decisions for next session:**

1. **Phase 2 close trigger + Phase 4 entry setup** (Phase 3 exit-gate CLOSED; ADR-012 joint-close now gates Phase 4 entry on Phase 2 close only):
   - **Phase 2 close action.** Phase 2 Steps 1-10 completed 2026-05-29 per ADR-013/014/015 trio; Visual Designer notification + Phase 2 close gate criteria check pending. Per ADR-012 joint-close: Phase 4 entry requires BOTH Phase 2 + Phase 3 close.
   - **Phase 4 entry setup** (post-Phase-2-close): Linear initiative creation per ADR-009 Decision 7 (Linear-MCP integration); team teardown of `phase-2-ux-design` + `phase-3-arch-tradeoffs` teams (substantive work concluded); MILESTONES Current Phase block transition Phase 2/3 → Phase 4.
   - **Alternative: Pause + tear down team** — clean stop-point after 13-PR Phase 3 ARCH streak + Mermaid saga + Phase 3 exit-gate close. Team `phase-3-arch-tradeoffs` left alive idle pending F/CTO Decision 1.

2. **Phase 5 detail-design inventory consolidation workstream** — formal status as of §8.6 close annotation (PR #74; updated to "22+" at PR-C row #3): "Architect follow-up workstream at Phase 4 entry; candidate artifact `docs/PHASE-5-INVENTORY.md` (per-territory tables grouping Plaid implementation items + observability items + concurrency items + settings-store items + tenant-isolation tests + other Phase 5 detail-design surfaces)." Per-§-source forward-pointer commitment is durable in ARCH §8.6 + every in-line Phase 5 forward-pointer at the source-§. Item count **22+ items** spread across §3.2 + §4 Observability + §5 + §7.1 + §6 + §6.1 + §8.3 (see inventory below). Question shape: "execute the workstream at Phase 4 entry" (PR #74+ framing). No change post-PR-79.

3. **Future workstream items tracked at PR-A row #4 audit** (out-of-scope for Phase 3 close; F/CTO routes when ready):
   - **SECURITY-side Observation B discharge** — `docs/SECURITY/index.html` §4.2 line 76 + §4.3 line 94 + §4.6 line 615 paraphrase the Decision 4 four-sub-layer composition into three-layer naming (same drift class `feedback_decision_4_instance_ledger_cross_check` codifies; SECURITY-side workstream). Sec preliminary lean: Path A verbatim-restore at SECURITY since SECURITY IS canonical security-posture rationale home.
   - **ARCH section-hint convention upgrade Observation A discharge** — §3 + §4 + §4.1 + §5 don't carry FULL section-hint canonical-territory statement convention (predate PR #69 codification; not violations). Optional housekeeping for future ARCH refresh PR.

**Phase 5 V1-SHIP-BLOCK + non-V1-SHIP-BLOCK detail-design inventory** (accumulating across PRs #67/#68/#69/#71/#72-candidate; **19+ items** spread across §3.2 + §4 Observability + §5 + §7.1 + §6 + §6.1):
- **PR #67 / §3.2:** PDF-JWT-to-RLS binding mechanism (`SET LOCAL request.jwt.claims` vs parametric `WHERE users_id = $1` vs other; constrained by RT-21(e) no-service_role-escalation per Lock 13 mod #1) — substantive deferral.
- **PR #67 / §3.2:** V1-web-app TypeScript-side privileged-context helper class name (parallel to pfin_back_etl Python `TenantBoundConnection`).
- **PR #68 / §4 Observability V1-SHIP-BLOCK:** F1 Coolify Discord payload PII audit (conditional flip-gate to Shape C fallback if verification fails).
- **PR #68 / §4 Observability V1-SHIP-BLOCK:** F3 RT-21 PDF-JWT-rejection audit-log commitment.
- **PR #68 / §4 Observability:** F4 notification-target access posture (Discord channel privacy + webhook URL secrets + retention; Shape C receiver auth + flat-file permissions + log-rotation).
- **PR #68 / §4 Observability:** Coolify-cron-status visibility verification.
- **PR #68 / §4 Observability:** Optional structured-JSON-to-stdout log-format conventions.
- **PR #69 / §5:** Staging environment ramp (V2-onboarding-triggers).
- **PR #69 / §5:** Network topology detail (port allocations / reverse-proxy / TLS termination per Coolify defaults vs custom).
- **PR #69 / §5:** F2 inter-container network isolation evaluation (RT-22 credential-absence sole barrier vs explicit network isolation defense-in-depth).
- **PR #69 / §5:** Operational/admin auth mechanism (Coolify defaults vs custom hardening + SSH key/passphrase posture + Coolify privileged-mode posture).
- **PR #71 / §7.1 V1-SHIP-BLOCK:** Sec-fallback posture binding at Phase 5 — Plaid error-code taxonomy distinguishing institution-side-grant-revoked + user-side-grant-revoked sub-classes (Sec-joint-collapse to 3-class OR deferred-discriminator-via-user-flow as escape paths; Sec joint-review-mandatory).
- **PR #71 / §7.1:** Per-endpoint audit-hook table completion — Sec-consult-mandatory at Phase 5 audit-hook lock.
- **PR #71 / §7.1:** Plaid-error-code → V1-state discriminator concrete assignment — Sec joint-review-mandatory per App B §2.4 (h) routing flag (bound to Sec-fallback above).
- **PR #71 / §7.1:** `pfin_back_etl` worker-concurrency posture for Plaid scheduled-poll — Sec-consult-mandatory at Phase 5 concurrency lock (intersects Plaid rate-limit budget sharing + `TenantBoundConnection` discipline cohesion + SD-19 row-growth observability).
- **PR #71 / §7.1:** Plaid product-tier quota concrete sizing — Phase 5 reconciliation against actual Plaid bill per PRD §7.1 (a) Architect-Phase-3 cost-shape forward-pointer.
- **PR #71 / ADR-016:** Concrete `+server.ts` file paths for canonical second + third allowlist entries (`/item/public_token/exchange` + `/item/remove` — Phase 5 routing convention).
- **PR #72-candidate / §6 (a):** Specific CI tooling selection (Vitest / pytest / ESLint / Prettier / ruff / hadolint / dep scanners / secrets scanner).
- **PR #72-candidate / §6 (b) Sec-consult-mandatory:** GitHub Actions workflow YAML authoring + secret-store config; `secrets-manifest.yml` + CI-automated overlap check golden-test fixture per Sec-2 (b)3.
- **PR #72-candidate / §6 (c) V1-SHIP-BLOCK + Sec-consult-mandatory:** RT-26 grep fence script authoring + golden-test fixture per Sec-2 (a)2 (deliberately-out-of-allowlist file referencing `SUPABASE_SERVICE_ROLE_KEY`).
- **PR #72-candidate / §6 (d) V1-SHIP-BLOCK + Sec-consult-mandatory:** `TenantBoundConnection` grep fence script authoring per Lock 13 mod #3 + golden-test fixture per Sec-2 (a)2 (deliberately-outside-class `psycopg2.connect()` invocation).
- **PR #72-candidate / §6 (e) Sec-consult-mandatory:** RT-22 Dockerfile audit script authoring + golden-test fixture per Sec-2 (a)2 (deliberately-violation-shaped Dockerfile with `ENV SUPABASE_*` + `RUN apt-get install postgresql-client`).
- **PR #72-candidate / §6 (f) Sec-consult-mandatory:** Coolify auto-deploy webhook configuration + 4 posture commitments per Sec-2 (a)3 (Coolify-watches-main-only / GitHub branch protection admin-bypass restricted/disabled / webhook URL is a secret / auto-deploy permission boundaries documented).
- **PR #72-candidate / §6 (g):** Branch protection + required status checks configuration (GitHub admin operation).
- **PR #74 / §8.3 (b)2 Sec-consult-mandatory:** `auth.uid()` provenance discipline implementation at metadata recommendation engine read path — `users_id` parameter bound from `auth.uid()` not `req.body` per Lock 14 user-facing-layer discipline.
- **PR #74 / §8.3 (b)3 Sec-consult-mandatory:** Per-tenant scope migration writeup for `pfin.merchant_subcat_mapping` (Lock 7 `user_taxonomy` precedent; `users_id`-keyed mapping; first-login bootstrap seed from F/CTO's existing Master.CashFlowCategories as one-time per-tenant write).
- **PR #74 / §8.3 (b)5 Sec-consult-mandatory:** Plaid `merchant_name` adversarial-input validation library selection + integration (length cap + Unicode-control-char strip + normalization) applied across all algorithm shapes at the validation layer.

**Long-standing Phase 3 entry-gate task** (out-of-band; Phase 1 closeout origin):
- **Plaid production-tier monthly minimum confirmation** — sales/onboarding call BEFORE V1 ships; only load-bearing cost-target unknown per ADR-011 Decision 20.

**Candidate P3 follow-up** (Phase 1 closeout origin):
- FMP API + stock-screening tables (already shipped in `pfin_back_etl`) are incumbent-exceeds-V1 per `feedback_incumbent_exceeds_v1_review` guardrail; PM consult at F/CTO's choice of timing.

**Team `phase-3-arch-tradeoffs` status:** **READY FOR TEARDOWN — Phase 3 close substantive work complete.** 7-deliverable track record at §8 + §8.1-§8.6 + §9 + §9.1 + PR-A row #4 + PR-B annotation + PR-C sweep + row #7 Sec sign-off audit + PR #81 Mermaid render saga (`arch` + `sec` teammates idle post-deliverables). Team teardown gated on F/CTO Decision 1 above (Phase 4 entry setup vs pause). If teardown executed, durable state preserved at: MILESTONES.md Pending block + memory codifications + DECISIONS.md ADR-011 Decision 4 §10 CHANGELOG annotation.

**13-PR Phase 3 streak summary** (for fresh-session context):
- **PR #65 / v1.37 (2026-05-29):** ARCH §4 + §4.1 Tech Stack table restructure (8-row Option C 3-container shape) + RT-26 file-glob allowlist anchor + F11 SECURITY forward-cross-refs to §4.1. Sec Finding V1: §10 instance-numbering drift caught at verify-pass. Memory `feedback_decision_4_instance_ledger_cross_check` created with instance-numbering scope.
- **PR #66 / v1.38 (2026-05-29):** ARCH §4 Auth row (Supabase Auth + native RLS Option A baseline per ADR-011 Decision 5 / Lock 1). Sec Finding F1: §10 layer-attribution drift caught at joint-review. Memory broadened to cover both dimensions (instance numbering + layer attribution).
- **PR #67 / v1.39 (2026-05-30):** ARCH §3 Data Flow expansion (prose refresh + §3.1 Plaid webhook + §3.2 PDF render cross-container sequenceDiagrams) + F11 SECURITY forward-cross-refs to §3.1/§3.2 at RT-02/RT-05/RT-21/RT-22/RT-26. Phase 5 PDF-JWT mechanism deferral introduced. Sec F8 commendation: Architect-side pre-emptive §10 self-audit at draft. Memory broadened to codify convergence-outcome pattern.
- **PR #68 / v1.40 (2026-05-30):** ARCH §4 Observability row (Coolify→Discord incumbent + Shape C named fallback per `reference_coolify_discord_notifications` memory). **NEW PROJECT CONVENTION: conditional-lock + named-fallback pattern** introduced; memory `feedback_conditional_lock_with_named_fallback` codified immediately post-merge per Sec endorsement. F1 V1-SHIP-BLOCK PII-vector gate + F2 §4.6 pull-based detection + F3+F9 RT-21 audit-log + F10 §4.6 inventory (v).
- **PR #69 / v1.41 (2026-05-31):** ARCH §5 Deployment Topology (substantive first-time content; replaces template scaffolding). F1 operational/admin trust boundaries + F2 inter-container network isolation Phase 5. **First cross-PR composition test of v1.40 conditional-lock pattern PASSES.** §10 attribution discipline 5th consecutive surface CLEAN. Sec F5: section-hint canonical-territory statement candidate new convention.
- **PR #71 / v1.42 (2026-05-31):** ARCH §7 Integration Points + §7.1 Plaid endpoint posture (substantive first-time content; replaces template scaffolding). **NEW ADR: ADR-016** enumerates V1 RT-26 three-entry service_role allowlist composition (Plaid webhook handler + `/item/public_token/exchange` + `/item/remove`) + durably ratifies SECURITY §4.2 webhook-allowlist annotation convention. Section-hint canonical-territory statement convention (v1.41) WORKS at second application at strongest-overlap surface yet (5 owners + 1 ADR). **§10 attribution discipline 6th consecutive surface CLEAN.** Sec joint-review: 3 load-bearing + 5 advisory + 1 SECURITY sub-edit; all addressable in v2 + companion edits with no structural rework. **Verbatim-vs-paraphrase Architect self-catch** demonstrates the discipline working: first v2 paraphrased Sec mods + misread F/CTO "path (α)" timing-ratify as ADR-shape-ratify; second v2 redone against verbatim with shape stripped pending separate ratify; preserved in ADR-016 Alternatives narrative.
- **PR #72-candidate / v1.43 (2026-05-31):** ARCH §6 CI/CD Pipeline + §6.1 Sec-test catalog mapping (substantive first-time content; replaces 6-row template scaffolding). CI tool family (i) GitHub Actions CI + Coolify auto-deploy hybrid; RT-22 PR-review→CI-automation transition; **TenantBoundConnection V1-SHIP-BLOCK surface at CI per Lock 13 mod #3 verbatim** (Sec-2 (a)1 correction of Sec's misread); golden-test fixture obligations on three CI fence scripts (RT-26 + TenantBoundConnection + RT-22); 4 Coolify auto-deploy posture commitments inline. **Section-hint canonical-territory statement convention (v1.41) STRONG DURABILITY EVIDENCE at 3rd application** (9 adjacent owners — strongest territorial-overlap surface yet; Sec-2 recommends ratifying as durable project-convention). **§10 attribution discipline 7th consecutive surface CLEAN** (Sec-2 commendation: "strongest §10-drift-temptation surface yet"). **Sec-vs-Sec dispute discipline catch**: Sec (original) misread Lock 13 mod #3 V1-SHIP-BLOCK status; Sec-2 (newly-spawned parallel teammate) caught + corrected; F/CTO supersession-ratified Sec-2's (3/5/3) full package. Team-lead boundary failure (initial ratify of Sec without Lock 13 mod #3 verbatim cross-check); Architect's blocker-pause + team-lead's sign-off cross-check + Architect's redo against Sec-2 verbatim preserved the streak. **V1-SHIP-BLOCK axis orthogonal to §10 catalogued-instance axis** convention codified at v2 §10 cross-check. **Post-ratify cross-check at v1 file pattern** flagged by Sec-2 as candidate memory codification. **No new ADR** (cleanest PR shape since §5); same-PR SECURITY §4.5 RT-22 row sub-edit only.
- **PR #74 / v1.44 (2026-05-31):** ARCH §8 Trade-offs & Alternatives + §8.1-§8.6 sub-sections (substantive first-time content; replaces 1-bullet TBD stub). 16 ADR-011 Lock rationale narratives (Decisions 5-20) in Chose-X/rejected-Y/because-Z future-self-memory shape; §7.1 PR #71 cost-architecture forward-pointer resolved; §7.1 PR #71 algorithm-choice forward-pointer resolved as **Hybrid (iii) per-vendor most-recent-Sub-Cat primary + (i) deterministic merchant-string augmented seed + (ii) trained classifier V2+ deferral** per F/CTO mid-cycle ratify; 62-entry App B routing-home consolidation (50 [Architect Phase 3] + 12 [Architect/Sec joint]); Phase 5 inventory consolidation named as separate Architect follow-up workstream at Phase 4 entry per §8.6 (candidate artifact `docs/PHASE-5-INVENTORY.md`). **Section-hint canonical-territory statement convention (v1.41) 4th application** at strongest-territorial-overlap surface yet (9+ neighbors; durable per PR #72 + Sec-2 (b)5 ratify). **§10 attribution discipline 8th consecutive surface CLEAN on FULL three-axis basis** — Sec caught §8.5 four-layer paraphrase drift (layer-attribution drift class per `feedback_decision_4_instance_ledger_cross_check`); Architect discharged via Path B drop-enumeration-let-link-carry at v2; Sec re-verify CLEAN. **Team-lead Sec-Lock cross-check discipline 2nd-application strong-validation** (`feedback_team_lead_sec_ratify_lock_cross_check`): team-lead pre-relay cross-checked Sec's Decision 4 lines 352-355 citation against DECISIONS.md verbatim BEFORE forwarding (a)1 finding to F/CTO; Sec's citation accurate, Architect's Path B fix landed CLEAN — first SUCCESS-application after PR #72 boundary-failure codification. **Path-B Decision-4-link-carries-the-lookup v2-fix shape codified** as sub-bullet on `feedback_decision_4_instance_ledger_cross_check` (deterministic choice criterion: Path B when §-surface section-hint frames REFERENCES-not-ABSORBS; Path A when §-surface ABSORBS-and-restates). Sec posture v2-narration tightenings: (b)2 auth.uid() provenance + (b)3 per-tenant scope commitment + (b)4 V2+ ML-classifier Lock 13+14+1 composition + (b)5 Plaid `merchant_name` adversarial-trusted section-level annotation. **No new ADR; no SECURITY §4 new bullet** (cleanest PR shape since §5 + §6).
- **PR #76 / v1.45 (2026-05-31):** ARCH §9 Open Questions + §9.1 (substantive first-time content; 1-bullet TBD stub → 8-row Phase 3 close ledger + closing boundary annotation; +70/-3 LOC at docs/ARCH/index.html lines 914-988). 4 ratify gates closed verbatim: scope-shape (c) hybrid 8 rows + section-hint statement shape verbatim (5th application; §8.6 own Phase 5 workstream + Phase 4 transition + MILESTONES.md + ADR-009/ADR-012 + ARCH §1-§8 own decisions) + phase-relative date anchors (5 anchors: Phase 3 close PR / Phase 3 exit gate / Phase 4 entry / Before V1 ships / F/CTO choice timing) + 8-row owner+resolve-by assignments verbatim with DOUBLE verbatim match at rows #7+#8 against MILESTONES.md line 36 Phase 3 exit criteria. Row #5 Path B applied **by-default** per PR #74 codification (first downstream re-application; foreclosed drift surface at v1 by design — names 3 drift-class catches by PR + links to ADR-011 Decision 4; does NOT enumerate four-layer composition). Sec joint-review CLEAN single sign-off: 0 (a) load-bearing; 7 (b) endorsements + 1 advisory ((b)1 §9.1 Phase 5 paragraph snapshot-vs-live-state delta — Sec preference Path A leave-as-is + endorse; F/CTO ratified Path A); 0 (c) sub-edits. **Team-lead Sec-Lock cross-check 3rd application** — verified §8.6 line 908 stale "19+" + MILESTONES.md line 106 live "22+" + §9.1 v1 lines 31+93 live "22+" before forwarding F/CTO. **§10 attribution discipline 9th consecutive surface CLEAN on FULL three-axis basis.** v1 ships AS v2 (Path A); Architect applied v1 body directly to ARCH §9. PR: PR #76 `phase/plan-arch-9-open-questions` merged at e9f4200.
- **PR #77 / v1.46 (2026-06-01):** ARCH §3 + §4 surgical Path B fixes (+2/-2 LOC) — PR-A of (c) HYBRID 3-PR Phase 3 close sequence discharging §9 row #4 cross-section consistency audit. F/CTO chose (c) HYBRID over (a) all-combined / (b) four-split per `feedback_late_phase_density_overload` decisional-tempo separation. Architect's cross-section audit returned **NO HARD DRIFT** across 15 §-surfaces + 3 soft Path B findings + 2 out-of-scope observations (Observation A: §3+§4+§4.1+§5 predate section-hint convention; Observation B: SECURITY §4.2 line 76 + §4.3 line 94 + §4.6 line 615 paraphrase Decision 4 four-sub-layer composition into three-layer naming — SECURITY-side workstream tracked future). Sec joint-review CLEAN single sign-off: 0 (a) load-bearing; 8 (b) endorsements with mixed (b)1-(b)3 dispositions (Path B at #1+#2 applied; KEEP at #3 — §4.1 IS canonical RT-26 anchor; numbered list IS substantive RT-positioning territory) + 1 (c)1 surgical sub-edit (audit working-artifact citation refinement: SECURITY §4.2 line 76 + §4.3 line 94 + §4.6 line 615 = 3 occurrences). **Team-lead Sec-Lock cross-check 4th application** — verified Sec's SECURITY §4.2/§4.3/§4.6 citation refinement accurate via independent grep. **§10 attribution discipline 10th consecutive surface CLEAN on FULL three-axis basis.** KEEP-at-canonical-anchor disposition pattern emerges as third disposition alongside Path A / Path B (codified in `feedback_decision_4_instance_ledger_cross_check` body at PR #75 → extended at this cleanup). PR: PR #77 `phase/plan-arch-row-4-consistency-audit` merged at 4e08dfd.
- **PR #78 / v1.47 (2026-06-01):** DECISIONS.md ADR-011 Decision 4 §10 attribution-discipline CHANGELOG annotation lands (+12/-0 LOC). PR-B of (c) HYBRID 3-PR sequence discharging §9 row #5 §10 catalogued-instance ledger CHANGELOG-style audit. F/CTO mid-cycle ratify: CHANGELOG narrative HOME = **Option 1 Meta-pattern annotation at Decision 4 (lightest)** over Option 2 ADR-011 closing addendum / Option 3 Decision 4 inline / Option 4 new ADR-017. Architect v1 ~20-line annotation: header + 3 drift-class entries (PR #65 instance-numbering / PR #66 layer-attribution / PR #74 §8.5 four-layer paraphrase verbatim per memory) + Discipline state paragraph (10-consecutive-CLEAN + 6-application-Path-B-track-record) + Maintenance note. Path B applied by-construction (5th application — annotation REFERENCES Decision 4 structure via "per the [bullet name] above" links; does NOT enumerate). Sec joint-review: 0 (a) load-bearing; 5 (b) endorsements + 1 advisory ((b)3 JWT-shape-layer terminology Option A KEEP — F/CTO ratified); 1 (c)1 surgical sub-edit — **Sec FOUND verbatim drift** in Architect's quote of PR #66 v2-mod wording (Architect dropped "of the §10 three-layer defense" from live ARCH §4 line 365). **Team-lead Sec-Lock cross-check 5th application** — grep-verified Sec claim accurate; (c)1 verbatim restore landed at v2. **§10 attribution discipline 11th consecutive surface CLEAN on FULL three-axis basis.** Annotation lands at DECISIONS.md lines 361-371 (Decision 5 heading shifted 361 → 373); Decision 4 canonical structure UNCHANGED. PR: PR #78 `meta/decisions-row-5-changelog` merged at a74ffee.
- **PR #79 / v1.48 (2026-06-02):** ARCH HTML mechanical sweep (+3/-3 LOC: line 897 anomaly fix + lines 908 + 910 §8.6 "19+"→"22+" stale-snapshot cohesion sweep with §8.3 distribution-list addition). PR-C of (c) HYBRID 3-PR sequence; combined §9 row #3 ARCH HTML header refresh + §9 row #6 ADR-011 Decision-N attribution verification. Architect audit: TOC CLEAN (9 §-entries map to existing anchors); footer-status CLEAN; §8.6 sweep applied at lines 908 + 910 (cohesion sweep mirrors PR #74 §8.2 cost-figure cohesion precedent endorsed at PR-A row #4 (b)4); cross-§ anchor sanity-check surfaced 1 anomaly at line 897 (self-inflicted PR #74 §8 v2 typo: ARCH §7.2 doesn't exist; only §7.1 sub-section). Row #6: 16-Lock-to-Decision pairing audit CLEAN; no-orphan reference check CLEAN; Sec-territory Lock attribution discipline (Locks 1 RLS + 13 TenantBoundConnection + 14 user-facing-layer + 15 schema-level orthogonality) all CLEAN per Sec independent spot-checks at line 509/537/744/783/734/746; random sample non-Sec-territory Locks 2/7/11 CLEAN; dual attribution form Lock-N-mod-#M / Decision-N-mod-#M = convention variance NOT drift. Sec joint-review CLEAN single sign-off: 0 (a) load-bearing; 6 (b) endorsements + 1 (c)1 surgical sub-edit (line 897 anomaly fix candidate (b) — BOTH href + link text update: `#sec-7-2` → `#sec-7-1` + "ARCH §7.2" → "ARCH §7.1"). **Team-lead Sec-Lock cross-check 6th application** — grep-verified Sec's line 897 verbatim citation. F/CTO ratified (c)1 only (skipped optional micro-clarification of first "§7.2" in parenthetical — PRD context implied from App B §7 (b) routing-home). **§10 attribution discipline 12th consecutive surface CLEAN on FULL three-axis basis.** **(c) HYBRID 3-PR Phase 3 close sequence COMPLETES at PR-C merge** — all 4 §9 Architect-owned rows (#3 + #4 + #5 + #6) discharged. Rows #7 + #8 Phase 3 exit gates open next. PR: PR #79 `phase/plan-arch-mech-sweep` merged at 09fca85.
- **PR #81 / 2026-06-02 (Mermaid render saga; Phase 3 exit-gate close-trigger):** F/CTO surfaced §3.1 + §3.2 sequenceDiagrams as not-rendering during row #8 ARCH v1.0 review via serve-docs comment widget. 6-round whack-a-mole saga (round-1 `<br/>` → `<br>` wrong direction; round-2 alias `<br>` flatten; round-3 aggressive alias simplification; round-4 `&#NN;` HTML entities defeated by browser pre-decode; round-5 `#NN;` Mermaid-entity syntax (NO `&` prefix) works; round-6 message-text shortening for readability + `.gitignore docs/test-*.html`). F/CTO meta-feedback ("why do we keep making the same mistakes?") at round-3 drove pivot to isolated `docs/test-mermaid.html` test-harness pattern with 8 cases (bare minimum → full §3.1) — pinpointed full constraint set in 2 iterations vs 3 prior guessing rounds. **NEW MEMORY `feedback_mermaid_sequencediagram_constraints` codified** capturing constraints (cascading-failure-across-blocks; NO line breaks of any type in messages; `"..."` wrap on special chars; `;` → `#59;` + `#` → `#35;` Mermaid entities NO `&` prefix; short messages render better) + recovery discipline (isolated test-harness pattern; converts debug cycle ~5+ min/round → ~30 sec/round) + browser-render-verification-at-drafting-time discipline (PR #67 introduced defect latent; PR #81 fixed 6 rounds later — future Mermaid PRs must browser-verify at drafting time). **§10 attribution discipline 14th consecutive CLEAN surface** by-construction (mermaid syntax fixes orthogonal to §10 territory; row #7 audit = 13th surface immediately prior). **Phase 3 exit-gate CLOSED** post-PR-81-merge: row #7 Sec sign-off APPROVED (per row #7 comprehensive audit; (b)1 SECURITY TOC/header drift deferred as housekeeping; team-lead Sec-Lock cross-check 7th application verified Sec citations at lines 32/33/100/366) + row #8 F/CTO sign-off on ARCH v1.0 APPROVED (via reading guide review + browser-verify of fixed mermaid). Phase 4 entry gates only on Phase 2 close per ADR-012 joint-close. PR: PR #81 `phase/plan-arch-mermaid-fix` merged at 4eb1562.

**Sync-mismatch echo investigation logged:** `feedback_self_triggered_task_assignment_echo` memory (created 2026-05-31). 14 sync-mismatch echoes / 20 task_assignment events (~70% rate) caused by self-triggered task_assignment notifications arriving async after agent's own TaskUpdate self-claim. Mitigation: pre-brief block in team-mode agent spawn prompts.

---

## Roadmap

[awk cutoff for SessionStart hook lives at this `## Roadmap` line per template's auto-load pattern. Content above this line is auto-loaded; content below is consult-on-demand.]

### Meta-process milestones

| # | Milestone | Status | Gate | Linear Project | Notes |
|---|---|---|---|---|---|
| **M0** | Research (PRD lock) | ✅ COMPLETE 2026-05-26 | PRD locked at end of mosko Phase 1 (Step 4 ratified + close-work merged via PRs #51 / #52 / #53 / #54 + this PR) | _TBD on /setup-linear-team_ | Retro-tagged issues: Step 3 + Step 3.5 PRs (see [Completed Features](#completed-m0-retro-tagged) below). Plus the ADR-009 brainstorm-adoption arc (PRs #39–#42). Plus the Step 4 architectural drilling cycle ratifying 16 locks + 4 meta-patterns + candidate P3 disposition per [ADR-011](DECISIONS.md#adr-011) (PRs #51–#55 close-work sequence). |
| **M1** | Plan (ARCH + SECURITY docs) | Active (Phase 3 entry-ready) | ARCH + SECURITY docs locked at end of mosko Phase 3 | _TBD_ | Initial issues: (a) Draft `docs/ARCH/index.html` consuming ADR-011 + 13 Phase 3 carry-over tasks; (b) Extend `docs/SECURITY/index.html` with Phase 3 architectural decisions (V1 Sec posture already landed via ADR-008 + ADR-011 + PRs #45 / #52 / #53 — Phase 3 extends with implementation-detail Sec decisions); (c) Populate product milestones in MILESTONES.md → see [`docs/MILESTONE-FRAMING.md`](docs/MILESTONE-FRAMING.md); (d) Plaid production-tier monthly minimum confirmation (out-of-band Phase 3 entry-gate); (e) further granularity TBD. Also catches Phase 4 (Project Scoping) work as part of issue (c). |
| **M2** | Build (V1.0 → V1.final) | Pending | V1 ship | _TBD; defined as M1 issue (c) output_ | Product milestones (V1.0 / V1.1 / V1.final) get defined as M1's "Populate product milestones" output. M2 then decomposes into sprints. |
| **M3** | Deploy & Iterate | Pending | V1 in production | _TBD_ | mosko Phase 7 territory; ongoing project-scale I+V loops post-V1 ship. V2 candidates from [`BACKLOG.md`](BACKLOG.md) feed here. |

### Product milestones

| Milestone | Status | Gate | Notes |
|---|---|---|---|
| V1.0 / V1.1 / V1.final / V2-X | _NOT YET DEFINED_ | _Defined by M1 issue (c) output_ | See [`docs/MILESTONE-FRAMING.md`](docs/MILESTONE-FRAMING.md) for the V1 sub-version convention + drop-replace migration pattern + Phase 4 handoff anchor per ADR-004. |

## Sprints (Linear cycles)

| Sprint | Milestone | Start | End | Features | Status | Notes |
|---|---|---|---|---|---|---|
| _none yet — sprints activate at Phase 6 entry per ADR-009 Decision 7_ | | | | | | |

## Features

### Completed (M0 retro-tagged)

Issues retroactively tagged under M0 (Research milestone). Full PR history at [`CHANGELOG.md`](CHANGELOG.md).

| Feature | PR | Merged | Notes |
|---|---|---|---|
| ADR-010 PR 2 — comments-sidecar pass 2 (server + widget + `/serve-docs`) | #48 | 2026-05-24 | Closes the ADR-010 two-PR landing. Python stdlib HTTP server + JS widget + `/serve-docs` skill + asset wiring in all three HTML docs (PRD / ARCH / SECURITY) + WORKFLOW.md Inline-authoring subsection. Both `/refine-doc` and `/serve-docs` now available; Step 4 PRD review can begin with widget support. |
| ADR-010 PR 1 — comments-sidecar pass 1 (convention + `/refine-doc`) | #47 | 2026-05-24 | Adopted comments-sidecar feature from `richmosko/project_template` per ADR-009 Decision 8 selective-adoption framework. Pass 1 of 2: convention + skill + gitignore + WORKFLOW.md Doc-review-loop section + ADR-010. |
| PR B — PRD content migration to HTML artifact set | #45 | 2026-05-23 | Tasks #12 + #13 + #14 (ADR-009 Decision 4 second stage; closes the architectural shift). Migrated §1/§2/§3/§6/§7/appendices to `docs/PRD/index.html`; §4 → `docs/SECURITY/`; §5 → `BACKLOG.md`; §8 → `docs/MILESTONE-FRAMING.md`; `PRD.md` archived. |
| PR C — SessionStart hook → MILESTONES.md head | #44 | 2026-05-23 | Task #11 (ADR-009 Decision 6 mechanics; compact-ledger auto-load) |
| MILESTONES.md initial ledger | #43 | 2026-05-23 | Tasks #7 + #15 (this file's creation) |
| Adapt doc-update skills + retire /ship-branch | #42 | 2026-05-23 | Task #17 (ADR-009 Decision 9 implementation) |
| Extract WORKFLOW.md changelog to CHANGELOG.md | #41 | 2026-05-23 | Task #10 (orthogonal cleanup) |
| PR A — HTML doc scaffolding for PRD/ARCH/SECURITY | #40 | 2026-05-23 | Task #18 (ADR-009 Decision 4 first stage) |
| ADR-009 — selective project_template adoption | #39 | 2026-05-23 | Task #9 (9-Decision synthesis ADR) |
| Step 3.5 PR 11 — post-rewrite verify pass closure | #38 | 2026-05-20 | v1.30; 14-gate VP walk; 3 surgical edits; Sec hard-line preserved |
| Step 3.5 PR 10 — Step 3.5 closure + Appendix B consolidation | #37 | 2026-05-19 | v1.29; 114-entry App B body lifted + classified |
| Step 3.5 PR 9 — §8 rewrite | #36 | 2026-05-19 | v1.28; last PM-led drafting task in Phase 1 Step 3 |
| Step 3.5 PR 8 — §7 rewrite | (prior PR) | 2026-05-19 | v1.27 |
| Step 3.5 PR 7 — §6 rewrite | (prior PR) | 2026-05-19 | v1.26 |
| Step 3.5 PR 6 — §5 rewrite | (prior PR) | 2026-05-19 | v1.24 |
| Step 3.5 PR 5 — §4 rewrite (Sec primary author) | (prior PR) | 2026-05-19 | v1.23 |
| Step 3.5 PR 4 — §3 rewrite | (prior PR) | 2026-05-19 | v1.22 |
| Step 3.5 PR 3 — §2 rewrite + Appendix C | (prior PR) | 2026-05-18 | v1.21; 32 story traces extracted |
| Step 3.5 PR 2 — §1 rewrite | (prior PR) | 2026-05-18 | v1.20 |
| Step 3.5 PR 1 — Step 3.5 kickoff + archive | (prior PR) | 2026-05-18 | v1.19; PRD-v1.18 source archived |
| Step 3 §1–§8 PRD section drafting | PRs #10–#24 | 2026-05-09 → 2026-05-18 | Initial PRD drafting + ADRs 002–008 + Sec at-lock passes |
| Step 2 + Step 1 + Phase 0 / 0.5 bootstrap | PRs #1–#9 | 2026-04-24 → 2026-05-09 | Operating model + agent roster + WORKFLOW.md / DECISIONS.md / CLAUDE.md foundations |

### In Flight (M1)

| Feature | PR | Milestone | Branch | Status |
|---|---|---|---|---|
| _none active — between PRs; Step 4 (Architect PRD ratification) is the immediate next deliverable_ | | | | |
| _Substantive M1 features open after Step 4 ratifies / Phase 3 entry_ | | | | |

### Backlog (M1+)

Pulled from Linear once activated. Until then, M1's initial issues are listed in the [Meta-process milestones](#meta-process-milestones) table above. V2 candidates live in [`BACKLOG.md`](BACKLOG.md).

## Releases

| Version | Date | Milestone shipped | Notes |
|---|---|---|---|
| _none yet — first tagged release at V1.0 ship (defined by M1 issue (c) output)_ | | | |

## Decisions

The Decision Log lives in [`DECISIONS.md`](DECISIONS.md) (consult-on-demand per [ADR-009](DECISIONS.md#adr-009) Decision 6). Hybrid ADR format policy (consolidation vs terse) per ADR-009 Decision 8.

**Most recent ADRs:**

- **[ADR-010](DECISIONS.md#adr-010)** (2026-05-24; terse) — Adopt comments-sidecar feature from project_template. Two-PR landing: pass 1 (convention + `/refine-doc` skill); pass 2 (server + widget + `/serve-docs` skill).
- **[ADR-009](DECISIONS.md#adr-009)** (2026-05-23; consolidation) — Selective adoption of richmosko/project_template patterns. 9 Decisions consolidating the template-adoption brainstorm.
- **[ADR-008](DECISIONS.md#adr-008)** (2026-05-18; consolidation) — Phase 1 Step 3 §4 lock: V1 security posture canonical reference. 5 Decisions including 14-entry SD matrix + 15-entry RT catalog.
- **[ADR-007](DECISIONS.md#adr-007)** (2026-05-17; terse amendment) — Amendment to ADR-002 Finding (b): tax-loss-harvesting reclassified V2+ → permanent non-goal.
- **[ADR-006](DECISIONS.md#adr-006)** (2026-05-17; terse amendment) — Amendment to ADR-004 Decision D: V1 input-layer characterization (bracket schedules + tax_character enum).
- **[ADR-005](DECISIONS.md#adr-005)** (2026-05-13; terse amendment) — Amendment to ADR-002 §1.2: planning-targets V1 static reference-value rendering.

Full ADR list and historical entries at [`DECISIONS.md`](DECISIONS.md).
