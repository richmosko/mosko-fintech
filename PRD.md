---
artifact: PRD.md
project: mosko-fintech
version: 0.1
status: draft — Phase 1 / Step 3 (section-by-section)
owner: Product Manager (agent) on behalf of Founder/CTO
last-updated: 2026-05-12
source-of-truth-for: V1 product scope, user stories, success metrics, non-goals
upstream: DECISIONS.md (ADR-001, ADR-002), WORKFLOW.md
downstream: ARCHITECTURE.md (Phase 3), Linear initiatives/projects (Phase 4)
---

# Product Requirements — mosko-fintech V1

> **Reading order:** this PRD is the single source of truth for what mosko-fintech V1 is. Architectural and infrastructure decisions live in `ARCHITECTURE.md` (Phase 3); backlog decomposition lives in Linear (Phase 4+). Anything locked here flows downstream; anything locked downstream must trace back to a requirement here.

## 1. Vision and target user

### 1.1 Vision

mosko-fintech is a personal financial observatory: a single, trustworthy view of one individual's complete financial position across every account they hold — checking, savings, credit, brokerage, retirement, loans, crypto — plus the activity (transactions and investment flows) and derived measures (net worth, asset allocation, monthly spending and income by category, estimated taxes) that make the position meaningful. V1 progressively replaces the working manual-spreadsheet system the V1 instance maintains today; the V1 done bar is functional parity with that system, with the **monthly Finance Report** as the canonical deliverable. V1 is observational in scope: it surfaces the user's position and the gap between their target allocation and actual allocation, but it does not move money, generate specific buy/sell recommendations, or act in any advisory capacity. The goal is clarity in the user's own hands — the user decides; the tool shows.

### 1.2 Target-user archetype

The V1 target user — *the self-directed multi-account owner* — is a financially-engaged individual with a fragmented multi-account portfolio, someone who has accumulated, over years, a mix of account types that no single bank or brokerage view covers and that public consumer-finance tools tend to oversimplify or misrepresent.

The archetype has the following defining attributes:

- **Multi-institution footprint.** Accounts spread across multiple banks, brokerages, retirement custodians, credit issuers, and at least one crypto venue. No single institution dashboard captures their position; aggregation is non-optional.
- **Mixed tax-treatment portfolio across multiple jurisdictions.** Holdings span taxable, tax-deferred, and tax-exempt accounts (and HSA, which is conditional on use). Their tax thinking is jurisdictional — they reason about Federal and state (California, in the V1 instance) tax obligations in parallel, not as a single combined number. Tax treatment and jurisdiction are first-class attributes of how they think about money, not afterthoughts.
- **Holds investment securities, not just deposits.** Brokerage and retirement accounts contain equities, funds, fixed income, and possibly other instruments. They care about cost basis and unrealized gain/loss, not just balances.
- **Self-directed, with an observational tool.** They make their own financial decisions — portfolio allocation, rebalance timing, spending changes, account-level moves — and they use this tool as input to those decisions, not as a source of them. They want to *see* their position clearly and *manage* the gap between their target allocation and their actual allocation.
- **Precise about categorization.** They maintain deliberate, two-level taxonomies — a top-level category and a sub-category — for both the holdings they own (e.g., Equity → US-Index_Non_Sector; Bonds → T-bill; Alternatives → REIT) and the cash-flow activity their accounts generate (Income, Expenses, and other flow types, each with sub-categories of their own). A coarse-bucket tool that summarizes "you have $X in equities" without distinguishing US-sector / international / index-vs-growth would be unusable to them. They assign holdings and transactions to their buckets themselves and update those assignments as their portfolio evolves; the categorization grammar is theirs to define and theirs to apply.
- **Privacy- and control-conscious.** They are uncomfortable with consumer-finance tools that monetize their data, push affiliate products, or surface "insights" that are really marketing. They prefer a tool they own, that holds their data on their terms.
- **Comfortable with substantial manual curation.** They maintain their financial system actively, by hand, on a monthly cadence — entering or reconciling transactions, refreshing valuations on held-away assets, and curating the taxonomies that categorize what they own. They will not tolerate a tool that pretends the un-aggregated accounts don't exist, and they accept that some accounts and asset classes will continue to require manual entry indefinitely.

The Founder/CTO is the V1 instance of this archetype. V1 is built for and validated by a population of one.

### 1.3 Why this archetype, not "me specifically"

The PRD frames the target user as an archetype rather than as the Founder/CTO personally because three locked decisions in `DECISIONS.md` make the archetype framing load-bearing rather than aspirational:

1. **Multi-tenant from day one** (ADR-002). The data model, auth boundary, and isolation guarantees treat the user identity as a first-class entity from V1. Tying the PRD to a single named person would be a fiction the architecture explicitly refuses to maintain.
2. **Forward-compatibility commitment** (ADR-002). V2+ broadens distribution to invite-only — a small cohort of *self-directed multi-account owners*. Defining the archetype now means V2 inherits a known target population rather than re-litigating who the next users are.
3. **Single-user usage model in V1** (ADR-002). The archetype framing does not contradict this — V1 ships with one user (the Founder/CTO) actively using it. The archetype is the *shape* of the user the product is designed for; the V1 *instance* of that shape is the Founder/CTO. The two are deliberately decoupled.

The practical consequence: V1 success means "this works for the Founder/CTO specifically." V1 product correctness means "every requirement traces to an attribute of the archetype, not to a one-off preference." When the two conflict — when a Founder/CTO preference is not a generalizable archetype attribute — the conflict surfaces as a flag for V1.0/V1.1 milestone-sequencing decisions (Phase 4 territory, not this section's problem). Post-script-audit, *V1 correctness* also carries an existing-system-replacement test: per the §8 V1-done definition, V1 is correct when it replicates the workflows the F/CTO maintains today in their manual-spreadsheet system, plus the explicit ADR-004 amendments.

### 1.4 What this PRD section is not addressing about the user

To keep the archetype defensible and avoid scope creep into adjacent personas, the following user-shape questions are explicitly **deferred**, not silently elided:

- **Household and joint-account semantics.** The V1 archetype is individual-scoped. Joint accounts owned with a spouse or partner appear in V1 as accounts the individual has access to, not as a multi-owner data model. Multi-scope ownership *within* a single user's household — multiple legal-ownership scopes (e.g., personal, trust, retirement custodial) held by one person — is a V1 data attribute per ADR-004 Decision B; the data model carries it. But household-level user-facing semantics (shared budgets, multi-owner net worth, partner visibility, per-scope user-facing reports) are out-of-scope for this PRD lifecycle and are routed to a future PRD revision if and when invite-only V2 demand surfaces.
- **Geographic, currency, and multi-jurisdictional tax scope.** V1 assumes a US-domiciled user with USD-denominated accounts. Multi-currency support is V2+ (ADR-002 reclassification). V1 tax handling is jurisdiction-specific to the V1 instance's situation — US Federal plus California state — per ADR-004 Decision D. Multi-state tax handling (a user with non-California state obligations) and non-US tax handling (RRSP, ISA, foreign tax credits, etc.) are V2+, not just non-USD currency.
- **Life-stage and goal-tracking framing.** The archetype is defined by what they own and how they think about their money today, not by retirement goals, savings targets, or life events. Goal-tracking is a candidate V2 surface; it does not shape the V1 archetype.
- **Advisor or accountant collaboration.** The advisor/fiduciary role is out-of-scope for this PRD lifecycle (ADR-002). Read-only exports for an accountant at tax time are a candidate V2 feature, not an archetype attribute.

These deferrals are not non-goals in the permanent sense; they are scope boundaries for this PRD lifecycle.

## 2. V1 user stories

### 2.1 Net worth

#### Primary stories

> **2.1.1 Current net worth (Net Asset Value).**
> As the self-directed multi-account owner, I want to see my current total net worth — formally my **Net Asset Value (NAV)**, defined as **Gross Asset Value** (the sum of all asset balances, including Real Estate) **minus Debt** (the sum of all liability balances) **minus Realized Tax Liabilities** (my current accrued Federal and California estimated-tax obligation, net of estimated payments already made) **minus Unrealized Tax Liabilities** (the estimated tax that would be owed if all current unrealized capital gains were realized today) — so that I have a single trustworthy number that reflects my whole financial position at a glance, conservatively adjusted for the tax obligations my position carries, without doing the arithmetic across institutions myself.
>
> *Traces to:* ADR-002 §1.0 (net worth ratified as a V1 surface); §1.3 + §1.5 + §1.9 (account-category coverage and per-account-type boundaries — every account category contributes to Gross or Debt); ADR-002 §1.7 (V1 carries aggregate cost basis and unrealized G/L per position — the input that drives Unrealized Tax Liabilities). **ADR-004 Decision D** (V1 includes estimated quarterly tax computation in primitive form with Federal + California FTB parallel marginal-rate inputs; "Realized vs Unrealized Tax Liabilities line items derivable from the estimated-tax surface" is bulleted as in-scope). **`docs/v1-parity-matrix.md` line 174** (existing-system NAV definition: `Gross + Real Estate − Debt − Realized Tax Liabilities − Unrealized Tax Liabilities`) and **line 99** (the Finance_Report Account Holdings table layout) — V1 preserves the existing definition.

> **2.1.2 Net worth over time.**
> As the self-directed multi-account owner, I want to see how my net worth has changed over time, displayed as a time series with a monthly default and the ability to view weekly or daily granularity on demand. I also want an **inflation-adjusted overlay** rendered as a second NAV line on the same chart — normalized to today's $ value using a CPI-U price-index series, visually distinct from the nominal line, covering a 60-month rolling window, and drawn simultaneously rather than behind a toggle. Together these let me understand whether my overall financial position is improving, holding, or declining over a meaningful horizon in both nominal and real-purchasing-power terms, and drill into shorter periods when I'm investigating a specific inflection point. The 60-month rolling window is fixed in V1; user-configurable rolling windows are V2+.
>
> *Traces to:* Preliminary finding (a) "over time" framing, ratified in ADR-002 §1.0. Granularity hybrid (monthly default with weekly/daily override) is a V1 product expectation; architectural cost of supporting multiple granularities is acknowledged and routed to Architect (existing "period aggregation for the time series" flag in §2.1's Open routing flags block). **`docs/v1-parity-matrix.md` line 77** ("Nav Chart … inflation-normalized 60-month rolling window") and existing-report ground truth from F/CTO direct inspection of `Finance_Report_2026_04.pdf` page 3 "Category Totals" chart: two NAV lines overlaid on a single chart — black solid "NAV" (nominal) + red dashed "NAV (Inf-Norm)" (inflation-normalized to today's $); both lines drawn simultaneously, not toggled. The today's-$ normalization basis here is intentionally distinct from §2.1.3's prior-Year-End reference basis: the chart shows continuous purchasing-power trajectory while the §2.1.3 panel shows spot-delta comparisons against a tax-year anchor. The §2.1.2 chart, §2.1.3 panel, and §2.1.4 reference-values table all consume the same CPI-U series — sourcing (live API vs. manual entry) is the Architect routing flag already in §2.1's Open routing flags block (parity-matrix open product decision #10).

> **2.1.3 Multi-horizon NAV-delta panel.**
> As the self-directed multi-account owner, I want my current NAV displayed alongside a panel of delta measurements over five fixed time horizons — **Month** (vs. one month ago), **YTD** (vs. the most recent year-end), **1-Year** (trailing 12 months), **3-Year** (trailing 36 months), and **5-Year** (trailing 60 months) — each shown as both a dollar change and a percentage change, with an **Inflation Adjusted** column displayed side-by-side covering the three multi-year horizons (1-Year, 3-Year, 5-Year) and referenced to the prior Year-End (Month and YTD have no Inflation Adjusted value), so that I can see at a glance whether my position is moving in the direction I expect over both my normal monthly decision cadence and the longer horizons that matter for compounding and retirement planning, and so that a multi-year nominal gain that inflation has consumed doesn't read as growth. The horizon set is fixed in V1; user-configurable horizons are V2+.
>
> *Traces to:* ADR-002 §1.0 (net worth is a V1 surface). **`docs/v1-parity-matrix.md` lines 100, 156, 157, 219** (existing-system multi-horizon NAV-delta panel with inflation-adjusted variants for multi-year horizons — V1 preserve; "Multi-horizon trailing returns" listed as a capability new to PRD). Existing-report ground truth from F/CTO direct inspection of `Finance_Report_2026_04.pdf` page 3 ("Net Assets Value (NAV) Performance" panel): two columns `NAV Delta` | `Inflation Adjusted`; five rows `Delta Month` / `Delta YTD` / `Delta 1-Yr (TR)` / `Delta 3-Yr (TR)` / `Delta 5-Yr (TR)`; Inflation Adjusted populated for 1-Yr / 3-Yr / 5-Yr only with a prior-Year-End reference basis per section footnote ("*Inflation Adjustment referenced to prior Year-End*"). Horizon set: fixed V1, user customization V2+. Historical NAV depth: V1 imports the existing Google Sheet's monthly NAV history (Dec-2015 forward) so the 5-Year horizon is meaningful at launch — F/CTO has locked the *whether*; the *how* is routed to Architect (Phase 3). CPI-U series and chart-overlay shape are queued as §2.1.2 downstream-extension flags; the §2.1.3 panel and §2.1.2 chart both consume the same CPI-U series.

> **2.1.4 NAV at three reference dates.**
> As the self-directed multi-account owner, I want my NAV displayed at three fixed reference dates: **This Month** (current), **Prior Month** (one month ago), and **Prior Year-End** (the most recent December close). I want each of those NAV values shown in two columns — **NAV** (nominal dollars) and **NAV — Prior Yr $** (the same value expressed in prior-Year-End dollars after inflation adjustment). Together these give me a quick read of my NAV at the anchor dates that frame my monthly review and tax-year context, in both nominal and inflation-adjusted terms. The reference-date set is fixed in V1; user-defined reference dates are V2+.
>
> *Traces to:* ADR-002 §1.0 (net worth is a V1 surface). **`docs/v1-parity-matrix.md` lines 100 and 158** (existing-system comparison anchor: "comparison anchor (This Month / Prior Month / Prior YE) with nominal + prior-year-$ inflation-adjusted columns" — V1 preserve; "Comparison anchor (This Mo / Prior Mo / Prior YE) — Asset Summary Summary panel — V1 preserve — Used in monthly report — Maps to PRD §2.1"). Existing-report ground truth from F/CTO direct inspection of `Finance_Report_2026_04.pdf` page 3, top sub-surface within the "Net Assets Value (NAV) Performance" section: three rows `Assets (This Month)` / `Assets (Prior Month)` / `Assets (Prior Year-End)`; two columns `NAV` | `NAV - Prior Yr $`; all six cells populated. Inflation reference basis is prior Year-End — consistent with §2.1.3's Inflation Adjusted column; same CPI-U series. This story is a sibling to §2.1.3 (the multi-horizon NAV-delta panel) within the same Finance_Report "Net Assets Value (NAV) Performance" section; together they answer "what is my NAV at the anchor dates that frame my review?" (this story) and "how much has my NAV changed over horizons?" (§2.1.3). CPI-U source decision remains the §2.1.2 downstream-extension Architect flag in §2.1's Open routing flags block.

> **2.1.5 Net worth composition.**
> As the self-directed multi-account owner, I want my NAV decomposed in a single integrated table that builds up from individual account rows through explicit subtotal lines to the final NAV. Each account row shows both the account's **current value** and its **unrealized gain/loss**, grouped by account-type category within the asset half of the table (depository, investment, retirement, crypto, manual/other), with Real Estate accounts forming their own group adjacent to the non-RE asset categories. The buildup subtotals run in order: **Total Non-RE** (sum of non-Real-Estate asset accounts) → **Gross Total** (Total Non-RE + Real Estate) → **Debt** (sum of liability accounts) → **Realized Tax Liabilities** (accrued Federal + California estimated-tax obligation, net of payments) → **Unrealized Tax Liabilities** (estimated tax on current unrealized gains) → **Net Assets Value (NAV)** at the foot. I want to expand any account-type category one level to see the individual accounts contributing to it. Together this makes the four-component NAV definition from 2.1.1 (`Gross − Debt − Realized Tax Liab − Unrealized Tax Liab`) visually traceable from its parts, so the single headline number doesn't hide its structure and I can see at a glance which categories or subtotal lines are driving the total or its change.
>
> *Traces to:* ADR-002 §1.9 (per-account-type boundaries are first-class); ADR-002 §1.0 (composition view dovetails with §2.2 asset allocation but is distinct: composition shows the buildup of *accounts* into NAV; allocation shows the distribution of *asset classes* across the position). ADR-002 §1.7 (per-position unrealized G/L is V1 — feeds the per-row unrealized gain/loss column). **ADR-004 Decision D**: Realized + Unrealized Tax Liabilities subtractions on the buildup trace to the Federal + California estimated-tax surface and the §2.1.1 NAV definition. F/CTO verified at Task #2 close that the Realized Tax Liab definition ("accrued Federal + California estimated-tax obligation, net of payments already made") and the Unrealized Tax Liab primitive form (marginal-rate × aggregate unrealized G/L, no ST/LT split) both match the existing Google Sheet's primitive calculation. **`docs/v1-parity-matrix.md` line 99** (existing Finance_Report Account Holdings table: "per-account values + unrealized gains, grouped, with subtotals: Total Non-RE, Gross Total, Debt, Realized Tax Liabilities (est. tax pending), Unrealized Tax Liabilities, Net Assets Value (NAV)" — V1 preserve). Composition buckets are user-meaningful, not Plaid-derived (see Appendix A). Drill-down stops at the account level by design — holdings live in §2.2, transactions live in §2.3. NAV at the foot of the table is the same NAV defined in §2.1.1; this section makes that definition visually concrete from its components.

#### Supporting stories

> **2.1.6 Investment net worth uses current market value.**
> As the self-directed multi-account owner, I want my investment account contributions to net worth to use current market value, so that net worth reflects what my holdings are actually worth today — not what I paid for them.
>
> *Traces to:* ADR-002 §1.7. The cost-basis comparison surface lives in §2.2 or a future investment-detail view; net worth uses market value.

> **2.1.7 Net worth is mine, not anyone else's.**
> As the self-directed multi-account owner, I want my net worth view to show only my own accounts and their data, with no possibility of another user's data appearing in my view. I want those accounts aggregated into a single full-household NAV by default, spanning every ownership scope I hold (e.g., personal, trust, retirement custodial, HSA). Together these give me a number I can trust absolutely, that reflects my entire household position the way I do today, and that honors the system's multi-tenant commitment from day one even though V1 ships to a single user. Per-scope reporting (one NAV per scope) and scope-aware filtering UI are V2+, not V1; the data model carries scope on each account from V1 (per ADR-004 Decision B) so the V2 expansion ships without a data migration.
>
> *Traces to:* ADR-002 §1.4 (multi-tenant from day one — tenant_id isolation foundational); **ADR-004 Decision B** (multi-scope ownership Rich / Trust / IRA / HSA is a V1 data attribute on accounts within a single tenant; V1 default report scope = full-household; per-scope reporting surfaces and scope-aware UI filtering are V2+). Jurisdictional scope of the NAV calculation (Federal + California per ADR-004 Decision D, US-domiciled per §1.4 deferrals) is established in §2.1.1 and is not re-litigated here — this story addresses tenant isolation and household-scope aggregation, not jurisdictional scope. **Flagged for Security Reviewer** per §8.0 (multi-tenant data isolation) — this section cannot move from draft to locked without a Security Reviewer pass; the Security Reviewer pass should additionally confirm that the multi-scope data attribute (per Decision B) is treated as a user-owned data label and not as a V1 isolation boundary (scopes are not tenants).

#### Open routing flags affecting §2.1

- **Architect — manual-vs-Plaid sourcing for net-worth contribution.** When an account is manual (no Plaid coverage) or in re-auth, what value contributes to current net worth and to the time series — last-known-good, user-entered most recent, zero, omitted? ADR-002 §1.5 establishes manual contributes; the exact contribution rule is an architectural decision. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — period aggregation for the time series.** The multi-granularity expectation (monthly default + weekly/daily override) means the storage model needs to support either pre-aggregated multiple-resolution storage or on-the-fly aggregation from a finer underlying resolution. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — historical NAV depth in V1 (how, not whether).** V1 imports the existing Google Sheet's monthly NAV history (Dec-2015 forward) so the 5-Year horizon in 2.1.3 is meaningful at launch — F/CTO has locked the *whether*. Open architectural questions: import mechanism, validation strategy, schema location, and whether per-asset-category NAV breakdown imports alongside total NAV. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — CPI-U source.** Live API (e.g., FRED, BLS public API) vs. manual user entry, with cadence, historical depth back to the Dec-2015 NAV anchor, and freshness expectations. Required for both the §2.1.3 panel Inflation Adjusted column and the §2.1.2 chart-overlay inflation-adjusted view. Parity-matrix open product decision #10. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Security Reviewer — §2.1.7 isolation + scope-attribute pass.** (a) Tenant isolation language consistent with ADR-002 §1.4 (tenant_id + RLS commitment); (b) multi-scope ownership per ADR-004 Decision B is a user-owned data attribute, not a V1 isolation boundary — scopes are not tenants. **Pass-with-comments recorded 2026-05-14 (Task #8).**

#### Acceptance flags

- Each story is a user-facing capability statement, not an implementation specification. Acceptance criteria (the testable conditions) get decomposed in Phase 4 / Linear, not in the PRD.
- §2.1 is **locked** as of 2026-05-14 (Security Reviewer pass-with-comments per Task #8). The Architect routing flags resolve during Phase 3 and surface in Appendix B before Architect sign-off; they do not block §2.1 lock at the PRD level.

### 2.2 Asset allocation

#### Primary stories

> **2.2.1 Two-level asset taxonomy and holding-to-bucket assignment.**
> As the self-directed multi-account owner, I want a **two-level asset taxonomy** — a top-level **Category** (Cash, Bonds, Equity, Alternatives, Liabilities, Real Estate — six in total) and a more specific **Sub-Category** within each (e.g., Equity → US-Index_Non_Sector; Bonds → T-bill; Alternatives → REIT) — seeded with my own existing categorization at V1 bootstrap, so the system speaks the categorization language I already use rather than imposing a coarser one. I want a **holding-to-bucket assignment UI** through which I assign each holding to a Sub-Cat (and its parent Cat by inheritance), both for Plaid-surfaced securities (each symbol gets a single Sub-Cat assignment that applies across all my accounts holding it) and for manual non-securities holdings (each manual asset's bucket is set when the account is created or edited). Together these make the taxonomy operational: I can express the categorization grammar I think at, and apply it actively as my portfolio evolves. **User-editable taxonomy CRUD** (creating, renaming, deleting Categories or Sub-Categories beyond the seeded set) is V2+; in V1, taxonomy editing happens via migration or direct database access.
>
> *Traces to:* **ADR-004 Decision C** (V1 includes a two-level user-meaningful asset taxonomy via hybrid operationalization: V1 data model carries the taxonomy; V1 seeded with F/CTO's existing taxonomy as migration/seed at bootstrap; V1 holding-to-bucket assignment UI required for active workflow; V1 does NOT ship user-editable taxonomy CRUD UI — V2 adds that). ADR-002 §1.8 (amended by ADR-004 Decision C — multi-level user-meaningful taxonomy supersedes the original "security_type as categorization attribute" framing for V1). §1.2 archetype attribute #5 (this story is where the archetype's two-level categorization grammar gets a V1 operational surface). **`docs/v1-parity-matrix.md`** line 119 (existing Master.AssetCategories: 6 top-level × ~35 sub-categories with F/CTO's lock — "100% duplicated work to implement a dumbed-down version"); line 122 (Master.AssetDB: Symbol → Cat/Sub-Cat/Description registry — the existing-system equivalent of per-symbol Sub-Cat assignment); line 187 (Sub-Cat numeric prefix sortable convention — `US-01-Basic_Materials` through `US-10-Utilities` etc. — preserved for UX-relevant sort order). The taxonomy and assignments this story establishes feed §2.2.2 (Non-RE allocation table) and §2.2.3 (US Equity sub-allocation). Architect routing flag affecting this story specifically: **multi-level user-scoped taxonomy data model** (tables, seed-on-first-use mechanism, forward-compat for V2 CRUD UI) — surfaces in §2.2's Open routing flags block at section lock. Other Architect flags (Sub-Cat-aware holdings aggregation query path; target allocation storage shape; non-liquid Cat semantics) surface in §2.2.2 / §2.2.3 traces, not here. New-account onboarding's bucket-assignment touch is cross-cutting territory and lives in §2.4 when that section drafts.

> **2.2.2 Non-RE allocation table.**
> As the self-directed multi-account owner, I want a Non-Real-Estate allocation table at **Sub-Category granularity, grouped under Cat-group headers** — the four asset Cat groups from the existing Finance_Report (Cash, Bonds, Equity, Alternatives) plus Liabilities as a V1 extension for leverage management. Each Sub-Cat row carries five columns: **% Target** (target share of total Non-RE), **% Alloc** (current actual share), **$ Target** (dollar target given current total Non-RE assets), **$ Alloc** (current dollar amount), and **$ ReAlloc** (the dollar gap between target and actual — positive means underweight, negative means overweight). I want to read this in a single table at a glance to see which Sub-Cats and which Cat groups are over- or under-target in both percent and dollar terms, so I can decide externally what trades to make to close the gap — preserving my decisional authority over which specific holdings to buy or sell. Real Estate is excluded from this rebalance view as a non-liquid asset that doesn't get rebalanced (house-fractions can't be sold on demand); its NAV contribution lives in §2.1.5 composition, not in this view. **Auto-generated rebalance suggestions** (a recommendation engine that says which specific holdings to buy or sell) are V2+; V1 ships the visualization only.
>
> *Traces to:* **ADR-004 Decision A** (V1 rebalance-target visualization with the 5-column structure across the allocation surface; auto-generated rebalance suggestions remain V2+); **ADR-004 Decision C** (two-level Cat × Sub-Cat taxonomy; this table renders at Sub-Cat granularity grouped under Cat headers, matching how the existing system surfaces the allocation table). §1.2 archetype attribute #4 (self-directed, observational tool — "system-generated buy/sell recommendation" explicitly rejected); the V1-visualization-only / V2-recommendation-engine split is what gives attribute #4 a concrete V1 surface. **`docs/v1-parity-matrix.md`** line 100 (Finance_Report Asset Allocation section: "Non-Real-Estate Allocation table" with the 5-column structure — V1 preserve); line 142 (per-account Asset Allocation Dashboard with the same 5-column structure); line 159 (cross-account allocation roll-up — multi-scope `$ Alloc` data model per Decision B, surfaced in §2.2.4 isolation story not here). **Existing-report ground truth** from F/CTO direct inspection of `Finance_Report_2026_04.pdf` page 4 "Non Real Estate Allocation" table: 4 Cat group headers (Cash, Bonds, Equity, Alternatives — bold, no row values), with Sub-Cat rows under each (examples: Cash → *Insured Checking/Savings* (4% T / 3% A), *CDs / T-Notes 6–12 months* (17% / 14%); Equity → *US - Sector Diversified* (56% / 51%; largest Sub-Cat row, with positive `$ ReAlloc` indicating underweight), *Intl - Developed Market* (0% / 5%; negative `$ ReAlloc` indicating overweight), *Intl - Emerging Market* (2.5% / 11%); Alternatives → *REITs* (6% / 4%), *Crypto / Foreign Currencies*, *Commodities / Other*, *Volatility Hedges*, *Private Equity*); Total row sums to 100% / 100% with `$ Target` equal to `$ Alloc` (zero overall `$ ReAlloc` across Sub-Cats). This table consumes the holding-to-Sub-Cat assignments §2.2.1 establishes. **Liabilities Cat group is an intentional V1 extension beyond existing-system parity** — the existing Finance_Report page 4 does not include a Liabilities Cat header. F/CTO articulated the rationale at Task #11 v1: the Liabilities Cat group functions as a leverage-management surface (target % expresses desired leverage level; $ ReAlloc reads as "under-leveraged, could productively take on more debt" rather than "buy more debt instruments" — same column structure carries the asymmetric semantic cleanly). V1 ships this extension; the Liabilities Cat group has its own Sub-Cat rows (Credit-Balance, Loan-Balance — per ADR-004 Decision C Sub-Cat enumeration). Real Estate exclusion rationale: F/CTO's "non-liquid asset" framing — RE doesn't get rebalanced because liquidating fractional positions isn't operationally feasible; RE NAV contribution is fully captured in §2.1.5 composition (as its own group between Total Non-RE and Gross Total subtotals). Architect routing flags affecting this story: (a) **Sub-Cat-aware holdings aggregation query path** (broadened from v1 "Cat-aware" — table renders at Sub-Cat granularity, so the aggregation query needs to compute per-Sub-Cat values; per-Cat values surface as group subtotals); (b) **target allocation storage shape** — where do user-defined target %s per Sub-Cat (and implicit per-Cat aggregates) live; (c) **Real Estate / non-liquid Cat semantics** — query-layer needs to know which Cats are excluded from the rebalance view; (d) **Liabilities-as-Cat semantics** — Liabilities Cat group functions differently from asset Cat groups (Liabilities $ Alloc values appear in the Debt subtotal of §2.1.5 composition, not in the asset side); query-layer needs to handle this consistently across §2.1.5 composition and §2.2.2 allocation views. All four queued for §2.2's Open routing flags block at section lock.

> **2.2.3 US Equity sub-allocation.**
> As the self-directed multi-account owner, I want a second allocation table that drills into the US Equity portion of my portfolio, covering twelve rows — the ten US Sector Sub-Cats (US-01 Basic Materials through US-10 Utilities) plus two non-sector US Equity Sub-Cats (Index Non-Sector for broad-market index holdings; Growth Non-Sector for non-sector growth holdings). The table uses the same five columns as §2.2.2 — **% Target** | **% Alloc** | **$ Target** | **$ Alloc** | **$ ReAlloc** (definitions per §2.2.2; sign convention positive = underweight) — evaluated against my **Total US Equity** (sum of all twelve rows), which equals the "US - Sector Diversified" Sub-Cat row in §2.2.2's Non-RE allocation table. I want to read this in a single table at a glance to see whether my US equity exposure is concentrated by sector or balanced, and whether my non-sector (index + growth) holdings are at the level I want — so I can decide externally where to rebalance at this resolution, preserving my decisional authority over which specific holdings to buy or sell. **Ex-US sub-allocation** (a parallel drill-down for international equity Sub-Cats — ExUS-Developed_Market, ExUS-Emerging_Market) is not in V1; V1 ships only the US Equity sub-allocation matching the existing Finance_Report's structure. **Auto-generated rebalance suggestions** are V2+ per the §2.2.2 V1/V2 split; V1 ships the visualization only.
>
> *Traces to:* **ADR-004 Decision A** (V1 rebalance-target visualization with the 5-column structure across the allocation surface; auto-generated rebalance suggestions remain V2+); **ADR-004 Decision C** (two-level Cat × Sub-Cat taxonomy; Sub-Cat-level surfaces are V1, including US-01 through US-10 sector Sub-Cats + non-sector US Equity Sub-Cats from the existing seeded taxonomy). §1.2 archetype attribute #4 (self-directed, observational tool — same V1-visualization-only / V2-recommendation-engine split as §2.2.2). **`docs/v1-parity-matrix.md`** line 100 (Finance_Report Asset Allocation section: "US Sector Allocation table" listed as a distinct sub-surface peer to the Non-Real-Estate Allocation table — V1 preserve; the table's actual content per PDF inspection extends beyond pure sectors to include non-sector US Equity Sub-Cats). **Existing-report ground truth** from F/CTO direct inspection of `Finance_Report_2026_04.pdf` page 5 "U.S. Sector Allocation" table: 12 rows — the 10 US sector Sub-Cats ([01] Basic Materials, [02] Communication Services, [03] Consumer Discretionary, [04] Consumer Staples, [05] Energy, [06] Financial Services, [07] Healthcare, [08] Industrials, [09] Technology, [10] Utilities) plus Index - Non Sector and Growth - Non Sector. 5-column structure confirmed (% Target / % Alloc / $ Target / $ Alloc / $ ReAlloc). Total row's `$ Alloc` matches the page-4 "US - Sector Diversified" Sub-Cat row's `$ Alloc` exactly — confirming page 5 is a drill-down into page 4's `US - Sector Diversified` row. Title note: existing-report title is "U.S. Sector Allocation" but the table contains non-sector rows (Index Non-Sector, Growth Non-Sector); the PRD story title is **"US Equity sub-allocation"** rather than "US Sector" to accurately reflect the table's actual scope. This table consumes the per-symbol Sub-Cat assignments §2.2.1 establishes. **Denominator confirmation:** %s and $s are computed against the user's Total US Equity (sum of all 12 rows in this table), which is identical to the parent Sub-Cat row's `$ Alloc` in §2.2.2; not against the whole Equity Cat or the whole portfolio. Holdings tagged with ex-US Sub-Cats (ExUS-Developed_Market, ExUS-Emerging_Market) are not in this table's denominator — they appear as their own Sub-Cat rows in §2.2.2 within the Equity Cat group. Architect routing flags affecting this story: (a) The **Sub-Cat-aware holdings aggregation query path** flag from §2.2.2 covers this story's resolution (the same query path computes %s and $s at Sub-Cat granularity); (b) the **target allocation storage shape** flag covers per-Sub-Cat targets here too; (c) **NEW: drill-down view capability** — §2.2.3 is structurally a drill-down into one specific Sub-Cat row of §2.2.2 (the `US - Sector Diversified` row). Architect Phase 3 question: does V1's allocation-view layer support drill-down as a general capability (any Sub-Cat could have its own sub-rows via user-extension), or is the US Equity drill-down a hardcoded special case for V1? Forward-compat consideration for V2+. Pending promotion to §2.2's Open routing flags block at section lock.

#### Supporting stories

> **2.2.4 Allocation is mine, not anyone else's.**
> As the self-directed multi-account owner, I want my allocation views (the §2.2.2 Non-RE allocation table and the §2.2.3 US Equity sub-allocation) to show only my own holdings, with no possibility of another user's data appearing in my view. I want those holdings aggregated into a single full-household allocation by default, spanning every ownership scope I hold (e.g., personal, trust, retirement custodial, HSA). Together these give me allocation tables I can trust absolutely, that reflect my entire household position the way I do today, and that honor the system's multi-tenant commitment from day one even though V1 ships to a single user. Per-scope allocation views (one set of allocation tables per scope) and scope-aware filtering UI are V2+, not V1; the data model carries scope on each account from V1 (per ADR-004 Decision B) so the V2 expansion ships without a data migration.
>
> *Traces to:* ADR-002 §1.4 (multi-tenant from day one — tenant_id isolation foundational); **ADR-004 Decision B** (multi-scope ownership Rich / Trust / IRA / HSA is a V1 data attribute on accounts within a single tenant; V1 default report scope = full-household; per-scope reporting surfaces and scope-aware UI filtering are V2+). Jurisdictional scope of the NAV calculation (Federal + California per ADR-004 Decision D) is established in §2.1.1 and is not re-litigated here; the asset taxonomy scope (per ADR-004 Decision C in §2.2.1) is similarly out-of-scope for this story. This story addresses tenant isolation and household-scope aggregation only. **`docs/v1-parity-matrix.md`** line 186 (multi-scope ownership data model: "Rich / M-Trust / IRA / HSA per account" — V1 preserve); line 159 (cross-account allocation roll-up with six `$ Alloc` columns by scope — V1 data model preserves the multi-scope shape even though V1 UI is full-household-only). **Flagged for Security Reviewer** per §8.0 (multi-tenant data isolation + scope-attribute treatment) — this section cannot move from draft to locked without a Security Reviewer pass; the pass should confirm tenant isolation (parallel to §2.1.7's Task #8 pass) and that the multi-scope data attribute (per Decision B) is treated as a user-owned data label, not as a V1 isolation boundary (scopes are not tenants). Sec's three forward-looking comments from §2.1.7 Task #8 pass (composition-view RLS test surface in Phase 3; Decision D marginal-rate storage callout for §4; re-engagement for §2.4 / §4) already cover the allocation surface implicitly; this pass should be lighter than §2.1.7's first time through. No new Architect flags from this story — multi-scope data model is covered by existing pre-§2.1 ADR-002 §1.4 work, §2.1.7's data-attribute treatment, and §2.2.2's per-scope target storage flag.

#### Open routing flags affecting §2.2

- **Architect — multi-level user-scoped taxonomy data model.** Per §2.2.1: V1 carries the Cat × Sub-Cat taxonomy in data tables (one taxonomy per tenant; per-user expansion V2+ if needed). Open architectural questions: tables and relationships for Cat + Sub-Cat; seed-on-first-use mechanism (F/CTO-seeded taxonomy loads at tenant bootstrap, not hardcoded); forward-compatibility for V2 user-editable CRUD UI (additive schema only, no breaking renames or restructure); per-symbol assignment storage (AssetDB-style registry); per-account assignment storage (for manual non-securities holdings). Architectural overlap with the **target allocation storage shape** flag below — both likely share the underlying multi-level user-scoped table set; Architect dedupes at Phase 3. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — Sub-Cat-aware holdings aggregation query path.** Per §2.2.2 + §2.2.3: computing `% Alloc` and `$ Alloc` at Sub-Cat granularity (with Cat-level subtotals as group aggregates) across the user's holdings; parallel to the §2.1 net-worth roll-up Architect flag (line 105). Covers both the Non-RE allocation table (§2.2.2) and the US Equity sub-allocation drill-down (§2.2.3). Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — target allocation storage shape.** Per §2.2.2: where user-defined target %s per Sub-Cat (and per-Cat aggregates derived from Sub-Cat sums) live. Open dimensions: per-tenant table vs. per-scope; how Sub-Cat targets relate to Cat-group targets (sum-derived vs. independently storable); how user-editable target updates persist; per-scope-target capability for V2 forward-compat. Architectural overlap with the multi-level user-scoped taxonomy data model flag above. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — Real Estate / non-liquid Cat semantics.** Per §2.2.2: the rebalance view (Non-RE allocation table) excludes Real Estate as a non-liquid asset that doesn't get rebalanced (F/CTO rationale: house-fractions can't be sold on demand). Query-layer needs to express which Cats are excluded from the rebalance view via either a hardcoded predicate (`Cat != 'RealEstate'`) or a taxonomy-attribute flag (`rebalanceable: bool` per Cat). The latter is forward-compatible if future user-defined Cats include other non-liquid types. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — Liabilities-as-Cat semantics.** Per §2.2.2: the Liabilities Cat group is an intentional V1 extension functioning as a leverage-management surface (target % expresses desired leverage; positive `$ ReAlloc` reads as "under-leveraged"). Asymmetric data flow: Liabilities `$ Alloc` values appear in §2.1.5 composition's Debt subtotal (liability-side sign) AND in §2.2.2 allocation table (leverage-target sign). Query-layer needs consistent handling across §2.1.5 composition and §2.2.2 allocation views. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — drill-down view capability.** Per §2.2.3: §2.2.3 is structurally a drill-down into one specific Sub-Cat row of §2.2.2 (the `US - Sector Diversified` row). Does V1's allocation-view layer support drill-down as a general capability (any Sub-Cat could have its own sub-rows via user-extension), or is the US Equity drill-down a hardcoded special case for V1? Forward-compat consideration for V2+. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Security Reviewer — §2.2.4 isolation + scope-attribute pass.** Pass-with-comments recorded 2026-05-14 (Task #14). (a) Tenant isolation language consistent with ADR-002 §1.4 (tenant_id + RLS commitment) — confirmed. (b) Multi-scope ownership per ADR-004 Decision B is a user-owned data attribute, not a V1 isolation boundary — confirmed. Comments captured for Architect Phase 3 + PRD §4 carry-over (see Task #14 verdict).

#### Acceptance flags

- Each story is a user-facing capability statement, not an implementation specification. Acceptance criteria (the testable conditions) get decomposed in Phase 4 / Linear, not in the PRD.
- §2.2 is **locked** as of 2026-05-14 (Security Reviewer pass-with-comments per Task #14). The Architect routing flags resolve during Phase 3 and surface in Appendix B before Architect sign-off; they do not block §2.2 lock at the PRD level.

### 2.3 Spending and income categorization

#### Primary stories

> **2.3.1 Two-level cash-flow taxonomy and transaction-to-bucket assignment.**
> As the self-directed multi-account owner, I want a **two-level cash-flow taxonomy** — a top-level **Category** (Income, Expenses, OtherCF, and AcctSetup — four in total at the data-model level) and a more specific **Sub-Category** within each (illustratively: Income → Salary; Expenses → Groceries; OtherCF → IRA-Contribution) — seeded with my own existing categorization at V1 bootstrap, so the system speaks the categorization language I already use rather than imposing a coarser one. V1's cash-flow rendering surfaces (the §2.3.2 cross-account multi-period rollup and the §2.3.3 per-account drill-down) render the three cash-flow Cats — Income, Expenses, and OtherCF; AcctSetup carries non-cash lifecycle-event transactions (stock splits, transfers-in-kind) whose surfaces live in §2.4 cross-cutting territory, but the Cat exists in the data model from V1 so AcctSetup-classified transactions persist alongside the cash-flow ones. I want a **transaction-to-bucket assignment UI** through which each transaction is independently classified to a Sub-Cat (and its parent Cat by inheritance) at the per-transaction level — unlike the per-symbol assignment pattern in §2.2.1, transactions are per-event rather than per-instrument, so each transaction carries its own bucket assignment. For Plaid-pulled transactions, Plaid's own category surfaces as a suggested default, but my two-level taxonomy is authoritative — I can override per-transaction, and the assignment UI may carry recurring-vendor inference (same merchant → suggest the Sub-Cat I last assigned to it). For manual transactions, Sub-Cat assignment happens when I enter the transaction. Together these make the cash-flow side of the categorization grammar operational: I can express the income-and-expense buckets I think at, and apply them actively as transactions arrive. **User-editable taxonomy CRUD** (creating, renaming, deleting Categories or Sub-Categories beyond the seeded set) is V2+; in V1, taxonomy editing happens via migration or direct database access.
>
> *Traces to:* **ADR-004 Decision C** (V1 includes a two-level user-meaningful cash-flow taxonomy via hybrid operationalization parallel to the asset half in §2.2.1: V1 data model carries the taxonomy; V1 seeded with F/CTO's existing Master.CashFlowCategories at bootstrap; V1 transaction-to-bucket assignment UI required for active workflow; V1 does NOT ship user-editable taxonomy CRUD UI — V2 adds that). ADR-002 §1.0 (spending and income categorization with monthly per-category summations is a V1 surface); §1.2 (budget targets per category, category-level trend charts, custom user-defined categories, recurring-transaction detection, and category alerts/notifications are V1 non-goals — the taxonomy here supports cash-flow rendering and per-category summations, not budget tracking). ADR-002 §1.3 (V1 transaction-tracking via Plaid Transactions + Investments — feeds Plaid-pulled transactions into the assignment UI) and §1.5 (manual transaction entry V1 — feeds manual-transaction Sub-Cat assignment). §1.2 archetype attribute #5 (this story is where the archetype's two-level categorization grammar gets its V1 operational surface for the cash-flow side; the asset side lives in §2.2.1). **`docs/v1-parity-matrix.md`** line 50 (Master.CashFlowCategories: Income / Expenses / OtherCF / AcctSetup taxonomy with sub-cats); line 121 (V1 preserve directive — maps to PRD §2.3); line 60 (per-account Cash Flow sheet renders Income / OtherCF / Expenses, with AcctSetup transactions present in the underlying ledger but not rendered in the cash-flow surface); lines 103–104 (Finance_Report Income + Expenses sections — exact cross-account rendering subset is §2.3.2's concern, not this story's); line 133 (AcctSetup non-cash events → §2.4). Illustrative Sub-Cat examples in this story (Salary, Groceries, IRA-Contribution) are not pinned to F/CTO's actual seeded sub-cats — the seed migration carries F/CTO's existing Master.CashFlowCategories contents verbatim at V1 bootstrap; the seeded sub-cat enumeration is not a PRD lock. The taxonomy and assignments this story establishes feed §2.3.2 (cross-account multi-period cash-flow rollup) and §2.3.3 (per-account cash-flow drill-down), parallel to §2.2.1's feed into §2.2.2 / §2.2.3. Architect routing flags affecting this story: (a) **cash-flow taxonomy data model** (tables for Cat + Sub-Cat; seed-on-bootstrap mechanism mirroring §2.2.1's asset-taxonomy flag; per-transaction Sub-Cat assignment storage; forward-compatibility for V2 user-editable CRUD UI — additive schema only, no breaking renames). Architectural overlap with §2.2.1's multi-level user-scoped taxonomy data model flag — Architect dedupes at Phase 3 if the cash-flow and asset taxonomies share the underlying table set. (b) **NEW — transaction-classification heuristic mechanism** (Plaid category consumed as suggested default; recurring-vendor inference for same-merchant repeat-classification — same vendor → suggest the Sub-Cat last assigned). Open question: does V1 ship the inference layer, or does V1 ship pure manual-per-transaction assignment with the inference layer deferred to V2? Story-body F/CTO call queued and pending promotion to §2.3's Open routing flags block at section lock. Both flags routing: ADR-002 §8.0 Architect flag, non-F/CTO-led. New-account onboarding's transaction-bucket-assignment touch, Plaid-pulled-vs-manual transaction reconciliation, and AcctSetup non-cash events (stock splits, transfers-in-kind) are cross-cutting territory and live in §2.4 when that section drafts.

> **2.3.2 Cash flow categorization across accounts by multi-period.**
> As the self-directed multi-account owner, I want my cross-account cash flow rendered as two sections — **Income** and **Expenses** — each showing a table with Sub-Category rows under it, evaluated across six time-period columns: **Month** (the current month, visually emphasized), **Q1**, **Q2**, **Q3**, **Q4**, and **YTD**. Each section header carries a short caption naming the section's coverage and my **authored income and expense target reference values** displayed inline in the caption — the income target as an annual figure, the expense target as a monthly figure — so I see my target alongside the actual totals at the foot of each table without any variance computation or alert behavior. Each table foots in a single **Total** row summing the section's Sub-Cats across each period column. Sub-Cats render flat (no in-table Cat-group header rows, no Cat-level subtotals); the Cat-group structure is expressed by the two-section layout itself. **OtherCF** transactions exist in the data model from V1 per §2.3.1 and render in the §2.3.3 per-account drill-down, but **do not render as a section in the cross-account rollup** — the Finance_Report-canonical surface this story instantiates is Income + Expenses only. **AcctSetup** does not render in this table at all; AcctSetup transactions live in §2.4 cross-cutting territory. Together this gives me one full-household view of my income and expenses at Sub-Cat granularity across the current month, every quarter year-to-date, and the YTD total, with my authored targets visible inline for reference. The full-household aggregation is the V1 default (per ADR-004 Decision B); per-scope filtering of the cash-flow tables is V2+, and the data model carries scope from V1 so the V2 expansion ships without a data migration (tenant isolation and scope-aware aggregation are detailed in §2.3.5). **Variance, alert, and budget-tracking mechanics** (computing actual-vs-target deltas, flagging overages, threshold alerts) are V1 non-goals per ADR-002 §1.2; V1 ships the static reference-value rendering only. **User-editable target reference values** through a V1 settings UI are V1-in-scope per the F/CTO planning-targets lock; the settings UI is part of this story's surface. **Rule-based auto-categorization** beyond the §2.3.1 recurring-vendor inference (e.g., user-authored category-assignment rules with regex matching) is V2+.
>
> *Traces to:* **ADR-002 §1.0** (spending and income categorization with monthly per-category summations is a V1 surface — primary anchor). **ADR-004 Decision B** (V1 default aggregation = full-household across all ownership scopes; per-scope rendering V2+; cross-section feed to §2.3.5 for the multi-scope-aggregation Supporting story). **ADR-002 §1.2 (V1 non-goals) plus F/CTO planning-targets lock 2026-05-14 (Option (a)(i))** — V1 renders user-authored income and expense target reference values inline in section captions, alongside actuals, with no variance/alert mechanic; user-editable through a V1 settings UI. Whether this rendering parity gets a dedicated §1.2 amendment ADR or absorbs into the §2.3 section-lock ratification is a CoS bookkeeping question pending §2.3 lock; the substantive V1/V2 split (V1 = static reference rendering + settings UI; V2 = variance/alerts/budget-tracking mechanics) holds either way. ADR-002 §1.3 (Plaid Transactions + Investments V1 — supplies the transactions this table sums) and §1.5 (manual transaction entry V1 — same). **`docs/v1-parity-matrix.md`** line 50 (Master.CashFlowCategories taxonomy); line 60 (per-account Cash Flow sheet shape Month/Q1-Q4/YTD — same column set as the cross-account rollup); lines 79–82 (Asset Summary Cash Flow + `_cfMonth`/`_cfQ*` rollups feed this surface); lines 103–104 (Finance_Report Income + Expenses sections — this story's primary parity anchor); line 121 (V1 preserve directive maps to PRD §2.3); line 144 (per-account Cash Flow V1 preserve — feeds §2.3.3 not this story); **line 160 (cross-account cash-flow rollup V1 preserve — this story's primary aggregation parity anchor)**; line 178 (income / expense planning targets locked as V1 Option (a)(i) — V1-stored, user-editable reference values, no variance mechanic) and line 199 (ADR-002 §1.2 caveat resolved by the (a)(i) lock). **Existing-report ground truth** from F/CTO direct inspection of `Finance_Report_2026_04.pdf` page 6: two-section rendering ("Cash Flow → Income" and "Cash Flow → Expenses"); 7-column table per section (Category | Month | Q1 | Q2 | Q3 | Q4 | YTD); Month column visually highlighted to mark current period; flat Sub-Cat rows under each section with a single Total row footing each table; no in-table Cat-group headers and no Cat-level subtotals — Cat-group structure expressed by the two-section layout; income target rendered as caption text under the Income section title; expense target rendered the same way under the Expenses section title; OtherCF section ABSENT from page 6 (cross-account rollup omits OtherCF — confirms parity-line-60 vs lines-103-104 discrepancy and resolves it: OtherCF renders only in §2.3.3 per-account drill-down). This story consumes §2.3.1's taxonomy + transaction-to-Sub-Cat assignments and feeds §2.6 monthly report output when that section drafts. Architect routing flags affecting this story: (a) **cross-account per-period cash-flow aggregation query path** (parallel to §2.2.2's Sub-Cat-aware holdings aggregation flag; query path computes Sub-Cat rows × {Month, Q1, Q2, Q3, Q4, YTD} columns × Cat-group (Income / Expenses) section split across all of the user's accounts; OtherCF/AcctSetup classified transactions exist in the data but are excluded from the query path's output for this surface); (b) **NEW — V1 settings UI plumbing for user-authored planning-target values** (first concrete V1 surface needing a user-editable settings store; Architect Phase 3 question is whether V1 introduces a generalized settings/preferences table for forward-compat against future V2 settings expansion, or ships planning-targets-specific storage narrower in scope); (c) **planning-targets storage shape** — likely simple in V1 (one income-target total value + one expense-target total value, no Sub-Cat-level breakdown; values may be period-typed — annual for income, monthly for expenses — per Finance_Report caption framing); Architect Phase 3 confirms. All flags routing: ADR-002 §8.0 Architect flag, non-F/CTO-led. New-account onboarding's transaction-bucket-assignment touch, Plaid-pulled-vs-manual transaction reconciliation, manual transaction entry mechanics, and AcctSetup non-cash events (stock splits, transfers-in-kind) are cross-cutting territory and live in §2.4 when that section drafts. The settings UI for planning-target reference values is V1 in §2.3.2's surface, NOT a §2.4 punt.

> **2.3.3 Per-account cash-flow drill-down.**
> As the self-directed multi-account owner, I want to drill from §2.3.2's cross-account rollup into a single account's cash flow by selecting which account to view, then see that account's cash flow in isolation rendered as three sections — **Income**, **OtherCF**, and **Expenses** (in that order) — each showing a table with Sub-Category rows under it. Each table uses the same column structure as §2.3.2: **Category | Month | Q1 | Q2 | Q3 | Q4 | YTD**, with the **Month** column visually emphasized to mark the current period. Each table foots in a single Total row summing the section's Sub-Cats across each period column; Sub-Cats render flat (no in-table Cat-group headers); Cat-group structure is expressed by the three-section layout. **OtherCF renders in this view even though it does not render in §2.3.2's cross-account rollup** — the asymmetry is intentional and matches the existing-system parity: per-account workbooks surface OtherCF (inter-account transfers, IRA contributions, special-status flows) because those flows are account-meaningful at the drill-down level even when omitted from the aggregate Income/Expense narrative. **AcctSetup** does not render in this view at all; AcctSetup transactions live in §2.4 cross-cutting territory. I also want an **as-of-date toggle** on this view — by default the cash flow renders as of today; I can toggle to a user-chosen historical as-of-date to view the account's cash flow as it stood on that date (used today for year-end reconciliation in my existing workflow). Together this gives me a per-account view of the same data §2.3.2 aggregates, with the additional date-toggle affordance for reconciliation moments. The full-household scope of §2.3.2 and per-account scope of this story are different filterings of the same underlying transaction data; **per-scope filtering** (one drill-down per ownership scope) is V2+ and consistent with §2.3.5's multi-scope-aggregation treatment. **Planning-target reference values do not appear in this view** — targets are aggregate concepts attached to §2.3.2's cross-account rollup, not to single-account scopes. **Variance, alert, and budget-tracking mechanics** remain V1 non-goals across the §2.3 surface family per ADR-002 §1.2. **User-editable per-account taxonomy overrides** (Sub-Cats unique to one account's workflow that don't exist in the global taxonomy) are V2+.
>
> *Traces to:* **ADR-002 §1.0** (spending and income categorization with monthly per-category summations is a V1 surface — primary anchor; this story instantiates the per-account view of that surface). **ADR-002 §1.5** (manual transaction entry V1 — per-account drill-down displays manual and Plaid-pulled transactions for the selected account on equal footing). §1.2 archetype attribute #5 (this story is where the archetype's reconciliation-cadence access to per-account detail gets its V1 surface). **`docs/v1-parity-matrix.md`** line 50 (Master.CashFlowCategories taxonomy — three Cats render here, AcctSetup data-only); **line 60 (per-account Cash Flow sheet shape — Income / OtherCF / Expenses × {Month, Q1, Q2, Q3, Q4 [, YTD]} with `Report Date / Test Date / USE TEST ?` date-window toggle — primary parity anchor for this story)**; line 121 (V1 preserve directive maps to PRD §2.3); **line 144 (per-account Cash Flow V1 preserve — this story replaces the existing per-account Cash Flow sheets)**; **line 145 (as-of-date toggle V1-locked Option (a) per F/CTO 2026-05-14 — feeds this story's date-toggle affordance)**; line 160 (cross-account rollup — feeds §2.3.2 not this story); line 133 (AcctSetup non-cash events → §2.4). **Replacement framing** (F/CTO 2026-05 audit framing): §2.3.3 is a V1 *replacement* for the existing per-account Cash Flow sheets in F/CTO's manual-spreadsheet system, not a layered alternative — V1's database becomes the data plane for this surface, and the existing per-account Cash Flow sheets retire when this surface ships. This story consumes §2.3.1's taxonomy + transaction-to-Sub-Cat assignments and is the per-account-scoped peer of §2.3.2's cross-account rollup; both surface the same underlying transaction data filtered differently. Feeds §2.6 monthly report output when that section drafts, particularly any per-account appendix views and custom-date report generation that consumes the as-of-date parameter. Architect routing flags affecting this story: (a) **NEW — as-of-date as system-wide query-time parameter** — exposed in §2.3.3's UI as the per-account drill-down's date toggle, but plausibly a query-path-level abstraction (single as-of-date parameter threading through all dated query paths, including §2.6 monthly report generation with a custom as-of-date). Architect Phase 3 question: ship the as-of-date as a query-path-level abstraction with forward-compat for §2.6 and any future dated surface, or ship a §2.3.3-local mechanic and refactor when §2.6 / other surfaces need it. PM recommendation: query-path-level abstraction in V1, given §2.6's monthly-report custom-date use case is already on the V1 roadmap and the abstraction cost is bounded. (b) **per-account scoping query path** — query path computes Sub-Cat rows × {Month, Q1, Q2, Q3, Q4, YTD} columns × 3 Cat-group sections (Income / OtherCF / Expenses) for one selected account; shares the Sub-Cat aggregation backbone with §2.3.2's cross-account query path. Architect dedupes the two query paths at Phase 3 (likely shared infrastructure with a single-account-filter parameter). (c) **drill-down view capability** — parallel to §2.2.3's drill-down flag. Does V1's cash-flow view layer support per-account drill-down as a general capability (any aggregate cash-flow view → drill into a constituent account), or is per-account drill-down structurally hardcoded as a specific surface? Forward-compat consideration for V2+. All flags routing: ADR-002 §8.0 Architect flag, non-F/CTO-led. New-account onboarding's transaction-bucket-assignment touch, Plaid-pulled-vs-manual transaction reconciliation, manual transaction entry mechanics, and AcctSetup non-cash events (stock splits, transfers-in-kind) are cross-cutting territory and live in §2.4 when that section drafts.

> **2.3.4 Historical Expenditures.**
> As the self-directed multi-account owner, I want my discretionary monthly expenses (the §2.3.2 Expenses scope — excludes tax cash flows) visualized as a time-series chart: a rolling 5-year window of monthly bars with a **12-month rolling-average overlay line** drawn on the same chart simultaneously, so I see both per-month magnitude and the smoothed underlying trend in a single read. Both the bars and the rolling line are **inflation-normalized to today's $ value** (matching §2.1.2's chart-overlay normalization basis, same CPI-U series). The 5-year horizon is fixed in V1; user-configurable horizons, **chart drill-down into specific months or Sub-Cats**, and **authored target reference line on the chart** are V2+. **Income time-series and multi-year historical income recordkeeping are V2+** per F/CTO 2026-05-14 lock; the asymmetry against this expenses-only chart is intentional and parity-justified — realized capital gains from rebalancing partially fund expenses, so an expenses-over-time chart isolates the expense signal cleanly, while an income-over-time mirror would entangle the income signal with realization decisions and carry less monthly-review utility for this archetype.
>
> *Traces to:* **ADR-002 §1.0** (cash-flow categorization V1 surface — primary anchor). **`docs/v1-parity-matrix.md`** line 121 (V1 preserve directive). **Capability not in original parity-matrix V1 enumeration** — Historical Expenditures chart caught via F/CTO direct inspection of `Finance_Report_2026_04.pdf` page 7 during §2.3.2 work and locked V1 Option (A) per F/CTO 2026-05-14. **Existing-report ground truth** from page 7: section "Historical Expenditures"; chart titled "Expense Totals (Inflation Adusted)" (verbatim — typo handling per parity-matrix open product decision #8); monthly bars "Expense (Inf-Norm)" red; ~5-year horizon May-21 through Mar-26 (prior-month-of-current-report endpoint, rolling-5-years-from-current); 12-month rolling-average line "Expense (1y Avg, Inf-Norm)" black; chart Y-axis $-denominated; range consistent with §2.3.2 Expenses-section discretionary-scope magnitudes — full-expense-including-tax magnitudes would be substantially larger; no horizontal target reference line; no interactive drill-down in the PDF rendering. **Cross-section structural parallel:** §2.3.4 is to §2.3.2 what §2.1.2 is to §2.1.1 — time-series chart companion to a current-state numeric surface; V1 inclusion follows the §2.1 precedent of pairing current-state-table + over-time-chart on the same conceptual surface (NAV → §2.1.1 table + §2.1.2 chart; Expenses → §2.3.2 table + §2.3.4 chart). Consumes §2.3.2's per-month expense aggregations extended back ~5 years; CPI-U series is the same one shared by §2.1.2 chart-overlay + §2.1.3 panel + §2.1.4 reference table (four V1 surfaces share one CPI-U-source decision; covered by §2.1's existing Architect flag — no new flag needed on this surface). Feeds §2.6 monthly report output when that section drafts. Architect routing flag affecting this story: **NEW — V1 expense-transaction data retention horizon** ≥ 5 years to populate the rolling-5-year chart; income transaction retention is narrower (current-year-sufficient per F/CTO 2026-05-14 (α) lock). Architect Phase 3 question: differential retention by Cat (expenses ≥ 5yr, income current-year-only) versus uniform retention across Cats with operational/storage implications. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.

#### Supporting stories

> **2.3.5 Cash flow categorization is mine, not anyone else's.**
> As the self-directed multi-account owner, I want my cash-flow views — the §2.3.2 cross-account multi-period rollup, the §2.3.3 per-account drill-down with its as-of-date toggle, and the §2.3.4 Historical Expenditures chart — to show only my own transactions, with no possibility of another user's data appearing in my view. I want those transactions aggregated into a single full-household view by default, spanning every ownership scope I hold (e.g., personal, trust, retirement custodial, HSA). Together these give me cash-flow surfaces I can trust absolutely, reflecting my entire household position the way I do today, and honoring the system's multi-tenant commitment from day one even though V1 ships to a single user. Per-scope cash-flow rendering and scope-aware UI filtering across all three surfaces are V2+; the data model carries scope on each account from V1 (per ADR-004 Decision B) so the V2 expansion ships without a data migration. **Scopes are user-owned data labels, not V1 isolation boundaries** — tenant isolation is the V1 isolation boundary; scopes are first-class attributes within a single tenant.
>
> *Traces to:* **ADR-002 §1.4** (multi-tenant from day one — primary anchor); **ADR-004 Decision B** (multi-scope ownership V1 data attribute; default cash-flow aggregation = full-household; per-scope rendering + scope-aware filtering V2+). **`docs/v1-parity-matrix.md`** line 121 (V1 preserve maps to PRD §2.3); per-section parity anchors live in §2.3.2 / §2.3.3 / §2.3.4 traces. **Cross-section parallel:** §2.3.5 is the third instance of the named-surface-scoped Supporting tenant-isolation + multi-scope-aggregation pattern across PRD §2 (§2.1.7 + §2.2.4 + §2.3.5). **Flagged for Security Reviewer** per §8.0; pass required before §2.3 lock. Sec pass covers (a) tenant isolation on the §2.3.2 + §2.3.3 + §2.3.4 query paths (parallel to §2.1.5 composition + §2.2.1 per-tenant registry flags from prior Sec passes; the §2.3.3 as-of-date toggle adds a date parameter to that query path worth confirming under multi-tenant tests), and (b) scope-attribute-not-isolation-boundary review (Sec Task #8 + Task #14 precedents). Routing: ADR-002 §8.0 Security Reviewer flag, non-F/CTO-led.

#### Open routing flags affecting §2.3

- **Architect — cash-flow taxonomy data model.** Per §2.3.1: V1 carries the cash-flow Cat × Sub-Cat taxonomy in data tables; seed-on-bootstrap from F/CTO's existing Master.CashFlowCategories; per-transaction Sub-Cat assignment storage; forward-compatibility for V2 user-editable CRUD UI (additive schema only). **Architectural overlap with §2.2.1's multi-level user-scoped taxonomy data model flag** — Architect dedupes at Phase 3 if the cash-flow and asset taxonomies share the underlying table set. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — transaction-classification heuristic mechanism.** Per §2.3.1: Plaid category consumed as suggested default; recurring-vendor inference for same-merchant repeat-classification. F/CTO locked V1 inclusion 2026-05-14 (per §2.3.1 accept-as-drafted). Architect Phase 3 question: implementation shape for the inference layer (deterministic merchant-string lookup vs trained classifier vs simple per-vendor most-recent-Sub-Cat lookup). Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — cross-account per-period cash-flow aggregation query path.** Per §2.3.2: query path computes Sub-Cat rows × {Month, Q1-Q4, YTD} columns × Cat-group section split (Income / Expenses) across all of the user's accounts; OtherCF + AcctSetup classified transactions exist in data but are excluded from this surface's query output. Parallel to §2.2.2's Sub-Cat-aware holdings aggregation flag (§2.2 line 145). Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — V1 settings UI plumbing for user-authored planning-target values.** Per §2.3.2 + F/CTO planning-targets lock 2026-05-14 (Option (a)(i)): first concrete V1 surface needing a user-editable settings store. Architect Phase 3 question: generalized settings/preferences table for forward-compat against future V2 settings expansion, vs. planning-targets-specific storage narrower in scope. **Sec re-engagement triggered** when this surfaces (per Sec Task #23 forward-looking comment #3 — write-path validation, audit trail, tenant-scoping confirmation). Routing: ADR-002 §8.0 Architect flag with mandatory Sec re-engagement, non-F/CTO-led.
- **Architect — planning-targets storage shape.** Per §2.3.2: likely simple in V1 (one income-target total value + one expense-target total value, no Sub-Cat-level breakdown; values period-typed — annual for income, monthly for expenses — per Finance_Report caption framing). Architect Phase 3 confirms. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — as-of-date as system-wide query-time parameter.** Per §2.3.3: exposed in §2.3.3's UI as the per-account drill-down's date toggle, but plausibly a query-path-level abstraction (single as-of-date parameter threading through all dated query paths, including §2.6 monthly report generation with a custom as-of-date). PM recommendation embedded in §2.3.3 trace: query-path-level abstraction in V1 given §2.6's monthly-report custom-date use case is on the V1 roadmap. **Sec Task #23 Phase 3 RLS test obligation attached** (forward-looking comment #1 — confirm tenant_id enforcement is independent of the date filter; no parameter-manipulation bypass). Routing: ADR-002 §8.0 Architect flag with mandatory Phase 3 RLS test, non-F/CTO-led.
- **Architect — per-account scoping query path.** Per §2.3.3: query path computes Sub-Cat rows × {Month, Q1-Q4, YTD} columns × 3 Cat-group sections (Income / OtherCF / Expenses) for one selected account; shares Sub-Cat aggregation backbone with §2.3.2's cross-account query path. Architect dedupes at Phase 3 (likely shared infrastructure with a single-account-filter parameter). Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — drill-down view capability.** Per §2.3.3: parallel shape question to §2.2.3's drill-down flag (line 149). Does V1's cash-flow view layer support per-account drill-down as a general capability, or is per-account drill-down structurally hardcoded? Forward-compat for V2+. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — V1 expense-transaction data retention horizon.** Per §2.3.4: V1 retains expense transactions back ≥ 5 years to populate the rolling-5-year chart; income transaction retention is narrower (current-year-sufficient per F/CTO 2026-05-14 (α) lock). Architect Phase 3 question: differential retention by Cat (expenses ≥ 5yr, income current-year-only) vs uniform retention across Cats with operational/storage implications. Routing: ADR-002 §8.0 Architect flag, non-F/CTO-led.
- **Architect — CPI-U series sourcing (cross-ref, no new flag).** Covered by §2.1's existing Architect flag; §2.3.4 adds a fourth V1 surface consuming the same CPI-U series (§2.1.2 + §2.1.3 + §2.1.4 + §2.3.4). One CPI-U-source decision serves all four.
- **Security Reviewer — §2.3.5 isolation + scope-attribute pass + as-of-date toggle axis.** Pass-with-comments recorded 2026-05-14 (Task #23). (a) Tenant isolation across §2.3.2 / §2.3.3 / §2.3.4 query paths consistent with ADR-002 §1.4 — confirmed; §2.3.3 as-of-date toggle as user-supplied query-time parameter on multi-tenant data path is a new axis vs §2.1.7 + §2.2.4 — product-level pass; Phase 3 RLS test obligation attached (see Architect as-of-date flag above). (b) Multi-scope ownership per ADR-004 Decision B is a user-owned data attribute, not a V1 isolation boundary — confirmed; §2.3.5's scope-attribute clause endorsed as canonical for §4 verbatim. Three new sensitive data classes added to running §4 matrix (per-transaction merchant/vendor identifier data; user-authored planning targets; derived cash-flow categorization patterns). Three forward-looking comments captured for downstream phases (see Task #23 verdict). Sec re-engagement mandatory at §2.4 + §4; primary author at §4.

#### Acceptance flags

- Each story is a user-facing capability statement, not an implementation specification. Acceptance criteria (the testable conditions) get decomposed in Phase 4 / Linear, not in the PRD.
- §2.3 is **locked** as of 2026-05-14 (Security Reviewer pass-with-comments per Task #23). The Architect routing flags resolve during Phase 3 and surface in Appendix B before Architect sign-off; they do not block §2.3 lock at the PRD level.

### 2.4 Cross-cutting stories (account onboarding, manual entry, re-auth)

## 3. Success metrics

## 4. Security and compliance posture

> **Locking precondition:** Security Reviewer pass required before this section moves from draft to locked.

## 5. V2 deferred candidates

## 6. Out-of-scope for this PRD lifecycle

> Per ADR-002 §3.0, items here are scoped out of the current PRD lifecycle, not declared permanent non-goals.

## 7. Constraints

### 7.1 Cost

### 7.2 Scale

### 7.3 Usage model (single-user V1, invite-only forward-compat)

## Appendix A — Traceability to ADR-002 verdicts

## Appendix B — Open routing flags (Architect / Security Reviewer)

## Changelog
