# BACKLOG — mosko-fintech

Two-purpose backlog per [ADR-009](DECISIONS.md#adr-009) Decision 4 + Decision 7 and [ADR-017](DECISIONS.md#adr-017):

1. **§5 — V2+ deferred candidates** (PRD §5 relocation home; created in PR B per ADR-009 Decision 4). Capabilities on the eventual product trajectory but locked-as-V2+ in V1 to preserve drop-replace-migration scope.
2. **§7 — V1 staging queue** (going-forward from Wave 6 per [ADR-017](DECISIONS.md#adr-017) Decision 2). Per the narrowed feature-flow scheme: `[PRD §2 User Stories] → [BACKLOG.md §7] → [Linear: current + next milestone only] → [Linear: Done] → [MILESTONES.md Completed]`. Existing V1.0–V1.4 issues already in Linear (SELF-181 → SELF-269) stay there; milestone rotation handles them at Phase 5 implementation start. Wave 6 (V1.5) onward decompositions land in §7 with full Source / Acceptance criterion / Dependencies specs, then promote to Linear at milestone-rotation time.

**Conventions:**
- §5.x sub-section grouping mirrors PRD §2.x source order (§5.1 net-worth deferrals from §2.1 / §5.2 allocation from §2.2 / etc.).
- Every §5 entry traces to an existing §2.x V1/V2 clause or an ADR-locked deferral (per §5 framing — §5 introduces no new V2+ items).
- §7 entries carry the same fields a Linear issue would (Source / Acceptance criterion / Dependencies); promotion to Linear is verbatim with the entry marked "Promoted to Linear at SELF-N" as durable historical reference.
- **§5 / §6 / §7 distinctions:** §5 = V2+ capabilities on the eventual trajectory; §6 = permanent product-identity non-goals (see [`docs/PRD/index.html` §6](docs/PRD/index.html#sec-6); not in this file); §7 = V1 staging queue (going-forward, repo-versioned V1 work-spec for milestones not currently active in Linear).
- Last updated: 2026-06-03 ([ADR-017](DECISIONS.md#adr-017) §7 staging-queue framing added; §5 content frozen at PRD lock 2026-05-18 / v1.30).

---

## §5 framing — capabilities on the eventual trajectory

§5 is the consolidation home for the V2+ deferred capabilities surfaced across the V1 PRD lifecycle — every V1/V2 boundary clause in PRD §2.1 → §2.6 and the deferral surfaces created by the relevant ADRs (ADR-002 Finding (b); ADR-004 Decisions A–D; ADR-005; ADR-006; ADR-007). The §5/§6 axis is sharp: §5 enumerates capabilities that **are** on the eventual product trajectory — locked-as-V2+ in V1 to preserve the single-user drop-replace-migration scope but anticipated as legitimate later work; §6 enumerates capabilities that are **not** in this PRD's universe at all (see PRD §6 for the full axis enumeration — five permanent product-identity axes per ADR-002 §3.0 + ADR-007). "Later" vs "not in this PRD's universe" is the operative distinction. §5 is to *capabilities* what §3.5 is to *measurement frames* — both make the V1 perimeter visible by its complement and prevent re-litigation of settled scope decisions. §5 introduces no new V2+ items; every entry traces to an existing §2.x V1/V2 clause or an ADR-locked deferral.

---

## §5.1 — Net-worth deferrals

- **User-configurable time-axis controls on NAV surfaces.** V1 fixes the §2.1.2 NAV-over-time rolling window (60 months), the §2.1.3 multi-horizon NAV-delta horizon set (Month / YTD / 1-Yr / 3-Yr / 5-Yr), and the §2.1.4 NAV-at-reference-dates set (This Month / Prior Month / Prior Year-End). User-configuration of any of these three V1-fixed sets is V2+ per the §2.1.2 / §2.1.3 / §2.1.4 V1/V2 clauses. Deferral rationale: V1 calibrates against the existing-system Finance_Report fixed shape for parity-grounded migration; configurability is post-parity-soak expansion.
- **Per-scope NAV reporting + scope-aware UI filtering.** §2.1.7 V1/V2 (ADR-004 Decision B). V1 data model carries scope on every account from V1 bootstrap so the V2 expansion ships without a data migration; V1 UI is full-household-only. Per-scope NAV views, per-scope NAV-over-time charts, and scope-filtering affordances on §2.1 surfaces are V2+.
- **Per-tenant CPI-U source override.** V1 settles on a single CPI-U sourcing decision per Architect Phase 3 routing flag at §2.1 (live API vs. user-managed seed); per-tenant override that lets the user choose between CPI-U sources or supply a custom inflation series is V2+. Deferral rationale: single-user V1 has no use case for per-tenant CPI-U divergence; V2+ multi-user expansion may surface it.
- **Historical NAV import beyond V1 Dec-2015-forward parity import.** V1 imports the F/CTO's existing Google Sheet monthly NAV history from Dec-2015 forward as the parity baseline (§2.1.3 trace; Architect routing flag at §2.1 for the import mechanism). User-facing historical NAV import surfaces beyond this V1 parity import (bulk CSV import of NAV history for new tenants in V2; user-driven historical correction workflows) are V2+.

---

## §5.2 — Allocation deferrals

- **User-editable asset-taxonomy CRUD UI.** §2.2.1 V1/V2 (ADR-004 Decision C). V1 seeds the F/CTO's two-level asset taxonomy (6 Cat × ~35 Sub-Cat) at bootstrap and ships the holding-to-bucket assignment UI, but taxonomy editing (create / rename / delete Cat or Sub-Cat) happens via migration / direct database access in V1. V2 adds the editing UI.
- **Auto-generated rebalance suggestions.** §2.2.2 V1/V2 (ADR-002 §1.1 + ADR-004 Decision A). V1 ships the visualization-only `$ ReAlloc` gap layer; the recommendation engine that says "sell $X of VTI and buy $Y of VOO" — including tax-lot-awareness, account-type awareness, brokerage-workflow adjacency — is V2+. Deferral rationale: archetype attribute #4 (decisional posture) treats V1 as observational tool; auto-generated rebalance suggestions cross into prescriptive surface that requires a separate scope expansion.
- **Ex-US sub-allocation drill-down.** §2.2.3 V1/V2. V1 ships only the US Equity sub-allocation table matching existing Finance_Report structure; a parallel drill-down for international equity Sub-Cats (ExUS-Developed_Market, ExUS-Emerging_Market) is V2+.
- **Per-scope allocation views + scope-aware filtering UI across §2.2 surfaces.** §2.2.4 V1/V2 (ADR-004 Decision B). Per-scope allocation tables, per-scope `$ ReAlloc` deltas, and scope-aware UI filtering across the allocation surface are V2+; V1 data model carries scope from V1 so V2 expansion ships without migration.
- **Per-account taxonomy overrides.** V1's two-level taxonomy is one-per-tenant (per ADR-004 Decision C hybrid operationalization); account-specific Sub-Cats unique to a single account's workflow are V2+. Parallel pattern to §5.3 per-account taxonomy override V2+.
- **General drill-down view capability across allocation surfaces.** §2.2.3 V1/V2 forward-compat consideration. Whether V1's allocation-view layer supports drill-down as a general capability (any Sub-Cat → own sub-rows) versus hardcoded US Equity special case is Architect Phase 3; user-extensible drill-down is V2+ regardless.

---

## §5.3 — Cash-flow deferrals

- **User-editable cash-flow-taxonomy CRUD UI.** §2.3.1 V1/V2 (ADR-004 Decision C). Parallel to §5.2's asset-taxonomy CRUD V2+; V1 seeds the cash-flow taxonomy (Income / Expenses / OtherCF / AcctSetup × user Sub-Cats) and ships the transaction-to-bucket assignment UI, but taxonomy editing happens via migration in V1.
- **Budget tracking mechanics.** §2.3.2 V1/V2 (ADR-002 §1.2 + ADR-005). V1 ships static reference-value rendering of two user-authored aggregate targets (one income, one expense) inline in §2.3.2 captions with no variance computation. V2+ delivers (per ADR-002 §1.1 broader budget-tracking-with-goal-setting surface):
  - Actual-vs-target variance computation.
  - Threshold alerts and category notifications (feature-class within this V2+ surface).
  - Category-level rolling budgets.
  - Per-category target authoring beyond aggregates.
  - The broader budget-tracking-with-goal-setting surface itself.
- **Rule-based auto-categorization beyond recurring-vendor inference.** §2.3.2 V1/V2. V1 ships Plaid-category suggested default + recurring-vendor inference (same-merchant → suggest last-assigned Sub-Cat per §2.3.1 ζ lock). User-authored category-assignment rules with regex / pattern matching beyond simple recurring-vendor inference are V2+.
- **Per-account taxonomy overrides.** §2.3.3 V1/V2. Account-specific Sub-Cats that don't exist in the global taxonomy are V2+.
- **Per-scope cash-flow rendering + scope-aware filtering UI.** §2.3.5 V1/V2 (ADR-004 Decision B). Per-scope rollups, per-scope drill-downs, and scope-aware filtering across §2.3.2 / §2.3.3 / §2.3.4 surfaces are V2+; data model carries scope from V1.
- **Income time-series chart + multi-year historical income recordkeeping.** §2.3.4 V1/V2 (F/CTO 2026-05-14 (α) lock). V1 ships expenses-only Historical Expenditures chart; mirror income time-series chart and the underlying multi-year income retention are V2+. Deferral rationale per §2.3.4 body: realized capital gains from rebalancing partially fund expenses, so an expenses-over-time chart isolates the expense signal cleanly while an income-over-time mirror would entangle income with realization decisions.
- **Historical Expenditures chart extensions.** §2.3.4 V1/V2. User-configurable chart horizon (V1 fixed at 5-year rolling), chart drill-down into specific months or Sub-Cats, and authored target reference line on the chart are V2+.
- **Non-monthly and custom-defined cash-flow periods.** ADR-002 §1.2 V2+ (pre-§2.3 lock). Weekly / quarterly / YTD defaults beyond V1's monthly default + V1-fixed Q1-Q4-YTD columns, and custom user-defined period scopes, are V2+.

---

## §5.4 — Cross-cutting onboarding / entry / re-auth deferrals

- **Auto-classification of new symbols from Plaid metadata.** §2.4.1 V1/V2. V1 surfaces Plaid security_type + description + ticker as recommendation hints in the notification-queue assignment UI but never auto-applies; full auto-classification without user confirmation is V2+ and requires metadata-accuracy evidence before the V1 commitment relaxes.
- **Onboarding workflow extensions.** §2.4.1 / §2.4.2 V1/V2. V1 ships single-account-at-a-time onboarding for both Plaid and manual paths. Bulk-connect-multiple-institutions (§2.4.1), bulk import for manual non-Plaid accounts (§2.4.2), and manual un-share of an already-shared Plaid account (§2.4.1) are V2+.
- **Pre-emptive notification surfaces.** §2.4.1 + §2.4.4 V1/V2. V1 ships in-app notification queue only for pending new-symbol assignments and persistent in-app banner only for re-auth state. Two distinct V2+ commitments with different Sec posture:
  - **Push / email / SMS notification of pending new-symbol assignments** (§2.4.1) is V2+. No Sec gate beyond standard V2 delivery-channel review.
  - **Pre-emptive Plaid re-auth reminders** (§2.4.4) are V2+ with **Sec consult required before V2 ship** — pre-emptive re-auth prompts expand the phishing-template surface (counterfeit re-auth prompt risk); per §2.4.4 lock the V1 reactive-only cadence trades a bounded stale-data window against a narrower phishing surface. Lands at [docs/SECURITY](docs/SECURITY/index.html) §4.6 V2-ship-gate inventory item (i).
- **Manual transaction entry extensions.** §2.4.3 V1/V2. V1 ships single-transaction-at-a-time entry + generic AcctSetup mode. V2+:
  - CSV bulk-import of historical transactions.
  - Event-type-specific AcctSetup wizards (versus V1 generic AcctSetup).
  - Auto-reconcile on balance-match (B3 axis per F/CTO 2026-05-15 (i) lock).
  - Free-text rules-engine for auto-categorization beyond §2.3.1 recurring-vendor inference (cross-ref §5.3).
  - Generic free-text "other" AcctSetup non-cash event entry (reserved per [ADR-033](DECISIONS.md#adr-033) Decision 3). Once **split** (→ `corp_action`) and **transfer-in-kind** (→ journal `group_type='transfer_in_kind'`) claim their cases, no distinct V1 "other" use case survives — a generic non-cash event reduces to a `standard` §2.3.1 cash-flow row classified by its Sub-Cat (Suspense-floored if uncategorized), so there is nothing distinct to build. V2+ if a genuine generic-event surface is later scoped.
- **External valuation integrations (Zillow, KBB, etc.).** ADR-002 §1.5 V2+ (pre-§2.4 lock). V1 manual asset valuation is user-updated. External-source live valuation ingestion for held-away assets is V2+.
- **Plaid product expansions.** ADR-002 §1.3 + §1.9. Two distinct V2+ product surfaces:
  - **Plaid Liabilities** (APR, statement balance, principal / interest split, escrow, payoff projections — credit cards + loans).
  - **Plaid Income** (wage / income aggregation surface as primary V1 income source — V1 derives income from Plaid Transactions + Investments instead).
- **Plaid coverage and instrument-level mechanics.** ADR-002 §1.8 + §1.9 V2+. V1 treats all Plaid-surfaced security types uniformly at transaction-and-position level with security type as categorization attribute. V2+ instrument-level mechanics:
  - **Underlying instrument-mechanics axes** — derivative Greeks / intrinsic-value / complex lifecycle events; bond YTM / duration / accrued interest / coupon scheduling; tax-character decomposition for REITs / MLPs (K-1 partnership-character splits); structured-product specifics.
  - **Off-exchange crypto wallets, on-chain transactions, and mining / staking-as-income mechanics** (§1.9).
  - **DRIP-pair detection + "income realized in cash vs reinvested" display split** (§1.9).
  - **Per-security user-configurable allocation classification** (§1.9).
- **Tax-treatment refinements.** ADR-002 §1.6 V2+. HSA "tax-free conditional" classification refinement (sub-flag or fourth bucket reflecting medical-withdrawal constraint) is V2+; V1 three-way tagging (taxable / tax-deferred / tax-free) does not differentiate HSA's conditional shape.

---

## §5.5 — Estimated-tax deferrals

- **Auto-categorization and user-editable CRUD on tax attributes.** §2.5.1 V1/V2. V1 seeds Sub-Cat `tax_relevant` boolean + `tax_character` enum at bootstrap from F/CTO existing system (per ADR-006 Axis 2); editing is migration-only in V1. Two distinct V2+ surfaces:
  - **Auto-categorization of tax-relevant Sub-Cats** from Plaid metadata or heuristic.
  - **User-editable Sub-Cat tax-attribute CRUD UI** (additive to the broader §2.3.1 + §2.2.1 taxonomy CRUD V2+ surfaces).
- **Multi-year tax surfaces.** §2.5.1 + §2.5.2 V1/V2. V1 scopes the §2.5.1 three-column decomposition to current calendar year and holds a single current-tax-year bracket-schedule set in §2.5.2. V2+:
  - Multi-year historical Ordinary Income / ST CG / LT CG tracking + time-series chart (mirrors §5.3 income-time-series V2+).
  - Multi-tax-year bracket version history.
  - Bracket inheritance from prior tax-year.
  - Custom fiscal-year overrides.
- **Full Federal AGI-line decomposition (ζ-3).** §2.5.1 V1/V2. V1 ships the three-column Ordinary / ST CG / LT CG decomposition with `tax_character` routing per ADR-006 Axis 2; per-Sub-Cat mapping to Schedule B / Schedule D / Schedule E / etc. for full Federal AGI-line decomposition is V2+.
- **Wash-sale and Section 1256 auto-detection.** §2.5.1 V1/V2 (ADR-002 §1.7 + ADR-004 Decision D). V1 ships user-marked wash-sale flag and user-classification of Section 1256 60/40 via the `Volatility-60/40` Sub-Cat. Auto-detection of wash-sale rule application and Section 1256 60/40 classification is V2+.
- **Lot-level tax features.** ADR-002 §1.7 + ADR-004 Decision D V2+ (unchanged by ADR-006). V1 ships aggregate cost basis per position with average-cost-fallback realized G/L. V2+:
  - **FIFO / LIFO / specific-ID lot-matching** for tax purposes.
  - **Per-lot UI** (lot tables, holding-period indicators).
  - **Lot-level cost-basis reporting** on tax surfaces.
  - **Tax-loss harvesting recommendations are NOT included in this V2+ surface** — reclassified to §6 per ADR-007.
- **In-state-vs-out-of-state municipal-bond differentiation for CA FTB `tax_exempt_interest` routing.** §2.5.1 / §2.5.2 V1/V2. V1 treats all `tax_exempt_interest` uniformly for CA computation (excluded); California in-state-issuer vs. out-of-state-issuer differentiation is V2+.
- **Live tax-data API ingestion of bracket tables.** §2.5.2 V1/V2 (ADR-006 Axis 1). V1 user-manually updates Federal + California bracket schedules + standard deductions at tax-year rollover. V2+:
  - **Live API ingestion** (IRS / California FTB public rate-table sources).
  - **Diff / version-history awareness** on ingested bracket tables.
  - **Bracket-table import from external CSV / IRS publication sources.**
  - **Sec re-consult MANDATORY at adoption** (per [ADR-011](DECISIONS.md#adr-011) Decision 18 / Lock 14 mod #8 advisory forward-compat fence). V2+ live-tax-API ingestion is a future privileged-context-write surface (§6 meta-pattern per ADR-011 Decision 1); inherits Lock 11 mod #2 cron tenant-binding + Lock 13 `TenantBoundConnection` discipline automatically when the surface lands. Activates the cross-tenant FK-bypass family chain (per ADR-011 Decision 3) on `pfin.tax_bracket_schedule.users_id` column (V1-safe by-construction since V1 settings writes are user-session-bounded) — at V2+ adoption, the Lock 12 mod #2 pattern (immutability DB-trigger fencing tenant-anchor `users_id` UPDATE post-creation) becomes V1-SHIP-BLOCK for that V2+ surface.
- **Multi-jurisdiction tax expansion.** ADR-002 §1.7 + ADR-004 Decision D V2+ (unchanged by ADR-006). Multi-state tax handling (any non-California state) and non-US tax handling (RRSP, ISA, foreign tax credits, etc.) are V2+. V1 supports Federal + California FTB only.
- **Filing-status enum.** §2.5.2 V1/V2 (ι lock). V1 carries a single standard-deduction scalar per jurisdiction applicable to F/CTO's current filing status (fixed at seed time). Filing-status enum (single / MFJ / HoH) with user-selectable filing-status at settings UI + multi-scalar standard deduction is V2+.
- **Bracket-aware tax credits and above-the-line deductions.** §2.5.2 V1/V2. V1 ships single standard deduction per jurisdiction; FTC, child tax credit, and other tax-credit handling beyond standard deduction are V2+.
- **Separate California LT CG schedule.** §2.5.2 V1/V2 (κ lock). V1 collapses CA LT CG to ordinary (no separate CA LT CG schedule); if existing-system divergence surfaces at V1 implementation, separate CA LT CG schedule is V2+.
- **Quarterly-installment-sizing refinements.** §2.5.3 V1/V2 (μ-2 lock). V1 ships bracket-derived expected-annual ÷ 4 only with no safe-harbor floor; Tax Balance Prior Year row appears as informational reference only. V2+:
  - **Safe-harbor floor computation** (Federal 100%/110%-of-prior-year + CA FTB safe-harbor rules).
  - **Alternative quarterly-installment-sizing** — μ-1 max-of bracket-derived-and-safe-harbor; μ-3 two-column rendering.
  - **Annualized-income installment method** (IRS Form 2210 method 2).
- **Withholding tracking.** §2.5.3 V1/V2. V1 simplification treats all incoming-tax-payments as "estimated payments" in the IRS / FTB ledgers; withholding (W-2 / 1099) tracked distinct from estimated payments is V2+.
- **Pre-emptive quarterly-payment-due-date reminders.** §2.5.3 V1/V2 (ξ-2 / ξ-3 V2+). V1 ships reactive in-table due-date surfacing with current-quarter visual emphasis. V2+:
  - **Pre-emptive in-app notifications** of quarterly due dates.
  - **Email reminders** of quarterly due dates.
  - **Calendar integration** for quarterly due dates.
- **Refund / overpayment surfacing extensions.** §2.5.3 V1/V2 (ν-2 V2+). V1 surfaces overpayment as negative single-line Estimated Funds Due (ν-1 lock). Separate "Refund Expected" line + distinct overpayment-status indicator is V2+.
- **Penalty computation and prior-tax-year computation surfaces.** §2.5.3 V1/V2. V1 ships no penalty computation; users responsible for understanding underpayment-penalty risk outside the system. Penalty computation against safe-harbor and prior-tax-year computation surfaces are V2+.
- **Bracket-aware Unrealized Tax Liability refinements.** §2.5.4 V1/V2 (ο-a lock). V1 Unrealized = simplified marginal × aggregate G/L per ο-a (Federal LT CG top-bracket rate × aggregate_unrealized_G/L_taxable + CA ordinary top-bracket rate × aggregate_unrealized_G/L_taxable; tax-advantaged accounts excluded per (π); `tax_character` enum routing NOT used on Unrealized; §2.5.3 engine NOT consumed by Unrealized). V2+:
  - **ο-b — Full bracket-aware as-if-realized.** Treat all aggregate unrealized G/L as if realized today, run through §2.5.3 progressive bracket computation engine with ST/LT split via §2.4.3 Open Date and `tax_character` enum routing.
  - **ο-c — Hybrid LT-only bracket-aware.** ST CG portion treated as marginal × G/L; LT CG portion runs through Federal LT CG bracket schedule.
  - **Federal-ordinary-top-bracket-rate as more-conservative alternative for the Federal_marginal_rate factor** — would treat unrealized G/L as marginal additional ordinary income rather than as LT capital gain.
- **Per-jurisdiction split rendering of Realized + Unrealized as separate lines.** §2.5.4 V1/V2 (ρ-2 V2+). V1 surfaces single combined-jurisdiction Realized scalar and single combined-jurisdiction Unrealized scalar on §2.1.5 composition. Per-jurisdiction Federal + CA split rendering on §2.5.4 + §2.1.5 is V2+.
- **Tax-deferred withdrawal-tax-liability as a fourth NAV-subtraction line.** §2.5.4 V1/V2. V1 (π) exclusion filters tax-deferred and tax-free accounts out of the `aggregate_unrealized_G/L_taxable` aggregation; tax-deferred account Unrealized "ordinary-income-on-withdrawal" treatment as a separate "deferred tax obligation" line, and withdrawal-tax-liability for tax-deferred accounts as a fourth NAV-subtraction line item, are V2+.
- **REIT / MLP K-1 partnership-character splits on unrealized G/L.** §2.5.4 V1/V2 (ADR-002 §1.8 V2+). Cross-ref §5.4 instrument-level mechanics deferral.
- **Multi-state Unrealized Tax Liability sourcing.** §2.5.4 V1/V2. Parallel to multi-jurisdiction tax expansion V2+.
- **Monte Carlo longevity modeling.** ADR-002 Finding (b). Projection / scenario surface for retirement / longevity planning; observational tool within §1.2 archetype attribute #4 framing.
- **Stock screening — possibly a separate tool rather than mosko-fintech scope.** ADR-002 Finding (b). The "possibly a separate tool" hedge from Finding (b) is preserved verbatim: this V2+ line is not a commitment to ship within mosko-fintech, only that it's not V1. **Candidate P3 disposition at Phase 1 Step 4 close (per [ADR-011](DECISIONS.md#adr-011) Decision 20 / Lock 16):** V1-default — `pfin_back_etl` FMP API ingestion continues (incumbent); stock-screening tables accumulate in `pfin_dash` schema; V1 UI does NOT expose stock-screening surface. V2+ trajectory: if the surface lands in mosko-fintech scope, existing ingestion provides backfilled data; if it ships as a separate tool, the FMP ingestion can be repurposed or wound down. The candidate-P3 incumbent-exceeds-V1 review (per `feedback_incumbent_exceeds_v1_review` memory) ratified V1-default at Lock 16 with F/CTO disposition; no V1 PRD-scope expansion. *[Annotation 2026-06-02 — Phase 4 entry-gate close per [ADR-011](DECISIONS.md#adr-011) Decision 20:* the deferred candidate-P3 PM consult was performed a second time at Phase 4 entry (Phase 4 Step 1 entry-gate PM ratify-pass; PM verdict CONFIRM under Option A F/CTO ratification at v1.44 joint-close) and **re-confirms the V1-default disposition** with no scope change. PRD §2 grep verified zero V1 stories depend on stock-screening; ARCH `sec-2-fmp-note` verified ingestion-only topology; existing BACKLOG entries verified durably codified. Disposition unchanged; this entry stands as the V2-trajectory canonical anchor. *Last fresh consult before V2 scoping.*]

---

## §5.6 — Monthly-report deferrals

- **User-configurable section ordering and composition.** §2.6.1 V1/V2 (ω-2 / ω-3). V1 ships fixed six-section sequence (Account Holdings → NAV Performance → Asset Allocation → Rebalancing Targets → Cash Flow → Estimated Taxes). V2+:
  - **User-configurable section ordering** — add / remove / hide sections via settings UI + user-selectable section visibility per-report + user-defined custom sections beyond the six.
  - **Reintroduction of Big Ticket Fund / Amortized Expenses** if F/CTO revisits the Phase 0.5 drop.
  - **Alternative inline-vs-standalone placement of Historical Expenditures.**
  - **Alternative cross-section composition** that decouples report sections from §2.1 – §2.5 live-surface boundaries (e.g., year-over-year comparison view).
- **Multi-scope reports.** §2.6.1 + §2.6.6 V1/V2 (parity-matrix line 180; ADR-004 Decision B). V1 default report scope is full-household with a single per-tenant owner-identification header; per-scope report variants (one report per scope, each with its own owner-identification header per ψ-2 multi-named-owner config) are V2+.
- **Auto-generated and hybrid Rebalancing Targets commentary.** §2.6.2 V1/V2 (σ-2 / σ-3). V1 ships σ-1 free-text user-authored commentary under four V1-fixed sub-sections (Cash / Bonds / Equity / Alternatives). Auto-generated commentary from §2.2.2 `$ ReAlloc` positive-delta rows (σ-2) and hybrid-with-edit (σ-3) are V2+; both require ADR-004 Decision A amendment. Per-asset-class sub-section template prompts as authoring scaffolding are V2+.
- **Rebalancing Targets editor extensions.** §2.6.2 V1/V2. V2+:
  - **User-configurable sub-sections** (rename / add / remove / reorder) paralleling §2.2.1 taxonomy-CRUD V2+.
  - **Markdown / rich-text formatting affordances** on the editor (bold / italic / lists / headings / inline links).
  - **Auto-pre-population of new-month commentary editor from prior-month content** as default behavior, plus a settings toggle to choose blank-vs-auto-copy as per-tenant default.
  - **Late-edit / amend-after-generation flows** on historical-month commentary with explicit revision tracking.
- **Generation-cadence and trigger extensions.** §2.6.3 V1/V2. V2+:
  - **User-configurable cron schedule** (date + time-of-day).
  - **Per-jurisdiction tax-aware quarter-end adjustments to cron cadence.**
  - **Alternative cron cadences** (weekly / quarterly / annual companion artifacts).
  - **Automated cron-retry + partial-run recovery.**
  - **In-app cron-failure notification to the user.**
  - **Alerting / monitoring infrastructure beyond system-level logging.**
- **Revision history for regenerated months.** §2.6.3 + §2.6.4 V1/V2. V1 ships overwrite-semantics regeneration (one snapshot per `(tenant, target-month)`, last-writer-wins, no prior revision retained). Revision-history-aware snapshot storage and "regenerate with revision-history" user-triggered flow are V2+. Resolves the §2.6.2-vs-§2.6.3 persistence-tension noted at §2.6.3 lock.
- **Alternative delivery channels — email / SMS.** §2.6.3 V1/V2. **Forward-Sec-consult flag (carry-forward to V2 scoping):** email / SMS delivery of generated reports introduces a new delivery channel V1 does not exercise — report content rendered into transit message body is a new sensitive-data exfiltration surface (especially over SMS, which is plaintext); Sec re-engagement required before V2 ship. Lands at [docs/SECURITY](docs/SECURITY/index.html) §4.6 V2-ship-gate inventory item (ii).
- **Alternative delivery channels — shared-link to external viewers.** §2.6.3 V1/V2. **Forward-Sec-consult flag (carry-forward to V2 scoping):** shared-link delivery intersects multi-tenant + access-control scope and may overlap §6 advisor-role boundary; Sec re-engagement + possible ADR re-litigation against §6 advisor-role boundary required before V2 ship. Lands at [docs/SECURITY](docs/SECURITY/index.html) §4.6 V2-ship-gate inventory item (iii).
- **Alternative output formats.** §2.6.3 V1/V2. V1 ships in-app rendered web page + on-demand PDF export (υ-1). V2+:
  - **Google Doc parity-exact** (the rejected υ-3 path, documented as V2+ optional companion if F/CTO revisits Google Workspace integration).
  - **Markdown export.**
  - **HTML email body.**
  - **Structured-data export** (JSON / CSV).
  - **Scheduled PDF auto-export to user storage** (Drive / Dropbox / S3 / etc.).
- **Drill-down from report sections to source surfaces.** §2.6.3 V1/V2. Drill-down from a section in the in-app view to its source PRD story / current-state surface (e.g., click NAV Performance → land on §2.1.2) is V2+.
- **Live-rendered date-filtered views of historical months.** §2.6.4 V1/V2 (φ-2 / φ-3). V1 ships φ-1 frozen-at-generation snapshot (parity-exact with existing-system Finance_Report PDF behavior); live-rendered date-filtered views of historical months and the φ-2 / φ-3 toggle complexity that supports them are V2+.
- **Snapshot-store retention and management extensions.** §2.6.4 V1/V2. V1 χ-1 retains every generated report indefinitely with no retention-window cleanup, archival-tier migration, or user-initiated deletion. V2+:
  - **User-initiated deletion of historical reports.**
  - **Automated retention windows** (e.g., retain 5 years then archive to cold storage).
  - **Archival-tier migration.**
  - **PDF caching.**
  - **Source-data-level snapshots** (re-renderable from underlying transactions, versus V1 rendered-value-level snapshots).
- **Owner-identification header extensions.** §2.6.4 V1/V2 (ψ-2 / ψ-3). V1 ships ψ-1 single per-tenant owner-identification string driving every report's header. Multi-named-owner config (ψ-2), per-report header override at generation time (ψ-3), and multi-line / rich-text owner-identification headers are V2+. ψ-2 specifically anticipates V2+ multi-scope reports.
- **Staleness-marker extensions.** §2.6.5 V1/V2. V1 ships α′-1 generate-with-markers with inline per-section indicator + report-level summary banner, uniform across all four §2.4.4 credential-error states, with live-read at render time. V2+:
  - **Per-credential-error-class differentiated marker visuals.**
  - **"Block with warning" mode** as user-selectable preference.
  - **User-initiated dismiss / acknowledge of staleness markers per report.**
  - **Staleness-history layer** showing when an account went stale relative to the report's as-of date.
  - **Marker-state snapshotting** — recording staleness state at generation time alongside the snapshot for a historical-audit V2+ feature.

---

## §5.7 — Cross-cutting V2+

- **Multi-user invite-only V2 expansion.** ADR-002 §1.4 V1-to-V2 transition. V1 ships to a single user (the F/CTO) on a multi-tenant data model with `tenant_id` on every user-data table, RLS policies enforced, and multi-tenant-capable auth infrastructure from day one. V2 adds the second user via invite-only onboarding; the V1 forward-compatibility commitment is that no data migration of V1 user data is required when the second user lands. V2+:
  - *Per-user taxonomy provisioning — **resolved in V1, not deferred here.** The gap where a second / open-signup user would land with an empty `user_taxonomy` and be unable to classify (SELF-200 picker dead-end) is closed by the V1 deliverable **[§7.5 O1 — Default taxonomy provisioning on signup (asset + cash-flow domains)]** (F/CTO ruling 2026-07-28: V1 signup is OPEN; default-taxonomy seeding promoted into V1). Multi-user therefore inherits a populated per-user taxonomy (both domains) at signup — no V2 prerequisite remains on this axis.*
  - **Friends-and-family onboarding.**
  - **Invite-flow UI.**
  - **Multi-user auth gates.**
  - **Per-user data-access boundary checks.**
  - Cross-reference: §7.3 (usage model: single-user V1, invite-only forward-compat) references this entry.
- **Multi-currency.** ADR-002 §3.0 reclassification from non-goal to V2+ deferral. The "in V1" qualifier in the original finding made multi-currency a deferral, not an identity statement; mixing it into the out-of-scope list was rejected as weakening the discipline of both buckets. V1 is USD-denominated. V2+:
  - **Multi-currency holdings.**
  - **Multi-currency transactions.**
  - **FX conversion surfaces.**
  - **Per-tenant base-currency selection.**
- **FMP API cost-saving levers** (per [ADR-011](DECISIONS.md#adr-011) Decision 20 / Lock 16 / Flag #11 cost-feasibility Outcome 1). V1 retains FMP starter plan per F/CTO ratification. Two V2+ cost-saving levers captured for future evaluation if V1 cost-shape pressures emerge:
  - **(b) FMP free tier downgrade** — $0/mo subscription IF the free tier's rate limits hold for V2+ needs. May degrade ingestion frequency or coverage; Phase 3 + V1.1+ verifies feasibility against query shape.
  - **(c) Yahoo / Google Finance scrape replacement** — $0/mo subscription cost replacement of FMP entirely. **Risk-flagged:** TOS ambiguity for commercial use; rate-limiting unpredictable; format-change breakage; data-quality concerns vs. structured FMP API. Sec re-consult MANDATORY before adoption (terms-of-service + data-quality posture); Architect re-consult for ingestion-code rewrite.
- **Stock-screening UI surface** (per [ADR-011](DECISIONS.md#adr-011) Decision 20 / Lock 16 candidate P3 V2+ trajectory). V1 ingests FMP data into `pfin_dash` stock-screening tables (incumbent) but exposes NO V1 UI surface. V2+ candidate: surface stock-screening capability (per-equity profile / EOD-price chart / statement viewer / screener UI). Cross-ref §5.5 above "Stock screening" line for the V2+ disposition narrative. PM consult required at V2-scoping time to scope the surface within the mosko-fintech-vs-separate-tool framing per ADR-002 Finding (b) hedge. *[Annotation 2026-06-02 — Phase 4 entry-gate close per [ADR-011](DECISIONS.md#adr-011) Decision 20:* the deferred candidate-P3 PM consult was performed a second time at Phase 4 entry (Phase 4 Step 1 entry-gate PM ratify-pass; PM verdict CONFIRM under Option A F/CTO ratification at v1.44 joint-close) and **re-confirms the V1-default disposition** with no scope change. PRD §2 grep verified zero V1 stories depend on stock-screening; ARCH `sec-2-fmp-note` verified ingestion-only topology; existing BACKLOG entries verified durably codified. Disposition unchanged; this entry stands as the V2-trajectory canonical anchor. *Last fresh consult before V2 scoping.*]
- **Hetzner cax21 → cax31 / cax41 VPS tier escalation path** (per [ADR-011](DECISIONS.md#adr-011) Decision 20 / Lock 16 + `reference_hetzner_cax21` memory). V1 ships on incumbent Hetzner cax21 (8 ARM vCores + 16 GB RAM + 160 GB disk at €9.50/mo). Phase 3 stress-test verifies headroom under full Lock 13 worker stack (Puppeteer + ETL + Supabase + Plaid concurrent load). V2+ escalation path: cax31 (~€18/mo; 2× headroom) → cax41 (~€36/mo; 4× headroom). Architect Phase 3 + first-quarter actual cost-tracking monitors squeeze-point; cost-feasibility re-review at V2 ship if cax21 no longer fits.
- **Promote `account_type` from `TEXT`+`CHECK` to a `pfin.account_type` lookup table** (per [ADR-022](DECISIONS.md#adr-022) account_type-taxonomy-rationale; SELF-187 migration `003`). ADR-022 records the V1 decision to model `pfin.account.account_type` as a fixed `TEXT`+`CHECK` 7-member enumeration (depository / investment / retirement / crypto / manual_other / real_estate / liability) rather than a lookup table, because `account_type` is a **closed, code-coupled taxonomy** — per [ADR-002](DECISIONS.md#adr-002) §1.9 each type drives ingest path + NAV grouping ([PRD §2.1.5](docs/PRD/index.html#story-2-1-5) composition membership) + asset-allocation bucket + income treatment — so a lookup table buys nothing in V1: adding a type is a **code event, not a data event** (a one-line `CHECK` alter). **Re-assessment trigger:** reconsider promotion when a type needs *per-type metadata* the schema would otherwise have nowhere to hold — display label, icon, default `tax_treatment`, Plaid-product mapping, sort order — i.e. when there is something for the table to *hold* beyond the bare type name. **Migration shape (contained, no data loss):** create `pfin.account_type`, seed the existing 7 members, swap the `CHECK` constraint for an FK on `pfin.account.account_type`, backfill — additive, no row loss. **Contrast — user-extensible taxonomy already has its home:** distinct from the user-editable Cat × Sub-Cat taxonomy in `pfin.user_taxonomy` per [ADR-004](DECISIONS.md#adr-004) Decision C (per-user, user-editable, seed-only in V1); `account_type` is the *closed* counterpart and stays code-coupled. **Honest scope:** a tracked re-assessment trigger, *not* a committed V2 ship — promotion happens only if/when the per-type-metadata need actually materializes.

---

## Linear overflow queue

Per ADR-009 Decision 7's feature-flow scheme, BACKLOG.md doubles as the overflow queue *above* Linear's ≤200-hot working set:

```
[PRD §2 User Stories]       ← intent (32 stories per Appendix C; never deleted)
        ↓ (Plan phase: stories sized + milestone-tagged)
[BACKLOG.md]                ← overflow queue, ordered by milestone, FIFO promotion
        ↓ (sprint boundaries, /sync-backlog promotes batch)
[Linear (≤200 hot)]         ← active set under work
        ↓ (features finish)
[Linear: Done]              ← /merge-pr marks status; /cleanup-linear archives
        ↓ (mirrored locally)
[MILESTONES.md → Completed] ← snapshot of done features
```

**Promotion mechanic.** `/sync-backlog` (adopted now per ADR-009 Decision 7 skill-suite phasing) promotes batches from this file into Linear at sprint boundaries. The §5.x deferrals above become the seed pool of V2-trajectory candidates the overflow queue draws from once V1 ships and V2-scoping work begins. Until V2-scoping, the §5 sub-sections function as forward-looking catalog only — no entries promote to Linear during M0/M1.

**Cross-references.**
- [PRD §2 user stories](docs/PRD/index.html#sec-2) — the intent funnel above this queue.
- [MILESTONES.md](MILESTONES.md) — the post-promotion state ledger (compact head; live state of done work).
- [DECISIONS.md ADR-009](DECISIONS.md#adr-009) Decision 7 — feature-flow scheme + initial milestones.
- [docs/SECURITY/index.html](docs/SECURITY/index.html) §4.6 — Sec V2-ship-gate inventory (subset of §5 entries above with mandatory Sec-consult-before-V2-ship gates).

**Forward-routing flags** (from PRD §5 lock, preserved for traceability):

- **§5 routing flag (a) — Sec V2-implementation:** Pre-emptive Plaid re-auth reminders pre-V2 ship (lands at [docs/SECURITY](docs/SECURITY/index.html) §4.6 V2-ship-gate inventory item (i)).
- **§5 routing flag (b) — Sec V2-implementation:** Email / SMS delivery of monthly reports pre-V2 ship (lands at [docs/SECURITY](docs/SECURITY/index.html) §4.6 V2-ship-gate inventory item (ii)).
- **§5 routing flag (c) — Sec V2-implementation:** Shared-link delivery of reports pre-V2 ship (lands at [docs/SECURITY](docs/SECURITY/index.html) §4.6 V2-ship-gate inventory item (iii)).
- **§5 routing flag (d) — Closure-trace:** TLH home is §6 (advisor/fiduciary axis) per ADR-007; reclassified from V2+ trajectory to permanent non-goal. Not listed in §5; see [PRD §6.3](docs/PRD/index.html#sec-6-3).
- **§5 routing flag (e) — Closure-trace:** §5 → §7.3 cross-reference closed at §7.3 lock (§5.7 multi-user expansion intersects §7.3 usage-model framing).
- **§5 routing flag (f) — Architect V2-scoping:** V2+ items requiring schema or migration scope decisions — general flag for Architect assessment at V2-scoping time (post-V1 ship). No V1 block.

---

## §7 framing — V1 staging queue

§7 holds going-forward V1 issue decompositions for milestones NOT currently in Linear per [ADR-017](DECISIONS.md#adr-017) Decision 2 (Linear holds current + next milestone only; everything else stages here with full Linear-grade specs). Promotion to Linear happens at Phase 5 milestone-rotation — when current completes, next becomes current, and the milestone after next promotes from §7 → Linear verbatim with the §7 entry marked **Promoted to Linear at SELF-N** as durable historical reference.

**Wave 6 (V1.5 + V1.final) — staged 2026-06-03.** Decomposed at Phase 4 Step 5 Wave 6 close per F/CTO ratify of 6 gates (Gate A unified helper per Wave 5 precedent → PM Issues 1+6+9 absorbed into Arch substrate; Gate B Option C — 4 named TEXT commentary columns on `monthly_report`; Gate C V1.x Platform scope for PDF worker; Gate D single calendar-gated V1.final issue; Gate E separate Sec PR post-Wave-6 for SECURITY §4.4 derivative-surface annotation; Gate F Option α native Coolify cron container). 18 issues total. Detail: [CHANGELOG v1.46 — TBD](CHANGELOG.md).

### §7.1 — Architect substrate (Platform / Cross-cutting V1.x; 8 items)

**A1. `pfin.monthly_report` header table + 4-named-TEXT commentary columns (Lock 11; Gate B Option C).** [V1-SHIP-BLOCK]
- **Source.** [ADR-011 Decision 15 / Lock 11](DECISIONS.md#adr-011) verbatim; [PRD §2.6.1](docs/PRD/index.html#story-2-6-1) header + §2.6.2 commentary persistence per Wave 6 Gate B Option C F/CTO ratify (4 named TEXT cols `commentary_cash` + `commentary_bonds` + `commentary_equity` + `commentary_alternatives` on header table — Lock 14 family stays at 5; no JSONB per forward-compat fence; PRD §2.6.2 fixed 4-sub-section verbatim count).
- **AC.** Schema with `users_id` UUID + `target_month` DATE + `generation_status` Lock-11-vocab enum (`draft`/`final`/`superseded`; PRD `not-yet-triggered`/`pending`/`generated` mapped at presentation) + `data_as_of` DATE + `generated_at` TIMESTAMPTZ + 4 commentary TEXT columns + `created_at` + `updated_at`; UNIQUE(users_id, target_month, generation_status) for `final`; Lock 11 9-mod inventory (INSERT-new-version regeneration per Lock 11 mod #2 Decision 15; supersession via SECURITY DEFINER `fn_supersede_monthly_report`; matched-tenant trigger on users_id FK per Decision 3 family — 6th instance; Lock 15 server-derived-only fence on data_as_of). RLS WITH CHECK `users_id = auth.uid()`. Sec joint-review on supersession mechanism + INSERT-new-version trigger.
- **Dependencies.** Upstream: SELF-232 fn_refresh_updated_at + SELF-233 settings write-path hardening. Downstream: A2, A3, A7, PM #2 in-app UI, PM #4 commentary editor.

**A2. `pfin.monthly_report_account_snapshot` sibling child table (Lock 12).** [V1-SHIP-BLOCK]
- **Source.** [ADR-011 Decision 16 / Lock 12](DECISIONS.md#adr-011) verbatim; PRD §2.6.4 snapshot historical retention.
- **AC.** Child table FK to A1 monthly_report; per-account snapshot rows (NAV components + balances + tax_treatment + scope at snapshot time); Lock 12 8-mod inventory including 3 V1-SHIP-BLOCK: (i) matched-tenant trigger on FK to parent (Decision 3 family — 7th instance); (ii) parent users_id + target_month immutability fence; (iii) service_role bypass DB-trigger on child. RT-21 HIGH + SD-12 child sub-class addendum. Read-only post-write per Lock 10 pattern adjacent. RLS via JOIN to A1.
- **Dependencies.** Upstream: A1, Wave 1 B4 account_trans, SELF-214 nav_daily, SELF-201 manual accounts. Downstream: A3, A7 cron worker.

**A3. SECURITY INVOKER read-composition helper (3 entry paths; absorbs PM Issue 1 per Gate A unified).** [V1-SHIP-BLOCK]
- **Source.** [ADR-011 Decision 15 / Lock 11](DECISIONS.md#adr-011) verbatim Lock 11 SECURITY INVOKER read-composition pattern; F/CTO Gate A Option B unified per Wave 5 precedent.
- **AC.** `pfin.fn_render_monthly_report(p_users_id UUID, p_target_month DATE, p_data_as_of DATE) RETURNS JSONB`. Composes 6-section report tree per PRD §2.6.1 verbatim section ordering: Account Holdings (consumes SELF-225 §2.1.5) + NAV Performance (SELF-218/219/220) + Asset Allocation (SELF-237/240 §2.2.2/§2.2.3) + Rebalancing Targets (reads A1 commentary cols) + Cash Flow (SELF-250/255 §2.3.2/§2.3.4) + Estimated Taxes (SELF-260/261 §2.5.1 + SELF-262 `fn_compute_tax_liability(p_data_as_of)` §2.5.3+§2.5.4). §2.5.4 NAV-components render on Account Holdings via §2.1.5 buildup NOT as Estimated Taxes rows per PRD §2.6.1 verbatim. SECURITY INVOKER preserves RLS at JWT-user-session layer (`auth.uid()` predicate composes across all upstream surfaces). 3 entry paths: in-app render (SSR), PDF render (via A5 endpoint), historical-month view. Sec joint-review on cross-tenant leak surface analysis (mirrors Wave 1 B5 + SELF-262 patterns).
- **Dependencies.** Upstream: A1 + A2 + SELF-262 (Wave 5 unified tax helper) + SELF-214 (nav_daily) + SELF-231 (user_taxonomy) + all Wave 1-5 NAV/allocation/cashflow substrate. Downstream: PM #2 in-app UI, PM #8 PDF export, A7 cron worker invocation.

**A4. Node PDF worker container scaffold (V1.x Platform scope per Gate C).** [V1-SHIP-BLOCK]
- **Source.** [ADR-011 Decision 17 / Lock 13](DECISIONS.md#adr-011) verbatim hybrid worker location; F/CTO Gate C V1.x Platform scope. RT-22 by construction (Lock 13 mod #2 verbatim "no SUPABASE_* env vars; no Postgres client installed in Dockerfile" — credential-absence by construction).
- **AC.** Node container scaffold with Puppeteer for headless Chrome PDF rendering; Dockerfile excludes Postgres client + `SUPABASE_*` env vars; reads JWT-bearer-token + composes HTML from JSON payload at `/internal/pdf-render` (A5); deploys on cax21 alongside `pfin_back_etl` per `reference_hetzner_cax21`. Coolify→Discord notification per `reference_coolify_discord_notifications` incumbent. NO database access by design (Lock 13 mod #2). Note: TenantBoundConnection fence (Wave 1 E2) applies to `pfin_back_etl` Python; PDF worker has NO DB connection so no TBC fence needed (per Architect Wave 6 v2 brief-drift catch #1).
- **Dependencies.** Upstream: cax21 incumbent + Wave 1 E2 (CI fence on pfin_back_etl, distinct). Downstream: A5 endpoint, A6 RT-22 audit fence, PM #8 PDF export.

**A5. V1 app `/internal/pdf-render` endpoint + RT-21 JWT verification battery.** [V1-SHIP-BLOCK]
- **Source.** [ADR-011 Decision 17 / Lock 13](DECISIONS.md#adr-011) verbatim 10-mod inventory.
- **AC.** SvelteKit `/api/internal/pdf-render` route accepts JWT-bearer signed by V1 app + JSON payload (composed report); validates JWT signature + tenant binding via `SET LOCAL request.jwt.claims` (Arch-locked binding mechanism per RT-21(e) no-service_role-escalation); invokes A4 PDF worker; returns PDF bytes. RT-21 (a)–(g) full verification battery: (a) JWT signature; (b) nonce replay protection; (c) tenant claim presence; (d) expiry; (e) no service_role escalation; (f) audience check; (g) issuer check. Sec joint-review mandatory on JWT verification battery (RT-21 HIGH).
- **Dependencies.** Upstream: A4. Downstream: PM #8 PDF export.

**A6. RT-22 Dockerfile audit CI fence script (closes catalogued §10 instance #1; deferred from Wave 1).** [V1-SHIP-BLOCK]
- **Source.** [ADR-011 Decision 17 / Decision 4](DECISIONS.md#adr-011) verbatim; RT-22 catalogued §10 instance #1 per Decision 4 catalogued numbered list.
- **AC.** CI script greps A4 PDF worker Dockerfile for forbidden patterns: `SUPABASE_*` env vars (other than `SUPABASE_URL`); Postgres client packages (`postgresql-client`, `libpq-dev`, etc.); explicit Postgres binary `RUN` statements. CI fails build if violation found. Closes RT-22 catalogued §10 instance #1 (Wave 1 E1 closed RT-26 #2 instance; both catalogued instances now have V1 CI automation). Sec joint-review on grep pattern coverage.
- **Dependencies.** Upstream: A4. Downstream: V1 deployability gate.

**A7. monthly_report cron worker on `pfin_back_etl` (Native Coolify cron container per Gate F α; absorbs PM Issue 6 per Gate A).** [V1-SHIP-BLOCK]
- **Source.** [ADR-011 Decision 15 + Decision 17 / Lock 11 + Lock 13](DECISIONS.md#adr-011) verbatim hybrid worker location; F/CTO Gate F Option α native Coolify cron container ratify 2026-06-03.
- **AC.** Native Coolify cron container on cax21 schedules monthly invocation (1st-of-month per PRD §2.6.3 verbatim). Cron container invokes Python worker in `pfin_back_etl` which loops over tenants (via service_role-bounded query for tenant list) + invokes A3 helper per-tenant via SECURITY INVOKER tenant-binding (NOT service_role for report data composition; only for tenant enumeration). Lock 11 mod #4 cron tenant-binding pattern; Lock 13 mod #4 audit log. Coolify→Discord on completion or error. `p_data_as_of` server-derived from cron context (1st-of-month - 1 day = end-of-prior-month) per Lock 15 server-derived-only fence. Sec joint-review on cron tenant-binding + service_role isolation.
- **Dependencies.** Upstream: A3, A4 (worker location parallel), `pfin_back_etl` incumbent infra. Downstream: PM #2 in-app rendering shows cron-generated reports; PM #7 on-demand UI.

**A8. `pfin.owner_identification` settings table (closes Lock 14 5/5 + Settings ramp 4/4).**
- **Source.** [ADR-011 Decision 18 / Lock 14](DECISIONS.md#adr-011) verbatim "four per-domain tables" — last originally-committed member; [ADR-013 P5](DECISIONS.md#adr-013) Settings 4th-of-four occupant.
- **AC.** Schema: `users_id` UUID + `owner_id_header_text` TEXT + `created_at` + `updated_at`; UNIQUE(users_id) per Lock 14 per-domain-table pattern. Lock 14 family closes at 5 named tables (planning_target + cashflow_target + tax_bracket_schedule + tax_bracket_row + owner_identification). RLS WITH CHECK `users_id = auth.uid()`. Reuses SELF-232 fn_refresh_updated_at + SELF-233 settings write-path hardening shared layer. Sec advisory (not joint-review — single-column user-scoped table with no chain).
- **Dependencies.** Upstream: SELF-232 + SELF-233. Downstream: PM #10 Settings 4th occupant editor; A3 helper reads for §2.6.1 report header.

### §7.2 — PM V1.5 domain + V1.final close (10 items)

**P2. §2.6.1.b Monthly report in-app rendering UI.** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.1](docs/PRD/index.html#story-2-6-1) verbatim composition + section ordering.
- **AC.** SvelteKit page at `/reports/monthly/{target_month}` invokes A3 helper via `+page.server.ts` SSR. Renders 6-section report: Account Holdings + NAV Performance + Asset Allocation + Rebalancing Targets (reads A1 commentary cols) + Cash Flow + Estimated Taxes (per PRD §2.6.1 verbatim ordering). NO inline edit per ADR-013 P5; "Edit commentary" button routes to P4. Live-recompute on upstream surface changes when viewing latest report; historical reports immutable post-final per Lock 11 mod #2.
- **Dependencies.** Upstream: A3 helper. Downstream: V1-SHIP-BLOCK V1.5 close.

**P3. §2.6.2.b Commentary editor UI (4 named text areas per Gate B Option C).** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.2](docs/PRD/index.html#story-2-6-2) verbatim 4-sub-section Rebalancing Targets free-text; ADR-013 INV-1 plain-text-only commentary is security-load-bearing (NO markdown sanitization at V1).
- **AC.** 4 plain text areas (Cash / Bonds / Equity / Alternatives) at `/reports/monthly/{target_month}/commentary`; reads A1 commentary cols; saves via REPLACE-all SERIALIZABLE write semantics (parallel to Lock 14 settings write pattern). Lock 14 V1-SHIP-BLOCK Sec mods applied: Zod `.strict()` + numeric-input adversarial battery (TEXT-input variant: control-char/length/encoding); mass-assignment prevention (users_id from auth.uid()). NO markdown rendering; plain-text-only per INV-1.
- **Dependencies.** Upstream: A1 (commentary cols). Downstream: P4 trigger integration.

**P4. §2.6.2.c Author-before-generate trigger integration.** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.2](docs/PRD/index.html#story-2-6-2) verbatim author-before-generate flow.
- **AC.** Cron worker (A7) checks for commentary presence per-tenant before generating monthly report; if all 4 commentary cols NULL/empty for target_month, skips report generation + sends Coolify→Discord notification "Awaiting commentary for {month}"; if commentary present, proceeds with generation. On-demand UI (P5) blocks "Generate" button if commentary empty + routes user to P3 commentary editor.
- **Dependencies.** Upstream: P3 commentary editor + A7 cron + P5 on-demand UI.

**P5. §2.6.3.b On-demand UI + pending queue.** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.3](docs/PRD/index.html#story-2-6-3) verbatim manual-trigger flow.
- **AC.** Page at `/reports/monthly/generate` lets user trigger on-demand generation outside cron cadence; pending queue shows reports queued/in-flight/done; on-demand invokes A3 helper via `/api/reports/generate` endpoint (NOT cron worker direct invocation per RLS + tenant-binding isolation). Blocks if commentary missing per P4. Reuses Lock 11 INSERT-new-version semantic.
- **Dependencies.** Upstream: A1 + A3 + P4. Downstream: PDF export P6.

**P6. §2.6.3.c PDF export via PDF worker container.** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.3](docs/PRD/index.html#story-2-6-3) verbatim PDF output format; [ADR-011 Decision 17 / Lock 13](DECISIONS.md#adr-011).
- **AC.** "Download PDF" button on P2 in-app render page invokes A5 endpoint with composed JSON payload + JWT-bearer; receives PDF bytes; serves as download. Filename pattern: `mosko-monthly-{target_month}-{generated_at}.pdf`. Layout matches in-app render (P2) via shared HTML template.
- **Dependencies.** Upstream: A4 + A5 + P2 in-app UI. Downstream: V1-SHIP-BLOCK V1.5 close.

**P7. §2.6.4.b Owner-identification Settings editor (4th-of-four occupant ramp closes).** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.1](docs/PRD/index.html#story-2-6-1) header owner-id; [ADR-013 P5](DECISIONS.md#adr-013) Settings 4th-of-four occupant.
- **AC.** SvelteKit Settings route at `/settings/owner-id`; single TEXT input for `owner_id_header_text` (e.g., "The Mosko Household"); replace-all SERIALIZABLE write to A8 table via Lock 14 V1-SHIP-BLOCK Sec mods (Zod `.strict()` + adversarial battery + mass-assignment prevention via SELF-233 hardening shared layer). Closes Settings area ramp at 4/4 (SELF-242 V1.2 + SELF-252 V1.3 + SELF-265 V1.4 + this V1.5).
- **Dependencies.** Upstream: A8 table + SELF-233 + SELF-242 shell. Downstream: A3 helper reads for report header.

**P8. §2.6.5 Staleness markers on §2.6 surfaces (SELF-208/229/243/258 framework extension).** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.5](docs/PRD/index.html#story-2-6-5) verbatim + [PRD §2.4.4](docs/PRD/index.html#story-2-4-4) verbatim staleness-ramp list naming §2.6 monthly report.
- **AC.** Generate-with-markers per α′-1 PRD §2.4.4 verbatim: when stale-Plaid-item constituents present at generation time, report renders with `<StaleConstituentBadge>` adjacent to affected section headers + per-row stale icon. Cron worker (A7) generates report regardless of staleness (NOT block); badges communicate degraded confidence. Extends SELF-208 (V1.0 framework) + SELF-229 (V1.1 NW) + SELF-243 (V1.2 §2.2) + SELF-258 (V1.3 §2.3) per ADR-013 D1.
- **Dependencies.** Upstream: SELF-208 + A3 helper. Downstream: V1-SHIP-BLOCK V1.5 close.

**P9. §2.5.x Staleness ramp to §2.5 surfaces (Wave 5 Gate D absorbed).** [V1-SHIP-BLOCK]
- **Source.** F/CTO Wave 5 Gate D ratify (defer §2.5 staleness ramp to V1.5 monthly-report close); ADR-013 D1 illustrative-not-exhaustive.
- **AC.** Extends SELF-208/229/243/258 framework to §2.5.1 / §2.5.3 / §2.5.4 surfaces (3-col tax decomposition table at SELF-264; quarterly tax tables at SELF-266; NAV composition Tax Liab rows at SELF-268). Per-row stale-constituent icon + section-header badge consuming `pfin.fn_aggregation_has_stale_constituent()` primitive.
- **Dependencies.** Upstream: SELF-208 framework + SELF-264 + SELF-266 + SELF-268. Downstream: V1-SHIP-BLOCK V1.5 close (paired with P8).

**P10. §2.6.6 RLS verification battery (V1.5 close-gate; tri-axis tenant × scope × tax_treatment + snapshot derivative-surface).** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.6.6](docs/PRD/index.html#story-2-6-6) verbatim "Monthly report is the user's, not anyone else's"; mirrors SELF-209/228/244/257/269 pattern.
- **AC.** RLS test battery covers A1+A2+A3+A5+A7+A8+P3 backend surfaces: cross-tenant injection rejected; SECURITY INVOKER A3 helper cross-tenant leak analysis; Lock 12 snapshot derivative-surface Sec annotation (NOT new SD class; snapshots of §2.5-grade values); A7 cron tenant-binding isolation; A5 PDF endpoint JWT-bearer tenant-binding; owner-id A8 cross-tenant; commentary P3 cross-tenant. Tri-axis orthogonality (tenant × scope × tax_treatment) verified per PRD §2.6.6 verbatim. V1.5 close-gate — no V1.5 issue closes until battery passes. Sec review pass + verdict recorded per SELF-269 precedent.
- **Dependencies.** Upstream: All Wave 6 issues. **V1.5 close-gate.**

**P11. V1.final §3.4 close-gate verification protocol (calendar-gated N=2 consecutive months).**
- **Source.** [PRD §3.4](docs/PRD/index.html#sec-3) verbatim (a)/(b)/(c) all-pass; [docs/MILESTONE-FRAMING.md §8.3](docs/MILESTONE-FRAMING.md) routing flag (d) handoff anchor.
- **AC.** Verification-protocol issue spanning N=2 consecutive months (structurally different from implementation issues per F/CTO Gate D ratify single-issue framing). Three sub-criteria: (a) PRD trace exit criterion = all 32 stories have ≥1 issue closed in Linear (post-rotation); (b) ARCH §10 SD+RT mapping verified post-implementation; (c) N=2 consecutive months of monthly report generation + commentary authoring + RLS clean. Closes V1 ship-gate per §8.3 routing flag (d) + drop-replace termination per §8.2. Decompose into (a)/(b)/(c)-month-1/(c)-month-2/V1.final-close-PR sub-issues at Linear promotion time IF F/CTO prefers finer granularity (per Gate D framing).
- **Dependencies.** Upstream: ALL V1 issues (V1.0+V1.1+V1.2+V1.3+V1.4+V1.5) + 2 calendar months of operation. **V1.final close-gate; final V1 issue.**

### §7.3 — GL-substrate stream (later V1.x; 2 items)

*Later-V1.x GL-substrate work decomposed out of the V1.0 §2.4 manual-entry track per [ADR-033](DECISIONS.md#adr-033) — GL-engine edits (net-new `fn_gl_entries` contra branches) that are a distinct work-class from the manual-entry INSERT-path slices. Milestone target (M4-GL-adjacent later V1.x) confirmed by F/CTO 2026-07-26; the exact product-V1.x label is pending roadmap sequencing with Architect. Promotes to Linear at milestone-rotation when the GL-substrate milestone becomes current + next.*

**G1. Manual in-kind transfer entry (`group_type='transfer_in_kind'` + net-new in-kind clearing contra).** [V1-SHIP-BLOCK]
- **Source.** [PRD §2.4.3.b](docs/PRD/index.html#story-2-4-3) (AcctSetup non-cash event entry — transfer-in-kind subtype) + [ADR-033 Decision 4](DECISIONS.md#adr-033) (transfer-in-kind deferral to its own SELF issue).
- **AC.** In-kind transfer entry modeled as journal `group_type='transfer_in_kind'` (`033`) over two `standard` security legs (out-leg + in-leg): (1) capture source account + destination account + position + cost-basis carry-over preserving original basis; (2) two `standard` legs — P1 cash branch skips (no cash), P2 emits a `trade_position` leg on each side; (3) **author the net-new in-kind clearing contra** in `fn_gl_entries` so the two legs offset (in-kind legs currently have no clearing contra → they park in Suspense, unlike cash transfers which get P3's Journal-Clearing contra via the `Transfer + journal_id` branch); (4) cost-basis carry-over + multi-lot reconcile; (5) per-security `Σ(quantity)=0` conservation enforced at close by `fn_journal_close_balance` (`037`); (6) **Sec-joint-review-mandatory** (GL/money-flow edit — a `fn_gl_entries` contra-branch change); (7) QA two-tenant pgTAP battery on the new contra branch + the conservation law.
- **Dependencies.** Upstream: [ADR-033](DECISIONS.md#adr-033) Decision 4 + GL machinery (`033` `journal.group_type` + `035`/`037` `fn_gl_entries` + `037` `fn_journal_close_balance` + `019` holdings). **Blocked-by:** the net-new in-kind clearing contra must be authored (net-new `fn_gl_entries` work; not present in the current engine). Evaluate for a Decision-3 fence at its migration (per ADR-033 Consequences). Sec joint-review gate. Downstream: completes the §2.4.3.b transfer-in-kind capability deferred from SELF-203.

**G2. Per-lot stock-split precision (lot-linkage substrate + per-lot `fn_create_stock_split`).** [refinement]
- **Source.** [ADR-033 Decision 2](DECISIONS.md#adr-033) + SELF-203 OWD-1. F/CTO chose per-lot (2026-07-26); the Architect feasibility pass established per-lot as a **substrate project, not a `039` line item**, so SELF-203 shipped POSITION-LEVEL (v1.114) and per-lot is deferred here.
- **AC.** Per-lot stock-split recording — one book-neutral `corp_action` row per open buy lot (vs SELF-203's position-level single row): (1) a lot-linkage self-FK on `account_trans` (e.g. `lot_parent_trans_id`) — a **NEW Decision-3 cross-tenant FK-bypass instance** (matched-tenant + matched-security fence, Sec-joint-review-mandatory) on the immutable ledger; (2) an authoritative DB open-lot-as-of read (`fn_open_lots_as_of`) — **depends on `lot_match` (036) matching inference being complete/authoritative** (currently Backend/worker-inferred, non-authoritative at write time — a DB open-lot read miscomputes until matching has run); (3) per-lot `fn_create_stock_split` computing per-lot deltas; (4) reverse-and-replace the SELF-203 position-level rows to per-lot (bounded — splits are rare). Sec-joint-review-mandatory.
- **Dependencies.** **Blocked-by:** authoritative lot-matching substrate (`lot_match` completeness — not built) + the lot-linkage FK. Architect feasibility memo: `temp/039-stock-split-design.md`. Note: per-lot tax-character precision is itself a deferred tax-layer refinement (`037` Option A), so the book GL loses nothing under position-level today. Downstream: upgrades SELF-203 position-level splits to per-lot precision.

### §7.4 — V1.6 reconciliation / statement tie-out stream (1 item)

*Read-only reconciliation work re-homed from the retired V1.0 §2.4.3 per-transaction reconcile mechanism per [ADR-035](DECISIONS.md#adr-035). Distinct work-class from §7.3 GL-engine edits: this is a read-only DETECTOR over existing SECURITY INVOKER helpers + service_role-populated checkpoints — it authors no `fn_gl_entries` contra branch and writes nothing to the immutable ledger. Homed in the dedicated **V1.6 — Reconciliation / statement tie-out** milestone (F/CTO-approved 2026-07-27; Platform/Cross-cutting project). The comparand substrate it reads is already shipped (see R1 Dependencies), so this is a standalone read-only feature over landed objects — not a pending substrate wave. **SELF-205 retains its ID, re-scoped to this entry.***

**R1. Period statement-vs-GL reconciliation control tie-out (read-only detector).**
- **Source.** [ADR-035](DECISIONS.md#adr-035) (supersedes SELF-205's per-transaction `reconciled_flag` / B1 / B2 mechanism; successor = statement control tie-out) + [PRD §2.4.3](docs/PRD/index.html#story-2-4-3) verbatim reconciliation prose.
- **AC.** Read-only DETECTOR, no auto-action: per reporting-period × account, compare the **GL-derived** as-of balance (cash, via `pfin.fn_compute_nav` cash-leg) and holdings (via `pfin.fn_holdings_as_of`) against the **statement-snapshot** comparands (`pfin.account_balance_checkpoint` cash snapshot + `pfin.holdings_checkpoint` per-asset snapshot + `pfin.reconciliation_event.statement_balance` / `.statement_quantity`); surface each mismatch (balance and per-position quantity) for the user to investigate. **No `reconciled_flag` / `reconciled_at` column, no B1 per-transaction trigger, no B2 bulk "reconcile mode"** — the reconciled verdict is an append-only `reconciliation_event` / `reconciliation_event_trans` row (`005`), not mutable row-state on the immutable `account_trans` ledger. Precise terminology (binding per ADR-035): this is a **statement-vs-GL account reconciliation control tie-out at period boundaries** — NOT a bare internal "trial balance check" (near-vacuous, since the double-entry GL already balances by construction at close via `fn_journal_close_balance` `037`). **Auto-reconcile on balance-match (B3) stays V2+** (per §5 auto-reconcile deferral) — the detector flags; it never auto-clears. Detection-only, consistent with the SELF-204 sync-dedup gate ([ADR-034](DECISIONS.md#adr-034)); SELF-204 DETECTS / SELF-205 INTERPRETS boundary preserved.
- **Dependencies.** Upstream: the comparand substrate — **already shipped** under Platform/Cross-cutting: `pfin.fn_compute_nav` / `pfin.fn_holdings_as_of` (`019` / SELF-277, SECURITY INVOKER, `set search_path=''`, Lock 11 read-composition), `pfin.account_balance_checkpoint` (`018` / SELF-276), `pfin.holdings_checkpoint` + `pfin.reconciliation_event.statement_balance` / `.statement_quantity` (`005` / SELF-188). The tie-out is therefore a standalone read-only feature over landed objects, not a pending substrate wave. **Build-time caveat:** verify whether the `005`-deferred `holdings_checkpoint` fan-out INSERT writer (the per-asset checkpoint populator) is fully covered by `018` or still outstanding — the holdings side of the tie-out needs populated checkpoints to compare against; if the writer is a gap, it carries its own migration + QA battery + Sec joint-review. **Sec joint-review + QA two-tenant pgTAP battery travel with this build** (read over the reconciliation substrate; not waived per ADR-035 Consequences). Downstream: closes the §2.4.3 external-agreement reconciliation commitment. **Re-scopes SELF-205** (retains ID; re-milestoned **V1.6 — Reconciliation / statement tie-out**; AC rewritten to this read-only-detector shape per ADR-035).

### §7.5 — Onboarding / provisioning (V1 cross-cutting; 1 item)

*Promoted into V1 per F/CTO ruling 2026-07-28 (**V1 signup is OPEN**, not gated to the F/CTO). Default taxonomy provisioning (**both `asset` + `cashflow` domains**) is a V1 deliverable so any open-signup user reaches a populated classify picker (and populated §2.4.3 manual-entry Sub-Cat dropdowns); without it the SELF-200 held-symbol classifier dead-ends for every user but the F/CTO (whose taxonomy is bootstrap-seeded). This **resolves the latent multi-user taxonomy gap in V1** — it is no longer a V2 prerequisite (see §5.7 note). Provisioning **mechanism** is Architect-owned, next cycle.*

**O1. Default taxonomy provisioning on signup (asset + cash-flow domains).** [V1-SHIP-BLOCK]
- **Source.** F/CTO ruling 2026-07-28 (open V1 signup + promote default-asset-taxonomy seeding into V1); surfaced by the SELF-200 UX-pass empty-picker gap; AC anchor = [PRD §2.2.1](docs/PRD/index.html#story-2-2-1) / [ADR-004](DECISIONS.md#adr-004) Decision C canonical asset taxonomy.
- **AC.** Every user is provisioned the canonical default `user_taxonomy` for **both domains — `asset` (§2.2.1) and `cashflow` (§2.3.1)** — on signup / first access in a **single provisioning pass** (don't provision twice). Asset domain = the 6-Cat × ~35-Sub-Cat grammar per PRD §2.2.1 / ADR-004 Decision C (Cash / Bonds / Equity / Alternatives / Liabilities / Real Estate top-level Cats + their ~35 Sub-Cats), seeding `tax_relevant=false / tax_character=NULL` on the asset side (matches the existing seed's V1-dormant asset tax-character decision); cash-flow domain = the §2.3.1 Income / Expenses / OtherCF / AcctSetup grammar per ADR-004 Decision C. **Demonstrable AC anchor stays the asset domain** the SELF-200 classify picker needs (the surface under test); the cash-flow domain is provisioned in the same pass so the §2.4.3 manual-entry Sub-Cat dropdowns are populated too. Both domains' canonical content currently lives **only** in the gitignored dev-only `seed.sql` (asset ← `pfin.asset_cat`; cash-flow ← `pfin.trans_cat`); it needs a **canonical, non-gitignored home** to provision from. **Open design point — Architect-owned, next cycle (NOT decided here):** the provisioning mechanism must respect the ratified *"no on-signup DEFINER trigger"* posture (024 `user_settings` D3 precedent). Candidates for the Architect to weigh: (a) **lazy app-upsert** on first taxonomy access (parallels `user_settings.mfa_policy` provisioning), or (b) a **migration-seeded canonical default-taxonomy template** + a controlled per-user provision step — either mechanism provisions **both domains in one pass.**
- **Dependencies.** Upstream: a canonical (non-gitignored) source for the default asset **+ cash-flow** taxonomy (both domains; migration-embedded template or equivalent — the content exists in `seed.sql` today but not in a provisionable home); `009_user_taxonomy` (target table — its V1-WRITE-DORMANT `authenticated` SELECT-only grant + the "no on-signup DEFINER trigger" 024 posture constrain the mechanism). **Architect design pass (mechanism selection) is the immediate next step** (post-SELF-200 merge, per team-lead scheduling). Sec joint-review if the chosen mechanism touches a privileged provisioning path (any DEFINER or service_role seed step). Downstream: unblocks open-signup users at the SELF-200 classify surface; resolves the §5.7 latent multi-user taxonomy gap.

### §7.6 — SELF-214 Sec joint-review follow-ups (10 items)

*Follow-ups raised at the SELF-214 (`nav_daily`) Sec joint-review, 2026-08-02, staged here per [ADR-017](DECISIONS.md#adr-017) Decision 2 (§7 is the repo-versioned home for V1 work not currently in Linear). **S1–S6 are in Sec's priority ranking.** The working specs live in `temp/` (gitignored — which is why these entries exist).*

*Two framing notes. **(1) The Linear free-issue limit is a BOOKING constraint, not a CAPTURE constraint.** `save_issue` currently fails workspace-wide ("You've exceeded the free issue limit"), and resolving it is an unresolved F/CTO cost decision — but §7 is the ratified home for exactly this condition and needs no Linear seat, so **nothing below is blocked on that decision.** Promote at the first milestone-rotation after the limit lifts. **No live Linear IDs are cited** — a merged PR referencing one auto-closes it. **(2) S7 was in the original four follow-ups and is absent from Sec's ranked six; its omission is an artifact of ranking scope, NOT a de-prioritization** — [ADR-040](DECISIONS.md#adr-040) canonically records it as "EXPLICITLY DEFERRED, not satisfied, not waived" and states a tracked follow-up is required. It is retained below on that basis. **(3) Cross-reference into §7.7:** the close-gate joint review (§7.7, 2026-08-03) filed **F2**, a live `nav_daily` tenant-enumeration defect that **feeds S7's AC** — read the two together; S7's forensic-gap argument narrows once F2 lands.*

**S1. pgTAP assertion that `authenticated` holds NO table-level SELECT on `pfin.plaid_items` / `pfin.linked_source` (converts ADR-023 C3 into an enforced invariant).** [highest value per Sec]
- **Source.** SELF-214 Sec joint-review C3 ruling. Correcting the false REVOKE prose (S2/S3) **withdraws** a false promise but does **not** discharge [ADR-023](DECISIONS.md#adr-023) C3 going forward — it would leave the actual control as "review discipline," the procedural-over-structural trade Sec ruled against earlier in this same review. Sec: *"This is the item worth booking, more than the prose fix."*
- **AC.** A pgTAP assertion in the two-tenant battery that the `authenticated` grant on `pfin.plaid_items` and `pfin.linked_source` **remains column-scoped**, and that the credential-handle columns (`access_token_secret_id`, `credential_secret_id`) stay **absent from the column list**. Fails in CI the instant anyone adds a broad table-level grant — precisely what the comment falsely promised the `REVOKE` would do. Cheap, non-vacuous, and it makes the widening structurally detectable instead of review-dependent. Pairs with the ADR-023 C6 standing condition (every `pfin` relation is internet-facing once granted; the §4.5 two-tenant battery is exposure-gating).
- **Dependencies.** Upstream: the existing two-tenant pgTAP battery; `007` / `015` grants as the asserted baseline (verified present + column-scoped, 2026-08-02). Owner: **QA**. Sec advisory on assertion wording. Downstream: converts C3 from an assertion into an enforced invariant; **is the structural half of S2/S3, which are only the prose half.**

**S2. Fold the ADR-023 C3 prose correction into [ADR-023](DECISIONS.md#adr-023) (disposition (c)).** [refinement]
- **Source.** SELF-214 Sec joint-review; `temp/followup-column-revoke-false-rationale.md`. `008:104–105` states *"Belt-and-suspenders re-assertion so no later broad grant can silently re-expose the handle"* — written as the discharge of Sec condition **C3**, so a Sec-load-bearing condition is documented as held by a mechanism that does not hold it. Postgres has no negative grants: a column `REVOKE` neither blocks a later table-level `GRANT` nor claws one back (both directions measured 2026-08-02 in a rolled-back transaction). **Ownership verified, not assumed:** `008`'s block is headed *"SEC CONDITIONS (Option A ratify, SELF-196)"* and cites *"Sec C6 (ADR-023)"*, so **ADR-023 owns C1–C6 including C3.** **"C3" is overloaded in this codebase** — [ADR-029](DECISIONS.md#adr-029) has a C3 (the aal2 backstop obligation, which `054` cites) and there is an admission-worker C3 — so this entry names **ADR-023 C3** unambiguously; a correction filed against the wrong C3 would be its own drift.
- **AC.** Record the correction in **ADR-023**, where a future reader of C3 actually looks. **Disposition (c); (b) is dead on structural grounds, not merely "confusing"** — the false claims live in `--` line comments inside `.sql` files, which are **not database objects**, so no follow-up migration can alter them, and a `comment on column` would add catalog text the engineer about to add a grant never reads. **(b) cannot reach the defect at all.** Note in the correction that C3's own wording already names `007`'s column-scoped SELECT as the mechanism with `008` merely *"re-asserting"* — so C3's substance was never the REVOKE.
- **Dependencies.** Upstream: none (`008` is merged; out of scope for the SELF-214 PR). Owner: **Architect** (`DECISIONS.md`). Sec ruled; F/CTO ratifies. Not a one-way door (prose). Downstream: **S1 is what actually discharges C3 forward** — this entry only withdraws the false promise.

**S3. INVERT the `007` / `015` column-`REVOKE` comments from reassurance to warning.** [refinement]
- **Source.** SELF-214 Sec joint-review, Sec's direct verification of both files. **There is NO live exposure — the entry says so plainly so nobody triages this as an incident.** `007` grants `select (item_id, users_id, plaid_item_id, …) on pfin.plaid_items to authenticated` with `access_token_secret_id` **not in the list**; `015` has the same shape on `linked_source` with `credential_secret_id` **not in the list**; and Sec checked the baseline rather than assuming — **no table-level grant to `authenticated` exists on either table** (independently re-verified 2026-08-02). The handles are genuinely unreachable today. **The real protection is the omission from the column list on the lines immediately above — not the REVOKE.** **Severity, per Sec's ruling:** in `054` this was a *new* no-op line with a false justification — a false fence standing alone. In `007`/`015`/`008` it is a **redundant line with a misleading comment, adjacent to a fence that actually works**: worse context (Vault credential handles), smaller defect. **Sec ruled it a NOTE** — follow-up, not urgent, correctly out of scope for this PR.
- **AC.** **Invert the comment; do not merely correct it.** The hazard Sec identified is not that the prose is inaccurate — it is that *"ensure the secret handle is not reachable via a broad grant"* invites a future engineer to conclude **it is therefore safe to add a broad grant**, because the REVOKE will catch it. It will not. **The comment could license the exact action it appears to defend against**, on a Vault-credential surface. Sec's suggested replacement, which is the AC: `-- The handle's protection is its OMISSION from the column list above. A table-level GRANT on this table WOULD expose it — column REVOKE cannot subtract from a table-level grant (Postgres has no negative grants). Any table-level grant here is Sec joint-review-mandatory.` Same treatment as `nav_value` in `054`: drop the executable no-op, keep the intent as a **warning rather than a reassurance**. **An entry that only said "correct the comment" would lose the point.** **SCOPING GUARD — this constrains the deliverable; carry it into any wider sweep this seeds, or the sweep will do damage.** Correct **forward-looking claims** (assertions about the future, e.g. *"no later broad grant can silently re-expose the handle"*): these were **never true** and decay into active hazards. **Leave point-in-time statements alone** (a ledger count at authorship, e.g. `008:50`'s *"ledger stays at 2 (RT-22 + RT-26)"* — **stale but NOT wrong**, accurate when `008` landed, before the 2→3 flip at SELF-212): these are the historical record of a merged migration and are **not defects**. Without this distinction a well-meaning sweep starts rewriting merged migrations' history and we lose the ability to read what was known when — a real risk given how many stale-but-correct counts exist across `001`–`053`.
- **Dependencies.** Upstream: none (both merged). Owner: **Architect** (`supabase/migrations/**`). Routing: Sec (both surfaces are Vault-credential handling). Downstream: the AC's scoping guard governs any wider comment sweep this seeds.

**S4. Pre-push hook fencing untracked / stale files under `supabase/` and `workers/`.** [refinement]
- **Source.** SELF-214 Sec joint-review; `temp/self214-devops-followup-prepush-untracked-fence.md`. **CI structurally cannot catch this class:** an untracked file is not in the pushed tree, so a green check certifies the tree CI was given, not the tree that was intended. No amount of added CI closes it — the fence must be local, because its input set is the working tree *including what git has not been told about*. **Three instances in one review**, which makes it a class, not a coincidence: `055_pfin_etl_role.sql` fully untracked (`??`) while CI ran fully green — a merge would have shipped a deployment runbook linking to a migration that did not exist, and an ETL with no database login identity; the paired QA RLS battery staged-then-modified (`AM`) — a commit would have shipped an older battery than QA verified; the deployment runbook `MM`. Related standing memory: `feedback_verify_paired_artifacts_before_push`.
- **AC.** A pre-push hook (Husky, alongside the existing locked lint + test + type-check + svelte-check + ruff + hadolint set) failing closed on offending paths from `git status --porcelain --untracked-files=all`. Per DevOps decision rules a new fence is a present-options decision and needs a catch criterion **and** a test proving it catches. **Design questions to settle at implementation (record; do not pre-decide):** (i) **must `AM`/`MM` fail as well as `??`** — the QA-battery case was `AM`, so untracked-only would have caught one of three; this is the question that decides coverage; (ii) scope — `supabase/` + `workers/` only, or whole-tree with an ignore list; (iii) an escape hatch for deliberate scratch files that does not make the fence trivially bypassable; (iv) a **paired test proving the fence fails on a real violation** — a fence whose pass condition can be satisfied without the discipline holding is theater.
- **Dependencies.** Upstream: the existing Husky pre-push set. Owner: **DevOps**. Intended project Platform / Cross-cutting; intended labels `role:devops` + `surface:ci`, **neither of which exists in the workspace yet**. Non-blocking for the SELF-214 PR. Search-before-create done, no duplicate. Downstream: closes a merge-integrity class CI cannot reach; **S8 is its procedural complement, not a substitute.**

**S5. Dedicated `pfin_provider_sync` login role for `workers/provider-sync` ([ADR-019](DECISIONS.md#adr-019) Condition C2).** [PHASE-7 DEPLOY GATE]
- **Source.** [ADR-041](DECISIONS.md#adr-041) sub-decisions verbatim. SELF-214 gave the ETL a dedicated `pfin_etl` NOINHERIT login role (option (B) ratified over sharing `authenticator`). Two sub-decisions follow: **(i) naming convention `pfin_<workers/ subdirectory>`, effective now** — `workers/provider-sync` → **`pfin_provider_sync`**, which **supersedes the `pfin_worker` name** in ADR-019 C2 (a generic name on the second member of a growing family is how naming drifts; the convention is mechanical and needs no future adjudication). C2's *substance* — a dedicated NOINHERIT role, Sec-joint-review-mandatory on adoption — is **unchanged**; only the name is pinned, and the rename rides C2's existing Sec gate. **(ii) C2 is PROMOTED from "V1.x hardening" to a Phase-7 deploy gate** — an **Architect call on Sec's recommendation**: B8's argument applies **more** strongly to provider-sync than to the ETL it was won for, since provider-sync holds `PLAID_SECRET`, decrypts SD-03 access tokens, and writes the tenant-scoped `account`/`account_trans` surface — the higher-value target, and the one still sharing `authenticator` with PostgREST. Leaving it on the shared credential after ratifying (B) for the lower-value container is not a defensible end-state.
- **AC.** `pfin_provider_sync` created as a NOINHERIT login role (member of `authenticated` + `service_role` only; drops `authenticator`'s inert `anon` membership) and set as provider-sync's `PFIN_DB_USER`, with its password provisioned in the **same Phase-7 deploy pass that switches on `pfin_etl`** — the cluster reaches a consistent role-graph in one operation rather than carrying a half-applied convention across releases. `pfin_etl` and `pfin_provider_sync` stay **deliberately separate, not converged** — collapsing them reintroduces the shared-credential blast radius and loss of independent revocation that option (A) was rejected for. **Sec joint-review-mandatory** (ADR-019 C2's condition, unchanged). Discharges the remainder of **[ADR-023](DECISIONS.md#adr-023) condition C1**: C1's rotation coupling narrowed from three consumers to two at ADR-041 (the ETL is out; PostgREST + provider-sync still bound), and **C1 is fully discharged only when `pfin_provider_sync` replaces `authenticator` in provider-sync's `PFIN_DB_USER`**.
- **Dependencies.** Upstream: `pfin_etl` (`055`) as the shipped precedent + the Phase-7 deploy pass where the credential is first provisioned (per [ADR-021](DECISIONS.md#adr-021) greenfield, it does not exist until deploy). **Explicitly NOT SELF-214 scope.** **Blocked-by:** Phase 7 arrival. Owners: Architect (role migration), DevOps (`secrets-manifest.yml` + `.env.example` + runbook), Sec (joint-review gate).

**S6. V1-scope ruling on the incumbent FMP financial-statement tables (PM + Architect; F/CTO decides).** [scope question — not a defect]
- **Source.** SELF-214 Sec joint-review. Eight `pfin` tables are referenced by `workers/etl/src/pfin_back_etl/core.py` but created by **no V1 migration** — independently verified 2026-08-02 (`create table` grep across `supabase/migrations/`: zero hits for all eight). Incumbent code carried in at the [ADR-019](DECISIONS.md#adr-019) monorepo fold and never reconciled; the path is dead against V1 under any credential and fails loud (`UndefinedTable`), so there is **no security consequence and it blocks nothing**. **THIS IS NOT A GRANTS TASK** — Sec and Backend both initially scoped it as one, and **you cannot `GRANT` on a table that does not exist**. It is a product-scope question, which is why it routes to PM rather than Architect. **PM finding — the eight are NOT homogeneous, and splitting them collapses most of the question:**
  - **Class A — already superseded in V1; the scope question is CLOSED and only dead code remains (2 tables).** `pfin.cpi` → superseded by **`pfin.cpi_u_index`** (`053`, SELF-230), whose own header says it reuses "the proven incumbent BLS CPI fetch logic." `pfin.asset_cat` → superseded by **`pfin.user_taxonomy`** (`009`/`010`) + **`pfin.user_asset_category`** (`022`); `010` explicitly restores the incumbent `asset_cat` `notes` content. For these two the V1 answer already exists — the residue is a **dead-code retirement in `core.py`**, not a product decision.
  - **Class B — the genuine open question (6 tables).** `equity_profile`, `reporting_period`, `income_statement`, `balance_sheet_statement`, `cash_flow_statement`, `earning` — FMP **company-fundamentals** data.
- **AC.** An F/CTO ruling on Class B, framed as **V1 / later-V1.x / V2+ / never**, recorded as an ADR. **PM position (to be argued, not assumed):** **none of the 32 locked [PRD §2](docs/PRD/index.html#sec-2) V1 stories requires company fundamentals** — V1 is net worth, allocation, cash flow, estimated taxes, and the monthly report; fundamentals serve *security analysis*, which sits on the advisor/fiduciary axis that [PRD §6.3](docs/PRD/index.html#sec-6-3) / [ADR-007](DECISIONS.md#adr-007) made a **permanent non-goal** for TLH recommendations. That is a strong prior for **V2+ or never**, but it is **not the same non-goal** and must not be waved through as one — fundamentals could serve a read-only display use with no advice attached. **This is an incumbent-exceeds-V1 decision and must NOT be auto-accepted via "selective adoption"** (memory `feedback_incumbent_exceeds_v1_review`): it gets a P-flag with A/B/C options + an ADR. Class A needs no ruling — only a retirement issue.
- **Dependencies.** Upstream: [ADR-019](DECISIONS.md#adr-019) (the monorepo fold that carried the code in without reconciling it). Owners: **PM** (frames the V1/V2/never options + the PRD §2 trace), **Architect** (migration cost if any class lands in V1; `core.py` retirement shape), **F/CTO** (decides). No Sec gate (no live surface — the path is dead). Downstream: either 6 new tables enter a milestone, or `core.py`'s fundamentals paths are retired and the dead-reference class closes.

**S7. Discharge [ADR-011](DECISIONS.md#adr-011) Decision 1 clause (d) for the W-1 NAV worker (audit-log deferral).**
- **Source.** [ADR-040](DECISIONS.md#adr-040) verbatim — clause (d) is recorded as **EXPLICITLY DEFERRED, not satisfied, not waived** (Sec joint-review B3, recorded at Sec's requirement). [ADR-011](DECISIONS.md#adr-011) Decision 1 commits non-JWT writes to a four-clause discipline and states a new privileged-context surface **MUST** adopt it at design time; it names **"Lock 7 NAV worker"** under *Confirmed reusability*, so this surface is one of the four D1 was generalized from, not merely covered by it. Clauses (a)/(b)/(c) are satisfied at ship; **(d) explicit audit log capturing the tenant-resolution chain is NOT** — `emit_audit_log` raises `NotImplementedError` and the worker emits no audit row. **ADR-040 states a tracked follow-up is required to carry the unblocking work; this entry is that record.** *(Absent from Sec's ranked six — a ranking-scope artifact, not a de-prioritization; see the section preamble.)*
- **AC.** The W-1 worker emits a same-transaction audit row capturing the **tenant-resolution chain**: run identity, which tenants were enumerated, per-tenant success/failure, and the fact of a checkpoint having been **attempted and failed** (today a tenant that errors leaves no durable trace at all). Scope note — `nav_daily` is itself immutable and already records the tenant-resolution *outcome* (`users_id`, `nav_date`, `nav_value`, `created_at`); what is missing is the *chain*, so **risk is forensic, not exploitative** — which is why this is a recorded deferral rather than a ship-block, and must not be restated as one. Sec joint-review-mandatory (ADR-011 Decision 2 audit-class).
- **Dependencies.** **Blocked-by:** the `plaid_sync_audit`-class audit table **plus** the general same-transaction audit-log insert helper — the **reserved, already-allowlisted 4th DEFINER slot**, still unauthored (its tracker is named in [ADR-040](DECISIONS.md#adr-040) / [ADR-026](DECISIONS.md#adr-026) as the manual-accounts issue's Task #7; the ID is deliberately not repeated here. **⚠ Verified 2026-08-03 against live Linear: that manual-accounts issue is CLOSED (Done 2026-07-06, PRs #141–144), so its "Task #7" is not a workable tracker** — the audit-log helper was deferred *out* of it at close (see [`MILESTONES.md`](MILESTONES.md) V1.0 close notes) and currently has **no live tracker of its own**. That is not a citation typo: the reference is historically accurate about where the work was scoped, and wrong only about whether that scope is still open. Re-derive or create a tracker when the Linear free-issue limit lifts; until then this §7 entry IS the tracker). **Authoring it moves no ledger** — the SECURITY DEFINER allowlist is committed at 4 (authored 3), and the §10 catalogued-instance ledger is separate and unchanged. Owners: Architect (audit table + DEFINER helper), Backend (`workers/etl` emit path), Sec (joint-review).

**S8. Record the working-tree-vs-`main..HEAD` lesson in the `finish-feature` / `merge-pr` skills.** [process control]
- **Source.** Architect proposal at the SELF-214 review, concurred by team-lead and PM. The pre-push-gate lesson — **compare the working tree, not `main..HEAD`** — is a process control, and a **skill** is where someone encounters it at the moment it matters; an ADR is not read at merge time. Compounds the standing memory `feedback_verify_paired_artifacts_before_push`.
- **AC.** Add the check to the `finish-feature` and `merge-pr` skill bodies: before push/PR/merge, verify the **working tree** (`git status --porcelain --untracked-files=all`), not only `git diff --stat main..HEAD` — an untracked or staged-then-modified file is invisible to the latter, which is exactly how `055` nearly merged omitted. **Explicitly subordinate to S4:** the hook is the structural fence, this is the procedural complement. **It must not be treated as a substitute** — a procedural-over-structural trade is what Sec ruled against elsewhere in this review, and if S4 lands this note becomes genuine belt-and-suspenders rather than the false kind S3 is about.
- **Dependencies.** Upstream: none. Owner: **team-lead / DevOps** (skill definitions). Distinct from S4 (the hook itself). Two-way door.

**S9. Record three battery-design lessons in `supabase/tests/rls/DESIGN.md`.** [process control]
- **Source.** QA's request at the SELF-214 review. Distinct from S8 (different file, different owner, different content): S8 is about *what to check before merging*; this is about *how to build a battery that cannot lie to you*. All three are failure modes where a battery reports success it did not earn.
- **AC.** Record, each with the SELF-214 instance that produced it: **(i) leg-independence** — an inversion suite proves the legs are *independent*, not merely that the fences have teeth; a battery that only asserts "fence fires" cannot distinguish one fence doing all the work from each leg holding on its own. **(ii) shape-agnostic assertions degrade silently** — an assertion written loosely enough to survive a pending decision keeps passing after that decision lands, having quietly stopped testing the thing it was written for; the degradation is invisible because the test still goes green. **(iii) the partial-run guard** — **"nothing failed" and "nothing ran" are indistinguishable unless you count.** A battery must assert its own expected assertion count, or a setup error that skips the suite reads as a clean pass. This one generalizes past RLS and is the strongest of the three.
- **Dependencies.** Upstream: the existing two-tenant pgTAP battery + `supabase/tests/rls/`. Owner: **QA** (`supabase/tests/rls/DESIGN.md`). Two-way door. Downstream: **directly strengthens S1** — the C3 invariant assertion is only worth booking if the battery carrying it cannot silently no-op, which is exactly lesson (iii). Sec generalises the point past S1: it holds for **every assertion-type entry in §7** — *"an invariant assertion's entire value is that it goes RED at the right moment; a harness that can abort past it and still report clean makes it **worse than absent** — absent, nobody is reassured; present-but-skippable, everybody is."* S1 is flagged only because "highest value *if it fires*" is the profile that suffers most from a silent no-op.

**S10. Capability-verify `compute_and_checkpoint_user` end-to-end under a real `pfin_etl` login before Phase-7 scheduling.** [PHASE-7 DEPLOY GATE]
- **Source.** Sec FINAL VERDICT **GREEN, condition 2** (SELF-214). The **B9 defect** — a targeted `ON CONFLICT` failing `42501` on every run under an INSERT-only grant — **existed because nobody had run the assembled statement.** It was invisible to every static review, including Sec's own. **Merging with the assembled path still unrun reproduces the exact condition that produced it.** Instance of the standing discipline `feedback_capability_verify_adr_db_primitives`: ADR/Lock-named DB primitives are **unrun assumptions** until executed under the real production role.
- **AC.** **Sec's ratified wording, verbatim:** *"Before the ETL container is scheduled in Phase 7: execute `compute_and_checkpoint_user` **once, through the Python worker**, **authenticating** as `pfin_etl`, against a real database, and verify a same-day re-run returns `row_count = 0`."* **What is already proven — do not re-litigate:** the SQL *composition*, executed against a live database in a rolled-back transaction — NOINHERIT denies ambient privilege → the impersonated read resolves the correct tenant → the B7 GUC survives teardown → the production statement inserts once and a re-run returns 0. **What is NOT proven, and is this entry's whole point — do not close it early on the strength of the above:** **(i) authentication** as `pfin_etl` — the verification *assumed* the role via `SET ROLE`; production **logs in**, which needs a password and a commit; **(ii) the Python layer** — `impersonate()`, `_check_tenant_statement`, and SQLAlchemy's binding all sit **above** what was executed. **Backend's test suite cannot reach this either: SQLite does not model Postgres grants.** So no existing green signal covers it.
- **Dependencies.** Upstream: the `pfin_etl` credential provisioned per deployment-runbook §6.1 (the `\password` two-step) + `055`. **Not a merge blocker** — nothing is deployed, the migration has never been applied anywhere, the cron is not scheduled — **but it IS a deploy blocker**, and it is better written as one now than discovered as one later. Owners: **DevOps** (credential + Phase-7 sequencing), **Backend** (the worker-path execution), **Sec** (condition-2 closure). Downstream: closes Sec's GREEN condition 2; gates ETL container scheduling.

### §7.7 — close-gate joint-review findings

*Findings raised at the `is_active` close-gate Architect + Sec joint review (8 passes), 2026-08-02/03, staged here per [ADR-017](DECISIONS.md#adr-017) Decision 2. **Specs transcribed from Sec's §16 copy-ready block**, which both agents agreed is the source of record for this filing (in preference to Architect's §21 table). The working docs live in `temp/` (gitignored — **which is why these entries exist**: seven joint-review passes and three live defects existed only in untracked files and a message thread, the exact `feedback_verify_paired_artifacts_before_push` shape).*

*Four framing notes. **(1) Placement — this is a NEW section, deliberately not §7.6.** Architect originally recommended §7.6; Sec corrected it and PM concurs. §7.6's own heading and preamble scope it to "SELF-214 Sec joint-review follow-ups," and most of the entries below are close-gate findings, not SELF-214 follow-ups. Filing close-gate work under a SELF-214 header would be the same **precedent-without-its-surrounding-conditions** shape this review caught five times — the shape *"Land the review checkpoints into the skill files"* exists to fence. The **`nav_daily` tenant-enumeration** entry is the one genuinely `nav_daily`-originated defect; it is homed here for provenance coherence and **cross-referenced from §7.6's preamble**, since it feeds **S7**'s AC. **(2) Cite entries in this section BY TITLE, never by `F#`.** The `F#` prefixes are positional labels in a growing list and are not stable identifiers (the ADR-040 ordinal-label ruling); the same reasoning is why **no entry title carries a count**. **(3) The six entries filed in the original 2026-08-03 batch were drafted against the close-gate packages, BEFORE [ADR-042](DECISIONS.md#adr-042) was ratified later that day** — that ADR retires `pfin.account.is_active` entirely (three-concept model; migrations `056`/`057`/`058`). **The reconciliation pass has run and is COMPLETE** (Architect 2026-08-03, with Sec ruling on the V2-grantee surface): three entries carry a **Disposition** bullet recording the outcome, and **the outcome was not uniform — one was discharged, one INVERTED, one lost its original argument while keeping its subject.** Do not assume a finding survives a ratify just because its subject still exists. **Sec's decomposition, which sharpens this and prevents over-filing:** *discharged* is **not a failure mode at all** (a finding fixed by unrelated work is good news you want to notice so nobody does it twice); *kept-its-subject-lost-its-argument* is **the already-named "requirement outliving its own precondition" variant** — do not double-file it; **only INVERSION is genuinely new** — the words stayed true and the behaviour they describe reversed, and **under V1 both forms are behaviourally indistinguishable, so the flip has no footprint.** Nothing goes red, nothing reads wrong, and it surfaces only when sharing ships. That is what **CP8** exists for. Entries added after that ratify are marked as such. **(4) The Linear free-issue limit is a BOOKING constraint, not a CAPTURE constraint** — §7 is the ratified home per [ADR-017](DECISIONS.md#adr-017) Decision 2 and needs no Linear seat, so nothing below is blocked on that unresolved F/CTO cost decision. Promote at the first milestone-rotation after the limit lifts. **No live Linear IDs are cited** — a merged PR referencing one auto-closes it.*

**F1. `pfin.eod_price` admits zero and negative prices — live NAV defect.** [flag]
- **Source.** Close-gate joint review 2026-08-02 (Architect found the `fx_feed` zero case; Sec sharpened to sign-flip). `019:220` declares `price numeric(20,4) not null` with `constraint eod_price_price_finite check (price <> 'NaN'::numeric)` as its **only** value constraint. **Impact.** A zero `fx_feed` rate zeroes a foreign-currency cash leg in `fn_compute_nav`; a **negative** rate flips its sign, so NAV silently *subtracts* money that exists. `= 0` and `abs(…) < ε` both miss the negative case.
- **AC.** Add `check (source <> 'fx_feed' or price > 0)`. **Source-conditional, not blanket** — a delisted security legitimately prices at `0`, and `future` is in the `asset_type` vocabulary (`016:202`) where negative settlement is real (WTI, April 2020). An asset-type-conditional rule is **not expressible as a CHECK** — `eod_price` carries no `asset_type` column (`019:528` / `019:563` reach it only by joining `pfin.asset`) — which is why the expressible constraint is the one that matters. Backfill review required: existing rows must be checked before the constraint lands, since a `NOT VALID` add would skip exactly the rows at issue. **Named residual (deliberate, Sec-ruled — do not close in V1).** A zero-or-negative **non-fx** price still zeroes or flips a *holdings* contribution and stays admissible. Not closeable declaratively. **A trigger policing a price domain that legitimately includes negatives would be guessing which negatives are real, and a fence that guesses is the class this project keeps removing.**
- **Dependencies.** Ingest-path review (`eod_price` is `service_role`/ETL-written, `019`). Owner: Architect (DDL) + Backend (ingest) + Sec (joint-review). Independent of the close gate — under D3 the gate is immune; this protects NAV.

**F2. `nav_daily` tenant enumeration skips instead of writing zero — live defect. DISCHARGED at the `is_active` drop; AC absorbed as an acceptance criterion of that slice.** [flag — discharged, do not schedule separately]
- **Disposition (Architect, 2026-08-03, post-[ADR-042](DECISIONS.md#adr-042)).** This is **no longer a schedulable backlog item** — it is **forced work inside the blast radius** of the ADR-042 slice that drops `is_active` (**`059`** — the fourth slice. **`058` leaves `is_active` untouched**; `059` asserts reconciliation, drops the assert fn and sync trigger, **drops `is_active`**, and does the as-of re-point. Confirmed by Architect 2026-08-03; [ADR-042](DECISIONS.md#adr-042)'s header and build sequence originally listed **three** slices and were corrected to four in the same pass — the split came from Sec's two-phase adjudication *after* the ADR merged, so any pre-correction citation of `058` as the dropping slice is reading a stale header, not a wrong file.). When the column drops, `nav_daily.py:214` **errors at runtime and the ETL worker breaks**. The forced change and the desired fix **coincide**: the correct re-point is `select distinct users_id from pfin.account` — **dropping the filter entirely rather than re-pointing it to `closed_at`** — which simultaneously fixes the skip-vs-zero defect. **Carry the AC below into that slice's acceptance criteria; do not leave it queued.** **Residual unchanged:** a tenant with **zero account rows** is still absent, since the query reads `pfin.account`; collapsing that case still requires enumerating from a tenant roster and remains **§7.6 S7**'s, not this entry's.
- **Source.** Close-gate joint review 2026-08-02. `workers/etl/src/pfin_back_etl/nav_daily.py:214` — `select distinct users_id from pfin.account where is_active = true`.
- **AC.** Drop the `is_active` filter. **The enumeration decides *whether to write a row* using a filter that only determines *what the value would be*** — a tenant whose true NAV is 0 should get a **0 row, not a gap**. Today a tenant whose accounts are all inactive-but-funded is not enumerated at all and their trajectory silently stops. The docstring (`nav_daily.py:203–206`) is not wrong; it is **written against an invariant that does not yet exist**. **Forensic consequence for S7.** Absence currently conflates **four** cases: (i) tenant has no accounts at all, (ii) all accounts inactive, (iii) cron errored on that tenant, (iv) cron never ran. Dropping the filter collapses (ii), leaving three. **Collapsing (i) additionally requires enumerating from a tenant roster (`auth.users` / `user_settings`) rather than from `pfin.account`** — a larger change, properly S7's call. This compounds with [ADR-040](DECISIONS.md#adr-040)'s B9 ruling, which grants `service_role` the date column *specifically* so gaps are readable, and with its D1(d) deferral (*"a tenant that errors leaves no durable trace at all"*). **Sec posture.** Dropping the filter is **net positive with no new disclosure class** — it enumerates strictly more tenants of the same kind (`users_id` only, already cross-tenant by construction per the docstring's stated exemption) and removes a gap-signal.
- **Dependencies.** Owner: Backend (`workers/etl`) + Sec (joint-review). Reproduces today with no gate in sight; the close gate is a **forcing function, not the cause** — it makes "all accounts closed" an intentional reachable end-state rather than an edge case. Feeds **§7.6 S7**'s AC (cross-referenced from that section's preamble).

**F3. The V2-grantee account-join on `fn_compute_nav` — fail-closed is the BACKSTOP; V2 must make the unresolvable state UNREACHABLE. RE-SCOPED: the finding inverts rather than survives.** [V2 precondition]
- **Disposition (Architect + Sec, 2026-08-03, post-[ADR-042](DECISIONS.md#adr-042); Sec RULED — this is Sec's V2 surface).** The original entry described a **fail-closed under-report**: `050`'s securities leg uses `coalesce(acc2.is_active, false)` over a LEFT JOIN, so a V2 grantee who sees the holdings (`rd_access`-scoped) but not the account row (owner-only) gets `acc2` NULL → `false` → **excluded**. Re-pointed naively to `acc2.closed_at is null`, that same invisible row gives `NULL is null` → **TRUE → included: fail-OPEN, over-reporting.** **In V1 the two are identical**, because the account row always resolves — which is exactly why the flip would be **chosen silently at the re-point** if nobody wrote it down. **Sec ruling: PRESERVE FAIL-CLOSED**, for three reasons, the first deciding. **(1) Fail-closed fails visibly; fail-open fails silently.** A grantee whose NAV is too low notices and complains — *that is how this entry came to exist*. A NAV that is too high because it counted positions from an account we could not see **looks exactly like a correct NAV**. **(2) Absence is not a determinate answer** — `closed_at is null` on an unresolvable row asserts *"not closed"* **on the basis of no information**. **(3) Fail-open's correctness is derivative** — it is safe only while the close gate holds (a closed account is empty, so including it costs nothing): true today, **unnamed as a dependency**, and false against any row the gate never validated.
- **Reframing — Sec's correction, and the load-bearing half. This entry is NOT "decide the V2 behaviour."** Both agents initially framed it that way and both were wrong. **The account row is unresolvable at all only because of a predicate mismatch that is already a separate obligation**: `account_select` is owner-only while the holdings legs use the `rd_access`-JOIN. **The correct V2 fix is to make those predicates agree**, after which the account row always resolves and **the default is never exercised by anyone**. So the entry reads: **fail-closed is a backstop, and V2 must make the unresolvable state unreachable** — *not* "pick a default." **That framing matters because it stops the under-report being recorded as an accepted cost.**
- **Source.** Close-gate joint review 2026-08-02 (Architect). The read predicates differ: `account_select` (`025:201–208`) is **owner-only** (`users_id = auth.uid()`); `fn_holdings_as_of` reads `holdings_checkpoint` (`019:441`) + `account_trans` (`019:450`, `019:460`), both **`rd_access`-JOIN**-scoped (`025:437` / `025:254`).
- **AC.** Reconcile `050`'s `left join pfin.account` against the ACL-scoped holdings legs. Under V2 sharing a non-owner grantee sees the holdings and not the account row → `acc2` NULL → `coalesce(acc2.is_active, false)` → **every shared holding silently drops from their NAV**. **Why V1 is safe and why that expires.** The two predicates coincide **only because `account_users` holds nothing but creator-grant rows**. Dormancy is the guarantee, not the FK.
- **Dependencies — TWO DIFFERENT TIMELINES. Do not collapse them (Sec correction, 2026-08-03).** Owner: Architect + Sec.
  - **SHIPS IN `059`, V1, NOW — not deferrable.** The legible conjunct **`acc2.account_id is not null and acc2.closed_at is null`** goes into the as-of re-point, establishing fail-closed as the backstop, **commented so that removing it visibly flips the default.** **This is a V1 DDL requirement, NOT a V2 obligation, and it is NOT on the B5 gate.** Its *reasoning* references the gate only because that is when its backstop role ends. **Sec's own correction, and the reason it matters: filed as a third V2 obligation this reads as deferrable to the sharing PR — and deferring it means `059` ships without the conjunct, which is the exact silent flip the ruling exists to prevent. A misfiled timeline would have defeated the finding.**
  - **B5 hard-gate — V2, same PR** (`003:152` already carries the trigger: *"MUST land in the SAME PR that first grants any authenticated write here"*). Two obligations: (i) **`rd_access` in the WITHHELD column set** (the entry below); (ii) **reconcile the `account_select`-vs-`rd_access`-JOIN predicate mismatch**, which is what makes the unresolvable state reachable at all. **Rider on (ii):** if (ii) is not fully discharged, **the fail-closed path must become observable** — a control that fires silently is one you learn about from user complaints.
  - *Named rather than numbered, per the ordinal-label convention — and the naming is what exposed the error: **ordinals conceal heterogeneity**, so three items in a numbered list read as three of the same kind when one of them shipped on a different timeline entirely.*

**F4. `rd_access` must be in the WITHHELD column set of the B5 hard-gate — V1-latent.** [V2 precondition]
- **Source.** Close-gate joint review 2026-08-02. The containment argument for the **`fn_compute_nav(_, true)` under-reporting** entry (§9.1) rests on **`{can UPDATE} ⊆ {can SELECT}`**: passing `account_update` entails being able to read the ledger the close fence measures, because a direct owner necessarily holds an un-deletable creator-grant row with `rd_access` (`003:191`, AFTER INSERT; `authenticated` holds SELECT only, `003:162`).
- **AC.** When the Lock-3-mod-1 column-level UPDATE restriction lands (`REVOKE UPDATE; GRANT UPDATE (nickname, notes)`), **`rd_access` must be in the withheld set, never the grantable set.** If an owner can ever revoke their own `rd_access`, **containment inverts and the close fence reopens as a live attack** — a caller could blind themselves to the ledger and walk the gate open. **Also record the coupling itself.** The containment is **emergent across four policies and declared nowhere**; relaxing `account_update`'s aal2 conjunct degrades the fence silently with every artifact still individually defensible (the ADR-040 assembled-statement shape). **Needs a QA assertion, not a comment:** aal1+`totp` is denied the UPDATE, and a caller without `rd_access` cannot reach the fence.
- **Dependencies.** **Sec-held V2 obligation #1**, same `003:152` B5 hook as the **`fn_compute_nav(_, true)` under-reporting** entry. Owner: Architect + Sec + QA.

**F5. Land the ADR-039 `p_active_only` semantic amendment as a comment-only migration. RE-SCOPED: the disclosure/control-surface argument is GONE.** [process control]
- **Disposition (Architect, 2026-08-03, post-[ADR-042](DECISIONS.md#adr-042)).** The original framing — `p_active_only` as a **disclosure control** whose removal would silently disable the excluded-value line, hence joint-review-mandatory — **does not survive the ratified model: no ignored class exists, so there is nothing to disclose**, and the joint-review-mandatory framing drops with it. What survives is a **different obligation on the same surface**: the [ADR-039](DECISIONS.md#adr-039) **semantic amendment**. `p_active_only` changes meaning from *current-state `is_active`* to *not-closed-as-of `p_as_of`*, and its documented temporal constraint (*"sound only at `p_as_of = current_date`"*) is **STRUCK, not reworded** — the parameter becomes temporally sound in both settings. **Consolidation (Architect):** the same `052`-shape comment-only migration carries **both** this and the `046:167` count correction — one mechanism, one file. **Still never an edit to merged `050` or merged `046`**, per §7.6 **S3**'s scoping guard.
- **Source.** Close-gate joint review 2026-08-02. Under remedy (c), `excluded_value = fn_compute_nav(as_of, false) − fn_compute_nav(as_of, true)` is the **sole zero-derivation path** to the number that makes a GL↔NAV divergence visible.
- **AC.** Land a **`052`-shape comment-only migration** (`052_self227_audit_trace_comments.sql` is the precedent for comment-only corrections as a new migration — **not** an edit to merged `050`, per §7.6 **S3**'s scoping guard) recording: *changes to `fn_compute_nav`'s signature or `p_active_only` semantics are **joint-review-mandatory because removing the parameter silently disables a disclosure control**.* **State the property with no neighbouring citation** — an *"on the same basis as RT-NN"* analogy is the PR #66 layer-attribution drift vector. **§10 catalogued ledger stays 3** (RT-22 / RT-26 / RT-27); RT-22 + RT-26 attributions untouched; no surface becomes "four-layer." **Conditionality.** True under **both** packages: `p_active_only` survives on §3e containment even under Package 2, where it becomes redundant only *for disclosure*.
- **Dependencies.** Owner: Architect (migration) + Sec (wording). No ledger move.

**F6. Land the review checkpoints into the skill files.** [process control]
- **Source.** Close-gate joint review 2026-08-02/03, §18 — the *precedent-without-its-surrounding-conditions* pattern fired **five times** (once endemic in `042`; four agent-authored under review conditions, with the pattern named and in view). Both agents agreed this is the process note worth recording above any individual finding, because it is the only one that predicts *where the next defect comes from* rather than describing where the last one was. **This title deliberately carries NO count.** It said "three" and there are now seven; the set grew because **each new check acquired its own failure mode** (CP5 misses consistency, CP6 misses broken patterns, CP7 misses incomplete ones), and **Sec's position is that the regress does not terminate.** A count in a title is a maintenance liability — the ordinal-label ruling applied to titles rather than to references. Sec's own §16 heading carried the identical defect ("five findings", enumerating six) and was fixed by **removing** the number, not correcting it.
- **AC.** Add the checkpoints to the skill bodies, since a skill is where someone encounters a control at the moment it matters — an ADR is not read at authoring time (the **§7.6 S8** reasoning). **CP1 — precedent surroundings.** *When copying a precedent, read the source's **surroundings** — its table comment, RLS policies, grants, section heading — not only the artifact being lifted. A precedent's conditions live around it, and reading the thing you are copying does not show them to you.* → **`apply-migration`**. **CP2 — constraint over comment.** *Before writing a comment that asserts an invariant, check whether the invariant is **declaratively expressible**. Where it is, the comment is the weaker artifact by definition — this project has already ruled that **code implies enforcement** ([ADR-040](DECISIONS.md#adr-040) B9), so an executable statement that enforces nothing is worse than a comment saying the same thing.* → **`apply-migration`** + **`spawn-sec-joint-review`**. **CP3 — test a principle against its full enumeration.** *Before adopting a principle extracted from a set of instances, test it against the **full enumeration it claims to summarize**, not against the cases that suggested it. A generalization is itself a copy — of the instances it is drawn from — so it inherits CP1's failure mode.* → **`brief-drift-catch`** (the correction was **away from** `apply-migration`: this is verify-before-you-generalize, the same discipline as verify-before-you-relay, and it applies well beyond migrations). **CP4 — absence is not a value.** *For any surface where a consumer distinguishes states, **write the explicit zero**, or make the absent case **structurally distinguishable** from the zero case. Never let a missing row carry meaning: omitting a row where an explicit zero belongs makes "we didn't measure" indistinguishable from "we measured zero" — a **detectability** failure, which is why it lands on audit and forensic surfaces rather than at random.* → **`apply-migration`**. **CP5 — citation sweep.** *Before forwarding any document to ratify, `grep -n` **every ordinal-label prefix the document uses** (`F#`, `S#`, `#N`, `B#`…) and **resolve each occurrence against its intended sequence**, rather than eyeballing the one that was flagged.* → **`brief-drift-catch`**. **CP6 — consistency sweep (body vs summary).** *Extract **every quantitative claim a summary block makes** and resolve it against the body. Run it **on your own artifact after any ruling that moves a number**, not only before forwarding — a document goes stale the moment a ruling is made elsewhere, and nothing prompts a re-read because the file was not edited. **Bodies get read by people following an argument; summaries get read by people checking a number**, so correcting the reasoning and leaving the count is the worst available outcome.* → **`brief-drift-catch`**. **CP7 — adversarial probe on a clean result.** *Any check returning **"clean"** on its first run gets **one probe with a known-present instance — the hardest known variant, not any instance** — before the clean result is believed. A check that fails open reports success while proving nothing, and CP5 and CP6 are themselves greps, so a failing-open sweep **disarms both while reporting success**. **For a completeness sweep, prefer a pattern that over-matches and filter by hand**: over-matching is auditable, **under-matching is silent and its silence reads as absence**.* → **`brief-drift-catch`** **and** **`apply-migration`** — the only checkpoint routing to both, because the same failure occurs at the DDL layer: **a guard that never fires has never been shown to fire**, so a migration-time assertion needs a seeded violator proving it aborts. **CP8 — re-derive, don't re-read.** *After a ratify that changes a mechanism, for each open finding written against it ask **not** "does this still parse?" but **"would this finding's assertion still be TRUE if I derived it fresh from the new mechanism?"** Re-reading confirms a claim is well-formed; **re-deriving confirms it is still true.** The distinction is the whole checkpoint: a relayed claim, an unverified ID, and a restated open item all survive re-reading and all fail re-derivation.* → **`brief-drift-catch`**. **CP8 stands as its own checkpoint — Sec ruled the substance, team-lead ratified the freeze 2026-08-03.** The stopping condition was always a **bar** (cheap, mechanical, leaves a re-runnable artifact), never a **number**; CP8 clears it. **Refusing a genuinely new class to preserve a count would be the count-in-a-title liability this entry exists to strip** — the same principle already applied to this title, to the §7.7 heading, and to the citation convention. **Freeze the bar, not the number.** **Known limit, and it is CP8's own:** re-derivation confirms each claim is **true**; it is **silent on whether two true claims describe the same thing.** See the DEFINER-referent trap (`self209_close_gate.sql`) — authored DEFINER = 3, allowlist = 4, both correct, **and the act of reconciling them is the defect.** **Stopping condition — the set is NOT claimed complete.** A checkpoint qualifies only if it is **cheap, mechanical, and leaves a re-runnable artifact**; the regress does not terminate, so treat seven as the current set, not the closed one. **Governing disposition preference, which is why the set is ALLOWED to be incomplete:** *where a blanket disposition is available, prefer it over an enumerated one — precisely because you cannot trust your own sweep's completeness.* An enumeration covers what the sweep found; a blanket supersession covers **what it never found, by construction.** It is the only move in this sequence that **ends a branch of the regress rather than extending it**, because it removes the dependency on the check being complete instead of improving the check. **Not an eighth checkpoint — it is not a check.**
- **Dependencies.** Owner: **team-lead** (all are skill files; neither Architect nor Sec owns them). No ledger move. Two-way door. **Canonicity caveat — read before building.** The canonical checkpoint text is also slated to land in [ADR-042](DECISIONS.md#adr-042)'s process section via a separate doc-only PR. **If both land, ADR-042 is canonical and this entry should reduce to a reference (Path B — reference, do not restate), exactly as `supabase/CLAUDE.md` treats the §10 ledger.** Transcribed in full here at Sec's explicit direction because a §7 entry must be self-contained enough to promote to Linear verbatim; the duplication is known, not accidental, and this note is the tiebreak rule.

**F7. `pfin.account.currency` is user-writable and silently restates historical NAV — record the event now, mark the affected range later.** [live defect] *(added 2026-08-03, post-[ADR-042](DECISIONS.md#adr-042) ratify)*
- **Source.** [ADR-042](DECISIONS.md#adr-042) Decision 4 (the closed-account half is fenced there; **this is the open-account half, deliberately out of that ADR's scope**). `015:538` adds `currency text not null default 'USD'`; `015:541` records that it *"Feeds the `fn_compute_nav` cash-leg FX multiplier."* `003:124` grants `authenticated` a **table-level** UPDATE on `pfin.account` **with no column list** — which is why it covers columns added later — and `pfin` is Data-API-exposed, so a direct `PATCH /rest/v1/account?account_id=eq.N {"currency":"EUR"}` reaches it without touching `+page.server.ts`. The app's `updateAttributes` Zod schema omits `currency`, but **app-layer is not a control**. Live today, independent of the close gate.
- **AC.** (1) A change to `pfin.account.currency` writes a row to `pfin.account_event` ([ADR-042](DECISIONS.md#adr-042) Decision 5) recording account, actor, `effective_date` **distinct from** `created_at`, and the from/to values — via the `event_type` CHECK-widen path (`030` `transaction_type` precedent; **widening is Sec-joint-review-mandatory**, and the `event_type` and value-vocabulary CHECKs widen **together**). (2) The recording is **NOT conditional on the marking half shipping** — *record now, mark later.* (3) **Marking half — routed as RATIFICATION, not implementation.** [ADR-013](DECISIONS.md#adr-013) D1 declares the **surface** set illustrative, but its governing trigger is *stale-account data*; a restatement is a **different cause**, so extending D1 to it is a larger move than the one D1's own provenance required F/CTO ratification for. **PM/UX define the presentation; F/CTO ratifies the extension. Do not build against D1 as existing authority.** (4) The event must distinguish **"believed then, still believed"** from **"believed then, since corrected"** — a provider restatement and a user's denomination fix currently produce **identical artifacts**, and a trajectory reader cannot tell them apart, or tell that anything happened at all. **Explicitly NOT in scope.** [ADR-040](DECISIONS.md#adr-040) is **working as designed**: a frozen `nav_daily` checkpoint records *what was believed at the time*, and recomputability was deliberately traded away. The permanent frozen-vs-fresh divergence is **not** the defect — **the missing marker is.**
- **Dependencies.** Upstream: `pfin.account_event` must exist ([ADR-042](DECISIONS.md#adr-042), migration `057`) — that is what makes this cheap and is why F/CTO took *record-now*. **Sec gate:** the `event_type` widening. **Not blocked on** the marking half. Owners: Architect (DDL + widen) + Backend (write path) + Sec (joint-review); PM/UX + F/CTO on the marking half per AC (3). **Urgency is a closing window, not a severity adjective.** *The information needed to mark retroactively is destroyed by not recording it now* — with no record of **when** the change happened, the affected range cannot be identified and that history is **permanently unmarkable**. The window closes **per user action, not per calendar day**: accrual is ≈zero today (greenfield, pre-deploy, effectively one user) and becomes real at **public signup**. **Consolidate rather than repeat** — this shares one deadline with [ADR-042](DECISIONS.md#adr-042)'s **`058`→`059` operational-quiescence window** — which exists precisely *because* `closed_at` and `is_active` coexist across it while the operator dispositions each pre-existing inactive account **through the real gated control** (an operator step, not a migration). **State it once as a single pre-public-signup gate**, not as a caveat repeated per finding.

**F8. Stale Decision-3 family counts across merged artifacts — POINTER ONLY; the finding is canonically recorded at [ADR-042](DECISIONS.md#adr-042)'s Consequences, not here.** [pointer — do not restate]
- **Source.** Close-gate joint review 2026-08-03. **This entry deliberately does NOT restate the counts.** The finding — that `046` and [ADR-037](DECISIONS.md#adr-037) D4's superseding bracket carry a stale Decision-3 family count — is **already committed in [ADR-042](DECISIONS.md#adr-042)'s Consequences**, whose own text reads *"This survives nowhere else in committed text — not in §7.7 — so it lives here."* **Two committed homes for one finding will drift and hand the next reader two different counts**, so §7 carries a pointer and nothing more (team-lead ruling, 2026-08-03). Read ADR-042 for the numbers; **do not copy them here, and do not cite a count from this entry.**
- **AC.** Nothing to build. Three routing facts that are *not* in ADR-042 and would otherwise be lost: **(i) `046` carries the count TWICE, with different dispositions.** The `--` line comment is **not a database object**, so no migration can reach it — "correct when `046` is next touched" is the only option, and Architect confirmed a real ride exists (the queued [ADR-039](DECISIONS.md#adr-039) doc-nit about `046`'s `netWorth.ts` claim, which itself has **two sites**: `046:65` header and `046:167` body — *fixing one and missing the other is the shape this review hit repeatedly*). The `comment on function` at **`046:167` IS a database object** and is correctable by a `052`-shape comment-only migration **without editing merged `046` at all** — the same migration carrying the `p_active_only` semantic amendment. **(ii) `046:167` carries three stale claims in one string** — the D3 count, the ADR-039 netWorth nit, and `RT-26 stays 4` / `§10 stays 3`, which want re-checking while someone is in there. **(iii) `supabase/CLAUDE.md`** — **Architect-owned and riding the ADR-042 migration PR, NOT queued here.** It is the only citation in the set that will actively mislead someone tomorrow: every other instance is a dated artifact where the point-in-time defense holds, but this is an **operating doc agents read every session**. Its D3 count, SECURITY DEFINER text and `is_active` conventions all move with ADR-042 together, so a queued follow-up would land after the facts had moved.
- **Dependencies.** Owner: **Architect** (all sites). No ledger move. **Blocked-by:** the [ADR-042](DECISIONS.md#adr-042) four-slice correction landing, since the D3 canonical count moves to 16/13 with it.

**F11. `account_event.reason_code` — open F/CTO ratify; POINTER ONLY, and it blocks `057`.** [pointer — do not restate]
- **Source.** [ADR-042](DECISIONS.md#adr-042) Decision 5 reserved this explicitly — *"Starting vocabulary — PROPOSED, grounded not invented. **PM to confirm, F/CTO to ratify**"* — and it was never ratified. **PM has now confirmed; the F/CTO ratify is outstanding and `057` cannot land without it**, since the migration's `account_event_reason_required` CHECK makes closure impossible without a reason and no trigger can invent one. **The vocabulary and rulings are NOT restated here** — they land as an **ADR-042 Decision 5 amendment** (Architect-authored, F/CTO-ratified), which is where a decision belongs; §7 is a work queue. Reading this entry is not a substitute for reading the amendment.
- **AC.** Nothing to build. Three items carry ratify weight, in ascending order: (1) confirm the vocabulary (one rename, one addition, `other` included); (2) confirm reason is **mandatory** — safe *only because* `other` is included, per ADR-042's own append-only asymmetry; (3) **the axis decision, which is the only irreversible one** — `reason_code` conflates *why the account ended* with *where the value went*, and a second `disposition` column **cannot be added later without being NULL for every prior row** in an append-only table. **The one-way door is COLUMN EXISTENCE, not requiredness** — requiredness is a CHECK and relaxes or tightens freely, so the two must not be decided as one.
- **Dependencies.** Owners: **Architect** (amendment + DDL), **F/CTO** (ratify), **PM** (product framing — done). Blocks `057`. **Urgency is bounded and belongs on the existing gate:** the one-way door's cost is proportional to closures written before the column exists, which today is **≈zero** (greenfield, pre-deploy, effectively one user) and becomes real at **public signup**. **So (1) and (2) unblock `057` now; (3) joins the single pre-public-signup gate already shared with the `currency` finding and the `058`→`059` quiescence window** — state it once there, not as a fourth caveat.

**F10. PRD §2.4.1 / §2.4.2 describe the retired `is_active` column — re-point after `059`.** [PM; BLOCKED-BY `059`]
- **Source.** [ADR-042](DECISIONS.md#adr-042) retires `pfin.account.is_active` at `059`. Two locked PRD passages describe it and will be false once it lands. [PRD §2.4.2](docs/PRD/index.html#story-2-4-2) verbatim: *"…and mark an account **inactive** when it's closed or sold; inactive accounts retain their transaction history for historical accuracy but are excluded from current-state surfaces by default."* [PRD §2.4.1](docs/PRD/index.html#story-2-4-1) V1/V2 boundary verbatim: *"manual un-share of an already-shared connected account is V2+ **unless §2.4.2's inactive-flag pattern covers the use case**."* **Note the second is a conditional that was already discharged YES** — at F/CTO decision **PM-1 (§2.4)**, carried in [ADR-013](DECISIONS.md#adr-013) handoffs **A1–A3** (*"inactive suspends the scheduled-poll for the Item"*) — which is why the connections use/ignore control existed at all. ADR-042 has since ruled that control a **mis-implementation** (concept 3 landed one level too high), relocating it rather than deleting it.
- **AC.** Re-point §2.4.2's inactive sentence to the ratified three-concept model (dated `closed_at`; the standing zero-value invariant; history retained). **Rewrite §2.4.1's "unless" clause rather than deleting it** — the capability it points at was **relocated to aggregator-side selection at connect time, not withdrawn**, so deleting the clause would misreport a live capability as absent. **Stay at the user-visible layer:** state what the user can do and what the product refuses; **do NOT describe the close gate's checks, the conjunct, or the migration sequence** — that is ARCH / SECURITY territory and the PRD has never carried it. **Blocked on an Architect ruling, not just on `059`:** ADR-042 must say explicitly **whether it supersedes or preserves ADR-013 A1–A3 / PM-1**, since A1 names a column that will not exist; the PRD wording follows that answer and should not guess at it.
- **Dependencies.** **Blocked-by `059`** (F/CTO-delegated sequencing, team-lead ratified 2026-08-03): **the PRD must not describe an end-state the schema has not reached.** A requirements doc describing a column that does not exist yet is **worse** than one describing a column about to be retired — the second is a known-stale artifact, the first is a claim about the system that is simply **false**. Owner: **PM** (`docs/PRD/index.html`), separate PR. Upstream: `059` + Architect's ADR-013 supersede-or-preserve ruling. **Booked rather than held in conversation precisely because "wait" is the disposition that gets lost.**

**F9. Sweep the `(N items)` counts out of the §7 section headings.** [process control]
- **Source.** Team-lead ruling 2026-08-03, extending Sec's no-counts-in-titles ruling from entry titles to section headings. **§7.7's heading count went stale twice in one day** (6 → 7 → 8) — the argument proving itself faster than it was made. It has been **dropped from §7.7 only**, which leaves §7.7 diverging from its six siblings (§7.1–§7.6 all carry `(N items)`). **A stale count in a heading is worse than a one-section divergence** — the divergence is visible and self-explaining, whereas a stale count reads as authoritative. This entry exists so the inconsistency is **temporary and tracked rather than permanent and unexplained**.
- **AC.** Drop `(N items)` from the §7.1–§7.6 headings, matching §7.7. **Do NOT replace the counts with corrected counts** — that reproduces the liability one cycle later, which is the enumerated-correction regress this section already documents twice (ADR-037 D4's bracket; the checkpoint entry's own title). The list is adjacent to its heading in every case, so anyone needing a count can read one that cannot go stale. **Scope fence:** section headings in §7 only — **not** §5, **not** the dated footer log entries (point-in-time records of what was filed on a given day, the S3 leave-the-record-alone class), and **not** counts that are load-bearing claims rather than inventory labels (e.g. the §10 catalogued-instance ledger, where the count *is* the commitment).

### §7.8 — Connection lifecycle

*One missing capability with four symptoms, surfaced by F/CTO questions during the ADR-042 close-gate review, 2026-08-04. Filed as **one** entry rather than four because they share a single cause: **nothing in the application ever writes `pfin.account.linked_source_id`.** Splitting them would produce four partial fixes to one gap. Verified against `main` @ `e72f652` — measurements are dated and expected to rot; the capability claim is not.*

**Connection lifecycle: disconnect, re-point, and add-accounts-to-an-existing-Item.** [V1 gap; not currently staged anywhere else]

- **Source.** F/CTO, during the ADR-042 close-gate review. Two questions, one cause. ADR-042 Decision 1 records aggregator linkage as concept 1 and states *"No new work; a link/unlink control is explicitly deferred"* — a deferral that was never converted into a staged item, so it survived only as a sentence saying "not now."

- **Measured state (dated — `main` @ `e72f652`).**
  - **`linked_source_id` is never written by any application path.** It is read throughout `api/src`; the only write-adjacent reference is `updateAttributes.server.test.ts:82`, which asserts a posted `linked_source_id` is **rejected** by `.strict()`. There is no control, no server action, and no worker path that changes it.
  - **The DB primitive already exists.** `015`'s matched-tenant fence on `pfin.account.linked_source_id` (Decision-3 canonical instance **#6**) states in its own comment: *"Covers UPDATE (**re-link path**), not just INSERT."* The fence was built for a control that was never built.
  - **Account identity is `(linked_source_id, provider_account_id)`** — the `021` partial UNIQUE index, and `042`'s ON CONFLICT arbiter.
  - **`linked_source` is unique on `(provider, external_connection_id)`** and carries **no uniqueness on `(user, institution)`**. One row per Item; nothing prevents two Items at one institution.
  - **The connect flow always mints a fresh link token.** `/accounts/connect` → `PlaidLinkConnect` → `/api/plaid/link-token`, which takes no existing-connection input. **No institution comparison exists anywhere in the connect path.**
  - **Plaid update mode is already implemented and in production** — `workers/provider-sync/src/http/linkToken.ts` passes an existing Item's `access_token` and omits `products`, per Plaid's update-mode rule. It is wired **only** to re-auth (`/api/reauth/start` → `kind: 'link_update'`).

- **The four symptoms.**
  1. **Cannot disconnect** an account from its aggregator and return it to manual.
  2. **Cannot move an account to a different aggregator** — live under [ADR-027](DECISIONS.md#adr-027)'s ratified Plaid-primary / SimpleFIN-secondary split, where an account may need to change provider.
  3. **A relink duplicates every account.** A new Item is a new `linked_source` row with a new `source_id`, and Plaid issues fresh account ids on a new Item — so **both halves of the identity change at once**, the `021` arbiter cannot match, and a second set of accounts lands beside the originals while the old Item keeps polling.
  4. **Re-selecting a connected institution mints a second billed Item.** Choosing an already-connected institution from *add connection* creates a new Item rather than reusing the existing one. **This has a recurring dollar cost** — Plaid bills per Item — and it is the symptom most likely to be hit repeatedly, because *"go add that account I skipped"* is a normal thing to do.

- **AC.**
  1. **Route symptom 4 through update mode rather than a fresh Link session.** The mechanism exists and is in production; what is missing is the entry point. Plaid update mode with account-selection is the supported flow for *"add accounts to an existing Item"* and creates no new Item and no new billing.
  2. **Detect an existing connection to the chosen institution** before minting a link token, and offer update mode instead. There is no institution comparison in the connect path today.
  3. **A re-point control** that updates `linked_source_id` on an existing account, composing with `015`'s #6 fence rather than bypassing it. **Sec joint-review-mandatory** — it writes a Decision-3 FK-shaped column across an isolation boundary.
  4. **A disconnect path** returning an account to manual (`linked_source_id = NULL`). Note the `021` index is **partial** (`WHERE linked_source_id IS NOT NULL`), so unlinked accounts leave the dedup surface by construction — confirm that is intended rather than incidental before relying on it.
  5. **Symptom 3 needs a re-identification step**, not just a re-point: on a new Item the operator must be able to say *"this existing account is that provider account on this new connection."* Both identity halves change, so no automatic match is available.

- **Dependencies.** Owner: **Architect** (control design + the #6 composition) with **Backend** (connect-flow routing) and **Sec** (joint review on AC 3). **Not blocked by ADR-042** — concept 1 is orthogonal to the closure work, and none of this waits on `059`. Independent of the `toggleAccount` removal in [ADR-042](DECISIONS.md#adr-042) Decision 1b, which governs *which accounts are imported*, not *whether a connection exists*.

- **Standing claim, measurement-independent.** The four symptoms are one capability. **Any fix that addresses fewer than all four leaves the same gap open under a different name** — that is why this is one entry, and why the *"link/unlink control is explicitly deferred"* sentence in ADR-042 should be re-pointed here rather than left as a bare deferral.
- **Dependencies.** Owner: **PM** (`BACKLOG.md`). Two-way door. No ledger move. Trivial, but deliberately booked rather than done inline — it touches six section headings that other artifacts may cite, and it is not close-gate work. **The observation worth preserving, and the reason this family is a pointer rather than an enumeration:** `supabase/CLAUDE.md` **already carries the blanket disposition** — *"read the canonical enumeration in [ADR-011](DECISIONS.md#adr-011) D3 live, **it grows**"* — **in the same sentence as a restated count.** It warns against trusting restated counts while restating one. And [ADR-037](DECISIONS.md#adr-037) D4's bracket is *itself* a correction (it superseded 14/12 with 15/13) that is now **two steps stale** — **the enumerated correction became the next stale citation.** That is the regress demonstrating itself in the artifacts, and it is the concrete case for the *prefer-a-blanket-disposition* preference recorded under **"Land the review checkpoints into the skill files."**

---

*Last updated: 2026-06-03 (Phase 4 Step 5 Wave 6 close — §7 V1 staging queue framing added per [ADR-017](DECISIONS.md#adr-017) Decision 2 + Wave 6 18 issues staged: 8 Architect substrate (A1-A8) + 10 PM domain (P2-P11; PM Issues 1+3+6+9 collapsed per F/CTO Gate A unified + Gate B Option C 4-col absorption); Settings area ramp closes at 4/4; Lock 14 family closes at 5/5; cumulative PRD §2 trace = 32/32 stories = Phase 4 exit criterion 1 fully discharged). [2026-07-26 — §7.3 GL-substrate stream added: **G1** transfer-in-kind follow-on staged per [ADR-033](DECISIONS.md#adr-033) Decision 4 handoff (later V1.x; decomposed out of SELF-203).] [2026-07-27 — SELF-203 stock-split entry LANDED (v1.114, POSITION-LEVEL); **G2** per-lot precision refinement staged (deferred as a substrate project — needs a lot-linkage FK + authoritative `lot_match`). Related SELF-203 follow-ups (also in PR #244 + CHANGELOG v1.114): **SELF-204** should promote the `fn_create_stock_split` source-of-truth guard to a BEFORE INSERT trigger once provider corp-action ingestion lands (Sec note); **UX Designer** copy pass for the entry-form empty-state/error strings.] [2026-07-27 — §7.4 V1.6 reconciliation / statement tie-out stream added: **R1** period statement-vs-GL reconciliation control tie-out (read-only detector) staged per [ADR-035](DECISIONS.md#adr-035); re-homes the retired V1.0 §2.4.3 per-transaction reconcile mechanism (`reconciled_flag` / B1 / B2 superseded — reconciled-by-construction via the double-entry GL); comparand substrate already shipped (`005`/`018`/`019`); homed in the new **V1.6 — Reconciliation / statement tie-out** milestone; SELF-205 re-scoped (retains ID, re-milestoned V1.6).] [2026-07-28 — §7.5 Onboarding / provisioning stream added: **O1** default taxonomy provisioning on signup (**both asset + cash-flow domains**, single pass), PROMOTED INTO V1 per F/CTO ruling (V1 signup is open); resolves the SELF-200 empty-picker dead-end for open-signup users + the latent multi-user taxonomy gap (§5.7 note updated V2→resolved-in-V1); provisioning mechanism Architect-owned next cycle. Rides in the SELF-200 PR.] [2026-08-02 — §7.6 SELF-214 Sec joint-review follow-ups added (10 items; S1–S6 in Sec's priority ranking): **S1** pgTAP assertion that `authenticated` holds no table-level SELECT on `plaid_items`/`linked_source` — converts [ADR-023](DECISIONS.md#adr-023) C3 into an enforced invariant (QA; **Sec ranks this above the prose fix**), **S2** ADR-023 C3 prose correction folded into the ADR (disposition (c); (b) structurally cannot reach `--` line comments), **S3** invert the `007`/`015` REVOKE comments from reassurance to warning — **no live exposure** (grants verified column-scoped, handles omitted, no table-level grant exists); carries the forward-looking-vs-point-in-time **scoping guard** so a sweep does not rewrite merged migrations' history, **S4** pre-push hook fencing untracked/stale files (DevOps; CI structurally cannot catch the class), **S5** `pfin_provider_sync` login role per [ADR-019](DECISIONS.md#adr-019) C2 promoted to a Phase-7 deploy gate by [ADR-041](DECISIONS.md#adr-041), **S6** V1-scope ruling on the incumbent FMP financial-statement tables (PM+Architect frame, F/CTO decides; PM finding — 2 of the 8 are already superseded by `cpi_u_index`/`user_taxonomy`, leaving 6 genuinely open), **S7** [ADR-011](DECISIONS.md#adr-011) Decision 1 clause (d) audit-log discharge for the W-1 NAV worker (per [ADR-040](DECISIONS.md#adr-040) — retained; absent from the ranked six as a ranking-scope artifact, not a de-prioritization), **S8** working-tree-vs-`main..HEAD` lesson into the `finish-feature`/`merge-pr` skills (subordinate to S4, not a substitute), **S9** three QA battery-design lessons into `tests/rls/DESIGN.md` (leg-independence; shape-agnostic assertions degrading silently; the partial-run guard — "nothing failed" and "nothing ran" are indistinguishable unless you count)), **S10** capability-verify `compute_and_checkpoint_user` end-to-end under a real `pfin_etl` **login** (not `SET ROLE`) through the Python worker before Phase-7 scheduling — Sec FINAL VERDICT GREEN **condition 2**, verbatim wording; a **deploy blocker, not a merge blocker**. **The Linear free-issue limit is a booking constraint, not a capture constraint** — §7 is the ratified home per [ADR-017](DECISIONS.md#adr-017) Decision 2 and needs no Linear seat, so none of these is blocked on it; no live Linear IDs cited. Promote at the first milestone-rotation after the limit lifts.] [2026-08-03 — **§7.7 close-gate joint-review findings added (6 items)**, transcribed from Sec's §16 copy-ready specs (agreed source of record over Architect's §21 table): **F1** `pfin.eod_price` admits zero **and negative** prices — **live NAV defect** (`019:220`'s only value constraint is a NaN check; a zero `fx_feed` rate zeroes a foreign-currency cash leg in `050`, a negative one **sign-flips** it so NAV silently subtracts money that exists); fix is the **source-conditional** `check (source <> 'fx_feed' or price > 0)` — blanket `price >= 0` ruled wrong (`future` is in the asset vocabulary, negative settlement is real, and `eod_price` has no `asset_type` column so the asset-conditional rule is not expressible); non-fx residual **deliberately open in V1**, **F2** `nav_daily.py:214` enumerates tenants via `where is_active = true` — **live defect**; a tenant whose accounts are all inactive gets **a gap rather than a `0` row**, and absence conflates four states; feeds **§7.6 S7**, **F3** `fn_compute_nav(_, true)` under-reports a V2 grantee's shared holdings (owner-only `account_select` vs `rd_access`-JOIN holdings legs) — **V1-latent, Sec-held V2 obligation #2** on the `003:152` B5 hook, **F4** `rd_access` must be in the **WITHHELD** set when the Lock-3-mod-1 column-level UPDATE restriction lands, or close-fence containment **inverts** — **V1-latent, Sec-held V2 obligation #1**, same B5 hook; needs a QA assertion, not a comment, **F5** `p_active_only` is a **control surface** — `052`-shape comment-only migration (never an edit to merged `050`, per S3's scoping guard); **§10 catalogued ledger stays 3** (RT-22 / RT-26 / RT-27), no attribution moves, **F6** land the three review checkpoints in the skill files (**CP1** read a precedent's *surroundings* → `apply-migration`; **CP2** prefer a declarative constraint over a comment asserting an invariant → `apply-migration` + `spawn-sec-joint-review`; **CP3** test an extracted principle against the **full enumeration** it claims to summarize → `brief-drift-catch`, **not** `apply-migration`) — the *precedent-without-its-surrounding-conditions* pattern fired **five times** in one review. **Filed as a NEW §7.7, deliberately not under §7.6** — that section's heading scopes it to SELF-214 follow-ups and four of six are close-gate findings; mis-filing under a scoped header is the exact pattern F6 fences (Architect proposed §7.6, Sec corrected, PM concurred). **All six hold under BOTH close-gate packages and none waits on the D1 product decision**; F1 + F2 reproduce on `main` today. Booking constraint only — no live Linear IDs cited.] [2026-08-03 (follow-up) — **§7.7 extended to 7 entries + two corrections.** **(i) NEW — `pfin.account.currency` is user-writable and silently restates historical NAV** (`003:124` is a **table-level** UPDATE grant with no column list, so it covers columns added later; `pfin` is Data-API-exposed, so a direct PATCH reaches it and the app's Zod schema is not a control; `015:538`/`015:541` bind the column to the `fn_compute_nav` cash-leg FX multiplier). F/CTO ratified **record now, mark later**: the event lands in `pfin.account_event` via a Sec-gated `event_type` CHECK-widen; the **marking half is routed as RATIFICATION, not implementation** (ADR-013 D1's trigger is *stale data*, and a restatement is a different cause — do not build against D1 as existing authority). **Urgency is a closing window, not an adjective** — the information needed to mark retroactively is destroyed by not recording it now, and the window closes **per user action**, ≈zero today, real at **public signup**; stated once as a single pre-public-signup gate shared with ADR-042's `058`→`059` quiescence window, not repeated per finding. **(ii) CORRECTION — the checkpoint entry retitled and extended 3 → 7** (CP1 precedent surroundings · CP2 constraint-over-comment · CP3 principle-vs-full-enumeration · CP4 absence-is-not-a-value · CP5 citation sweep · CP6 body-vs-summary consistency sweep · CP7 adversarial probe on a clean result — CP7 the only one routing to **both** `brief-drift-catch` and `apply-migration`). **The title now carries NO count, deliberately** — it said "three", the set grew because each new check acquired its own failure mode, and **Sec's position is that the regress does not terminate**; a count in a title is a maintenance liability (the ordinal-label ruling applied to titles). Stopping condition recorded in place of a completeness claim: a checkpoint qualifies only if **cheap, mechanical, and leaving a re-runnable artifact**. Carries the governing disposition preference — ***where a blanket disposition is available, prefer it over an enumerated one, precisely because you cannot trust your own sweep's completeness*** — explicitly **not** an eighth checkpoint, since it is not a check. **(iii) Convention:** entries in §7.7 are cited **by title, never by `F#`** (positional labels in a growing list, per the ADR-040 ordinal-label ruling), and **no entry title carries a count.** **(iv) Post-ADR-042 caveat added to the preamble:** F1–F6 were drafted BEFORE [ADR-042](DECISIONS.md#adr-042) retired `pfin.account.is_active` (2026-08-03; three-concept model, migrations `056`/`057`/`058`), so the three entries turning on `is_active` semantics need an Architect + Sec reconciliation pass — findings survive, framing may not. **DEFERRED from this pass:** the stale Decision-3-count finding (`046` / ADR-037 D4) is **NOT filed here** — it is already committed in ADR-042's Consequences, whose own text reads *"This survives nowhere else in committed text — not in §7.7 — so it lives here"*; filing a copy would create two homes for one finding. Pending team-lead ruling.] [2026-08-03 (follow-up 2) — **§7.7 post-ADR-042 reconciliation + the stale-count pointer.** **(i) Reconciliation pass run (Architect + Sec) and COMPLETE, and the outcome was NOT uniform — this is the entry worth reading:** the **`nav_daily` tenant-enumeration** entry is **DISCHARGED**, not merely urgent — when `is_active` drops, `nav_daily.py:214` **errors at runtime and the ETL breaks**, so the fix is forced work inside that slice's blast radius and its AC is absorbed as an acceptance criterion (the correct re-point drops the filter entirely rather than re-pointing to `closed_at`, which fixes the skip-vs-zero defect at the same time); the **V2-grantee** entry **INVERTS** and was re-scoped from *"fix the under-report"* to *"fail-closed is the backstop; V2 must make the unresolvable state unreachable"* — `coalesce(acc2.is_active, false)` fails **closed**, the naive re-point `acc2.closed_at is null` fails **OPEN**, the two are identical under V1 (no sharing), so the flip would be **chosen silently at the re-point**. **Sec RULED preserve-fail-closed** (fail-closed fails visibly, fail-open fails silently — a too-high NAV looks exactly like a correct one; `closed_at is null` on an unresolvable row asserts "not closed" from no information; fail-open's safety is derivative of the close gate and unnamed as a dependency) **and corrected the framing both agents had wrong: this is NOT "decide the default."** The unresolvable state exists only because `account_select` is owner-only while the holdings legs are `rd_access`-JOIN-scoped — **V2 must reconcile those predicates so the state is unreachable**, with fail-closed as a backstop. **That framing stops the under-report being recorded as an accepted cost**; the **`p_active_only`** entry **loses its disclosure/control-surface argument entirely** (no ignored class exists, so nothing needs disclosing) and survives only as the **ADR-039 semantic amendment** — the *"sound only at `p_as_of = current_date`"* constraint is **STRUCK, not reworded** — carried by one `052`-shape comment-only migration that also fixes `046:167`. **Lesson recorded in the preamble: do not assume a finding survives a ratify just because its subject still exists.** **(ii) NEW — stale Decision-3 counts entered as a POINTER ONLY** per team-lead ruling (b): the finding is canonically committed at ADR-042's Consequences and is **not restated here**, because two homes drift and hand the reader two counts. The entry carries only what ADR-042 lacks — that **`046` holds the count twice with different dispositions** (the `--` comment is unreachable by any migration; **`046:167` is a `comment on function`, a database object**, fixable by a `052`-shape migration without editing merged `046`), that **`046:167` carries three stale claims in one string**, and that **`supabase/CLAUDE.md` is Architect-owned and rides the ADR-042 migration PR rather than queueing here** — it is the one citation that will actively mislead someone tomorrow, since every other instance is a dated artifact where the point-in-time defense holds. **(iii) The section heading no longer carries an item count**, extending Sec's no-counts-in-titles ruling from entry titles to the heading — it went stale twice in one day. **(iv) CP5 self-check caught three stray `F#` citations in this file** — including inside the note that declares the cite-by-title convention — and a **CP5 catch on an incoming disposition**: `059` was cited as the slice dropping `is_active`, but ADR-042 **as merged** listed only `056`/`057`/`058`. **Resolution (Architect, confirmed): the citation was ahead of the artifact, not wrong.** Sec's two-phase adjudication split the last slice **after** ADR-042 merged, so `059` is real and the ADR's own header and build sequence were stale — corrected to four slices in the same pass. **`058` leaves `is_active` untouched; the column drops at `059`**, and the `058`→`059` quiescence window is genuine (both columns coexist while the operator dispositions accounts through the gate). Numbers now pinned. **The interim phrasing — "the slice that drops `is_active` (`058` per the ADR's own slice list)" — was verifiable-today and correct-under-either-answer, and is the pattern to reuse for anything transcribed before its underlying file settles.** **The flag was the right hook precisely because the mismatch was an ordering artifact rather than an error**, which is the case for flagging assumptions rather than resolving them silently in either direction. **(v) NEW — §7 heading-count sweep booked** as its own entry, per team-lead: §7.7's heading count went stale twice in one day, so it is dropped there and the six sibling sections are booked for the same treatment rather than left permanently divergent.] [2026-08-03 (follow-up 3) — **Sec corrections after the §7.7 batch merged (PR #299).** **(i) TIMELINE SPLIT on the V2-grantee entry — the correction that matters most in this section.** Sec had described the fail-closed backstop as *"the third obligation"* on the B5 hard-gate; **it is not a gate obligation at all.** It is a **V1 DDL requirement shipping in `059`, now** — the legible conjunct `acc2.account_id is not null and acc2.closed_at is null`, commented so removing it visibly flips the default. **Filed as a third V2 obligation it reads as deferrable to the sharing PR, and deferring it means `059` ships WITHOUT the conjunct — the exact silent flip the ruling exists to prevent. A misfiled timeline would have defeated the finding.** Correct shape: **two** gate obligations at V2 (`rd_access` withheld · reconcile the `account_select`-vs-`rd_access`-JOIN mismatch) **plus a rider** (if the reconcile is not fully discharged, the fail-closed path must become **observable**), **plus one V1 shipping requirement in `059`.** **Naming rather than numbering is what exposed it** — *ordinals conceal heterogeneity*, so three items in a numbered list read as three of the same kind when one shipped on a different timeline entirely. **(ii) CP8 added — "re-derive, don't re-read."** *After a ratify that changes a mechanism, ask not "does this still parse?" but "would this assertion still be TRUE if I derived it fresh from the new mechanism?"* Routed to `brief-drift-catch`. Sec ruled it real on the substance: **the freeze was on a bar (cheap, mechanical, re-runnable artifact), not on a number** — refusing a genuinely new class to preserve a count is the count-in-a-title liability this very entry strips. **Team-lead ratified the eighth 2026-08-03** — *"freeze the bar, not the number"*; the stopping condition was always a bar (cheap, mechanical, re-runnable artifact) and CP8 clears it. **CP8's own limit recorded with it:** re-derivation confirms each claim is true and is **silent on whether two true claims describe the same thing** — the DEFINER-referent trap (authored = 3, allowlist = 4, **both correct, and reconciling them is the defect**) is the case no checkpoint reaches, because it contains no wrong claim anywhere. **(iii) The ratify-survival lesson sharpened, and narrowed.** Of the three dispositions only **INVERSION** is a new class: *discharged* is not a failure mode (good news worth noticing so nobody redoes the work), and *kept-its-subject-lost-its-argument* is the already-named **requirement-outliving-its-precondition** variant — **do not double-file it.** Inversion is novel because **the words stayed true and the behaviour reversed**, and under V1 both forms are behaviourally indistinguishable, so **the flip leaves no footprint**. **(iv) NEW — the PRD §2.4.1 / §2.4.2 re-point booked as BLOCKED-BY `059`** (team-lead ratified sequencing 2026-08-03). Both passages describe the retired column; §2.4.1's *"unless §2.4.2's inactive-flag pattern covers the use case"* clause gets **rewritten, not deleted**, because ADR-042 **relocated** that capability (aggregator-side selection at connect time) rather than withdrawing it. **The PRD must not describe an end-state the schema has not reached** — a requirements doc naming a column that does not exist yet is worse than one naming a column about to be retired: the second is known-stale, the first is simply false. **Booked rather than held in conversation because "wait" is the disposition that gets lost.**] Edit via `/start-doc-update <slug>` per ADR-009 Decision 9.*
