# Deployment Runbook — V1 greenfield stand-up

**Owner:** DevOps. 🔒 = security-sensitive (Sec joint-review gates it). Production is Coolify on Hetzner cax21 — no other deploy surface. Three parts: **accounts → `.env` → run the scripts.** Full rationale (MEASURED findings, incident history, Sec dispositions, design history) for every row below lives in the cited script's own header comment, in [`DECISIONS.md`](../DECISIONS.md), or in the frozen archive: [`docs/archive/deployment-runbook-rationale-2026-09-20.md`](archive/deployment-runbook-rationale-2026-09-20.md). This file states the execution path only.

---

## Part 1 — Accounts

Create these before starting. Coolify and Supabase need **no account** — both are self-installed by the scripts in Part 3.

| Provider | What to create | What you copy into `.env` | Where |
|---|---|---|---|
| **Hetzner Cloud** | Account + payment method; an API Token (Read & Write permission) | `HETZNER_API_TOKEN` | console.hetzner.cloud → project → Security → API Tokens |
| **Domain registrar** (currently **Porkbun**, confirmed live via `dig NS pfindash.com`) | Account + (**not yet — skip until [BACKLOG §7.36 item 72](../BACKLOG.md) lands**) an API key pair; Porkbun has a documented DNS API. Step 9's DNS work is BY-HAND in the console today and reads no API credential. | *(nothing yet)* | porkbun.com |
| **Resend** (V1 default SMTP; SES is the documented alternative — [`docs/email-smtp-runbook.md`](email-smtp-runbook.md)) | Account + a verified sending domain | `SMTP_PASS` (API key) | resend.com |
| **Plaid** (primary provider) | Developer account; sandbox keys (for CI, separate from this `.env`) + production API access | `PLAID_CLIENT_ID`, `PLAID_SECRET` | dashboard.plaid.com |
| **SimpleFIN** (fallback provider) | Bridge access token | `SIMPLEFIN_TOKEN` | bridge.simplefin.org |
| **Financial Modeling Prep (FMP)** | API key | `FMP_API_KEY` | financialmodelingprep.com |
| **BLS (Bureau of Labor Statistics)** | Free API registration | `BLS_API_KEY` | bls.gov/developers |
| **Discord** | A server + a webhook URL for the channel that should receive deploy/health notices | `DISCORD_WEBHOOK_URL` | target channel → Settings → Integrations → Webhooks |
| **GitHub** | Already have repo admin access. Also needed: an Actions secret, an Actions variable, and an Environment (all set in Part 3, not here) | *(no `.env` value — these live in GitHub, not the box)* | this repo's Settings |

---

## Part 2 — `.env`

One file: [`scripts/provision.env.example`](../scripts/provision.env.example) — the **complete, CI-fenced contract** (`fence-operator-env-template.sh` fails the build if a script reads a name this file doesn't list). Copy it to a gitignored `.env` at the repo root and fill in the values Part 1 produced.

- **Operator-provided** (Part 1's values): `HETZNER_API_TOKEN`, `COOLIFY_ADMIN_EMAIL`/`_NAME`/`_PASSWORD` (human-chosen, not generated), `SMTP_PASS`/`SMTP_ADMIN_EMAIL`/`SMTP_SENDER_NAME`, `CI_MIGRATE_SSH_PUBKEY` (path to a keypair — see Part 3), `PDF_WORKER_SIGNING_KEY`/`WORKER_ADMISSION_SHARED_SECRET` (`openssl rand -hex 32`), `DISCORD_WEBHOOK_URL`, `FMP_API_KEY`, `BLS_API_KEY`, `PLAID_CLIENT_ID`/`PLAID_SECRET`, `SIMPLEFIN_TOKEN`.
- **Not yet needed — do not mint speculatively:** `PORKBUN_API_KEY`/`PORKBUN_SECRET_KEY` — skip until [BACKLOG §7.36 item 72](../BACKLOG.md) lands (Sec: a registrar credential is DNS control, which is cert-issuance control, which is an MITM surface — don't hold one with no consumer). Step 9 (DNS) stays a console action until then.
- **Script-written — leave blank, do not fill in by hand:** `BOX_IP`, `MIGRATOR_SERVICE_UUID`, `SUPABASE_STACK_UUID`, `MIGRATOR_TASK_UUID`, `APP_UUID`, `ETL_UUID`, `PDF_RENDER_UUID`, `PROVIDER_SYNC_UUID` — all seven UUIDs are written by `scripts/provision-vps.sh --apply` / `scripts/record-coolify-uuids.sh --apply` in Part 3.
- **Named but never set here** (box-generated or script-minted; zero effect if you set them anyway): `COOLIFY_API_TOKEN`, `JWT_SECRET`, `SUPABASE_SERVICE_ROLE_KEY` (minted LAST by `scripts/mint-supabase-jwt-keys.sh` as a JWT signed from `JWT_SECRET` — never `openssl rand`; the template's own placeholder says so) — see the template's own "BOX-GENERATED" block for why.
- `PFIN_DB_PASSWORD` is **not** in `.env` at all — generated by `scripts/db-role-handoff.sh` at Part 3's role-handoff step.

---

## Part 3 — Run

Every step: preflight first (no flag), read the plan, then `--apply`. Stop on non-zero exit. Scripts are idempotent — re-running a done step reports "already satisfies."

Row order follows original section order §1→§3→§4→§6.x→§7.1(app)→§7.2(workers)→§5(secrets)→§2(DNS)→§6.9→§8/§9/§10→GitHub CI setup, **with one deliberate deviation, flagged where it happens**: §5 (secrets push) is sequenced *after* §7.1/§7.2 create their target Coolify resources, not before §6.x — the runbook's own [ADR-072](../DECISIONS.md#adr-072)-era ratified note says this script "runs functionally after §7, not before it, despite being numbered §5," and `push-production-secrets.sh` fails closed on a missing resource. **Every BY-HAND row below is graded strictly**: "nobody scripted it yet" is a defect, not a reason, and is cited to the BACKLOG item that books the fix — the only rows that stay BY-HAND without a booking are genuine credential/account/decision moments.

| # | What | Command | Expected (last line) | Status | Reason |
|---|---|---|---|---|---|
| 1 | Provision + harden the box, install Coolify, bootstrap the admin account (§1+§3) | `scripts/provision-vps.sh --apply` | `BOX_IP=<ip>` printed | SCRIPTED | |
| 2 | Stand up the Supabase stack; mint real JWT keys (§4) | `scripts/standup.sh --apply` (wraps step 1 + this) | verification battery passes | SCRIPTED | |
| 3 | 🔒 Database bootstrap: create `pfin_owner`/`migrator`, apply migrations, create the vault decrypt view (§6.3) | `psql -U supabase_admin -f supabase/roles.sql` → `-f supabase/auth-grants.sql` → engine-backstop `REVOKE`s → `\password migrator` + `ALTER ROLE migrator LOGIN` → role-comment files → `docker compose exec migrator supabase db push --yes --db-url "$PROD_DB_URL"` → `psql -U supabase_admin -f supabase/post-step-vault-view.sql` (full command text: script headers / archive §6.3) | ownership census: every `pfin` object owned by `pfin_owner`; `bootstrap_complete = t`; exactly one decrypt view | BY-HAND | NOT YET SCRIPTED — [BACKLOG §7.36 item 75](../BACKLOG.md) books `db-bootstrap.sh`, reusing PR #846's proven piped-stdin `\password` mechanism for an OPERATOR-RUN script over SSH (the old "gated on SELF-395" reason is stale for that case — #846 falsified the premise that a hands-off credential handoff needs client-side SCRAM). Does **not** touch [ADR-072](../DECISIONS.md#adr-072) Decision 6's Scheduled-Task clause — the migrator task must never own this bootstrap |
| 4 | 🔒 Activate `pfin_etl` login (§6.1) | `scripts/db-role-handoff.sh pfin_etl --apply` | verified handoff, `PFIN_DB_PASSWORD` pushed | SCRIPTED | |
| 5 | 🔒 Activate `pfin_provider_sync` login (§6.2) | `scripts/db-role-handoff.sh pfin_provider_sync --apply` | verified handoff, `PFIN_DB_PASSWORD` pushed | SCRIPTED | |
| 6 | Create the `app` Coolify resource, deploy (§7.1) | `scripts/provision-app.sh --apply`; `scripts/deploy-app.sh --require-network --resolve-host` | resource exists, network attached, healthy | SCRIPTED | |
| 7 | Create `etl`/`pdf-render`/`provider-sync` Coolify resources, deploy (§7.2 — still devops/W-3-owned; folds into this table's shape once W-3 lands) | `scripts/provision-worker.sh --apply` (×3) | resources exist, networks attached, healthy | SCRIPTED | |
| 8 | 🔒 Push the mapped production secrets to their Coolify resources — **sequenced here, after step 6/7, see the note above** (§5) | `scripts/push-production-secrets.sh --apply` | names-only report, grouped by resource | SCRIPTED | |
| 9 | DNS — snapshot existing records, point A/AAAA at the box + `www`, lower TTL, verify propagation, assign the Coolify Domain, wait for the LE cert (§2) | registrar console (no API credential exists yet — [BACKLOG §7.36 item 72](../BACKLOG.md)) | HTTPS `200` on the assigned domain | BY-HAND | NOT YET SCRIPTED — item 72 books `assign-app-domain.sh` against Porkbun's DNS API |
| 10 | 🔒 Pre-flip gates: VETO trigger, applied-migration count, `025` presence (§6.9 steps 1–3) | (1) `select has_schema_privilege('anon','pfin','USAGE');` — expect `f`; (2) enumerate every `pfin` relation `anon` holds any grant on (archive §6.9 query (b)) — expect zero rows; (3) `select count(*) from supabase_migrations.schema_migrations;` — expect the migration-file count at the ruling's baseline (re-count if `main` advanced); (4) `select version from supabase_migrations.schema_migrations where version like '025%';` — expect exactly one row | all four checks clean | BY-HAND | one-time measurement — STOP on any violation, do not proceed to step 11; [BACKLOG §7.36 item 66](../BACKLOG.md) already books scripting this |
| 11 | 🔒 Flip the `pfin` Data-API exposure, once step 10 passes (§6.9 steps 4–6) | `scripts/coolify-env.sh set pfin-supabase-stack PGRST_DB_SCHEMAS=public,graphql_public,pfin --apply --deploy --post-check '...'` (full post-check string: script header) | exit 0 | SCRIPTED | |
| 12 | Re-establish Coolify → Discord notifications (§8) | Coolify dashboard → Notifications | test event received | BY-HAND | NOT YET SCRIPTED — [BACKLOG §7.36 item 74](../BACKLOG.md) books measuring whether Coolify's API exposes this before committing to a script |
| 13 | Smoke-test: CA-2/CA-7 reachability, TZ-1 pin, RLS isolation, PDF round-trip, ETL poll, Discord fires (§10) | see archive §10; **PENDING PR #848 (W-3)** — once merged, becomes SCRIPTED rows naming `smoke-admission-endpoint.sh`/`smoke-etl-poll.sh`/`smoke-pdf-roundtrip.sh`/`worker-scheduled-task.sh` + a §10 go/no-go table | all pass | BY-HAND (pending #848) | one-time measurement; ship-block gate on step 14; RLS/auth rows stay QA-owned |
| 14 | Cutover: confirm step 13 is green, then tear down the incumbent `pfindash.com` stack (§9) | F/CTO go/no-go (future: `provision.sh --confirm-cutover`, [BACKLOG §7.36 item 76](../BACKLOG.md)) | incumbent retired | BY-HAND | **one-way door — F/CTO decision, not a probe; deliberately never bundled into a bare `--apply`** |
| 15 | 🔒 CI-trigger keypair + box-side wiring (`ci-migrate` user, forced command, scoped Coolify token) (§6.4) | `ssh-keygen -t ed25519 -N '' -f ~/.ssh/id_ed25519_ci_migrate` (check-and-create, non-interactive); `scripts/provision-vps.sh --apply` (re-run, with `CI_MIGRATE_SSH_PUBKEY`/`MIGRATOR_SERVICE_UUID`/`MIGRATOR_TASK_UUID`/`APP_UUID` set) | keypair present; idempotent apply completes | SCRIPTED-IN-W-5 | folds into [BACKLOG §7.36 item 76](../BACKLOG.md) (`scripts/provision.sh`) — non-interactive already, no reason this needs a human once the wrapper exists |
| 16 | 🔒 GitHub-side CI setup: Actions secret, Actions variable, `production-migrator` Environment + required reviewer (§6.4) | `gh secret set CI_MIGRATE_SSH_PRIVATE_KEY`; `gh variable set PROD_SSH_HOST`; `gh api .../environments/production-migrator` (reviewer rule) | all three saved | BY-HAND | NOT YET SCRIPTED — [BACKLOG §7.36 item 73](../BACKLOG.md) books `github-ci-setup.sh` |
| — | GitHub Environment reviewer approval, **every** migrator trigger fire (push or `workflow_dispatch`), from step 16 onward | Actions tab → Review deployments | approved | BY-HAND | interactive-credential moment — Sec-ruled required-reviewer gate, recurs on every fire; explicitly out of scope for item 73's script |

**§4.1's TimeZone-pin verification (Sec-gated CI anchor — this exact block is fenced by `scripts/ci/check-tz-sweep-identical.py`, kept token-identical to (T3) in `supabase/tests/01_session_timezone.sql`; do not reword):**
```sh
psql "$PROD_DB_URL" -Atc \
  "select r.rolname, d.datname, c as setting
     from pg_db_role_setting s
     join pg_roles r on r.oid = s.setrole
     left join pg_database d on d.oid = s.setdatabase
     cross join lateral unnest(s.setconfig) as c
    where s.setrole <> 0
      and c ilike 'timezone=%'"
```
Required: zero rows. Run after step 3, before step 13. `PGRST_DB_SCHEMAS` ruled literal ([ADR-023](../DECISIONS.md#adr-023)): `PGRST_DB_SCHEMAS=public,graphql_public,pfin`.

**Done looks like:** the `app` URL answers over TLS; step 13's smoke gate is fully green; Discord received at least one deploy notification.

**Resuming after a stop:** no `--from <step>` flag exists yet — [BACKLOG §7.36 item 76](../BACKLOG.md) books `scripts/provision.sh [--from <step>] [--confirm-cutover]`, the orchestrator this table is the interim spec for. Until it lands, every script above is preflight-safe and idempotent — re-run from the step that failed; confirm its preflight output shows the prior steps already satisfied before passing `--apply`.

**Unavoidable manual moments — every other BY-HAND row above is a booked gap, not a justification:**
- Every credential Part 1 names (account creation, API token minting) — no script can create an account for you.
- Step 14 (cutover) — a one-way F/CTO decision, not a probe; deliberately never scripted into a bare `--apply`.
- The GitHub Environment reviewer approval — recurs on **every** migrator trigger fire, Sec-ruled, explicitly out of scope for any automation.

---

**§7 (Workers)** moved to the archive, verbatim; folds into this table once W-3 (PR #848) lands.

**§11 (GDPR erasure)** is an unbuilt operational routine, not a stand-up step — archive §11.

Everything else — MEASURED findings, incident history, Sec dispositions, every command this table abbreviates, the full §1–§11 narrative as it stood before this conversion — is in [`docs/archive/deployment-runbook-rationale-2026-09-20.md`](archive/deployment-runbook-rationale-2026-09-20.md).
