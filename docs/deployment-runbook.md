# Deployment Runbook — V1 greenfield stand-up

**Owner:** DevOps. 🔒 = security-sensitive (Sec joint-review gates it). Production is Coolify on Hetzner cax21 — no other deploy surface. Three parts: **accounts → `.env` → run the scripts.** Full rationale (MEASURED findings, incident history, Sec dispositions, design history) for every row below lives in the cited script's own header comment, in [`DECISIONS.md`](../DECISIONS.md), or in the frozen archive: [`docs/archive/deployment-runbook-rationale-2026-09-20.md`](archive/deployment-runbook-rationale-2026-09-20.md). This file states the execution path only.

---

## Part 1 — Accounts

Create these before starting. Coolify and Supabase need **no account** — both are self-installed by the scripts in Part 3.

| Provider | What to create | What you copy into `.env` | Where |
|---|---|---|---|
| **Hetzner Cloud** | Account + payment method; an API Token (Read & Write permission) | `HETZNER_API_TOKEN` | console.hetzner.cloud → project → Security → API Tokens |
| **Domain registrar** (currently **Porkbun**, confirmed live via `dig NS pfindash.com`) | Account + (**not yet — skip until [BACKLOG §7.36 item 72](../BACKLOG.md) lands**) an API key pair; Porkbun has a documented DNS API. Step 9's DNS work is BY-HAND in the console today and reads no API credential. | *(nothing yet)* | porkbun.com |
| **Resend** (V1 default SMTP; SES is the documented alternative — [`docs/email-smtp-runbook.md`](email-smtp-runbook.md)) | Account + a verified sending domain | `SMTP_PASS` (API key) | resend.com |
| **Plaid** (primary provider) | Developer account; sandbox keys (for CI, separate from this `.env`) + production API access | `PLAID_CLIENT_ID`, `PLAID_SECRET` | dashboard.plaid.com |
| **SimpleFIN** (fallback provider) | Bridge access token | `SIMPLEFIN_TOKEN` | bridge.simplefin.org |
| **Financial Modeling Prep (FMP)** | API key | `FMP_API_KEY` | financialmodelingprep.com |
| **BLS (Bureau of Labor Statistics)** | Free API registration | `BLS_API_KEY` | bls.gov/developers |
| **Discord** | A server + a webhook URL for the channel that should receive deploy/health notices | `DISCORD_WEBHOOK_URL` | target channel → Settings → Integrations → Webhooks |
| **GitHub** | Already have repo admin access. Also needed: an Actions secret, an Actions variable, and an Environment (all set in Part 3, not here) | *(no `.env` value — these live in GitHub, not the box)* | this repo's Settings |

---

## Part 2 — `.env`

One file: [`scripts/provision.env.example`](../scripts/provision.env.example) — the **complete, CI-fenced contract** (`fence-operator-env-template.sh` fails the build if a script reads a name this file doesn't list). Copy it to a gitignored `.env` at the repo root and fill in the values Part 1 produced.

- **Operator-provided** (Part 1's values): `HETZNER_API_TOKEN`, `COOLIFY_ADMIN_EMAIL`/`_NAME`/`_PASSWORD` (human-chosen, not generated), `SMTP_PASS`/`SMTP_ADMIN_EMAIL`/`SMTP_SENDER_NAME`, `CI_MIGRATE_SSH_PUBKEY` (path to a keypair — see Part 3), `PDF_WORKER_SIGNING_KEY`/`WORKER_ADMISSION_SHARED_SECRET` (`openssl rand -hex 32`), `DISCORD_WEBHOOK_URL`, `FMP_API_KEY`, `BLS_API_KEY`, `PLAID_CLIENT_ID`/`PLAID_SECRET`, `SIMPLEFIN_TOKEN`.
- **Not yet needed — do not mint speculatively:** `PORKBUN_API_KEY`/`PORKBUN_SECRET_KEY` — skip until [BACKLOG §7.36 item 72](../BACKLOG.md) lands (Sec: a registrar credential is DNS control, which is cert-issuance control, which is an MITM surface — don't hold one with no consumer). Step 9 (DNS) stays a console action until then.
- **Script-written — leave blank, do not fill in by hand:** `BOX_IP`, `MIGRATOR_SERVICE_UUID`, `SUPABASE_STACK_UUID`, `MIGRATOR_TASK_UUID`, `APP_UUID`, `ETL_UUID`, `PDF_RENDER_UUID`, `PROVIDER_SYNC_UUID` — all seven UUIDs are written by `scripts/provision-vps.sh --apply` / `scripts/record-coolify-uuids.sh --apply` in Part 3.
- **Named but never set here** (box-generated or script-minted; zero effect if you set them anyway): `COOLIFY_API_TOKEN`, `JWT_SECRET`, `SUPABASE_SERVICE_ROLE_KEY` (minted LAST by `scripts/mint-supabase-jwt-keys.sh` as a JWT signed from `JWT_SECRET` — never `openssl rand`; the template's own placeholder says so) — see the template's own "BOX-GENERATED" block for why.
- `PFIN_DB_PASSWORD` is **not** in `.env` at all — generated by `scripts/db-role-handoff.sh` at Part 3's role-handoff step.

---

## Part 3 — Run

One script. `.env` is Part 2 — copy `scripts/provision.env.example` to `.env` first.

```sh
scripts/provision.sh --dry-run   # preflight only, touches nothing, always exits 0
scripts/provision.sh             # the real run
```
Neither needs a `BOX_IP=` prefix — step 1 discovers/creates the box and writes `BOX_IP` into `.env` itself. `--dry-run` prints one line per step (`VERIFIED`/`SKIPPED`/`MANUAL`/`would likely fail`, each `(dry-run)`); `--list` prints the plan without running it.

**Done looks like:** every step through `remaining-checks`/`discord` reads `VERIFIED`/`SKIPPED` in the printed summary table, `pfin-app` answers over TLS, and the run stops at the last step, `cutover` (exit 1) — see below.

**If it stops:** it prints `resume: scripts/provision.sh --from <step>` plus the summary so far. Fix that step, re-run that line.

**Three unavoidable manual moments:**
1. **Account creation** (Part 1) — before running; no script creates an account.
2. **Cutover** — `cutover` refuses without `--confirm-cutover`, printing the exact re-run line. Even confirmed, it still stops (exit 1): tear down the incumbent `pfindash.com` stack by hand, then confirm — a one-way F/CTO call, never scripted, never bundled into a bare `--apply`.
3. **GitHub Environment reviewer approval** — not a `provision.sh` step (its `ci-keypair`/`github-ci` steps only wire the gate up; `db-bootstrap` applies the *initial* migration directly over SSH, bypassing it). Recurs on every **future** `ci-migrate` CI fire, Sec-ruled, out of scope for any script. ⚠ A fire-and-poll mode inside `provision.sh` (dispatch, then print/wait on the review URL instead of pointing at the Actions tab) was scoped this pass and deliberately deferred — too risky to build without a live box to verify against — [BACKLOG §7.36 item 73](../BACKLOG.md) carries the follow-up.

**🔒 §4.1's TimeZone-pin verification (Sec-gated CI anchor — this exact block is fenced by `scripts/ci/check-tz-sweep-identical.py`, kept token-identical to (T3) in `supabase/tests/01_session_timezone.sql`; do not reword):**
```sh
psql "$PROD_DB_URL" -Atc \
  "select r.rolname, d.datname, c as setting
     from pg_db_role_setting s
     join pg_roles r on r.oid = s.setrole
     left join pg_database d on d.oid = s.setdatabase
     cross join lateral unnest(s.setconfig) as c
    where s.setrole <> 0
      and c ilike 'timezone=%'"
```
Required: zero rows. Run after `db-bootstrap`, before `remaining-checks` (`provision.sh`'s own `remaining-checks` step names this query; it is one of the four checks that step still reports BY-HAND). `PGRST_DB_SCHEMAS` ruled literal ([ADR-023](../DECISIONS.md#adr-023)): `PGRST_DB_SCHEMAS=public,graphql_public,pfin`.

---

**§11 (GDPR erasure)** is an unbuilt operational routine, not a stand-up step — archive §11.

Everything else — MEASURED findings, incident history, Sec dispositions, every command this table abbreviates, the full §1–§11 narrative as it stood before this conversion — is in [`docs/archive/deployment-runbook-rationale-2026-09-20.md`](archive/deployment-runbook-rationale-2026-09-20.md).
