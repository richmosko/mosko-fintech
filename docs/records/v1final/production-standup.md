# Production stand-up (SELF-386 / B.5 — Phase 7 entry; V1.final month-1 precondition)

DevOps-owned. This record is the single authority `docs/records/v1final/self365-protocol.md` §B.5 names for **deploy date**, **deployed sha**, **tenant-accounts-live date**, and the two derived months (**M0**, **M1**) — B.1 and B.3 read these fields, nothing else derives them. This session (RESEARCH + PLAN only, per team-lead brief) resolves the three `⟨OPEN⟩ DevOps` markers and produces the stand-up plan. **No deploy has happened.** The table and log below are opened empty and filled at first deploy / first tenant-accounts-live event.

---

## Authority table (empty — fill at events)

| Field | Value |
|---|---|
| Deploy date (repo clock, `-0700`) | *(not yet deployed)* |
| Deployed sha | *(not yet deployed)* |
| Tenant-accounts-live date | *(not yet live)* |
| M0 (deploy month; never counts) | *(derived from deploy date)* |
| M1 (first full calendar month after tenant-accounts-live date) | *(derived from tenant-accounts-live date)* |

## Deploy log (one line per deploy — empty; opens at first deploy)

| Date (`-0700`) | Sha | Components (health) | Notes |
|---|---|---|---|
| — | — | — | — |

---

## 1. OPEN-1 resolved — which Coolify record DevOps reads the running sha from

**Recommendation: the Coolify API's per-application `git_commit_sha` field, cross-checked against the deployment-history endpoint's per-deployment commit field, both read at each deploy and transcribed into the log above — never held as a memory.**

Two candidate mechanisms exist:

1. **Coolify's own deployment record (API or UI).** Coolify persists the resolved git commit for each deployment ( `GET /api/v1/applications/{uuid}` → `git_commit_sha` field on the application resource; `GET /api/v1/deployments/applications/{uuid}` → per-deployment commit history). The UI's Deployments tab shows the same data. **Recommended primary.** It is Coolify's own resolution of what it built and pushed to the running container — an observable of the deploy action, not a build-time artifact V1's own Dockerfiles would need to carry.
2. **A container label / image tag baked at build time** (e.g. an `org.opencontainers.image.revision` `LABEL`, or a build-arg embedding the sha, set from a Coolify build variable and inspectable via `docker inspect`). Requires adding a build-arg + `LABEL` line to all four Coolify-managed Dockerfiles (`api/`, `workers/etl/`, `workers/pdf-render/`, `workers/provider-sync/`) and confirming Coolify actually populates the build-arg it document (Coolify names this `COMMIT_SHA` or similar depending on version — unconfirmed until read on the real instance).

**Why (1) over (2):** Coolify tracks and exposes the deployed commit for every managed application natively — no per-Dockerfile change, no risk of one of the four containers being wired and the others forgotten, and no dependency on a Coolify build-arg name that is version-specific and currently unverified against this instance. (2) is a reasonable defense-in-depth addition later (a label surviving inside the running container even if the Coolify record were ever wrong or unavailable), but it is not the primary mechanism — flagged as a Phase-7-or-later hardening item, not a stand-up blocker.

**Verify at first deploy:** for each of the four Coolify-managed services, read `git_commit_sha` via the API (or the Deployments tab) immediately after deploy and confirm it equals `git rev-parse HEAD` of the `main` tip that triggered the auto-deploy (per ARCH §6, Coolify auto-deploys on push to `main` after CI green). Record the confirmed sha in the deploy log above — the log line is a transcription of this read, never typed from memory. If the four services ever report different shas (a partial deploy), the log records each service's sha separately and the AC1 "all healthy at one sha" bar is not met until they converge.

**Source could not fully settle this in-tree** — the runbook (`docs/deployment-runbook.md` §3, §7) names Coolify as the control plane and documents zero of its API surface; this recommendation is sourced from Coolify's public docs/API behavior (see PR body / Linear comment for citations — not restated here per hand-off convention), not from anything already committed to this repo.

---

## 2. OPEN-2 resolved — Plaid production-access lead time, product-tier coverage, and team provenance (fully resolved 2026-09-08 via direct dashboard read)

**Confirmed, from Plaid's own current public materials (dated 2026; direct fetch of `support.plaid.com` pages returned HTTP 403 — Cloudflare-gated — so these are sourced via search-engine synthesis of Plaid's docs/support content, not a directly-fetched quote; treated as confirmed only where corroborated across independent results):**

- **Plaid retired "Limited Production" self-serve signups for new US/Canada developers as of 2026-04-15.** New developer teams (any created on or after that date) instead get a **Trial plan**: free, real production data, auto-approved for most applicants, up to **10 Production Items**, and — this is the material fact for V1's product set — **both Transactions and Investments are included** among the Trial plan's supported products, alongside Auth/Balance/Identity/Assets/Liabilities/Statements. Existing Limited Production customers (teams created before the cutover) keep their existing access.
- **The Trial plan also grants access to most OAuth institutions** (Bank of America, Chase, Wells Fargo named explicitly in Plaid's materials) without first completing full Production registration.
- **Moving from Trial to a paid Production plan** is done from the Dashboard's Plans page ("upgrade to a paid plan to create more Production Items… at any time") — no evidence of a lengthy approval gate for that upgrade path specifically.
- **Full (non-Trial) Production access application** — the older, general path — asks for company/use-case information; pricing for Pay-as-you-go/Growth plans is shown at application submission, Custom plans go through sales. **No authoritative lead-time figure could be confirmed** from a directly-fetched Plaid page (both attempted `support.plaid.com` fetches 403'd); one search-engine synthesis surfaced "a couple of business days" but that number is **unconfirmed** — do not commit to it as a planning input.

**RESOLVED 2026-09-08 — team-lead's direct read of F/CTO's Plaid dashboard (supersedes the "unconfirmed" framing this section previously carried for the team-provenance question):**

- **The mosko-fintech Plaid team is a brand-new, post-cutover team — OPEN-2's original question is answered, not just narrowed.** The prior developer team was Pay-As-You-Go and held **nine orphaned production Items whose access tokens were already lost** (no live credential exists to reattach any of them to anything). F/CTO **deleted that team outright on 2026-09-08 and created a new one**: fresh **Trial plan**, **10 free Production Items**, **new `client_id`/`secret`** (values never entered into the repo, this record, or any agent conversation). This is unambiguously a team created **after** the 2026-04-15 cutover — the Trial-plan analysis above (Transactions + Investments included, most OAuth institutions reachable, no Limited-Production legacy framing) **applies in full, not conditionally**. The B.5 AC-2 "limited-production tier" wording remains stale terminology per finding #7 below, now confirmed rather than inferred.
- **The 10-Item cap is live against this new team starting from zero, and removed Items do not refund a slot.** A Production Item counts against a Trial team's cap for the life of the team regardless of later disconnection — there is no "9 already spent, 1 left" arithmetic carried over from the deleted team (that team and its Item count no longer exist), but there is equally no assumption that a test-then-disconnect Item during stand-up is free. **This sharpens, not just confirms, the constraint that already justified striking old step 7** (see §4 below): any Item created against this team's Production keys — including one created by mistake during testing — permanently consumes one of the 10 slots.
- **Whether every one of the F/CTO tenant's real institutions is reachable under Trial/Plaid at all.** Per ADR-027 (2026-07-08 empirical test), **Fidelity is already routed to SimpleFIN, not Plaid** — general aggregators (Plaid included) do not reliably return Fidelity transactions; this is a known, already-designed-around limitation, not new risk. The remaining institutions (Schwab, Capital One, Wells Fargo, Synchrony per the ADR-002 Items pricing worksheet) are the ones that need to work under Plaid Trial/Production. Wells Fargo is explicitly named as Trial-OAuth-supported in Plaid's public materials. **Schwab's status could not be confirmed** — one 2025-11 industry piece (not a Plaid primary source) describes an active dispute between several large banks (including Schwab and Fidelity) and data aggregators over account-data-access terms; whether this affects Plaid's ability to serve Schwab data by V1's stand-up window is **unconfirmed** and is a genuine external risk, not something DevOps can resolve from documentation. **Surfaced to F/CTO as a scope fact per the AC's own instruction ("if the tier gates any V1 product, that is an F/CTO scope fact, surfaced, not worked around") — recommend a Plaid Link smoke test against the F/CTO's actual Schwab login be run early in the stand-up sequence (step 9 below), not deferred to the tenant-accounts-live step, so a gap surfaces while there is still schedule slack.**
- **Citation correction:** the team-lead brief and (following it) casual usage attribute "Plaid PRIMARY, SimpleFIN SECONDARY" and the Transactions+Investments product set to **ADR-027**. Checked against the tree: **ADR-027** (2026-07-08) built the provider-agnostic abstraction and *explicitly deferred* provider selection to a later gate; **ADR-037** (2026-07-28) is the ADR that actually **ratifies** Plaid-primary/SimpleFIN-secondary (its own title says so) and resolves ADR-027's gate (i). The Transactions+Investments product-set commitment traces to **ADR-002 §6.0 / PRD §1.3** (Phase 1), not to ADR-027. This is a citation-attribution drift worth fixing wherever "ADR-027 (Plaid PRIMARY)" is repeated — the *conclusion* used in this record is correct, only the ADR number is wrong in the inherited phrasing.

---

## 3. OPEN-3 resolved — every gate the runbook's STUB sections need beyond §7.15 / §7.6 S5 / S10

**§7.6 S10 status check:** already ✅ DISCHARGED 2026-08-09 (capability-verify of `compute_and_checkpoint_user` under a real `pfin_etl` login) — not an open gate. Its one named residual (TLS transport never exercised end-to-end, S11 cross-ref) is a real gap but is TLS-transport verification, which is subsumed by this checklist's item 9 below (the stand-up's first real TLS-terminated deploy).

Checklist (✓ = already satisfied by a shipped migration/mechanism and only needs a deploy-time confirmation; ⚠ = genuinely new work or a decision the runbook flags as unresolved):

| # | Gate | Owner | Status |
|---|---|---|---|
| 1 | §7.15 NAV-chart render-latency (800ms/1.5s p95) | QA (measure) + DevOps (environment) | Deploy blocker for the chart surface only, not the whole stand-up |
| 2 | §7.15 NAV-delta-panel latency (200ms p95) | QA + DevOps | Deploy blocker for the panel surface only |
| 3 | §7.6 S5 — `pfin_provider_sync` dedicated NOINHERIT login role (supersedes `pfin_worker`), provisioned in the same pass as `pfin_etl` | Architect (role migration) + DevOps (secrets-manifest/.env.example/runbook) + **Sec joint-review mandatory** | ⚠ Not yet built — role migration does not exist on the tree today (grep-verified: `055_pfin_etl_role.sql` exists; no `pfin_provider_sync` migration does). **Blocks AC1** (`provider-sync` cannot run off `authenticator` under the S5-superseded posture without re-opening the shared-credential finding S5 exists to close) |
| 4 | §5 Secrets-provisioning procedure + rotation | DevOps + **Sec joint-review mandatory at lock** | ⚠ STUB — procedure not written; this is standing work, not new to this record |
| 5 | §6.1 `pfin_etl` two-step credential handoff (`\password` then `ALTER ROLE … LOGIN`, in that order) + the `rolcanlogin`/`rolinherit` read-back | DevOps (executes) | ✓ Fully specified and Sec-ruled already (flag #10 resolved); needs execution + read-back at deploy, not new design |
| 6 | §7 provider-sync **CA-1** — deploy-time verification that the admission-endpoint's env-var prefix regex actually matches this Coolify version's real injected FQDN/URL var names | DevOps | ⚠ Named "at first deploy, and after any Coolify upgrade" — must run every time, not a one-shot |
| 7 | §7 provider-sync **CA-4** — api/ and provider-sync MUST be co-located in the same Coolify project (internal DNS dependency) | DevOps | ⚠ A deploy-config discipline with no automated guard other than the §10 CA-2 smoke below |
| 8 | §7 `PDF_WORKER_SIGNING_KEY` length (≥32 chars) + same-value-on-both-containers precondition, verified BEFORE the PDF worker's first deploy | DevOps | ⚠ Manual pre-deploy check; asymmetric failure mode is already documented, not yet gated by tooling |
| 9 | §10 **CA-2** — admission-endpoint external-reachability NEGATIVE smoke (provider-sync `:8081` unreachable from outside the project network) + the positive internal-reach control | DevOps (infra-reachability) + QA (RLS/cross-tenant legs of the same gate family) | ⚠ Named ship-block; unexecuted (no deployment exists yet) |
| 10 | §10 **TZ-1** — DB TimeZone pin read-back (`source = database`, not `configuration file`), run as each login role (`authenticator`, `pfin_etl`), plus the provenance limb (`schema_migrations` row for `061`) | DevOps | ✓ Migration `061_pin_database_timezone_utc.sql` **already exists on the tree** (verified — see finding below; the runbook's own Open-flags row #11 is stale, claiming the migration "needs to be authored") — this gate is a deploy-time read-back of an already-shipped mechanism, not new design |
| 11 | §10 **TZ-1b** — wire the R3 TimeZone-drift-sweep Coolify Scheduled Task (ratified 2026-08-06, deliberately deferred to Phase 7) and confirm one run reports to Discord, before declaring stand-up complete | DevOps | ⚠ Ratified design, unbuilt — "a decided thing awaiting a box" per the runbook's own words. Also confirm the α premise (does a non-zero exit from a Coolify post-deploy command actually fail the deployment, or only log?) — the runbook flags this as answerable **today**, independent of the new box, and unresolved |
| 12 | §10 end-to-end smoke checklist (TLS reachability; auth login; two-tenant RLS isolation; a migration-backed query; PDF render round-trip via signed JWT; ETL runs one poll; Discord notification fires) | DevOps (infra) + QA (RLS/isolation) | ⚠ STUB — no explicit pass/fail criteria written yet; needed before this AC's item 3 ("deploy gates walked") can be marked pass |
| 13 | §11 GDPR/user-deletion detach-then-cascade erasure routine (journal-grouping `ON DELETE RESTRICT` interaction) | Backend (build) + Sec (joint-review at build) | Not a month-1 precondition (no erasure will be run against the F/CTO's own tenant at stand-up) — recorded so it is not silently assumed built; **not scoped into this AC** |
| 14 | Container Dockerfile status reconciliation | DevOps | Finding, not a gate: the runbook (§7) still describes the PDF worker Dockerfile as "currently a placeholder" — the tree shows a 108-line Dockerfile with lock-anchored comments, marked "EXTENDED, not scaffolded" per SELF-348. Runbook text is stale; no action blocks stand-up, but the runbook should be corrected at the same time §5/§6/§7 STUBs are filled in |
| 15 | **NEW (F/CTO fact, 2026-09-08) — production-key safety guard.** No quickstart, probe script, or sandbox-by-accident run is ever pointed at this new Plaid team's Production `client_id`/`secret`; every test/dev exercise uses Plaid's **Sandbox** environment; every Production Item's access token is written to Vault at exchange time (already the design — no new mechanism, a verification gate on the existing one) | Backend (execution discipline) + DevOps (env wiring — `PLAID_ENV` set correctly per container) + Sec (verify at sign-off) | ⚠ NEW gate, named 2026-09-08. Not yet executed — the new team holds zero Items as of this record; the risk is real starting the moment anyone runs code against it, since the 10-Item Trial cap (row above / OPEN-2) does not refund a slot for a test-created Item |
| 16 | **NEW (F/CTO ruling 2026-09-09, Q5) — public signup stays OFF; standing gate, not a stand-up step.** Turning `GOTRUE_DISABLE_SIGNUP` back off (opening production public signup) is **blocked** until an operator allowlist exists on the Plaid Link-token route — booking pending in PM's round 3 PR on `docs/records/v1final/standup-preconditions.md` (not yet merged as of this record's edit; PM's Q5 in that record was still open/unruled at Round 2 (`docs/records/v1final/standup-preconditions.md` §F), so this ruling supersedes it). This gate does **not** close at stand-up completion — it stays open through the full V1.final soak; F/CTO's invited tenant (step 5a) is the only account for the duration | DevOps (holds the gate: `GOTRUE_DISABLE_SIGNUP=true` stays set) + PM (owns the allowlist booking) + Backend (builds it, once scheduled) | ⚠ Standing gate — deliberately not scheduled to discharge inside this stand-up's step range |

**Explicitly excluded from this AC (per B.5's own scoping), not omitted by oversight:** §9 cutover/teardown of `pfindash.com` and backup/restore validation are Phase-7 **exit** criteria, not month-1 preconditions.

**Finding — B.5 AC1's deployed-component enumeration is incomplete.** AC1 lists "the app, the ETL worker (`nav_daily`), the PDF worker, and the monthly-report cron container" as the four things Coolify must run at one sha. This **omits the `provider-sync` worker** — the 4th Coolify unit per the ADR-019 amendment, and per ARCH §5 the **sole code-layer holder of `PLAID_CLIENT_ID`/`PLAID_SECRET`**. AC2 ("Plaid production credentials obtained and applied… one Link session against a real institution succeeds") is structurally unsatisfiable without provider-sync deployed and healthy — the web-app reaches Plaid only by relaying through provider-sync's internal admission surface. Recommend AC1's component list be corrected to five: app, ETL worker (incl. `nav_daily` + the monthly-report cron Scheduled Task run against it), PDF worker, and provider-sync worker (incl. the daily-poll Scheduled Task + the admission endpoint). Also, "the monthly-report cron container" is imprecise — per the runbook's own "Pattern A" convention, the monthly-report cron is a Scheduled Task executed against the resident **ETL** container, not a separate container; there is no 4th "cron container" distinct from the ETL and provider-sync units.

---

## 4. The stand-up plan

**Ordered steps, today (2026-09-08) → tenant accounts live in production.** Each row: owner, artifact produced, what blocks it.

| # | Step | Owner | Artifact | Blocked by |
|---|---|---|---|---|
| 1 | F/CTO decides hosting target (§5 options below) | F/CTO | ✅ **Decided 2026-09-08: Option A, CAX21 class, corrected spec** — recorded in this file §5 + runbook §1 | Nothing — decided |
| 2 | Author `pfin_provider_sync` role migration (S5) | Architect | New migration; Sec joint-review | Step 1 not required first; can run in parallel |
| 3 | Provision VPS + install Coolify (runbook §1, §3) | DevOps | Running Coolify instance | Step 1 |
| 4 | DNS / domain decision + records (§2) | F/CTO (decision) + DevOps (execution) | DNS records live | Step 1 (needs the box's IP) |
| 5 | Stand up self-hosted Supabase (production compose with `studio`/`meta` sections removed per §5's compose-trimming finding, unless F/CTO names a reason to keep Studio); apply migrations incl. `061` TimeZone pin + `055`/S5's new role migration; run the `pfin_etl` + `pfin_provider_sync` two-step credential handoffs (§6/§6.1) | DevOps | Migrated DB; both dedicated login roles live; trimmed compose running | Steps 2, 3 |
| 5a | **NEW (F/CTO ruling 2026-09-09, Q5) — disable production Auth signup; create the F/CTO tenant by invitation.** Set `GOTRUE_DISABLE_SIGNUP=true` on the production `auth` (GoTrue) container's Coolify env (self-hosted GoTrue's own signup-disable switch — confirmed from the `supabase/auth` repo's `example.env`, "Signup config" section; default `false`, this deploy sets `true`). This is a container env var, distinct from the local-dev-only `supabase/config.toml` `[auth] enable_signup` / `[auth.email] enable_signup` flags (both `true` at `supabase/config.toml:181,228`) — the CLI-generated local compose reads those; the production self-hosted `docker-compose.yml` reads `GOTRUE_DISABLE_SIGNUP` from its own env block, and the two are not the same binding (setting the local `config.toml` value does not touch production, and vice versa). F/CTO's own account is created by **invitation**, not public signup: the Auth Admin API's `inviteUserByEmail` (Supabase JS `supabase.auth.admin.inviteUserByEmail`, service-role-key-gated, calling GoTrue's admin invite path) — confirmed runnable as a **standalone script/API call against a self-hosted instance**, no Studio dependency, per Supabase's own JS reference doc for that method. This is the correct mechanism given step 5's compose trims `studio` out of production — the Studio "Invite user" button is only a UI wrapper around the same Admin API call, so removing Studio does not remove the invite capability, only its point-and-click form. **Verification:** with `GOTRUE_DISABLE_SIGNUP=true` live, a direct signup attempt against the production `/auth/v1/signup` endpoint (e.g. `curl` or the app's own signup form) is refused (GoTrue returns its signup-disabled error, not a created session) — run once after step 5 and recorded pass/fail in this file. | DevOps (env wiring + verification) + F/CTO (runs the invite script/call for their own tenant) | `GOTRUE_DISABLE_SIGNUP=true` confirmed live; refused-signup verification recorded; F/CTO tenant created via invite, not public signup | Step 5 (same Supabase stand-up pass); gates step 12 (F/CTO's own account must exist before "connects every account") |
| 6 | Deploy the four Coolify services (app, ETL, PDF worker, provider-sync) from one `main` sha; inject secrets (§5); run CA-1/CA-4/PDF-key preconditions | DevOps | First deploy-log line in this record (OPEN-1 mechanism) | Step 5; Sec joint-review of §5 secrets-provisioning procedure |
| 7 | ~~Directly register F/CTO's 9 existing Plaid Items~~ — **STRUCK 2026-09-08.** F/CTO deleted the old Plaid team (its 9 Items were already orphaned, tokens lost) and created a new Trial team from zero; there is nothing left to adopt, so this step has no work to do. **Historical backfill of several years of categorized transactions for F/CTO's accounts, walked with F/CTO** — unchanged content from the old step 8, promoted here per the ruled sequencing (deploy → backfill walk → attach-capable Link → Plaid connection) | Owner TBD — **spec pending (PM/Backend audit 2026-09-08)** | Backfilled + categorized transaction history for F/CTO's accounts, in **manual** accounts (no provider attached yet) | Step 6 |
| 8 | **REPLACES the struck direct-register step.** Build "attach a provider-linked account to an existing manual account at Link time" — uses the already-shipped `pfin.account.linked_source_id` / `provider_account_id` / `backfill_cutover_date` columns; the account mapper currently only **inserts** new accounts and needs an update path to attach instead | Backend — **spec pending (PM scoping now, 2026-09-08)** | Attach-at-Link-time capability deployed | Step 6 for deploy; **independent of Step 7** — can proceed in parallel with the backfill walk, but per the ruled sequencing must be live before Step 9 runs; **Sec joint-review mandatory** (new INSERT+UPDATE mapper path against `pfin.account`) |
| 9 | Confirm Plaid environment = `production`; run Plaid Link session(s) for F/CTO's real institutions (Schwab first, per the unconfirmed-coverage risk in OPEN-2), using Step 8's attach capability so each linked account maps onto its already-backfilled manual account rather than creating a duplicate; observe the production-key safety guard (OPEN-3 gate #15 — Sandbox only for any test exercise, Vault write at token exchange) | F/CTO (owns the real credentials/login) + DevOps (wiring) | AC2 satisfied; provider accounts attached, not duplicated | Steps 7 and 8 both — backfill must exist and attach capability must be live before any real institution connects |
| 10 | Run §10 smoke checklist (TLS, auth, RLS two-tenant, migration-backed query, PDF round-trip, ETL poll, Discord notify) + CA-2 negative/positive reachability + TZ-1/TZ-1b (incl. wiring the drift-sweep Scheduled Task) | DevOps (infra) + QA (RLS legs) | Recorded pass/fail per gate in this file | Step 6 (runs independently of steps 7–9) |
| 11 | Sec pre-production sign-off (WORKFLOW Phase 7 agents list) | Sec | Sign-off attached/cited in this file | Steps 3, 5, 6, 10's Sec-mandatory sub-gates all green, plus **Step 8's mandatory joint-review of the attach-at-Link-time mapper change** and **gate #15's production-key safety guard (§3)** — do not assume clear until the attach-build review has actually happened, not just been scheduled |
| 12 | F/CTO signs up in production (ADR-036 open-signup + email-confirmation), connects every Plaid/SimpleFIN account, enters every manual account the monthly review depends on | F/CTO | Tenant-accounts-live date recorded; M0/M1 derived | Steps 1–11 all closed |
| 13 | Handoff: liaison fills B.3's month placeholders from step 12's date; Backend runs the M0 historical-completeness check | Backend + liaison | `docs/records/v1final/a-m0-completeness.md` | Step 12 |

**Critical path to the earliest M1 (per §D's calendar) — recomputed 2026-09-08, evening, with the F/CTO's Plaid-team facts and the ruled sequencing (deploy → backfill walk → attach-capable Link → Plaid connection → month-1 clock) applied:** the chain is steps 1→12 above (tenant-accounts-live still at position 12), then §D's own downstream chain (M1 whole → cron fires 1st of M1+1 → same-day authoring/export). **Neither of the two new steps has a duration estimate yet, and that has not changed from the last round — say so plainly rather than implying progress that didn't happen:**
- **Step 7 (historical backfill walk)** — still **spec pending (PM/Backend audit 2026-09-08)**, no duration estimate. A years-deep, F/CTO-walked, categorized transaction backfill reads as multi-day-to-multi-week work by its own description, not a same-day checklist item.
- **Step 8 (attach-at-Link-time build)** — this is new work as of tonight's facts (it did not exist as a named step before), and it too has **no duration estimate**: PM is only scoping it as of 2026-09-08, Sec joint-review has not started, and the account-mapper change (insert-only → insert-or-attach) has not been sized by Backend.

**Honest statement, not a re-stated old number:** the prior "steps 1–10 complete by 2026-09-30 → M1 = 2026-10 → close early-Dec-2026" claim assumed a chain with no PM/Backend-scoped work in it; that assumption did not hold before tonight's facts and holds even less now that a second unscoped build step (Step 8) sits on the same critical path as the backfill walk. **This record does not commit to 2026-09-30** for steps 1–12 until the PM/Backend audit returns duration estimates for **both** Step 7 and Step 8. The realistic planning assumption in the meantime remains §D's next row — deploy+accounts live by **2026-10-31** → earliest close pushes to early **2027-01** — with 2026-09-30 remaining reachable only if both steps turn out to be fast. Recommend this date not be locked in Linear or any downstream record until the PM/Backend spec lands for both steps.

**Secrets that must exist in Coolify at deploy (names only, per `secrets-manifest.yml` `production_only`, never values in this record or anywhere else):** `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `PDF_WORKER_SIGNING_KEY`, `PLAID_CLIENT_ID`, `PLAID_SECRET`, `SIMPLEFIN_TOKEN`, `WORKER_ADMISSION_SHARED_SECRET`, `DISCORD_WEBHOOK_URL`, `PFIN_DB_PASSWORD` (two distinct values, one per login role — `pfin_etl` and `authenticator` — same secret name, container-scoped different value, per the manifest's own note), `FMP_API_KEY`, `BLS_API_KEY`. Non-secret companion env vars needed alongside: `PFIN_DB_USER` (differs per container: `pfin_etl` vs `authenticator`), `PLAID_ENV=production`, `GOTRUE_DISABLE_SIGNUP=true` (self-hosted `auth`/GoTrue container — step 5a, F/CTO ruling 2026-09-09).

**Config note (2026-09-08 evening fact, names only — no values recorded here or anywhere in this repo):** `workers/provider-sync/.env` on disk currently carries the **deleted** Plaid team's `client_id`; it is stale as of tonight. F/CTO is replacing it locally with the new team's `client_id`/`secret`. Production Coolify's `PLAID_CLIENT_ID`/`PLAID_SECRET` entries (same names, listed above) must be set to the **new** team's values before the first deploy that exercises Plaid — this is a Coolify UI action for F/CTO, not something this record or any agent executes or observes the values of.

**Sec-consult points (mandatory, not optional, per the routing already established in the tree — listed so nothing is missed at execution):**

- §5 secrets-provisioning procedure + rotation, at lock (runbook flag #5).
- The `pfin_provider_sync` role migration (S5) — Sec joint-review mandatory per ADR-019 C2's condition, unchanged by the rename.
- **The attach-at-Link-time account-mapper build (stand-up step 8, new 2026-09-08)** — Sec joint-review mandatory: it changes the mapper from insert-only to insert-or-attach against `pfin.account`, touching `linked_source_id`/`provider_account_id`/`backfill_cutover_date`, columns that carry cross-tenant-isolation weight per the §10 catalogued instance family.
- **The production-key safety guard (OPEN-3 gate #15, new 2026-09-08)** — verify at sign-off that no quickstart/probe/sandbox-by-accident code path has run, or can run, against this new team's Production `client_id`/`secret`, and that the Vault-at-exchange-time token write is exercised (not just designed) before any real Item is created.
- The Coolify auto-deploy webhook configuration lock (ARCH §6 item (f)) — watches-main-only + admin-bypass-disabled verification.
- Any RT-26/RT-27-surface change touching the admission endpoint's env-signal heuristic (CA-1) if the Coolify version's actual FQDN/URL var names turn out to differ from what the worker's regex expects — that is a live tripwire-shape change, not a pure ops task.
- Pre-production sign-off itself (WORKFLOW Phase 7, named agent).
- The GDPR-erasure routine (§11) at build time — not a stand-up blocker, listed so it is not silently skipped when it does get built.

---

## 5. Options for F/CTO (2–3 each, no unilateral pick)

### Hosting target — RULED 2026-09-08 (F/CTO): Option A, corrected spec

**Ruling: Option A — a new Hetzner box, CAX21 class.** ⚠ **Vendor-spec correction (F/CTO-supplied 2026-09-08, cross-checked against Hetzner's own product page the same day): CAX21 = 4 ARM vCPU / 8 GB RAM / 80 GB NVMe disk, Germany.** The "8 ARM vCores / 16GB RAM / 160GB disk, ~€9.50/mo" figures this record's Option A (below, kept for the historical record of what was actually compared), `docs/ARCH/index.html` §5, and `docs/deployment-runbook.md` §1 previously attached to "cax21" are **CAX31's** tier — a one-tier misattribution, now corrected in all three. **Price: unverified.** A direct fetch of `https://www.hetzner.com/cloud/` and `https://www.hetzner.com/cloud/cost-optimized/` on 2026-09-08 confirmed the 4 vCPU/8GB/80GB spec from Hetzner's own page content but did not render a price figure (client-rendered; the cost-optimized page also showed a "currently unavailable" status for CAX21 at fetch time — unconfirmed whether that is a real ordering block or a rendering artifact of the fetch; **flagged, not resolved, here** — recommend re-checking the Hetzner console directly before provisioning). Do not carry forward "~€9.50/mo" as CAX21's price without re-reading it live.

**Note on the incumbent:** the incumbent `pfindash.com` box's *own* actual tier is unestablished from anything in this tree — it is referenced by name ("cax21") only, never read back from a live Hetzner console. It may in fact be running the CAX31 spec that was mistakenly attached to "cax21" everywhere, or it may be a real CAX21 undersold by the wrong numbers. This does not block the ruling (V1 provisions a *new*, greenfield box per ADR-021 — the incumbent is reference-only either way), but it means the "incumbent headroom" argument originally made for Option A rested on an unverified premise; the sizing evidence below substitutes a real measurement for that premise.

**Sizing evidence (team-lead, `docker stats` on Apple Silicon — same ARM architecture family as CAX21's Ampere cores — 2026-09-08, local Supabase stack idle):**

| Container | Idle RSS | Production-needed? |
|---|---|---|
| db | 713 MiB | ✓ needed |
| realtime | 286 MiB | ✓ needed *if the app subscribes to anything — see below* |
| kong | 221 MiB | ✓ needed |
| storage | 196 MiB | ✓ needed |
| rest | 191 MiB | ✓ needed |
| auth | 21 MiB | ✓ needed |
| analytics | 856 MiB | dev-only/optional |
| vector | 266 MiB | dev-only/optional |
| studio | 239 MiB | dev-only/optional |
| pg_meta | 98 MiB | dev-only/optional |
| inbucket | 13 MiB | dev-only/optional |
| **Total (11 containers)** | **3.1 GiB** | — |
| **Production-needed subset** | **≈1.6 GiB** | — |
| **Dev-only/optional subset** | **≈1.5 GiB** | — |

**Caveats (stated, not hidden):** idle measurement only (no load, no NAV/report/PDF workload running); seed-sized DB (production data volume will grow this, `db`'s RSS in particular); dev Postgres config (production tuning — shared_buffers, work_mem — will shift `db`'s footprint, direction not yet measured); Apple Silicon is architecturally comparable to CAX21's Ampere ARM cores but is not the same silicon, host OS, or kernel — this is a headroom *estimate*, not a CAX21-verified number. Against CAX21's 8GB RAM, the ~1.6GB production-needed subset leaves substantial headroom for the OS, Coolify itself, the four V1 app-layer containers (app, ETL, PDF worker, provider-sync), and production-scale DB growth — supporting Option A's class choice, though not a substitute for a real post-deploy `docker stats` read against the actual box.

**Production Supabase compose trimming.** Per Supabase's self-hosting docs (`supabase.com/docs/guides/self-hosting/docker`, read 2026-09-08) and the reference self-host `docker-compose.yml` (`github.com/supabase/supabase/blob/master/docker/docker-compose.yml`, read the same day): **`analytics`, `vector`, and `inbucket` are not present in Supabase's own self-hosted production compose file at all** — they are additions the local-dev CLI (`supabase start`) layers in for developer convenience, not services the production reference stack ships with. No stand-up action is needed to exclude them; they were never going to be part of the production deploy. **`studio` and `meta` (`postgres-meta`) ARE present in the self-host compose** and require a deliberate action to drop: Supabase's docs' documented mechanism is **manually removing the service's section and its dependents from `docker-compose.yml`** ("If you don't need specific services… you can remove the corresponding sections and dependencies from `docker-compose.yml`") — **not** Docker Compose profiles; profiles are not mentioned anywhere in Supabase's self-hosting guidance as a supported mechanism. **UNVERIFIED:** whether removing `studio` breaks anything else in the compose graph that depends on it (not confirmed from the docs fetch; check before removing at stand-up). Added as stand-up step 5 (below): the production compose removes `studio` and `meta`'s service blocks (their sections deleted, not profiled off) unless F/CTO wants Studio's UI available in production for ad-hoc DB inspection, which is a real, nameable reason to keep it — call this at execution time, not here.

**Realtime usage — grepped, not assumed.** The team-lead brief named `web/src`; **that directory does not exist in this tree.** The SvelteKit web app lives at `api/src` (repo layout: the app is the `api/` directory, not `web/`) — **flagging this as false against the brief's assumed path, corrected here.** `grep -rniE "realtime|\.channel\(|supabase\.channel|postgres_changes|removeChannel" api/src` returns **zero genuine hits** (the only matches are Vitest's unrelated `useRealTimers` fake-timer calls in a schema test, a false-positive substring match, not Supabase Realtime usage). **The app does not subscribe to anything via Supabase Realtime as of this tree's current state.** Per the brief's own framing ("realtime stays only if the app subscribes to anything"), this means `realtime` (286 MiB idle) is *also* a candidate for exclusion from the production compose today — recorded as a finding, not acted on here, since dropping a Coolify-managed service is a stand-up-time decision DevOps executes at step 5/6, not a doc-time one, and a future PRD story could add a Realtime subscription before stand-up actually runs.

**Historical record — options originally compared (kept for the ADR-style "what was considered" trail; Option A above is what F/CTO ruled, with the spec now corrected):**

- **Option A — new Hetzner box, same `cax21` class** (spec as originally written: 8 ARM vCores / 16GB / 160GB, Germany, ~€9.50/mo — **now known to be CAX31's spec, not CAX21's; see the ruling above for the corrected number**). *Why chosen:* ARM architecture continuity with the incumbent's container images; familiar Hetzner billing/console for F/CTO; the sizing evidence above shows the corrected (smaller) CAX21 spec still carries comfortable headroom over the measured production-needed footprint. *Losing side:* a resize is a real (if routine) Hetzner operation, not zero-cost, if GL-feature-set growth or V2 onboarding needs more before a planned resize.
- **Option B — new Hetzner box, a larger class** (headroom sized for V1.final's cron/worker count — 4 Coolify units vs the incumbent's fewer — and near-term V2 multi-tenant onboarding). *Why:* avoids a near-term resize. *Losing side:* pays for capacity V1 single-user scale does not need yet — the sizing evidence above suggests CAX21 already has substantial headroom, weakening the case for this option unless V2 growth is imminent; a specific class number was never chosen here.
- **Option C — a non-Hetzner VPS provider.** *Why it's listed:* the DevOps charter defaults to Hetzner+Coolify with "no third deployment surface… without a forcing function," and no forcing function is visible anywhere in the tree — no cost, compliance, or capability gap that Hetzner can't meet. *Losing side, stated plainly:* introduces a new billing relationship, a new region/hardening posture, and provider-specific quirks with nothing in the record to justify the switch. **Not recommended** absent a forcing function F/CTO can name — not chosen.

### Deploy-then-accounts sequencing — RULED 2026-09-08 (F/CTO): Option 1

**Ruling: Option 1 — deploy, then connect real accounts immediately; no dry-run interval.** The deploy month is the soak (R12 clause 1). Once stand-up step 11 (Sec sign-off) clears, F/CTO proceeds straight to step 12 (connect every real Plaid/SimpleFIN/manual account). M0 starts immediately; per §D's calendar this is the path that reaches the earliest close date. *Losing side, accepted by the ruling:* any defect the smoke checklist (step 10) didn't catch is discovered against **live financial data** on the F/CTO's actual accounts, not a rehearsal.

- Option 2 (not chosen) — a dry-run interval before connecting real accounts, trading speed for confidence-building (cron reliability, notification delivery, one TZ drift-sweep firing) before every account connects. *Losing side:* every day of dry-run before the tenant-accounts-live date pushes M0/M1/M2 by the same amount — per §D, a full month's slip if the dry-run crosses a month boundary before conversion.

The ruling only affects **when** step 12 (F/CTO connects every account) happens relative to step 11 (Sec sign-off) — immediately, per Option 1 — not whether any gate in §4 is skipped.

### Production signup gating (Q5) — RULED 2026-09-09 (F/CTO): signup-off-until-allowlist

**Ruling: production public signup stays OFF (`GOTRUE_DISABLE_SIGNUP=true`, step 5a above) until an operator allowlist exists on the Plaid Link-token route.** F/CTO's own account is created by invitation for the duration. Signup does not turn back on as a side effect of stand-up completing — OPEN-3 gate #16 holds it closed through the whole V1.final soak, independent of steps 1–13.

This ruling was framed to team-lead against four options — **accept-now**, **allowlist-now**, **Pay-As-You-Go**, and **signup-off-until-allowlist** — and the option taken is the fourth:

- **signup-off-until-allowlist (option 4, taken).** Close production signup entirely for the soak rather than leave it open with an unenforced gate; reopen only once the operator allowlist exists on the Plaid Link-token route. *Why:* the failure mode this closes is a stranger's Link session spending one of the 10 Trial-plan Production-Item slots (OPEN-2/OPEN-3 gate #15) that F/CTO's own institutions also draw on, with nothing in the app able to see it happen — closing signup for the soak removes that exposure at zero build cost, at the price of ADR-036's open-signup story not being exercised until the allowlist ships.
- **accept-now (not taken).** Leave signup open per ADR-036 and accept the shared-quota exposure, watching for it rather than gating it — this was PM's Round 2 lean (`docs/records/v1final/standup-preconditions.md` §F Q5, option (b)) before this ruling. *Losing side:* a stranger's Link session during the soak permanently consumes a slot the F/CTO's own accounts need, with no telemetry to notice it happened.
- **allowlist-now (not taken).** Build the operator allowlist on the Plaid Link-token route immediately, as the gate, while leaving signup itself open the whole time. *Losing side:* signup stays open during the allowlist's own build window — the exposure the allowlist exists to close is live for as long as that build takes, and a build slipping past the soak's start leaves the same unenforced-gate problem the taken option avoids by closing signup outright first.
- **Pay-As-You-Go (not taken).** Upgrade the Plaid team off the 10-Item Trial cap onto a paid Production plan, removing the quota-collision risk without touching signup or Link at all. *Losing side:* it pays to make room for exposure rather than closing the exposure — an unvetted signup can still create a real financial-account connection under the F/CTO's Plaid credentials; the quota was one symptom of that, not the whole problem, so this option was ruled out as treating a proxy rather than the risk.

**Losing side of the taken option:** the open-signup story (ADR-036) is not exercised during the soak — no second tenant, no multi-tenant Link flow, gets a live run until the allowlist ships and signup reopens. This is a deliberate, scoped narrowing of what the soak proves, not an ADR-036 reversal.

---

## Findings surfaced by this research pass (not resolved here — F/CTO / Architect / Sec action)

1. ~~ARCH §5 vs §6 self-contradiction~~ — **RESOLVED 2026-09-08.** §5's parenthetical and all three §6 mentions now read consistently ("a new greenfield VPS; CAX21 class ruled 2026-09-08 for V1"). No longer an open finding.
2. **B.5 AC1's component enumeration omits the `provider-sync` worker**, making AC2 (Plaid production credentials + a successful Link session) structurally unsatisfiable as the AC is currently scoped — provider-sync is Plaid's sole credential holder per ARCH §5. **Still open — required correction to the SELF-365 protocol record §B.5 and the SELF-386 description; this record states the finding, team-lead routes the actual edit (this record does not touch `docs/records/v1final/self365-protocol.md`).**
3. **"ADR-027 (Plaid PRIMARY)" is a citation-attribution drift.** The provider-primacy ruling is ADR-037; ADR-027 explicitly deferred it. The Transactions+Investments product-set commitment traces to ADR-002 §6.0 / PRD §1.3, not to ADR-027 either. The *substance* everyone has been citing it for is correct; the ADR number attached to it is not.
4. **Runbook Open-flags row #11 is stale** — it claims the TimeZone-pin migration still needs authoring; migration `061_pin_database_timezone_utc.sql` already exists on the tree (and the runbook's own §4.1/§10 sections assume it exists and build rich verification procedure on top of it).
5. **Runbook §7 still describes the PDF worker Dockerfile as "a placeholder."** The tree shows a substantial, lock-annotated 108-line Dockerfile marked "EXTENDED" per SELF-348. No stand-up impact; a doc-accuracy item for whoever next touches the runbook.
6. **Plaid's institution-access landscape for the F/CTO's actual accounts (Schwab in particular) carries genuine, currently-unconfirmable external risk** — recommend an early Plaid Link smoke test (now stand-up step 9, renumbered) rather than discovering a coverage gap at the tenant-accounts-live step, where it would cost real schedule.
7. **The Plaid tier terminology in B.5 AC2 ("limited-production tier") is stale relative to Plaid's 2026-04-15 product change** — the operative tier for a team likely created after that date is "Trial plan," which does admit Transactions + Investments. The AC's *outcome* concern is answered; its *wording* should be corrected wherever it is repeated (this record does so; the live Linear issue is F/CTO's or PM's to correct, not touched here per this session's no-Linear-writes instruction).
8. **The `reference_hetzner_cax21` spec (8 ARM vCores/16GB/160GB/€9.50-mo) was CAX31's, not CAX21's — corrected 2026-09-08** in this record, `docs/ARCH/index.html` §5/§6, and `docs/deployment-runbook.md` §1 (the three surfaces this round's brief named). **Not corrected here, flagged as residual drift:** the same wrong figures still appear at `docs/ARCH/index.html` lines ~845, ~863, ~988 (Lock 16 cost-feasibility math, the incumbent-baseline cost bullet, and a Phase-5 entry-gate task) and at lines ~63/~160/~386/~393/~413/~420 (descriptive incumbent references, no numeric claim). The Lock 16 cost-feasibility math in particular (`ADR-011 Decision 20`) is built on a "$10/mo already-paid baseline" that assumed the wrong tier's price — worth an Architect/F/CTO pass on whether that changes Lock 16's ≤$50/mo conclusion, since neither CAX21's nor CAX31's actual price was read live before this round. Out of this PR's scope (only the three brief-named surfaces were touched); the `reference_hetzner_cax21` memory correction is separately tracked (per team-lead's own MEMORY.md entry, "fix booked").
9. **Hetzner's own cost-optimized pricing page showed CAX21 as "currently unavailable" on a direct fetch 2026-09-08** (`https://www.hetzner.com/cloud/cost-optimized/`), alongside confirming the 4 vCPU/8GB/80GB spec. Unconfirmed whether this is a real ordering block (regional stock-out) or a rendering artifact of the fetch tool against a JS-heavy page — **recommend F/CTO check the Hetzner console directly before provisioning (stand-up step 3)**; if genuinely unavailable, Option A's ruling may need a same-class substitute (e.g. a different region) rather than a re-opened options discussion.
10. **This round's brief named `web/src` for the realtime-usage grep; that path does not exist.** The SvelteKit web app lives at `api/src` (the repo directory is named `api/`, not `web/`) — corrected in this record's sizing-evidence section; the grep was re-run against the real path.
11. **Critical path honesty note:** the two new F/CTO preconditions (stand-up steps 7–8, Item registration + historical backfill) have no duration estimate yet (PM/Backend audit pending as of 2026-09-08). This record deliberately does **not** restate the prior "close by early 2026-12" date as still reachable — see §4's critical-path paragraph for the reasoning and the fallback date used instead.
12. **RESOLVED 2026-09-08 evening — OPEN-2's team-provenance question, closed by direct dashboard read, not inference.** Team-lead measured this directly in F/CTO's Plaid dashboard: the old team (Pay-As-You-Go, 9 orphaned Items, tokens already lost) is deleted; a new Trial team exists (10 free Production Items, new `client_id`/`secret`, values never recorded here). Superseded finding #7's hedge ("likely created after the cutover") — it is now confirmed, not likely.
13. **Stand-up step 7 ("directly register 9 existing Plaid Items") is STRUCK, not merely revised** — there is nothing to register against a team that starts at zero Items. **Replaced by a new build step (8): "attach a provider-linked account to an existing manual account at Link time,"** which PM is scoping as of 2026-09-08 using the already-shipped `pfin.account.linked_source_id` / `provider_account_id` / `backfill_cutover_date` columns. This is new scope on the critical path with **no duration estimate**, not a like-for-like swap — flagged in §4's critical-path paragraph so it isn't read as a wash.
14. **New guard, not previously named: production-key safety.** F/CTO's new Plaid team's Production keys must never be exercised by a quickstart, probe script, or an accidental Sandbox/Production mix-up — every Item created against Production, including a test mistake, permanently consumes one of the 10 Trial-cap slots (finding #12 above). Recorded as OPEN-3 gate #15 and as a Sec-consult point (§4); Sandbox is the only environment for testing, and the existing Vault-at-exchange-time token design is the control being verified, not a new one being built.
15. **`workers/provider-sync/.env` on disk is stale as of 2026-09-08** — it holds the deleted Plaid team's `client_id`. F/CTO is replacing it locally; production Coolify's `PLAID_CLIENT_ID`/`PLAID_SECRET` must be repointed to the new team's values before any deploy that exercises Plaid. No value is recorded in this record, the repo, or any agent conversation — this finding names the config drift, not the secret.
