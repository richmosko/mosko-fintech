# V1.4 execution log — rulings under F/CTO delegation

**Delegation (F/CTO, 2026-09-03, verbatim):** *"I would like for you to execute V1.4 to completion. You are authorized to go with your rec for any decisions that come up... and are authorized to merge prs."*

**What that changes.** For the V1.4 build loop, the team-lead recommendation stands in for the F/CTO ruling at every decision point — including the SELF-263 tax-value inventory session, which the sitting log (R5) described as F/CTO time. Every such ruling is recorded here with provenance **TEAM-LEAD RULING UNDER DELEGATION** per [ADR-063](../../../DECISIONS.md#adr-063) Decision 3, never flattened into "F/CTO ruled." The reversal window on each is open until V1.4's close-gate merges; F/CTO can reverse any entry by saying so. Merges: on green, by team-lead, per the same delegation; doc-only and feature PRs alike.

**What it does not change.** Sec's veto stands. The live walk-through gate stands on every user-facing PR. Sec joint-review is mandatory on all ten issues per the sitting log's map. The ruled dispatch order is [`../v14-preflight/sitting-log.md`](../v14-preflight/sitting-log.md) § *Ruled dispatch order*; the ACs are the re-derived text on each Linear issue (applied 2026-09-03 from `rederived-acs.md` at `762f793`).

**Migration numbers reserved up front** so three parallel branches do not collide: `100` = SELF-263 seed delta + `tax_relevant` column comments · `101` = SELF-259 bracket tables · `102` = SELF-267 `tax_jurisdiction` + YTD-Paid primitive + the `051` leaf-set exclusion. Later numbers allocate at dispatch.

---

## Rulings

_(entries appended as they are made; one per decision; each names the options weighed and the losing side)_

### E1 — `bracket_rate` unit = FRACTION (`0.22`, bound `<= 1`) · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
SELF-259 AC 2 leaves the unit to the author with the requirement that it be stated in words in the column comment. Ruled fraction: the PRD's §2.5.3 arithmetic multiplies taxable income by the rate directly, and a fraction needs no ÷100 at the one consumer (SELF-262). **Losing side:** percent (`22`) reads more naturally on the §2.5.2 settings editor and matches how the IRS publishes brackets — the editor converts at the boundary (SELF-265) and the seed (SELF-260) is written in fractions. Recorded in `101`'s header by Architect.

### E2 — SELF-259's replace-all endpoint ships in the SAME PR as `101`, as a LABELED separate commit on a sibling branch · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
AC 6 says "split it or label it." Ruled label: one PR keeps the DDL and its only writer reviewable together (Sec reads the endpoint's tenant handling against the RLS it relies on), and a second PR would re-open the joint-review on the same surface. **Losing side:** a split PR gives the migration a cleaner Sec record and lets `101` land before the endpoint is green. Mitigation: Backend's commits are prefixed `feat(api)` and the PR body lists the two halves separately.

### E3 — The `051` leaf-set exclusion lands in SELF-267 (`102`), not in SELF-268 · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
The re-derived SELF-267 AC 2a already places it there; ruled explicitly because SELF-268 also replaces `051` (the tax scalars), so `fn_nav_composition` is CoR'd twice across the milestone. Grounds: R3 rider 0b makes the designation's default-state hazard part of SELF-267's walk ("mark → BOTH figures move"), which is unobservable unless the exclusion is live when the designation ships. **Losing side:** one CoR of `051` at SELF-268, fewer signature-preserving replacements to verify; it would leave the R3 E-2 double-count latent between the two merges while the tax literals are still `0` — not a live defect, but a walk that cannot see its own hazard.

### E4 — SELF-263 inventory session: the four decisions · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Source: PM's proposal at `docs/records/v14-execution/self263-inventory.md` (`feature/self-263-pm` @ `e472bd5`, md5 `b0645816…`), read in full. Row sets measured by PM from the migration files: 29 `posting_prototype_default` rows, 38 `taxonomy_default` rows.

- **D-i `Equity / Contribution` → `tax_relevant = false`, `tax_character = NULL`; the ADR-062 D4 "flag-for-review" rider is REMOVED and replaced with a plain description; the backfill UPDATEs `posting_prototype.notes` equality-guarded on the exact rider string.** PM's (A). Grounds: a contribution of capital is not income; deductibility is per receiving account (`account.tax_treatment`), which no prototype row can carry; fail-closed direction. **Losing side:** (B) keep `true` — with no reviewer after this session and an Income reader that may not be class-scoped, a capital deposit sums as Ordinary Income. (C) deduction semantics — a §2.5.2 line that does not exist. ADR-062 gets a one-line amendment (Architect, in `100`'s PR). Booked V2+: O-3.
- **D-ii Bond Premium: CONFIRM `ordinary`; notes corrected** (the seeded note names mark-to-market, which is the §1256 concept the asset-side `Volatility-60/40` row owns). **Dividend: PM's (C′), not (C)** — the generic `Revenue / Dividend` row becomes `ordinary` (fail-closed default), and a new `Revenue / Dividend - Qualified` = `true / qualified_dividend` row is ADDED (`091`'s row-add + backfill shape). Grounds: fail-closed on tax is this project's standing direction (R11, Sec F-2); the LT-CG walk stays live through the qualified row (SELF-264 AC 3a's premise holds); and (C′)'s only cost — re-sorting an existing user's dividend history — is **zero on a greenfield deployment** (no incumbent data migrates; the incumbent is reference-only). **Losing side:** (C) generic = qualified — fail-open for an unsorted user: MMF / REIT / bond-fund dividends taxed at LT-CG rates, understating Federal tax. ⚠ **F/CTO reversal window explicitly flagged**: PM notes the right default depends on the actual dividend mix, which F/CTO knows and PM does not; if the mix is overwhelmingly qualified, (C) is the better default and the flip is one seed-row edit.
- **D-iii asset-side principle: ADOPTED as PM stated it** — `tax_relevant = true` iff the Sub-Cat is disposed through the §2.4.3 lot machinery as a taxable event; Real Estate `false` EXPLICITLY because its disposition is unmodelled, not tax-free; Cash / Liabilities `false`. Not on V1.4's critical path (R1-A); lands in the seed delta anyway (same act, cheap).
- **D-iv: 24 × `long_term_capital_gain_eligible` (Bonds 4 / Marketable Securities 15 / Alternatives 5, incl. `Volatility-60/40` with the 60/40 split carried by Sub-Cat identity) + 1 × `short_term_only` (`Cash / T-Bill`).** A sixth `section_1256` code was weighed and not taken for V1 (no consumer on the tree; bookable).
- **Open items O-1…O-6 → BACKLOG §5 (PM, same PR).** The eight `041` spelling errors are hygiene and stay OUT of `100` (a separate doc-class fix; booked).

### E5 — `100` ships the seed delta, the pair backfill, the four `comment on column`s, AND the two-row additions in ONE migration · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
AC 3/4/6 already co-locate the delta, the backfill and the comments. The `Dividend - Qualified` row add rides in the same file because it is the same backfill shape (`091`) and the same Sec review. **Losing side:** a separate row-add migration keeps `100` a pure value correction; it would double the backfill-reach proof for no review benefit.

### E6 — SELF-259 "bracket-row monotonicity" = bracket_RATE non-decreasing in ascending floor order · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Architect (departure 3 on `101`): `unique (schedule_id, bracket_floor)` already makes a schedule's floors a distinct, hence totally ordered, set — a floor-ordering leg could never fire (the by-construction-property watcher ADR-062 D2 rejects). The falsifiable reading is the rate: non-decreasing as floors ascend. Ruled that reading; probe 1 on `101` demonstrates it firing at commit after the multi-row INSERT succeeded. **This commits the pair to PROGRESSIVE schedules**; all three V1 `schedule_type` values are. **Losing side:** a jurisdiction with a regressive bracket would need the leg amended — stated in `101`'s header, table comment and function comment so the amendment site is named. QA's "non-monotone batch" leg is written against RATES.

### E7 — `101` departures accepted; two additions ordered; the Decision 3 fold-in is OWED BY THE SELF-259 PR · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Accepted as authored: typmods `numeric(20,4)` / `numeric(12,8)` (a bare `numeric` admits ±Infinity, which rider 8's NaN CHECK cannot see); no EXECUTE grant on the two trigger functions (Postgres does not check EXECUTE when firing a trigger; `074` precedent — the checklist's "then grant" was wrong where the AC was right); no sign bound on `tax_balance_prior_year` (an overpayment is a real state); an empty schedule passes the set fence (absence is unset); the child `users_id` index; the `do $$` guard on the schema's FIRST `create type`. **Ordered:** (i) `check (tax_year >= 1913)` — cheap and falsifiable; losing side: beyond the AC's letter; (ii) **the ADR-011 Decision 3 fold-in of canonical instance #18 lands in this PR**, per D3's own rule that the fold-in is due in the PR that DDL-realizes the instance (#16 is the recorded failure case). Architect owns it on the same branch. Also recorded: the set fence's completeness rests on #18 (strike the tenant fence and the set fence narrows silently) — both fences are documented as coupled.

### E8 — The SELF-259 replace-all is ONE SECURITY INVOKER plpgsql function, serialized by a `FOR UPDATE` lock on the owner's schedule row; Decision 18's "under SERIALIZABLE" is realized that way and amended to say so · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Backend measured the transport limit `045` already records: PostgREST runs each `.rpc()` / `.from()` as its own transaction and cannot hold a client-side `BEGIN SERIALIZABLE`; and `SET TRANSACTION ISOLATION LEVEL` cannot be issued inside a function body (the calling SELECT has already taken its snapshot). So the atomic replace-all body is **`pfin.fn_tax_bracket_schedule_replace_all(p_schedule_id bigint, p_tax_year smallint, p_schedule_type pfin.tax_schedule_type_enum, p_standard_deduction numeric, p_tax_balance_prior_year numeric, p_rows jsonb) returns void`**, `security invoker`, `set search_path = ''`, `volatile`, EXECUTE revoked from PUBLIC and granted to `authenticated`, **no tenant parameter** (R4 rider 4; Sec D-2). Its first statement locks the caller's own schedule row — `select id from pfin.tax_bracket_schedule where id = p_schedule_id for update` under RLS, which refuses (zero rows → RAISE) for another tenant's or an absent schedule — so two concurrent replace-alls on one schedule cannot interleave their DELETE/INSERT pairs. ⚠ **Rationale corrected 2026-09-03 after Architect struck the lock on a scratch copy and ran the race:** the schedule does NOT end as A ∪ B on this pair — the zero-floor fence puts `bracket_floor 0` in every non-empty set, so `unique (schedule_id, bracket_floor)` converts the union into (i) a second-writer abort on a duplicate-key error naming a constraint unrelated to the cause, or (ii) when the second caller sends an EMPTY set to clear the schedule, **no error and a silent lost update** (the schedule keeps the first caller's rows). The lock removes both; the ruling stands; the build-side texts (`101` header, function comment, D18 amendment) carry the measured modes. A header asserting the unreachable union would have sent QA's battery hunting a failure this schema cannot produce. Then DELETE the schedule's rows, INSERT `p_rows` (each element `{bracket_floor, bracket_rate}`), UPDATE the scalars, and let the deferred set fence and the matched-tenant fence fire at commit. Schedule CREATION is an ordinary INSERT under RLS (an empty schedule is legal per `101`); the function refuses an absent schedule rather than creating one. **Losing side:** true SERIALIZABLE isolation would also catch write skew across *different* schedules of one user, which nothing on this surface reads across, so the lock's narrower guarantee covers every hazard the surface has; and a `default_transaction_isolation` change on the PostgREST role would be global and untestable per surface. **ADR-011 Decision 18 gets a dated amendment paragraph** (Architect, in the SELF-259 PR) recording that "replace-all under SERIALIZABLE" is realized as this function + row lock, with the transport reason — a downstream register cannot carry an amendment to a lock. Sec joint-review reads the function.

### E9 — Backend's SELF-259 judgment calls · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
(a) `TenantBoundClient` does NOT apply in `api/` — the TBC-node CI fence scopes `workers/provider-sync/src/` only; every `/api/settings/*` route uses the `hooks.server.ts` `locals.supabase` chokepoint. The brief's mention was wrong; no fence extension is ordered. (b) App-layer `tax_year` bound is `[1913, 2100]` so the lower bound is single-sourced with `101`'s CHECK; `rows[]` cap 50 stands as defensive. (c) `tax_balance_prior_year` may be negative (matches `101`). (d) DB `P0001` from either fence maps to one generic 4xx — matches the `planning-target` precedent of not relaying trigger internals; not distinguishable to the client. (e) `bracket_rate` app sanitizer accepts up to 8 decimal places to match `numeric(12,8)`. (f) RT-24's row text in `docs/SECURITY/index.html` (BEFORE-trigger, `lower_bound`) is stale against R4 — **Sec's to reconcile at the SELF-259 joint review**, carried into that brief.

### E10 — Stale `#18` forward-pointers outside Decision 3 (ADR-058 lines 632 / 765 at `fcd8e98`) · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Architect's D3 fold-in re-pointed the two sentences inside Decision 3 and then found two more in ADR-058 by re-grepping the whole file (its own first grep had been filtered to D3 — the "filtered grep is a claim about the filter" failure, named by Architect). Ruled: **765 is re-pointed** to #19 — it directs a future migration author to read D3 live and is therefore an instruction, not a record; **632 keeps its number and gains an inline annotation** — it is the dated record of Sec's numbering pin at `084`, and #16's precedent in D3 is that a historical label note stays as written with the collision explained beside it. **Losing side:** re-pointing both is simpler and leaves no stale digit anywhere; it would destroy the record of what Sec pinned and when.

### E11 — `102` (SELF-267) design calls accepted as authored · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
(1) **`fn_ytd_paid_per_jurisdiction` returns NULL when no ledger is designated for that authority, 0 for a designated ledger holding nothing.** Collapsing them reports "not set up" as "nothing paid" — R3 rider 0b's overstated-Funds-Due half arriving as a plausible figure. NULL propagates loud through a Funds Due subtraction. **Carried to SELF-262 / SELF-266: consumers decide what NULL renders as (an explicit "no ledger designated" state, never `$0`).** Losing side: a scalar 0 is simpler to compose; it is the silent-zero class this project has booked before. (2) **No `transaction_type = 'standard'` filter** — the figure is `056`'s balance-as-of composed, not a third copy of its roll-forward; consequence: an `acct_setup` opening row on a designated ledger counts (a user stating payments-already-made as the opening balance). Flagged for Sec at joint review. (3) Native currency, inherited from `056`; V1 authorities are USD. (4) **Not clamped** — a net-negative YTD Paid (refunds exceed payments) is reported, not floored; deliberately asymmetric with R9, and the comment says so. (5) The shared predicate is set-returning `fn_tax_authority_ledgers()` rather than a scalar boolean — the jurisdiction filter is a refinement of one shared set, and the predicate has exactly one executable home (catalog-verified). (6) The "ADR-063 Decision 2" citation in the ACs and R3 rider 1 is malformed (ADR-063 numbers items inside one Decision) — already booked as the §7.31 citation sweep; `102` cites the item form. (7) `051`'s comment: the DEFINER-allowlist count dropped (self-inconsistent with the sentence's own disclaimer) and the default-state clause no longer forward-references SELF-268's unbuilt rendering.

### E12 — Tax-authority designation is MANUAL-ACCOUNT-ONLY, enforced at the app layer · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
PRD §2.5.3: the IRS and FTB accounts are V1 instances of §2.4.2 manual non-Plaid accounts. Ruled: the designation write path refuses a provider-linked account with a field error; the `102` column is not fenced by account origin at the DB. **Losing side:** a DB CHECK or trigger tying `tax_jurisdiction` to manual accounts would be the stronger fence; it would couple `102` to the provider-link representation and re-open Sec review for a constraint the PRD states as product scope, not isolation. Bookable if a provider ever exposes a tax-authority ledger.

### E13 — `100` (SELF-263) departures accepted; battery consequences; template rebuild owed · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Accepted: (1) the D3 statement per column as Architect wrote it — `tax_character` IS FK-shaped on three tables, referent a global value registry with no `users_id` → family +0; `taxonomy_default.tax_character` still carries `041`'s inline CHECK, never converted to the FK (a hygiene booking, not this migration's); (2) E5's phrase "two-row additions" meant one Sub-Cat on both tables of the pair — ONE new Sub-Cat, `Revenue / Dividend - Qualified`, is correct; (3) `display_order = 65` beside `Dividend` (60) — a picker alternative, not a new class, so `091`'s past-the-maximum rule does not apply; (4) every UPDATE equality-guarded on the value it replaces — idempotent and non-clobbering; a miss is silent and the header says so; a row-count assertion was weighed and rejected (would fail a deploy over a hand-edited row). **Battery consequences, by design:** nine legs across `041` / `082` / `091` batteries are falsified by the 29 → 30 row count; eight are RE-PINNED (each stating what the count is over); `091` leg 11 (E1), which asserts Contribution `true` + the D4 rider verbatim, is INVERTED — it is the watcher proving the rider removal. `054` legs 25/28/29 are pre-existing cluster-role reds, not `100`'s. **`tax_character` column comments not shipped** (AC 6 names `tax_relevant` only) — booking candidate. **`scripts/db-template-build.sh` must be re-run after `100` merges** or every later clone refuses on the staleness fence — carried to the merge step.

### E14 — SELF-267 ships with an INTERIM headline/foot divergence, closed by SELF-268 · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Once a ledger is designated, `051`'s foot excludes it while the §2.1.1 headline (still `fn_compute_nav`) includes it, until SELF-268 AC 1a moves the headline's read source (R3 rider 0). Accepted as an interim state inside the milestone: no deploy occurs between the two merges (Phase 7 not started), and folding the headline move into 267 would widen its Sec surface past what R3 placed there. The walk records the divergence as SEEN-and-expected. **Losing side:** a self-consistent 267 — one small `netWorth.ts` change — at the cost of SELF-268's AC 1a being half-built in another issue's PR.

### E15 — `100`'s four `tax_relevant` column comments phrase the rule WITHOUT spelling the negated string · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
QA's COM3/COM4 legs (a literal absence check for "found not tax-relevant") failed honestly: two of the four comments carry the phrase inside a `never "…"` quotation. Ruled: rephrase those two to the form the other two use, in place (unmerged file), and keep the watcher literal. **Losing side:** loosening the predicate to allow the quoted-negation form — a grep-shaped watcher cannot tell a quotation from an assertion, and the point of AC 6 is that the reading a future reader takes from `\d+` is unambiguous.

### E16 — SELF-259 battery scope growth accepted; the `pg_catalog.coalesce` defect is fixed in place · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
QA extended the battery 69 → 91 legs to cover `fn_tax_bracket_schedule_replace_all` (a new directly-callable cross-tenant surface) and the `tax_year` CHECK without being asked — accepted; a battery that ignores a surface its own branch grew is the silent-gap class. QA's RA3 isolated a real defect: `pg_catalog.coalesce(...)` — `coalesce` is a SQL special form, not a `pg_catalog` function, so the malformed-`p_rows` error path threw an unrelated internal error. Fixed in place (unmerged file). ⚠ `pfin_tmpl` is now cross-branch stale (its marker names `101`, a sibling branch's migration): every QA verified by sequential apply; the template is rebuilt after each migration merge (E13).

### E17 — SELF-259 Sec AMBER dispositions · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
Sec F-1: the `FOR UPDATE` lock lives only inside `fn_tax_bracket_schedule_replace_all`; `authenticated` holds direct DML on both tables, so two concurrent direct row UPDATEs on one schedule take no schedule lock and the deferred set fence judges each on its own snapshot — a non-monotone committed set is reachable (same-tenant, narrow interleave). The D18 amendment's "covers every hazard the surface has" was therefore false. **Ruled Sec's option (B): the set fence's first statement takes `FOR UPDATE` on the parent schedule row**, so every transaction that touches a schedule's rows serializes at commit on that row and the fence re-reads the combined set after the wait. The D18 sentence is rewritten to name exactly what the lock covers. **Losing side:** (A) accept and name the residual — cheaper, but a canonical Lock register would then carry a known hole beside a sentence a reviewer stops at. (C) revoking direct DML is unavailable under INVOKER. F-2: the third `#18` forward pointer (ADR-042 D5) loses its numeral (Path B), and D3's consequence (b) is corrected to say three sites were found, two re-pointed, one de-numbered. F-3: the two `comment on table` texts asserting SERIALIZABLE are rewritten to the shipped semantics. F-4 (Backend): `tax_year` joins the reject-not-coerce battery. F-6: stale "no RPC" caveats replaced with the confirmation. F-5: Sec authors the RT-24 doc correction as a separate doc PR that lands no later than the SELF-259 merge.

### E18 — SELF-263 Sec AMBER dispositions · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-03
A-1 (blocking, QA): five legs (Sec D1–D5) assert the REAL default tables' post-state — generic `Dividend → ordinary`, Bond Premium notes, and the 25 `taxonomy_default` asset corrections — proved by inversion (strike `100`'s statement (3) and D3–D5 go red while the replay-shaped legs stay green). The replay-of-copies shape for the per-user tables stays (`091`-established; the user-side tables cannot be observed directly). F-1: ADR-062 Amendment 1 and `100`'s AC-5 record D3's hard precondition as F/CTO-discharged on 2026-08-25 (SELF-245 comment 2) and CONFIRMED here under delegation — Sec's replacement text verbatim. F-2: `100`'s AC-5 files the discharge under F-6b (the `tax_relevant`/`tax_character` inventory), not the retracted M-6. F-3 / F-4 → SELF-262 AC clauses + a QA leg (Revenue-class scope; Trade STC/BTC excluded). F-6 accepted as-is: the Dividend UPDATEs overwrite `notes` unguarded, reachability NIL (no UPDATE grant on any of the four tables at any tier — Sec-measured). F-7 → BACKLOG hygiene line. Sec supports C′ as shipped on the E4 D-ii reversal window.

### E19 — SELF-267 Sec AMBER dispositions · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-04
B1: `fn_ytd_paid_per_jurisdiction` is the designated ledger's balance since inception (R8's rider, not re-opened); from tax year 2 onward it carries prior payments unless the user rolls the ledger over. Ruled: the `comment on function` and header state what the YEAR is — the ledger's life, with rollover by **CLOSING the old ledger, clearing its designation, and designating a fresh ledger** (the index permits one designated ledger per authority at a time). ⚠ **Corrected 2026-09-04 on Sec's C1 (re-review of 2dc8e53):** as first written the rollover said only "clear and re-designate" — but clearing a designation returns the ledger to `fn_nav_composition`'s leaf set immediately (the exclusion is a live read; the battery proves the reversion at L3i/L3j), so NAV would RISE by every payment ever made to that authority — the A-9 double-count re-entered through the sanctioned path. Closing the old ledger (V1 has account close; `fn_compute_nav(_, true)` and the composition read active accounts only) is what keeps the paid money gone. Sec's commit-ready text is in `102`'s comment; the §2.5.3 copy at SELF-266 states the three-step rollover; a one-step year-end affordance is booked V2 — using Sec's replacement text; plus the two further clauses Sec named (the partial unique index is not a Decision-4 layer; the equality-leg enumeration corrected). **Losing side:** a year-scoped balance (rows dated within the tax year) — it would re-derive `056`'s roll-forward a third time and diverge from the PRD's "cumulative balance of these ledgers"; the product affordance for year-end (settle/drain) is booked V2 and the rollover is stated in §2.5.3 copy at SELF-266. B2: the PR is opened so `db-tests.yml` runs the battery (a local psql run does not substitute). B3 → SELF-262 and SELF-266 ACs with failing watchers (carry-forward). Notes: Frontend consumes `form.accountId` on the create-conflict branch (route to the created account with the field error); the index predicate is named as the one non-routable copy of the shared predicate in `102`'s header; the manual→linked drift is booked. Sec's rulings accepted: `acct_setup` opening rows count; app-layer manual-only stands; NULL semantics endorsed; create-then-UPDATE without rollback is the only shape available (no DELETE grant on `pfin.account`).

### E20 — SELF-259 F-1 (B) as built, and its follow-ups · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-04
Architect's two-session probe reproduced Sec F-1 with the lock struck (non-monotone set committed, nothing saw it) and showed the shipped fence blocking S2 at commit until S1's parent-row lock released, then rejecting on leg B. Accepted as built with two shape departures from Sec's literal (B): the fence takes the lock FIRST, reads the set, RETURNS on `count = 0` (the `on delete cascade` path — a lock-then-RAISE order would have made deleting a schedule impossible), and only then raises on an unresolved parent — a RAISE unreachable for an RLS-bound caller while #18 holds, kept as an OBSERVER for #18's absence (it fires for an RLS-exempt writer or under replica role). **40P01**: a transaction touching two schedules' rows takes two parent locks in row order; two such in opposite order deadlock — an error, never a corrupt set. Ruled RECORDED, NOT TESTED: pgTAP is single-session and no V1 writer touches two schedules in one transaction (the RPC is per schedule; SELF-265 saves one schedule per request — carried as an AC there). Sec's F-2 enumeration was short by three (six `#18` sites in the file, not three); the last open one (D3's #17 entry) is annotated in place. Sec's N-1 parenthetical on 263 re-attached by team-lead (mechanical splice fix).

### E19b — Rollover precondition measured by Architect at the C1 fix · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-04
The `058` close gate refuses an account holding a non-zero cash balance (leg 2 of 3), and a designated ledger's balance IS the accumulated payments — so "close the old ledger" as step 1 is refused by the DB on exactly the ledgers the procedure is about. The comment now states the precondition: the ledger must be drained first. Architect's residual, accepted as a PRODUCT question routed to PM (not a DDL fact): emptying by transfer to another owned account lands the cash on an in-set leaf and raises NAV by the same amount clearing would; the only V1 drain that preserves the invariant is an Expense-class manual row on the ledger — available by hand, named nowhere, and it will show on the §2.3 spending surfaces as a second outflow. **Ruled:** V1.4's SELF-266 copy describes the procedure honestly (drain by an expense-class "settled" row → close → clear → designate) and names the spending-surface artifact; the one-step year-end settle affordance is booked V2 with this analysis as its spec input. **Losing side:** shipping a settle primitive in V1.4 — a new money-flow write with its own Sec review, outside the milestone's ruled set.
