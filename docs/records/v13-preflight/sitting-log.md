# V1.3 pre-flight batch-ratify sitting — running log

F/CTO rulings, in order, 2026-08-22. Durable landing: the close-out PR (amendments + ADR/WORKFLOW notes) after the sitting completes. Baseline: `0491830`.

1. **Start-order:** SELF-245/246/247 **promoted into V1.3** (Cash flow project, V1.3 milestone — liaison read-back-verified, comment IDs on the issues) AND receive the same two-sided pre-flight analysis as the other 11. V1.3 = 14 issues.
2. **D-3 (`is_tax_payment`):** **Option A re-confirmed** — to be recorded repo-durably this time (the original ratify existed only in a Linear description). Placement: **both `posting_prototype` + `posting_prototype_default`**, `085`-shaped (named CHECK, no helpful DEFAULT, fail-closed), with the ADR-057 provisioning-reach decision stated in the migration header. Feeds the SELF-245 re-scoped AC set.

2a. **D-3 sub-rulings (team-lead, under the ratified default-and-notify protocol — F/CTO reversal window open until the amendment batch lands):** column shape = **A1** (`boolean not null`, no DEFAULT, no CHECK — "085-shaped" is the discipline, not the letter; a CHECK on a NOT NULL boolean cannot fire, per the standing dead-leg rule). Sequencing = **(i)** — the F/CTO marking enumeration (245-AC5) is a **hard precondition on SELF-255 shipping**; no third "undecided" state enters the vocabulary. Architect's vehicle call stands: NEW ADR (ADR-057 provisioning-reach's first worked example) + migration header for the measurement trail.

3. **S-1 (classifiability boundary): RULED (b)** — the classify queue holds ONLY rows outside the mechanical posting vocabulary (trades/transfers/splits arrive pre-answered by `posting_prototype`); §2.3 surfaces sum only cashflow-domain-annotated rows. The concrete predicate (which transaction_types) is Architect's to define in the amendment batch; the PRD §2.3.1 "each transaction" passage amends accordingly (PM).

3a. **S-1 predicate encoded (Architect, drafting-ready):** `classifiable(row) := transaction_type='standard' AND security_id IS NULL AND split_count=0 AND (annotation IS NULL OR annotation.journal_id IS NULL)`; queue grain = unsplit transaction OR split child. The ruling's "cashflow-domain-annotated" phrase is delivered STRUCTURALLY by the `084` FK (no domain column exists — the ACs express it as a join, not a filter). E3 (LEFT-JOIN-not-inner, mandatory clause on 248/250/253/255 + a 257 battery leg), E4 (grain correction to PRD §2.3.1), E5 (per-security income attribution structurally unavailable in V1 — recorded product bound).
3b. **E2 (team-lead, default-and-notify — reversal window open):** `compound` group-type treatment in §2.3 is GATED AT THE FIRST CREATOR — a named constraint goes on whichever issue first creates a compound journal; no behavior today, M3 stands as written until then.
3c. **E1 — QUEUED FOR F/CTO (money-correctness, Sec-visible at SELF-250 joint review):** reverse-and-replace rows enter the classify queue with no link between a reversal's annotation and its original's → classified original + unclassified reversal = permanently overstated Sub-Cat. Options: (a) exclude `is_reverse`, net structurally via `replaces_trans_id` (Architect + team-lead lean); (b) auto-copy sub_cat_id at write; (c) user classifies both halves.

4. **S-2 (unclassified-state rendering): RULED (b) — loud exclusion.** Unclassified rows stay out of §2.3 sums; every §2.3 table carries the banner ("N transactions unclassified — classify") with a CTA into the §2.3.1 queue, totals footnoted as partial — the V1.2 loud-unpriced pattern applied. N derives from Architect's `in_queue` predicate (item 3a), one source. Lands as PRD amendment A-3 + one AC each on 251/254/256. Option (c) stays the §5.3 V2 candidate.

4a. **S-2 encoding refinements (team-lead, default-and-notify — window open):** banner copy = **"N items unclassified"** (queue grain is a union of unsplit transactions + split children; "transactions" is wrong under a queued split child). Per-surface N scoping stands as Architect encoded (251 = rendered year; 254 = account + year + THE SAME as-of predicate as its table; 256 = 5-year window with copy that must NOT claim "expenses"). Footnote renders iff N>0 from the same query as the total. **E2 watcher owner = QA**: the zero-`compound`-rows assertion rides the 257 battery (or earlier); "S-2b" is Architect's provisional label, not a ratified name.

5. **S-3 (period grammar × as-of): RULED (a) with the three companions.** Month = the month containing as-of date D. Companions: (1) YTD = Jan 1 of year(D) → D; (2) quarters not yet started relative to D render em-dash, NEVER $0; (3) "today" is server-derived per Lock 15, ADR-044 two-clock discipline — which clock is Architect's call. Grammar stated ONCE (Architect citation-table style); Finance_Report p.6 parity verified at build.

6. **S-4 (cross-domain correction): RULED (a).** Domain moves happen ONLY via the §2.4.3 edit / reverse-and-replace flow; the §2.3 picker stays strictly within the cashflow domain (matches the `023` fence; a domain move is a re-posting and reverse-and-replace is the ledger's sanctioned re-post). Costs one §2.3.1 pointer sentence telling the user where cross-domain fixes live.

5a. **S-3 encoding (Architect) + one default-and-notify ruling (team-lead, window open):** clock = `pfin.fn_server_today()` (ADR-044 D2 applied, not a new ruling), resolved ONCE per request and threaded to every consumer incl. the S-2 banner; new sibling factory in `asOf.ts` (fence enumerability kept); ADR-044's outstanding Decision-3 correction NOT declared done. Em-dash rule two-sided: `start(k) > D` → NULL → em-dash; `start(k) <= D` with no rows → `$0` (never collapsed by a coalesce). ΣQ1..Q4 = YTD partition identity = a SELF-257 battery leg (owner QA). Total row sums DOWN columns, never across. **Ruled (default-and-notify): `p_year` STRUCK from the 250 + 253 signatures — the year derives from `year(D)`;** two parameters that can contradict about the same fact, with no stated winner, is a defect generator. Clock half named at the SELF-247/D-6 Sec review, not settled silently.

6a. **S-4 encoding (Architect) + one default-and-notify ruling (team-lead, window open):** half one (cross-vocabulary/tenant/Trade writes) discharged by construction post-`084`. Half two is a measured money defect: no write-path enforcement of the read-side predicate — an Expense prototype on a journaled transfer leg posts as spending via `084:869`'s ordered CASE, silently. **Ruled: the SELF-248 endpoint refuses writes where `classifiable()` is false** (all four mechanical rules, generalizing the split-parent refusal). **D-8 QUEUED:** DB-fence hardening of `029`'s deliberately-soft precedence rule — options + Sec at SELF-248's joint review, F/CTO disposes. **A-2's §2.3.1 pointer sentence RE-GATED on E1** (the sanctioned route is E1's unresolved case). §2.3.1 copy must not promise transfers "cancel out" (journal-less transfers fall to Suspense). Sec's 248 review brief must name the M3 finding + what half one already fences.

7. **S-5 (split vs assignment granularity): RULED (a).** The V1.3 classify UI is whole-item (item = the S-1 queue grain: unsplit transaction OR split child — NOT whole-transaction); splitting stays in the shipped §2.4.3 flow. ⚠ CORRECTED WORDING (Architect measurement — the original log phrase "`038` split write, per-child `p_sub_cat_id`" was a false compound): `038` authors one parent-level function; split children are written by direct table DML under the `038:269` grant, category = the `sub_cat_id` COLUMN on `029`, balanced by the deferred Σ=parent trigger. Children classify independently in the queue. No new UI (`038` Part A shipped; 249's split row is a link, not a build). §2.4.3 hosts TWO mechanisms — reverse-and-replace (S-4's route) and the split path (S-5's) — cited separately, never interchangeably. **ALL FIVE SEAMS RULED.**

8. **E1 (reversal rows in the classify queue): RULED (a).** `is_reverse` rows are EXCLUDED from the classify queue and never classified by anyone; §2.3 readers net the pair structurally through the `replaces_trans_id` join, inside the original's Sub-Cat, invariant under later reclassification. Only the replacement row enters the queue. Unblocks the two dependents (248 guard scope; A-2's gated sentence). Sec's SELF-250 joint review names this invariant explicitly. **F/CTO rider question routed to Architect for measurement (not to be answered from recall): for a NEW reversal row, how do (1) the annotation/sub_cat_id and (2) journal_id populate relative to the reversed original in the SHIPPED reverse flow — in particular whether reversing a journaled leg requires journal membership on the reversal to preserve ADR-031 D5's Σ=0 conservation.** Under (a)'s design intent, sub_cat_id is NOT populated on reversals — confirm the shipped flow matches, and reconcile if it doesn't.

8a. **E1 rider answered by measurement (Architect) + two default-and-notify rulings (team-lead, window open):** (1) reversals carry NO annotation in the shipped flow — E1(a) matches merged code, no reconciliation owed; (2) a reversal of a journaled leg CANNOT be in the journal (journal_id lives on the annotation) — Σ=0 holds over original members, the reversal's contra falls to Suspense; a leg of a CLOSED balanced journal can be reversed with neither guard firing (pre-existing, now recorded). **Ruled: the §2.3-vs-GL netting disagreement (both-correct-not-equal, Suspense offset) is BOOKED now** (BACKLOG §7 at close-out, closed-journal note attached, named in Sec's SELF-250 brief — the §7.24 item-3 watcher and §5.3 P&L would otherwise meet it as a "discrepancy"). **Ruled: the partial-unique index on `replaces_trans_id` is PROMOTED from code comment to migration, same wave as the §2.3 readers** — under structural netting, double-reversal = double-net = money defect; what was edit hygiene is now an invariant.

9. **D-1 (§2.3 read substrate, ruled jointly with 247-AC3): RULED (iii) — the hybrid.** ONE extracted §2.3 reader over `account_trans` housing every reader rule once (S-1 predicate · split XOR · E1 netting join · E3 LEFT JOIN · S-3 period grammar · the Lock 15 dual-column as-of filter); 250/253/255 compose on it, adding only their own shaping. `fn_gl_entries` untouched. **247-AC3 lands INSIDE the shared reader for free** (dual-column filter written once, where `created_at` is available). One battery leg on 257 covers the rules once. The reader is V1.3's real foundation piece, landing with SELF-250.

10. **D-5 (§2.3.4 vs BACKLOG §7.14 ordering): RULED — §7.14 ships FIRST, as a hard precondition on SELF-255** (the SELF-245 marking-gate pattern). The `053` CPI positivity fence discharges before a fourth consumer lands on the store; the fence is the explicit finite-and-positive form (a bare `> 0` re-admits NaN AND Infinity — Sec condition (1)'s named trap); Sec's four binding conditions on §7.14 apply. Mechanics: §7.14's promotion (Linear issue vs fold-into-255-critical-path) settles at the AC-landing step.

9a. **D-1 reader spec encoded (Architect: provisional `pfin.fn_cashflow_items(p_as_of date)`, non-ratified name) + findings:** row set = union of what §2.3 sums and what the banner counts (one query, drift impossible by construction); item_kind names the grain in the data; 257 gets one leg per rule incl. the un-annotated-rows fixture and the created-on-D leg. **⚠ D-9 QUEUED FOR F/CTO — Lock 15's verbatim `created_at <= $1` predicate is DEFECTIVE (measured live):** date→midnight promotion silently drops every row created ON the as-of date; the reader writes half-open `created_at < D+1`; requires an ADR-011 Decision 19 AMENDMENT, never a quiet deviation. **Ruled (team-lead, default-and-notify, window open): reversing a SPLIT PARENT is REFUSED at the write path** (one predicate beside the existing reversal guard; remedy = unsplit first, then reverse) — the alternatives invent apportionment or re-introduce double-counting. Watcher scoping note: §7.24 item 3's totals-equality watcher must know about this reader when built (two independent readers agree only by discipline, nothing enforces it).

11. **D-2 (§2.3.3 middle section): RULED (B) — Transfer ∪ Equity, parity label "Other Cash Flows" kept.** Renders identically to Transfer-only today (Equity CHECK-legal, unseeded — `041`: 12/7/4/4/0); the section's contract (account-meaningful non-income non-expense flows) survives the vocabulary change; class-set parameter on the D-1 reader. Copy rider stands (no cancel-out promise — Suspense reality). **A-10's disposition = the §3.3-pattern label-mapping footnote (PM option i), ENTAILED by (B)'s composition** (Income→Revenue · Expenses→Expense · Other Cash Flows→Transfer∪Equity; Architect co-owns the mapping table) — logged under the same reversal window rather than as a separate docket item. **ALL TEN PRD PASSAGES NOW UNGATED.**

10a. **unsplit measured LOSSY-UNRECOVERABLE (Architect) + two default-and-notify rulings (team-lead, window open):** `unsplitTrans` bare-DELETEs the child set; split children have NO history table (`031` audits only the 1:1 annotation) — so the split-parent refusal (9a) carries TWO obligations: refuse, AND state the cost in the message ("removing the split will discard its N line categories, which cannot be recovered"). **Ruled: §7.14's vehicle = Shape 1 (its own promoted Linear issue)** — a precondition that lives inside the thing it gates is not a precondition. **Third booking added: the `023`/`029` audit-coverage asymmetry** (annotation reclassifications audited; split-child reclassifications not, incl. `writeSplitSet`'s REPLACE bare-DELETE) — not V1.3's to fix, V1.3's to make visible; books with the other two at close-out. D-9 carries the literal-string sweep rider (the Decision 19 predicate is quoted in SELF-247's own description — the amendment sweeps for the quote, not just the ADR).

11a. **D-2 encoding (Architect) + incident + one new queued item:** class-set is NOT a reader parameter — `where cat = any(p_cats)` drops every NULL-`cat` row and would silently zero the S-2 banner on every surface; each surface partitions, vocabulary lives in a shared `cashflowSections.ts` module (provisional name; `usEquitySubCats.ts` precedent). A-10 mapping table cited from `028`/`041` + the non-mapping AcctSetup row. **Incident recorded: Architect's D-2 edit deleted the whole D-3 block; self-caught (line count moved the wrong way + grep 0), restored in-turn, restoration recorded visibly. Team-lead snapshot backup of all six files taken to the session scratchpad (md5 7ecb4f40 verified) — temp/ durability risk is demonstrated; the close-out PR homes everything.** **EQUITY-SEED DISPOSITION QUEUED FOR F/CTO** (surfaced by D-2 (B)): `041` seeds ZERO Equity prototypes, so "Other Cash Flows" renders Transfer-only and no user can classify anything as Equity — while PRD §2.3.1's OWN OtherCF example ("IRA-Contribution") is Equity-shaped. Options: (a) seed Equity/Contribution + Equity/Distribution, riding D-3's migration with the same ADR-057 reach decision (Architect + team-lead lean — the PRD's own example is unreachable without it); (b) ship the class structurally-available-but-empty and say so in the PRD.

12. **D-6 (unmet Lock 15 mod #2 range bound): RULED (C).** The existing shared schema gets the bound and a surface-neutral rename (`asOfSchema`); one constant serves §2.2 + §2.3; the ceiling NAMES ITS CLOCK (server-derived per the S-3 ruling); plus one QA watcher leg on the merged §2.2 route (reject pre-floor + future) — the remediation is verified on the surface that had the gap. SELF-247's amended AC states the V1.2-remediation half honestly. Sec-mandatory (inside the Lock 15 fence).

12a. **D-6 encoding (Architect) + one default-and-notify ruling (team-lead, window open):** ceiling is INJECTED not embedded — `asOfSchema(maxAsOf)` factory, fed by the request's once-resolved D (ADR-044 discipline); any `new Date()` inside the validator = defect. `AS_OF_FLOOR = '2015-12-01'` declared once, net-new, architecture-exposed per the `nav-boundary.ts` principle. QA legs: below-floor/above-D rejected + BOTH inclusive boundaries accepted; the route-level leg's coverage precedent deliberately left to §7.25 item 3 (not set by side effect). **Ruled: the D-6 remediation AC ships in SELF-247's FIRST PR, decoupled from §2.3 threading** — the V1.2 fix must not inherit V1.3's schedule. D-6 and D-9 land as separate PRs (a bound is not a filter). Sec briefs for D-7/D-8 made cold-reader self-contained.

13. **Sec bounded consult returned (no veto; detail at temp/sec-v13-d7-d8.md):** **D-7 = reading (A), HIGH confidence** — the clause is §2.6-scoped, still true as written; NO live client as-of exists anywhere (all four route loaders call serverTodayAsOf(); §2.2's is capability-only, unwired). The amendment KEEPS the clause + adds the scope qualifier (commit-ready text in Sec's file, byte-for-byte); the wiring event is named as the joint-review trigger. **Premise corrections:** team-lead's D-6 framing ("live hole on merged surface") corrected to LATENT (validator unbounded but unreachable — D-6 (C) stands, urgency reframed); `asOf.ts` header's "FIRST live path" is false in the code comment → Backend one-line correction owed. **D-8 = (C) reformulated (C′ predicate derived from the defect: journaled ⇒ cat NOT IN (Revenue,Expense,Equity) — the naive non-Transfer form would refuse every in-kind transfer; C″ state-invariant WHEN (new.sub_cat_id IS NOT NULL AND new.journal_id IS NOT NULL) — the transition guard leaves classify-then-attach open; C‴ new function + new trigger, never editing the once-retargeted existing one) with SEVEN binding conditions** (NULL-safe fail-closed; INVOKER + empty search_path; pg_prove battery covering BOTH orders + lives_ok controls; violating-row count reported pre-land; app guard still ships with COMMENT explaining M1/M4 staying app-layer; UX for the newly-failing attach path; lands at SELF-248 joint review as an ADR-011 D2 surface). Not cross-tenant (own-rows only) — hence conditions, not veto. **NEW SCOPE CALL QUEUED: accounts/new future-dated as_of_date on a live write path** (pastOrTodayDate() schema variant; deliberately NOT a Decision 19 attribution — false-composite guard). Sec's temp file needs durable homes at close-out.

14. **D-9: AUTHORIZED — the single combined Decision 19 amendment.** Edit 1: predicate corrected to half-open `created_at < D + 1` with Architect's runnable measurement attached + the literal-string sweep (SELF-247's description quotes the defective form; sweep everywhere, a byte-exact quote of a corrected source is still wrong). Edit 2: the "ONLY surface" clause KEPT + explicit §2.6 scope qualifier + the wiring event named as the joint-review trigger — Sec's commit-ready text byte-for-byte, no re-flow. One amendment, its own PR (separate from the D-6 bound), Architect commits, drafted in the batch.

15. **D-8: CONFIRMED — Sec's (C′/C″/C‴) with all seven binding conditions.** New function + new trigger; raises on the defect state (journaled leg carrying Revenue/Expense/Equity classification — the GL CASE's fall-through set, derived not chosen); state-invariant WHEN closing both orders; in-kind (Trade) and Transfer pass. Conditions ride SELF-248's joint review verbatim from Sec's file. App guard (6a) still ships in full. (B) explicitly not required; `029`'s soft rule not amended by side effect.

15a. **Post-consult corrections (all visible, none merged away):** Architect's "live §2.2 as-of" premise refuted and corrected in four places (grep hit inside a comment read as a call site); **team-lead's 12a "ship in 247's first PR" ruling WITHDRAWN — its premise (live unmet SHIP-BLOCK) is gone; 247's internal sequencing returns to build-time discretion.** D-9 sweep measured: exactly ONE repo hit (DECISIONS.md itself) + SELF-247's Linear description (routed to liaison). Sec's Edit-2 text extracted programmatically, byte-for-byte (no retranscription). ⚠ CORRECTED at D-9 staging: the "0491833 vs 0491830" flag recorded here was FALSE — Sec's file carries 0491830 throughout; the stray digit was Architect's own hand-retyped paraphrase (cause named in its findings), caught by measuring the source at edit time. There was nothing to verify; Sec was right. **Ruled (default-and-notify, window open): the AS_OF_FLOOR is KEPT with its derivation recorded in the constant's comment** (Lock 15's NAV anchor floor) — resolves the floor tension within the D-6 (C) ruling as made. Sec's citation correction recorded (the "does not bound" sentence lives in the Lock-14 DELETE-policy posture bullet, not SD-22's matrix row).

16. **Equity-seed disposition: RULED (a).** `Equity/Contribution` + `Equity/Distribution` seeded per `028`'s own ratified map, RIDING D-3's migration — one migration, one ADR-057 reach decision (answered once, covering both the `is_tax_payment` column and the seed rows, stated in the migration header per the D-3 encoding), one Sec joint review. Battery legs accompany. The PRD's "IRA-Contribution" example becomes reachable; D-2 (B)'s section contract is fulfilled on both classes.

16a. **Equity-seed encoding (Architect) + findings + rulings:** ⚠ D-3's column REQUIRES a paired app-side edit — `taxonomy.ts`'s cashflow `DEFAULT_PROVISION_COLUMNS` must widen or fresh-signup provisioning fails SOFT to zero rows (the `085`/F4 hazard on the other branch; encoded as a paired-PR AC; battery leg asserts ROW COUNT, never absence-of-error). **Ruled (team-lead, default-and-notify, window open): the ADR-057 reach ruling = REACHES already-provisioned users BY EXPLICIT BACKFILL** (provisioning is existence-guarded and cannot deliver it; `077` precedent; covers column + seed rows in one header statement). Architect's authorized call recorded: the two Equity rows enter pre-marked `is_tax_payment = false` (true by class definition AND unreachable by the only consumer — 255 filters Expense first), with the `comment on column` REQUIRED to scope the flag's meaning to Expense-class prototypes so pre-marked false never reads as an answered question. **CONFIRM QUEUED: the two Equity seed rows' `tax_relevant`/`tax_character` values are PROPOSALS with no `041` precedent — F/CTO confirms before the migration lands** (joins the scope-call queue).

17. **D-4: CONFIRMED — the `cashflow_target` shape stands as ratified.** The `074` transplant holds (per-domain table, uuid `auth.uid()` anchor, identity PK, four aal2-conjoined policies, UPSERT-in-place); the D3-family citation was a mis-citation, struck, family count +0; the app-layer fence half owed at build with the two `074`-paid carries (NaN-explicit two-sided CHECK; no omitted DELETE policy per SD-22). All corrections live in SELF-246's amended ACs; the shape itself unchanged. Both auditors independent-concurred.

18. **Wording batch: AUTHORIZED WITH BOTH RIDERS IN.** The ten §2.3 amendments (A-1..A-10) + the §2.2.2 assets-only package (5 passages + §2.2.1 pointer) + Rider 1 (leverage-intent → BACKLOG §5.2 V2 candidate) + Rider 2 (ADR-061 alignment of §2.2.2's degenerate-state prose). PM stages the doc PR (standard flow, baseline sha per §7.19 AC5); landing gates on team-lead diff-verify.

19. **Scope calls: RULED AS RECOMMENDED (F/CTO, all four).** (1) SELF-245 AC4 STRUCK as discharged (measured at `041`); PM's mid-milestone tax inventory session BOOKED as V1.4 fuel (§7 note at close-out). (2) Unset-is-NULL-never-0 adopted as a new AC on 246 + 252 (the SELF-242 DELETE precedent; a stored $0 is a target, absence is not). (3) `accounts/new` future-date guard BOOKED as its own small Platform V1.x hardening item (`pastOrTodayDate()` schema variant + client mirror; liaison creates it). (4) Equity seed rows: **Distribution CONFIRMED** `tax_relevant=false, tax_character=NULL`; **Contribution's `tax_relevant` remains F/CTO's one open value** (the IRA-Contribution deductibility tension — the rec deliberately reserved it; `tax_character` NULL confirmed either way). SELF-332-arc team (backend/frontend/qa) torn down at F/CTO order; duplicates architect/sec stopped earlier.

19a. **Ruling (2) mechanism corrected (team-lead, default-and-notify, window open):** the SELF-242 precedent's PRINCIPLE transplants (never a stored $0), the DELETE verb does NOT — `cashflow_target` is one wide row with two independent scalars, and a row DELETE unsets both. **Adopted: unset one target = write NULL to that column; always-NULL-never-DELETE** (one representation; a two-NULL row and an absent row must not mean different things). Companion AC: SELF-252's endpoint accepts explicit `null` = clear vs omitted key = leave-alone (else unset is UI-unreachable under `.strict()`). Both presented at SELF-246's Sec review as a shape-driven deviation from Sec's own ruled precedent.

19b. **19a completion (Architect):** the equivalence clause is a READ obligation — both reachable shapes (zero rows vs one both-NULL row) mean "no targets set" and arrive as different driver result shapes; encoded as a new clause on SELF-250 AC6, the site where it is honoured or quietly broken. Close-out carries a provenance line so the 250 builder knows the clause's origin (it lands in a different issue from the ruling that generated it).

20. **Equity/Contribution `tax_relevant`: RULED `true`, with the notes rider.** Failure-mode asymmetry: false-and-wrong = silent omission of deductible flows from a money computation; true-and-overbroad = visible rows the booked V1.4 inventory prunes. The flag reads "the tax computation must look at this class," not "always deductible." Seed row's `notes` states it: "potentially deductible; resolve per account type at the V1.4 tax inventory." `tax_character = NULL` stands (no single FK fits a class spanning both treatments; NULL forces explicit V1.4 resolution). **THE SEED SPEC AND EVERY DECISION ITEM OF THE SITTING ARE NOW CLOSED.**

21. **AC RE-DERIVATION AND LANDING: AUTHORIZED.** Track A (Architect): assemble the 14 re-derived AC sets into one landing-ready document from the encoded findings. Track B (PM): stage the PRD amendment PR (item 18's authorized batch). Then: team-lead spot-verify → liaison lands ACs in Linear with read-backs → landing PRs in order (PRD → D-9 → D-3 ADR → close-out with bookings, durable homes incl. this log, protocols ADR, ledger update naming SELF-246 first dispatch). Provenance discipline: F/CTO rulings vs default-and-notify rulings at their distinct strengths; the reversal window closes at batch merge. Two cross-issue carries ride close-out: the SELF-250 reader clause (19a-origin) + the SELF-249 notes-surface revival condition (D-3-origin).

**THE SITTING IS CLOSED. 21 items ruled/authorized; execution is landing.**

**Landing log:** PR #544 (PRD batch) MERGED at `9985945`. PR #545 (D-9 amendment, `f63cb575`) open, update-branch applied, CI re-running. ADR-062 (`0b105659`, cut from `9985945`) tree-verified: +57 additive, numbered from the live tail; PR next after #545 merges (same-file sequencing — update-branch, never rebase). **Close-out sweep additions:** the two ADR-0NN placeholders resolve to ADR-062 · ADR-057's stale "Proposed" Status line corrected (its ratify condition met at `077`) · TWO "today's $ value" PRD passages (§2.1.2 ~187 + §2.3.4 ~259) corrected to the shipped `coverage_through` basis (falsifier: `067`; team-lead default-and-notify, F/CTO reviews in the close-out PR) · ADR-062's merge = the closure event for the 2a reversal window (its Status line dates that). → AC re-derivation authorization → close-out (bookings · durable homes · protocols ADR · Linear AC landing via liaison) → wording-batch authorization (A-1..A-10 + §2.2.2 package + 2 riders) → scope calls (245-AC4 deferral; unset-is-NULL; accounts/new future-date) → AC re-derivation authorization → Equity-seed disposition → wording-batch authorization → scope calls (245-AC4 deferral; unset-is-NULL; accounts/new future-date) → AC re-derivation authorization → Equity-seed disposition → wording-batch authorization (A-1..A-10 + §2.2.2 package + 2 riders) → scope calls (245-AC4 deferral; unset-is-NULL) → AC re-derivation authorization (A-1..A-10 + §2.2.2 package + 2 riders) → scope calls (245-AC4 deferral; unset-is-NULL) → AC re-derivation authorization → D-6 → D-7 → D-8 → wording batches (A-1..A-10 + §2.2.2 package + riders) → scope calls (245-AC4 deferral; unset-is-NULL) → AC re-derivation authorization → S-4 → S-5 → D-1 (rule JOINTLY with 247-AC3 — the fn_gl_entries created_at coupling) → D-2 → D-5 → **D-6** (Lock 15 mod #2 range bound UNMET on merged §2.2 — team-lead tree-verified 2026-08-22: no 2015-12-01 constant in api/src, no floor/ceiling in the shipped allocationAsOfSchema; Architect lean C = bound the existing shared schema + QA leg on the §2.2 route; partly V1.2 remediation; Sec-visible) → **D-7** (Lock 15 "ONLY surface" clause vs the shipped §2.2 client toggle — supersession-vs-drift fork; Architect leans reading A but explicitly routes the ruling to Sec + F/CTO because reading B = a merged surface exceeded a ratified fence) → PRD §2.3 wording (A-1…A-10) → §2.2.2 package (+ 2 riders) → PM's two scope calls (245-AC4 deferral to V1.4; unset-is-NULL-never-0 for 246/252) → 245 re-scope confirm (IMPOSSIBLE-as-scope-shrink: strike 4 discharged ACs, keep is_tax_payment + provisioning-reach halves, AC8 escalated to Sec-mandatory) → AC re-derivation authorization.

Architect extension totals: 0 CLEAN / 9 DRIFT / 5 IMPOSSIBLE across 14; Sec joint-review mandatory on 9 of 14 (gate map = architect-findings §6). Proposed dispatch order after amendments: 246 → 245 → 247 → 248 → 249 → 250 → 251/252 → 253 → 254 → 255 → 256 → 258 → 257 (close-gate last). Naming: Lock 15's p_data_as_of vs shipped p_as_of — settle once in the ADR at amendment time.
