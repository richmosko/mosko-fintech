# V1.5 execution log — rulings under F/CTO delegation

**Delegation (F/CTO, 2026-09-05, verbatim):** *"I want you to drive all issues in V1.5. You have authority to go with your recs and merge PRs. ping me when this is complete for review"*

**What that changes.** For the V1.5 build loop, the team-lead recommendation stands in for the F/CTO ruling at every decision point. Every such ruling is recorded here with provenance **TEAM-LEAD RULING UNDER DELEGATION** per [ADR-063](../../../DECISIONS.md#adr-063) Decision 3, never flattened into "F/CTO ruled." The reversal window on each is open until V1.5's close-gate (P10 / SELF-362) merges; F/CTO can reverse any entry by saying so. Merges: on green, by team-lead, per the same delegation.

**What it does not change.** Sec's veto stands. The live walk-through gate stands on every user-facing PR (ADR-063 Decision 4). Sec joint-review is mandatory on every V1.5 issue except P8 / P9 per the sitting log's R13 gates. The ruled dispatch order is [`../v15-preflight/sitting-log.md`](../v15-preflight/sitting-log.md) R13; the ACs are [`../v15-preflight/rederived-acs.md`](../v15-preflight/rederived-acs.md) as carried on each Linear issue.

**Already landed before delegation:** A8 / SELF-352 at PR #629 `30ac2cc` (migration `106`, 47-leg battery, Sec GREEN). One default-and-notify taken there, window now closed by F/CTO's sign-off: the `not_blank` CHECK stays.

**Migration numbers reserved up front:** `107` = A9 / SELF-353 (component-checkpoint table). Later numbers allocate at dispatch.

---

## Rulings

_(entries appended as they are made; one per decision; each names the options weighed and the losing side)_

### E1 — Wave 1 = Track 3 in full, in parallel: P7 + A9 + A6 + P9-after-P7 + the A3 latency probe · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
R13 lists Track 3 as five unblocked items. Ruled: dispatch all five at once across five agents (Frontend P7 then P9; Architect A9 DDL; DevOps A6 fence; Backend the A3 probe then A9's worker write; QA as responder on each). **Losing side:** strict serial order per R13's numbering keeps one Sec review in flight at a time and one QA pairing at a time; it would idle four agents behind the walk gate on P7. Mitigation: one branch per item, no shared files across the five (P7 `routes/settings/owner-id`, A9 `107` + `workers/etl`, A6 `scripts/ci` + `security-scan.yml`, P9 `routes/taxes/*`), Sec reviews queue on frozen shas.

### E2 — Cleanup of SELF-364 and SELF-326 · F/CTO DIRECTED · 2026-09-05
364 → Done (content merged at PR #625 `8e51d6e`). 326 → reopened to Backlog: `fn_gl_entries(date)` and `fn_holdings_as_of(date)` are still `provolatile = 'v'` at `30ac2cc`; the Done state named no delivering PR. Not V1.5; stays unmilestoned in Platform.

### E3 — The A3 latency probe runs on a scratch clone with a synthetic production-shaped tenant, never on the dev DB · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Backend found the local dev DB at `104` (two migrations behind `main`) and asked for `supabase migration up` against it; the classifier had blocked the call. Ruled NOT granted: that database is F/CTO's, and verification work never mutates it, additive or not (the standing scratch-DB rule). Route: clone `pfin_tmpl` (rebuilt through `106` by QA this session) via `scripts/db-template-clone.sh`, copy fixture data across by read-only `pg_dump --data-only`, and synthesize one ~20-account / ~5,000-transaction tenant in the scratch so the measurement is taken at production shape rather than on the 2-account fixture Backend measured as the largest on hand. Both columns reported; the recommendation rests on the synthetic one, cross-checked by the O(accounts)/O(transactions) scaling argument. **Losing side:** `migration up` is genuinely additive and would have been faster; it would also have set the precedent that a teammate may move F/CTO's database when the change "looks safe," which is the precedent the rule exists to refuse. **For F/CTO:** the local dev DB is two migrations behind `main` (`105`, `106`) — your call when to apply them.

### E4 — A6's lockfile-only golden fixture must fail on the lockfile route ALONE · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
QA's first fixture named `knex` in `package.json` with `pg` in the lockfile; DevOps's fence (`feature/self-350` @ `e97a98e`) reported `manifest:dependencies:knex,lockfile:knex,lockfile:pg` — `knex` is itself denylisted, so the manifest leg fired too and the lockfile-scan route had no watcher that reds by itself. Ruled: the fixture's manifest names only non-denylisted packages and `pg` enters via the lockfile's resolved tree alone, so the inversion output for that directory is `lockfile:pg` and nothing else. **Losing side:** the two-token fixture was a stronger violation and still proves the fence fails closed; it just could not tell a broken lockfile parser from a working one — the invariance-is-blindness class. QA fixes on `feature/self-350-qa`; merge and Sec spawn wait for it.

### E5 — P7 trims leading/trailing whitespace on save; the server-side files Frontend authored get a Backend re-read before Sec · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Frontend (`feature/self-359` @ `e879c35`) took two calls. (1) Trim-to-canonical before write: `106` carries no trim CHECK, so padded values are DB-legal; ruled keep the trim — the 120-character bound should measure what a reader sees, and a later `btrim` CHECK (the `101` `schedule_label` shape) would then find nothing to reject. **Losing side:** storing exactly what the user typed is the more literal reading of "plain text"; reversible with no migration. (2) My brief named `+page.server.ts` and the server hardening schema to Frontend because no Backend issue was paired with P7 — a role-boundary crossing on ARCH §4.1's server surface, flagged by Frontend in both file headers. Ruled: Backend re-reads the three server-side files against the SELF-233 conventions before the Sec spawn and either signs or lists fixes for Frontend to apply; the walk (QA) runs in parallel. **Losing side:** re-authoring them under Backend would be cleaner provenance for a ~150-line surface and would cost a second loop for no behavioural change. Recorded so the next P-item with a server half pairs Backend at dispatch rather than after.

### E7 — A9 captures leaves from `fn_account_unrealized_gl` (049), not `fn_nav_composition` (051); same-transaction kept; only `current_market_value` captured · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Architect (`feature/self-353` @ `a295d89`, migration `107`) found the AC's named leaf source stale: ADR-054 Decision 3 and AC 3 name `051`, but `102`/`105` since made `fn_nav_composition` anti-join out tax-authority-designated ledgers while `nav_daily` stays gross pre-tax (ADR-067 D3), so Σ(051 leaves) = `nav_value` − Σ(designated balances) and AC 6's reconciliation would be false on day one for any user with a designation. Ruled: build to `049`'s `current_market_value`, whose sum is `fn_compute_nav(as_of, true)` exactly; the ADR is not rewritten — `107`'s header records the finding and ADR-054's next amendment carries it. **Losing side:** `051` is the ratified name and the ADR's coherence note checked it; it checked the definition axis, not the leaf-set axis. (2) Same-transaction write kept over ADR-054 Decision 2's independence bullet: it is what makes Σ(leaves)=scalar a property by construction; D2's three bullets argue against widening `054`'s table, not against transactional coupling. **Losing side:** a leaf-side raise now rolls back that tenant's scalar for the day — stated in `107`, made observable by a QA leg, Sec rules at review. (3) `unrealized_gl` / `cost_basis` NOT captured: only `current_market_value` participates in the reconciliation, and `105` records a live residual (wash-sale `basis_adjust` and `corp_action` Suspense-parked) that would freeze a known-wrong figure into an append-only table indistinguishable from a measurement. **Losing side, and it grows daily:** every day before such a column exists is a permanent observation hole, and adding it later gives every prior row a NULL nobody measured — ADR-054's own asymmetry argument. ⚠ **F/CTO reversal window explicitly flagged:** cheap to add in this wave, not after; say the word and Architect adds the two columns before A9's PR opens. Also: the #19 entry Architect folded into ADR-011 D3 says "Sec-pinned at the SELF-353 joint-review" before that review has run — a forward reference to a mechanism not yet true; routed to Sec's brief for that PR.

### E8 — A6: Sec's F-1 / F-2 / F-3 all taken in this PR; F-4 (allowlist) recorded for A4 · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec AMBER on PR #630 @ `19a5bdd`. F-1 (pass-if-absent pinned a path — the rot class R6 rider 2 removed, one level down) and F-2 (the inversion step asserted only rc≠0, which is also what malformed JSON and a missing `node` return, so E4's route-independence had no CI watcher) taken as blocking. F-3 (npm alias evades both routes; `optionalDependencies`/`peerDependencies` unscanned; `@supabase/postgrest-js` missing) taken too — Sec offered a fast-follow and I declined it: ~8 lines, and a fence that ships with three measured evasions ships a claim. **Losing side:** the fence lands one loop later. F-4 (a denylist over an open registry is the wrong shape for a ~3-dependency manifest; allowlist as the fence + denylist as legibility, Sec's option B) is recorded for A4 / SELF-348's brief — flipping shape is itself a fence-boundary change and returns to Sec there. F-5 (SECURITY §4.5 RT-22 under-describes its CI surface) is Sec-owned on a follow-up branch, bundled with SELF-369.

### E9 — A3's render budget: on-demand generation p95 ≤ 2000 ms synchronous; the in-app view never composes live; A3 reuses `fn_nav_composition`'s tax envelopes · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Backend's probe (`meta/v15-a3-latency-probe` @ `7e0deb2`): composed set 549 p50 / 556 p95 ms on a synthetic 20-account / 4,903-transaction tenant with a second separate `104` call included; linear in transaction count; checkpoint population measured and traced as NOT bounding the scan (`holdings_checkpoint` has no reader in the chain; `fn_gl_entries` walks full history). Ruled per Backend's recommendation, which matches R1 (A): the in-app surfaces read the frozen payload, the composition runs only at generation. The double `104` evaluation (~30% of composed cost) is closed in A3's contract by reading the envelopes from `fn_nav_composition`'s payload. **Losing side:** an async on-demand path would be safe at any tenure; at V1 scale it buys nothing and adds a job state P5 was ruled not to have ("pending = awaiting commentary, no job states"). **Booked, not built:** a monthly materialized classification rollup (the `nav_daily` shape) as the mechanism that bounds the scan for aging tenants — Architect owns the BACKLOG entry via the consolidated ADR's Consequences. Two probe-side notes recorded: F/CTO's dev DB was read only (`pg_dump`); a `psql` without `PGPASSWORD` briefly hung the shared container's connection slots mid-probe and recovered on its own.

### E10 — A9 W3's did-it-insert signal is the row count, not `RETURNING nav_id`; `054`'s column grant is not widened · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
QA's battery (`feature/self-353-qa` @ `b291d31`, plan(47), both strikes clean) measured that the worker contract's `insert … on conflict do nothing returning nav_id` is rejected for `service_role`: `054` grants `select (users_id, nav_date)` only, and RETURNING needs SELECT on every returned column. Verified against `054:589`. Ruled: no RETURNING; the statement's row count (psycopg `cur.rowcount`) is the signal — 1 writes leaves, 0 writes none. Architect amends `107`'s W3 header in place (unmerged); Backend builds to the corrected form; QA's leg already uses it. **Losing side:** `grant select (nav_id)` is one line and keeps the header verbatim; it hands the RLS-exempt writer a read it does not need on the table whose narrow-grant model `054` spends a page defending. Also recorded from QA: an extra positive control (M1) and a watcher-teeth pair (RR4a/b) added beyond the leg list; the `qa` worktree's copy of its agent-memory index has diverged from `main` with an uncommitted stray file — housekeeping at close, not committed from that worktree.

### E11 — A4 dispatched as two halves on sibling branches; the RT-22 manifest fence flips to an allowlist inside A4's PR · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
A4 / SELF-348 goes out before the A1+A2+A3 unit reports (R13: A4 ∥ 6; the manifest fence is at PR #630 with CI green, awaiting Sec's flip). Backend owns the worker (Dockerfile extension, `package.json` at the minimum dependency set, the `setContent` + non-`data:`-abort render path, SD-20 auth, the exfil catch criterion with the two-aborts assertion) on `feature/self-348`; DevOps owns AC 4c (compose manifest with the RT-27 sentinel, private-bind wiring, golden fixture) on `feature/self-348-devops`, plus Sec's F-4 allowlist flip once Backend's dependency list exists — reviewed in A4's single Sec joint-review. **Losing side:** one branch, one owner, one review is simpler; it would serialize a DevOps task behind a Backend one that shares no files with it, and the allowlist cannot be written before the manifest exists anyway. Also: Backend's A9 module docstring restated W3's `returning nav_id`; corrected to the row-count form before assembly.

### E12 — A6 merged on Sec GREEN at `a696d32`; Sec's F-6–F-10 fold into A4's allowlist flip as one rule · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec re-verified `bf4aee5` by measurement (15-case evasion matrix, both fixtures against the workflow's own token assertions, a positive control proving the F-2 assertion fires on malformed input, three-axis CLEAN, CI-fenced RT set identical at both shas). Merged at the `454ef38` head after confirming the PR's files were byte-identical to the reviewed sha (main had moved under it; `gh pr update-branch`, never `--admin`). Sec's five new flags (tarball-URL / git / file specs, `overrides`, proxy-URL `resolved` parsing, and the discovery root being itself a pinned path) are low-reachability behind two independent Lock 13 layers; ruled fast-follow on `feature/self-348-devops`, collapsed into the allowlist flip's single rule — a spec that is not a plain registry semver of an allowlisted name is a violation — rather than five patches to a denylist Sec already rated the wrong shape (F-4). **Losing side:** F-7 alone is a two-line fix and could have shipped in #630; taking it there would have re-opened the review for a class the allowlist retires anyway. Recorded against myself: after the merge I ran `git checkout --detach main` in the DevOps worktree while DevOps was standing on `feature/self-348-devops` — a merge is a dispatch, and parking a teammate's worktree is one too; DevOps was told to re-checkout and audit before continuing.

### E13 — The A1+A2+A3+AH unit at `be7aed6`: eight Architect findings ruled · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Architect's unit (`108`–`111`, ADR-068, D3 #3/#4 realization, ARCH halves; `110`/`111` split so the DEFINER argument reads alone). Rulings: **F-1** (`fn_nav_delta_panel` / `fn_nav_reference_dates` at `097` take no as-of, so a regenerated report would freeze today's delta into a past month) — the catch criterion is stated (as-of-correct or `unavailable` with a stable code, never today's numbers as a measurement); Architect evaluates an as-of OVERLOAD that keeps the zero-arg signatures (no `regprocedure` break, no DROP) and adds it as `112` if it holds, else the shipped `reader_not_as_of_threadable` envelope stands and the follow-up is booked in ADR-068. **F-2** the render budget is stated over TWO `104` evaluations (the second is structural: `decomposition` / `jurisdictions` / `basis_year` / `prior_year_q4_window` are not in `fn_nav_composition`'s payload), closed with the measured 549/556 ms and the ≤ 2000 ms p95 on-demand budget (E9). **F-3** R5's dormancy is a product-path fact, not a grant fact (`reconciliation_event` has an `authenticated` INSERT grant + policy): the construction-only leg ships as ruled and QA adds a firing leg beside it; the rider amendment goes to Sec at review. **F-5** the audit helper is SECURITY DEFINER, realizing the reserved D9 slot, because A10 runs under the user's session and an INVOKER+grant path is the forged-history shape D9 already refused — accepted subject to Sec's veto; R7 rider 1's INVOKER reading is recorded as tried. **F-6** Architect correctly did not edit SECURITY; Sec authors the RT-21 row on a sibling branch at review. **F-8** A9 (`107`) merges FIRST; Architect then rebases the unit and writes the combined D3 opening sentence (nineteen labeled, eighteen DDL-realized) — neither branch's text taken whole. **One draft per month:** NO partial unique index — A10's AC makes regenerate an INSERT-new-version, so multiple drafts are the audit-class shape; `110` composes from the highest-`report_id` draft and echoes `source_report_id`; P4 finalizes that one. **Losing side:** unbounded draft rows per month and a listing that must hide superseded drafts (PM confirms P5 copes, or objects). `[PM PENDING]` (commentary bound, Sec N-5: PM's number) routed to PM.

### E14 — P2 dispatched stacked on the unit at `be7aed6`; P3 waits for its Architect half · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Frontend was idle after P9 while the unit's markers close. Ruled: P2 / SELF-354 (the in-app render — the same Svelte template A5 pushes to the PDF worker under R2) starts now on `feature/self-354` cut from the unit tip, with the standing predicate that `git diff <unit-tip> feature/self-354 -- supabase/ DECISIONS.md docs/ARCH` is EMPTY before its PR opens (a branch cut from an integration branch carries superseded sibling blobs — sec-265 F-1). **Losing side:** waiting for the unit to merge keeps every P-item serial behind one Sec review; the cost is one rebase. P3 / SELF-355 was the alternative and is NOT dispatched: its AC 5 write path is a SECURITY INVOKER plpgsql body taking a `FOR UPDATE` row lock (the `101` shape) — a migration, Architect's, not in the unit — and AC 3's side-by-side layout shape is Architect's too. P3 opens after Architect's marker closures with that RPC as its first commit; recorded so the next dispatch pairs the DB half at the start rather than discovering it in the Frontend brief.

### E15 — P9 walk GREEN with two legs disclosed as not walked; E13's draft ruling REVERSED to one live draft per month; commentary bound 4000 with code-point counting · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
**P9:** QA's walk (`self361-walk.md`) is GREEN on the four legs it could reach; leg 4 (loader fail-soft) had no sanctioned lever short of a grant mutation on the shared dev DB and is covered by the loader unit tests; leg 5 (`/` NAV regression) is blocked by a pre-existing environment gap (`layercake` / `d3-*` absent from the main checkout's `node_modules`) and the component is untouched by diff. Ruled sufficient for a non-Sec-classified item; PR opened, merge on CI + Backend's loader SIGN. **Losing side:** an environment fix and a re-walk would close both legs; it would hold a two-route badge behind an `npm install` in F/CTO's checkout. **Drafts:** PM confirmed latest-draft composition but objected to "no constraint" with a falsifying case (two tabs: edit draft #1, Generate → draft #2, Save lands on #1, P4 finalizes #2 blank — silent commentary loss, orphan permanent under 6(a)). E13 reversed: partial unique index on `108` `(users_id, target_month) where generation_status = 'draft'`; Generate on a live draft opens it; Regenerate is final-only. PM drafts the A10/P5/P3 AC copy; Linear carries it at the next liaison batch. **Losing side:** app-only enforcement is exactly the control the two-tab case defeats. **Bound:** 4000 characters per commentary column (PM); the client mirror counts CODE POINTS (`Array.from(s).length`) so the same value is refused at both layers (Sec N-5) — a deliberate difference from `106`/P7, where `.length` was accepted as the stricter direction.

### E16 — A draft composes live on view; only `final` reads the frozen payload · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Architect (Finding 5 on `feature/self-345` @ `9fc6d98`): `108`'s status CHECK permits a NULL payload precisely while `draft`, because R1 writes it once at finalization and the cron creates the draft row before any payload exists — so "the in-app view reads the frozen `final`/`draft` payload" (E9's wording, and P2's brief) is true of `final` and cannot be true of `draft`. Ruled: a draft composes live through `110` on view, with the row's `data_as_of` threaded; `final`/`superseded` read the payload column; the template takes a payload and never knows which path fed it; P2 tests both paths for shape identity. The probe's "do not render inline" stands for hot per-visit pages; an authoring view visited a few times a month is not one. **Losing side:** writing a provisional payload at draft creation would let both statuses read the column, at the cost of a stale draft showing numbers about to be finalized that are no longer true — the freeze's own failure inverted. Also from Architect: the probe's follow-up (a) (~30% saving by envelope reuse) is CORRECTED — reuse was already in `110`; the second `104` evaluation is structural because §2.6.1 Estimated Taxes needs `decomposition` / `jurisdictions` / `basis_year` / `prior_year_q4_window`, none of which `fn_nav_composition` emits; the ≤ 2000 ms budget was set over 549 ms and is unaffected; a tripwire of ~17,000 transactions (≈3.5× measured volume) is written into `110`'s clause as the signal to build the booked rollup or take the async shape. And a correction to myself: I cited `origin/meta/v15-a3-latency-probe` after it had merged and been deleted (PR #631, `b0335bd`); Architect read main instead, which is right.

### E17 — Sec F-6 closes by retiring pass-if-absent at A4, not by a readiness heuristic · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
DevOps (`feature/self-348-devops` @ `8a8fe81`) proposed a heuristic: FATAL when `workers/pdf-render/Dockerfile` contains `apt-get` but no manifest is found. Rejected: a fence keyed to a guess about how Backend will extend the Dockerfile is a convention with no mechanism — the class R6 rider 2 named — and it fails silently in exactly the case the guess is wrong. Ruled: the commit that lands `package.json` (A4's PR, in the allowlist-flip commit) makes the production step unconditional — zero manifests under `workers/pdf-render/` ⇒ FATAL, pass-if-absent retired and its retirement stated in the header. **Losing side:** the heuristic would have fired earlier than A4 in the one scenario it covers; that scenario has no owner and no watcher. F-10 (`lastIndexOf('/-/')`, bug demonstrated on a proxy URL) and the Dockerfile fence's forward pointer stand at `8a8fe81`. F-7/F-8/F-9 are folded into `isPlainRegistrySpec()` — a character-allowlist over every spec across six manifest sources incl. recursive `overrides`/`resolutions` — in the still-uncommitted allowlist draft, awaiting Backend's dependency list. PM's E15 AC copy is now on Linear (SELF-366 / 357 / 355, appended verbatim, read back clean).

### E18 — A4 worker at `d44d7d0`: the render endpoint is A4's; Sec's two-aborts criterion measured as one; Node 22 accepted · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Backend's A4 (`feature/self-348`): Dockerfile extended (base `node:20` → `node:22-bookworm-slim`, forced by the `puppeteer-core` version that clears an `npm audit` high; apt pins for hadolint), `package.json` at TWO dependencies (`puppeteer-core`, `jsonwebtoken`; Node's built-in `http` for the server), HS256 JWT auth per SD-20's own text (explicit alg allowlist, `users_id` claim required but never read past verification, 60 s `iat` window, per-container nonce store), `setContent` + interception aborting every non-`data:` scheme, browser-context-per-render, metadata scrub, Chromium child processes stripped of the signing key; 14 tests against real Chromium incl. a `docker build` smoke. Rulings: (1) the render endpoint is A4's by my scope call — A5 is the app-side caller + the RT-21 battery, and Backend's flag that A5 is NOT silently done is correct and recorded. (2) Sec's catch criterion says the interception handler records TWO aborts; measured: ONE — Chromium refuses the `file:///proc/self/environ` iframe under its own local-resource policy before any request event fires, so only the `http://169.254.169.254/` vector reaches the fence; both vectors are blocked and Backend's inversion (fence disabled → both assertions red) holds. Routed to Sec as their own criterion; the literal "2" cannot stand and the mechanism differs per vector. (3) Node 22 accepted; the reason is in the Dockerfile header. DevOps finalizes the allowlist to those two names and retires pass-if-absent in the same commit (E17); A4 assembles from both branches for one Sec review. **Losing side on (1):** splitting the endpoint into A5 would give A4 a Dockerfile-only diff and no behaviour to review; the RT-21 battery needs a real endpoint to red against.

### E19 — A4 assembled at `3c449cc` from both halves; the two halves land together · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
DevOps's allowlist commit (`831c76b`) retires pass-if-absent, so `feature/self-348-devops` alone would FATAL the manifest job; combined with Backend's worker the discovery loop finds the real manifest and passes. Assembled into one PR rather than two sequential ones. While running my third test case (`"x": "github:foo/bar"`) DevOps found its own `if / else if` bug — the spec-shape check ran only when the name was also allowlisted, so an unallowlisted name with a bad spec reported one token, not two; fixed so both checks fire independently. Recorded because DevOps's self-test with a temporarily permissive allowlist masked exactly that interaction — the invariance-is-blindness class again. Three fences rc=0 on the combined tree. Two Sec instances run concurrently (A9 at `5df28c2`, A4 at `3c449cc`) — the one case the reuse rule allows.

### E20 — A9 Sec AMBER: three text fixes taken plus the two coverage gaps; the unit's F-1 closes with distinct `_as_of` names inside `110` · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
**A9 (PR #633 @ `5df28c2`, verdict transfers to `defca86`):** security substance CLEAN on every item (#19's trigger realization forced and correct; F-4's narrowing right; aal2 byte-faithful; grants/immutability exact; worker GUC/role/gating holds; §10 and D9 untouched, three axes CLEAN). Taken before merge: F-1 (the fold-in's consequence (g) says "both `101` functions are INVOKER" — SELF-259 residue; `107` authors four — Architect, Sec's sentence verbatim), F-2/F-3 (the `(R0)` watcher's own message named the forbidden fix, "widen the grant"; the `R` header still said `RETURNING nav_id` — QA, Sec's text verbatim). Also taken now rather than as follow-ups, because QA is in the file: F-4 (three barred values on `…_value_finite` unwatched) and F-5 (#19's unresolvable leg unwatched). **Losing side:** Sec offered F-4/F-5 as follow-ups; a coverage gap on an append-only table with no correction path is cheapest closed before the table exists on main. Sec's ruling on same-transaction (item 2) stands with a correction to Architect's framing: ADR-054 D2 bullet 3's *block* clause IS contradicted for the write transaction, so the carve-out rides the queued ADR-054 amendment. F-6 (the five worker tests are `integration`-marked and `etl-ci.yml` runs `unit` + `pgtest` only — pre-existing class) → Linear item, DevOps + Backend. Sec confirmed "Sec-pinned at the SELF-353 joint-review" stands, and does not disagree with the E7 capture rider. **Unit (`feature/self-345` @ `2a6eefd`):** F-1 closed with DISTINCT names (`fn_nav_delta_panel_as_of(date)` / `fn_nav_reference_dates_as_of(date)`) rather than my overload proposal — Architect's reason is outside Postgres: shipped app code calls these over PostgREST RPC with no arguments, and an overload turns an empty body into a candidate-set resolution that does not exist today; the overload's own DROP objection is withdrawn by Architect as wrong. Zero-arg forms re-issued as delegators with volatility re-declared (`CREATE OR REPLACE` resets it — silent otherwise), equivalence proved non-vacuously. An ordering defect (panels first as `112`, which `110` referenced → cannot apply in filename order) was caught ONLY on a clean-chain apply; now `110` Part 1 / Part 2. Second clean-chain catch this session. `[PM PENDING]` closed at 4000 with the code-point mirror rule stated; live-draft index landed with E13's position recorded as the losing side.

### E21 — Under the allowlist, E4's lockfile-only fixture must carry only allowlisted names in its manifest · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
PR #634's CI (RT-22 manifest job, inversion step) failed on the assembled A4 tree at `3c449cc`: with the allowlist as the fence, the `lockfile-transitive` fixture's stand-in `objection-lite` is not allowlisted, so the manifest leg fires and E4's "no `manifest:` token" assertion FATALs. Two rulings collided — E4 (the lockfile route must red alone) and E8/F-4 (allowlist as the fence) — and the collision is the correct outcome: the assertion did its job. Ruled: E4's isolation stays; the fixture's manifest names only allowlisted packages and `pg` enters through the lockfile tree alone. **Losing side:** relaxing the assertion to ignore `(not-allowlisted)` tokens would let a fixture that trips both legs pass as lockfile-only, which is the vacuity E4 exists to refuse. Recorded against myself: before opening the PR I ran the three fences on the combined tree but not the workflow's inversion step, and the fence alone cannot show what the step asserts about the fence's output — the instrument cannot observe the property. Sec-a4's review at `3c449cc` transfers to the fixture-only delta on read.

### E22 — P3's and A10's write paths are authored as `112` / `113` on a stacked branch while A9 clears · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Architect is idle between A9's F-1 fix and the post-A9 rebase of the unit. Ruled: author the two DB halves Track 1 cannot proceed without — `112` the P3 commentary-write RPC (P3 AC 5: INVOKER plpgsql, `FOR UPDATE` first, `auth.uid()` tenant, replace-all + `commentary_disposition = 'authored'`, draft-window only) and `113` A10's generate-draft RPC (idempotent on a live draft per E15; INSERT-new-version shape shared with the cron; `fn_emit_audit_log` in the same transaction; the partial-unique race handled by re-read; Regenerate-on-final as `113` or `114` at Architect's call) — on `feature/self-355-db` stacked on the unit at `9174db4`, rebasing when the unit does. **Losing side:** waiting for the unit to merge avoids one rebase and keeps `112`/`113` numbering certain; it idles the wave's scarcest role behind a Sec flip that is text-only. Also recorded from Architect: the copied-consequence-block class (a claim whose shape is right and whose referent is wrong) is now three instances in this wave — (g) at the SELF-353 fold-in, and the #3/#4 entry descriptions amended at `108`/`109`; the rule Architect states — re-MEASURE a copied block's migration number and counts, never re-read them — goes to the apply-migration skill at close.

### E23 — P2 at `8894481`: nine Frontend calls ruled; NAV Performance re-renders from the unit's as-of readers after rebase · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Frontend built P2 on the unit's earlier tip (`be7aed6`) — my rebase message crossed with its work — so its calls (2) and (3) (no reuse of the NAV panels; a generic `reader_not_as_of_threadable` fallback) described a `110` that no longer exists: at `2a6eefd` the composer reads `fn_nav_delta_panel_as_of` / `fn_nav_reference_dates_as_of` and emits the shipped readers' real shapes. Ruled: rebase onto `9174db4`; reuse `NavDeltaPanel` / `NavReferenceDatesPanel` payload-fed (an entry point, not a fork), with the live components' "temporarily unavailable" copy gated off for a frozen source; the static series table stays (NavHistoryChart's interactive controls are wrong on a frozen report — Frontend's reason (a) stands). Other calls: (1) the P8 staleness slot ships un-wired — intended, P8 wires it; (4) Asset Allocation as a grouped table with a $ subtotal and no % — accepted, and widening the frozen payload with `total_non_re` is booked to Architect, not this PR; (5) `noTaxAuthorityDesignated` and `priorYearQ4` absent from the frozen payload — accepted, per-jurisdiction reasons still render; (6) `YYYY-MM` route param — accepted, P3/P5 match it; (7) final-wins tiebreak — accepted (E15 makes final+draft coexistence transient inside Regenerate's transaction); (8) Backend re-reads the loader before Sec, the P7/P9 pattern; (9) one additive optional prop on `TaxDecompositionTable`. **Losing side on (2):** forking a frozen-report panel would avoid touching two live components; it would also be a second copy of the §2.1.3/§2.1.4 presentation to keep in step with the readers `110` now delegates to.

### E24 — A5 at `5c472ea` on its own branch; RT-21 (g)'s worker half lands on A4's branch; A5's PR opens after A4 merges · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Backend's A5 (`feature/self-349`, cut from main `6c52cdb`): `api/src/lib/server/pdf/renderClient.ts` — `renderReportHtml(usersId, html)` returning `{ok, pdfBytes} | {ok:false, status}`, `mintRenderToken(usersId)` exported for the battery; RT-21 letters (a)–(g) re-derived under R2 (C) as vitest legs each failing for its own reason, the additions (tenant-claim presence required; audience/issuer declined by design and tested so an accidental add cannot slip in) labelled; RT-25 stated not applicable (the HTML is already rendered). Letter (g)'s worker-side half — a bounded rejection counter + structured log line keyed on the AuthError enum, 6 tests — was committed to `feature/self-348` (`6fbcec5`) because `auth.js` exists only there; Backend states it is the MINIMAL form and does not meet ADR-050 D4's retention clause, and routes the question to Sec in the header. Ruled: accepted as a cross-branch split (the alternative — blocking A5 on A4's merge or duplicating worker code onto A5's branch — is worse), A4's Sec review absorbs (g) at `6fbcec5`, and A5's PR opens after A4 merges so its (g) reference resolves on main. Two Sec proposals ride the module headers unresolved: item 6's `⟨OPEN⟩` (`users_id` claim vs an opaque correlation id — SD-20 ships as ratified) and the ADR-050 D4 retention gap. QA pairs the RT-21 battery's DB-free legs at A5's PR. Also from Backend: `npm run check` shows 15 pre-existing svelte-check errors from the `d3-*` / `layercake` module-resolution gap QA found — the same environment issue, now visible to the type checker as well as at runtime.

### E25 — A4 Sec AMBER at `3c449cc`: both conditions plus six of seven non-blocking findings taken now; "two aborts" struck from the criterion · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec ran the three fences, the RT-27 inversion, a 12-case evasion matrix against the allowlist and the worker's 14 tests against real Chromium; no veto trigger. **Conditions:** F-11 — `npm ci` runs `postinstall` and the allowlist fence does not parse `scripts`, so a lifecycle script installing `pg` passes CLEAN; taken BOTH ways (Backend `--ignore-scripts` in the Dockerfile; DevOps `scripts` as a seventh fence source with a golden fixture). D-1 — a pre-existing "stays 2-instance" bullet in `scripts/ci/README.md` now sits three lines from this PR's "third catalogued instance" sentence; Sec's Path-B replacement lands verbatim. **Rulings by Sec:** the literal "two aborts" in its own R2.2 criterion is STRUCK — Backend's three legs (a reachable `http://` positive control, `file://` attributed to Chromium's own policy, `data:` not aborted as the discriminating negative) are stronger than the number; ⚠ the same literal sits in AC 4b for P10 and must be corrected before P10 is built (Sec's wording placed with this log). RT-21 (g) at the worker is DISCHARGED by the stderr reason-code emission and NO storage surface may be created there (zero DB reach is a standing veto); a durable rejection ledger, if ever wanted, lives at the app (A5). SD-20 as JWT/HS256 confirmed as SD-20's own meaning. The PDF metadata scrub's limit is acceptable, recorded for SECURITY at the R14 doc PR. **Taken now, non-blocking:** F-13 (the PR body carried Backend's report of a docker smoke, `/healthz` and a 9-child-env check that are not in the tree — Backend commits a child-env watcher + `/healthz` test; the body is corrected; the smoke stays a recorded manual run), F-14 (no CI job runs the worker battery and the test self-skips GREEN without `PUPPETEER_EXECUTABLE_PATH` — DevOps adds a job that fails when the variable is unset), F-15 (compose `expose: 8082` vs Dockerfile `8080` — compose moves to 8080; no `PORT` env, SD-20 permits one variable), F-17 (`cap_drop: [ALL]` + `no-new-privileges`), F-18 (JS enabled on the page leaves WebSocket/WebRTC outside CDP interception — Backend decides `setJavaScriptEnabled(false)` against P2's SSR template and adds a "script does not execute" leg), N-3 (four stale (A)-direction sentences). **Recorded, not taken:** F-12 (lockfile `resolved` host unchecked — not live, all 41 entries registry-resolved) and F-16 (the `chromium` apt pin freezes the largest attack surface and hadolint has no CI watcher) → SELF-370's family. **Losing side of taking six findings now:** A4 lands one loop later; a worker that renders network-supplied HTML is the wrong place to bank findings. **Correction to my own brief:** the CI-fenced RT set DID change at this PR (`RT-30` comment-cited at `security-scan.yml`) — the grep returns comment-cited labels by design and must not be tightened; catalogued {RT-22, RT-26, RT-27} and fenced {RT-05, RT-22, RT-26, RT-27, RT-30} now differ by two labels, and are not reconciled.

### E26 — `112` / `113` / `114` at `5789c2d`: the 113/114 split stands; the N-5 mirror has a third layer only the client can hold · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Architect authored three SECURITY INVOKER, volatile-declared, `search_path=''` functions with no tenant and no `data_as_of` parameter: `fn_save_monthly_commentary` (`112`; the `FOR UPDATE` predicate carries `generation_status = 'draft'`, so a `final` row is not among the rows it can lock and the illegal statement is never constructed — `108`'s trigger stays the fence for every OTHER caller, disjoint callers, two controls), `fn_open_monthly_report_draft` (`113`; read-with-lock, else INSERT, on `unique_violation` re-read so both racing callers get the same id; an unrelated `unique_violation` is re-raised), `fn_regenerate_monthly_report` (`114`; `final → superseded` then delegates the insert to `113`; regenerate-on-draft inserts nothing). Ruled: the split stands — a boolean flag would make "what can supersede a final report" a question about argument values at every call site, and one unconditional function would let Generate on a final-only month supersede it because the user pressed the wrong button; the TOCTOU objection fails because each function reads state under its own lock. **Losing side:** two functions now lock rows of the same month — a lock-ordering surface (bounded: same order, `114` calls `113`) that any third writer must be checked against. A consequence taken on purpose: `SELECT … FOR UPDATE` is checked against the UPDATE policy's `USING`, so the aal2 step-up gates the commentary write through the lock statement and none of the three bodies mentions aal2. Recorded from Architect: E15 item 11's `\r\n → \n` client normalization is the only reason the two layers agree on a multi-line body and the database cannot verify it — `112` deliberately does not normalize (a visible refusal beats an invisible rewrite of stored prose), so N-5's equality holds at a third point invisible from both ends; QA documents the dependency with a leg. And a correction to myself: I cited `ac-amendments-e15.md` to Architect while it existed only on my unpushed record branch — the second stale pointer this session; `meta/v15-execution-log` is now on origin.

### E27 — P2 rebased at `a117826` with the NAV panels reused directly; P3 dispatched with the `$ ReAlloc` reference as an inline right-hand panel · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Frontend re-read `110`'s `delta_panel` / `reference_dates` CTEs after the rebase and found the field names match `NavDeltaPanelRow` / `NavReferenceDateRow` exactly, so `NavDeltaPanel.svelte` / `NavReferenceDatesPanel.svelte` are reused directly (the first pass's envelope notices removed; the dead `reader_not_as_of_threadable` copy gap closed by the payload no longer emitting it); the E16 same-shape leg is in the loader tests. P3 / SELF-355 dispatched on `feature/self-355` stacked on `feature/self-355-db` @ `5789c2d` (the `112` write RPC). AC 3 leaves the side-by-side layout shape to Architect; ruled here instead, on PM's own sub-heading copy — *"Keyed to this month's $ ReAlloc for {Cat} — shown at right"* — which already names the shape: an inline right-hand read-only panel reusing the shipped rebalancing-targets table. **Losing side:** a modal or a linked page keeps the editor narrower; both contradict "shown at right", and the layout decision was Architect's only because no copy had settled it. "Finalize {Month YYYY}" renders as a disabled control pointing at P4's route slot — P4 owns the transition. ⚠ The walk backlog is now three user-facing items (P7, P2, P3 to follow) behind the one dev-DB migration question with F/CTO; no walk can run until it is answered.

### E28 — A4 Sec addendum at `6fbcec5`: RT-21 (g)'s minimal form ACCEPTED; its retention clause becomes a DevOps configuration item, not code; ADR-050 D6's feasibility premise recorded as falsified by R2 (C) · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec re-read both head moves: `f5f0354` fixture-only (the CI red at `3c449cc` was the E4 watcher working — relaxing it would have destroyed the only thing the fixture proves; the fixture's coupling to the production dependency set is inherent under an allowlist, and NARROWING the allowlist re-breaks it, watched by the same leg); `6fbcec5` ruled R-6: five of ADR-050 D4's six clauses met (bounded key space, queryable JSON, no attacker-controlled content, no tenant resolution — the clause D6 warned about — retained-partial); clause 6 (retention independent of container-log rotation) not met and not meetable at a zero-DB-reach worker in code. Ruled with Sec: ship the container's stderr to a sink with its own retention and a volume cap — a Linear issue for DevOps carrying Sec's two forbidden shapes (echoing the reason code to the caller; an app-side counter, which measures the wrong population) and the discharge criterion. ADR-050 Decision 6's feasibility half presupposed a verifier with DB reach (the app's retired route); R2 (C) moved the verifier — Architect records it against D6. Sec corrected its own F-12 remediation (a registry-host regex would have red-lit both fixtures; the corrected predicate — `nameFromResolved(resolved)` equals the entry name and `integrity` present — is host-agnostic) — SELF-370's text is corrected accordingly. F-14 splits: the auth half needs no Chromium and gets a CI job now; the render half's self-skip must FAIL, not pass, when `PUPPETEER_EXECUTABLE_PATH` is unset. F-13 still open until Backend's watcher tests land; the PR body is corrected. **Losing side of accepting the minimal (g):** the one clause not met is the one D4 itself calls "the property actually being lost today"; the alternative — a storage surface at the worker — would give the PDF worker a database client, which is the veto.

### E29 — The ADR-054 amendment at `8aab836` is parked behind A9's merge; its two governance additions are accepted · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Architect's `meta/adr054-amend` (19 insertions, zero deletions — every original sentence stands): Decision 3's leaf-source sentence superseded by `049`'s `current_market_value` as a dated identification, not a decision (the ruling — leaf granularity, re-aggregability, the growth commitment — untouched); Decision 2 bullet 3 stated in both halves (superseded for the write transaction, surviving for the surface) with the residual stated, not argued away; the reusable lesson recorded at length — a coherence note covers only the axis its author was thinking about. Two governance additions not in the queue file, accepted: the #19 fence could not take the `WITH CHECK` form Decision 3 names first (a policy clause is not evaluated for a `rolbypassrls` writer), generalised as "realization form is decided per instance by its writer set, not by column arity"; and the trigger form's cost under `session_replication_role = replica` owes a post-load validation in any restore/bulk-load runbook. Ruled: the PR opens only after the ADR-050 D6 annotation lands on the same branch AND PR #633 (A9) merges — the amendment cites label #19 by name-anchor, and `107` is not on `main`; merged first, a reader following the read-D3-live discipline would correctly conclude the text invented a label. The constraint is written into the file so it survives the queue; the rebase after A9 re-reads it as historical. **Losing side:** merging the doc first keeps the ADR ahead of the DDL it describes, which is the direction that reads as fabrication.

### E30 — A4 worker fixes at `2b1370c`: JavaScript stays ENABLED in the PDF page, on a measured content reason; A7 dispatched stacked on `113` · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Backend: `--ignore-scripts` (F-11A, losing side in the Dockerfile — a future dependency needing an install script must be allowlisted AND have its script reviewed); `healthz.test.js` + `render-env.test.js` (F-13 — the child-env watcher and `/healthz` now committed; the docker smoke stays a manual run); five (A)-direction sentences corrected across the Dockerfile and two per-directory CLAUDE.md files (N-3; two more in `.env.example` routed to DevOps). **F-18 decided: JS stays on.** Backend read the actual template the worker renders (`MonthlyReportView.svelte` @ `a117826`): the Historical Expenditures section mounts a LayerCake chart whose sizing is measured client-side (the `NavHistoryChart` precedent), so `setJavaScriptEnabled(false)` would ship a degenerate SVG in every PDF — a content defect traded for closing a gap reachable only through an XSS that AC 5's escaping is the control for. Accepted pending Sec's re-verify, with Backend's proposal routed: widen P6's leg to assert injected `<script>`/handler content never survives template compilation as live markup; a new render leg proves the mechanism today (a script executes AND its own `fetch()` is caught by the same interception fence). **Losing side:** JS off closes WebSocket/WebRTC exfiltration structurally; it would need a server-side chart renderer for the PDF path, which is a P6/Visual-Designer item nobody has. A7 / SELF-351 dispatched on `feature/self-351` stacked on `feature/self-355-db` @ `5789c2d`: the cron calls `113` per tenant under impersonation, so the INSERT shape is shared with A10 by construction (R9 rider 1) rather than by discipline.

### E31 — The unit battery landed at `be7aed6` before the A9 fix; order corrected; four sibling batteries' DEFINER-count assertions moved 3 → 4 · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
QA delivered the A1+A2+A3+AH batteries (`feature/self-345-qa`: `108` plan(32), `109` plan(17), `110` plan(11), `111` plan(19); one inversion strike per file with the blast radius named) pinned at `be7aed6`, ahead of the ~30-line A9 fix that gates PR #633 — my priority message crossed with the work already underway. Ruled: A9's fix first, then the re-pin to `9174db4` with the E15/E20 legs (the live-draft partial index in both directions; delegator-equals-`_as_of` on a non-empty set; `provolatile = 's'` on all four panel functions; the 4000-code-point boundary with an astral case). `111`'s DEFINER function cascaded into four OTHER battery files (`057`, `058`, `103`, `self209_close_gate`) that hardcoded "3 SECURITY DEFINER functions"; QA moved them to 4 in `108`'s commit. Recorded for Sec at the unit's review: those are D9-allowlist assertions (the allowlist has four ENTRIES with three authored on `main`; `111` authors the fourth — the "committed = 4, authored = 3" distinction Sec drew at PR #629), and a battery asserting a function COUNT is the derivable-count class this project drops rather than corrects — Sec rules whether the legs should assert the four NAMES instead. QA's finding, generalised into its memory: any DML whose WHERE clause reads a column needs SELECT on that column — the same privilege family as E10's `RETURNING`, the WHERE variant; a test-only `GRANT UPDATE, DELETE` without SELECT reds with "permission denied" on an immutable table. Pre-existing and out of scope: `054`'s battery reds 3/75 on the shared test container's doubled `pfin_etl` role membership (cluster state from repeated template rebuilds; needs `REVOKE … GRANTED BY` — DevOps/F/CTO).

### E32 — A4 assembled at `68cf8d7` from both fix branches; the pdf-render CI job is run-always; one `npm test` defect found by DevOps goes to Backend · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
DevOps (`168401d`): `scripts` as a seventh fence source (any key outside `{start, test}` is a violation, with a `lifecycle-script` golden fixture isolated to its own token), Sec's D-1 sentence verbatim, `expose: 8080` (the Dockerfile's real port), `cap_drop: [ALL]` + `no-new-privileges`, a `pdf-render` job in `worker-ci.yml` whose guard FATALs when `PUPPETEER_EXECUTABLE_PATH` is unset (Chromium is pre-installed on the GitHub runner image, apt fallback otherwise), the (A)-direction `.env.example` sentences. Accepted DevOps's deviation from my brief — run-always rather than path-triggered — on its stated ground: no heavy install, and run-always is `worker-ci.yml`'s own design philosophy. DevOps also found, while verifying F-14, that the committed `"test": "node --test test/"` crashes with MODULE_NOT_FOUND on node 22 and 26 — `npm test` as committed runs nothing for any developer; routed to Backend as a one-line change on the assembled branch before Sec's re-verify. Assembled: Backend's `3dcf082` + DevOps's `168401d` + main; all three fences rc=0 on the combined tree. Recorded against myself a second time: I parked the DevOps worktree by `checkout --detach` while assembling, after logging the same act at E12; DevOps's work was already on origin, and DevOps was told to verify rather than trust my read of "clean".

### E33 — A9 merged at `326a9f2` on Sec GREEN; the unit and `112`–`114` rebase onto it; the ADR doc PR opens; D3 memory moves to 19/16 with no "next" label · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec re-verified `08cfd57` byte-by-byte against its own supplied strings (F-1's splice boundary at char 493 of bullet (g); the 454-character `(R0)` message; the `R` header line), re-counted plan(52) mechanically, traced `(N4)`'s inserted row against every later count in the file, and confirmed `(M4)` reds under the #19 strike by flipping to a plain FK violation — the ordering proof. Merged; `107` is on `main`. Post-merge chain: Architect rebases `feature/self-345` and `feature/self-355-db` and writes the combined Decision 3 opening sentence (nineteen labeled, eighteen DDL-realized once `108`/`109` realize #3/#4); Frontend and QA rebase their stacked branches onto Architect's tips after they land; the ADR-054/ADR-050 doc PR opens now — its ordering constraint (label #19 must exist on `main`) is satisfied. Memory: the team-lead and Sec copies of the Decision-3 reference now read nineteen labeled / sixteen DDL-realized on `main`, with the instruction that NO "next" label is named anywhere (the forward numeral was dropped, not re-pointed) and that the unmerged unit will move the realized figure — do not pre-count it. Sec's one post-GREEN note recorded: QA's rewritten `(R0)` comment says `054` requires review for "any grant change"; `054`'s own text scopes it to WIDENING — over-strict, nothing weakened; the accurate phrasing goes in whenever the file is next touched. F-6 (the five `integration`-marked worker tests never run in CI) → Linear, DevOps + Backend. Linear: SELF-353 Done.

### E34 — P3 at `b9ce3cb`: six Frontend calls ruled; the P2/P3 shared module is extracted when P3 stacks on P2 at the post-A9 rebase · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Frontend built the commentary editor stacked on `112` (four textareas in Cat-group order reusing `NONRE_TABLE_CAT_ORDER`; the `$ ReAlloc` panel as `<NonReAllocationTable>` fed from the same live reads `/allocation` uses — SELF-330's staleness already live on it, so no P8 slot there; copy-from-prior per section and global; the E15 riders; Save via `112`; Finalize as a permanently disabled stub — P4 owns the transition). RT-11 legs: 22 loader cases incl. the 3,996-ASCII + 4-astral body accepted (4000 code points / 4004 UTF-16 units), 4001 refused before any RPC call, mass-assignment (five params, `target_month` from the route), both P0001 messages and 23514 collapsed to one generic sentence; 18 DOM cases. Rulings on the bubble-ups: (1) `parseTargetMonth` / `CommentaryValues` duplicated from P2 — ruled: at the post-A9 rebase P3 stacks ON P2 (`feature/self-355` onto `feature/self-354`) and imports one shared module; the duplication does not reach a PR. (2) `disabled` prop on `TextAreaField` — additive, accepted. (3) copy-from-prior predicate (per-section disabled when that prior section is blank; global only when all four are) and "copy" as overwrite — accepted on the PRD's "as a starting point" framing. (4) the editor-note microcopy is self-authored — accepted as the same class as the shipped editors' hints; UX may reword at any walk. (5) a real `<textarea>`'s `.value` already normalises CRLF → LF per the HTML spec, so the E15 client normalisation is idempotent on the typed path and load-bearing only for a non-browser caller of `112` — recorded; the step stays. (6) the `42501` branch is defensive and unreachable via `112` today (the aal2 case resolves through the same zero-rows P0001) — accepted. Backend re-reads the loader before Sec, the standing pattern.

### E35 — The ADR-054 / ADR-050 doc PR merged at `c6884c2`; the unit and `112`–`114` are rebased onto post-A9 main · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
PR #635 (doc-only, 39 insertions, zero deletions, verified against `main` after a branch update) merged under the pre-cleared doc class; ADR-054's leaf-source sentence and Decision 2 bullet 3 are amended beneath their dated text, and ADR-050 Decision 6 carries the R2 (C) feasibility annotation. The tree shows `feature/self-345` at `99b19e6` and `feature/self-355-db` at `f08a266`, both descending from `326a9f2` (A9); Architect's report on the combined Decision 3 sentence is pending and the stacked Frontend/QA branches rebase onto those tips once it arrives. Note for the unit's PR: main moved again with this merge, so `gh pr update-branch` will be needed before its merge — the BEHIND state, never `--admin`.

### E36 — The unit rebase's combined Decision 3 sentence and three Architect judgment calls accepted; QA's out-of-scope battery repairs accepted · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Architect rebased `feature/self-345` → `99b19e6` and `feature/self-355-db` → `f08a266` (both re-applied from `001` clean; DEFINER set unchanged at four names). The D3 opening sentence was rebuilt from main's bytes with exactly two substitutions — "seventeen" → "eighteen" DDL-realized, and the "#3 + #4 … remain DDL-deferred" clause → their realization at `108`/`109` with the conclusion that no labeled instance is DDL-deferred any longer — neither branch's text taken whole; fidelity by construction. Accepted: resolution-block (f) retained as "MERGE HAZARD — RAISED IN ADVANCE, AND NOW DISCHARGED" (a hazard written down and handled looks identical, afterwards, to one that never existed — the record keeps it); the unit's (a) annotates `107`'s dated (a) rather than editing it; the `meta/adr054-amend` governance-ordering note is NOT folded into ADR-068 (it served its purpose and reads as historical on `main`). QA's re-pin (`e29cf2f`): closing Finding 1 silently broke two `110` legs (LEG 5 asserted an envelope that is now a real array; LEG 7 asserted the retired fallback verbatim) and E15's index made the E13 two-drafts fixture abort the file with an uncaught 23505 — QA repaired all three beyond the literal ask; ruled correct: a battery shipped with legs that red on non-defects is worse than one with fewer legs, and each repair carries a new watcher (a past month anchors on that month, from Architect's worked example; the composer reads the single remaining draft). The E15 legs (14a: second draft → 23505; 14b: a draft beside final + superseded accepted, proving the index is partial), D1 delegation equivalence and D2 `provolatile = 's'` landed; the astral 4000/4001 leg follows. **Losing side of accepting repairs beyond the ask:** the ask should have named them; it did not, because I had not re-read `110`'s new shape against the battery — recorded.

### E37 — A7 at `f7aa971`: `113`'s audit trigger source is hardcoded `'on_demand'` (defect, Architect fixes with a non-forgeable source); the cron's write through `impersonate()` is ruled the R3 (i) shape provisionally, Sec ratifies · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Backend built the cron (`run_monthly_report.py` + `monthly_report_cron.py` + a minimal `notify_discord.py`; 13 integration + 8 unit tests on a scratch clone through `114`; per-tenant transaction and try/except; `RESET ROLE` leak test; `target_month` from the worker's own Postgres `current_date` per `070`'s documented ETL exception; tenant enumeration mirrors `nav_daily.py`'s account-owning predicate) and did NOT work around two contradictions it measured. (1) `113` passes the literal `'on_demand'` to `fn_emit_audit_log` (verified at line 242), so the cron's rows are indistinguishable from on-demand ones and R12 clause (2)'s month-count would read the wrong provenance — Backend committed a `test_MEASURED_GAP_…` leg asserting the wrong value on purpose, to be flipped when `113` changes. Ruled: Architect fixes `113` against a catch criterion — `'cron'` from the cron's impersonated session, `'on_demand'` from the app, and an `authenticated` PostgREST caller cannot produce `'cron'` — so a caller-supplied parameter is excluded (forgeable provenance); a transaction-local GUC the cron sets (the `054` pattern) is the candidate. (2) `113`'s Gate A shape (no tenant parameter, `authenticated`-only EXECUTE) makes the cron's only path an RPC call under `TenantBoundConnection.impersonate()`, whose docstring forbids wrapping a write and whose fence names this exact residual. Ruled provisionally, Sec to ratify at A7's review: it IS the R3 (i) impersonation shape — the session is the tenant binding, the INSERT is attributed by `auth.uid()` under RLS, and the rule was written for direct DML by an RLS-exempt role; Backend's alternative (a `p_users_id` for a `service_role` call) reopens Gate A and is not taken. If ratified, `connection.py`'s docstring names the exception. **Losing side:** a cron-specific sibling of `113` callable by `service_role` with a tenant parameter keeps `impersonate()` pure and puts the cron on the same writer model as `nav_daily.py`; it would be the second write path for the same row shape, which R9 rider 1 ("the same INSERT shape") exists to prevent. A7 is cut from the pre-rebase `5789c2d` and rebases onto the fixed `113` tip. Also: the A4 `npm test` one-liner had NOT landed (crossed messages; `origin/feature/self-348` still at `df87b9e`) — Backend re-pointed; it gates the A4 merge.

### E38 — The unit PR opens at `ddb62fb` (PR #636) with one Sec joint-review; Sec authors the RT-21 SECURITY row on a sibling branch · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Assembled from Architect `99b19e6` + QA `b57c9a0` (86/86 across four batteries on the rebased tip; the QA delta is tests-only) + main `c6884c2`; the combined D3 sentence present once; four DEFINER-declaring migrations on the tree (`111` is the fourth). The PR closes SELF-345 / SELF-346 / SELF-347 together — one review, per R13 step 6 and R14. Sec's brief carries the four items the PR body flags for it (RT-21's row; the DEFINER-count edits in four sibling batteries — names vs count is Sec's call; the R5 dormancy rider; the N-5 third layer) and, as the one write it may make, the RT-21 row rewrite on `feature/self-345-sec` — Architect declined to edit a Sec-owned artifact, which was right, and the substance is in the placed design notes. **Losing side:** splitting the unit into three PRs would give each migration a narrower review and three Sec passes on one composed surface; R14 ruled one review and the DDL agrees — `110` reads `108` and `109`, `111` is called by nothing until `113`.

### E39 — A10's write path is `113` + `114` + P5's form actions; the freeze point (A10 item 6 / P4) becomes `115`, the finalize RPC · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Reading A10 (SELF-366) against what exists: items 1–5 and 9–11 are discharged by `113` (open-or-create, idempotent, audit row, the E15 race handled) and `114` (final-only regenerate) called from P5's form actions under the caller's session with no `data_as_of` parameter (RT-25). What no migration yet holds is item 6 — the freeze point: when P4 promotes `draft → final`, `110` composes and the payload is written ONCE in the transition's transaction. Ruled: `115_fn_finalize_monthly_report` (INVOKER, `FOR UPDATE` on the live draft, compose via `110` with the row's `data_as_of`, write payload + schema version + disposition, flip to `final`, audit row, one transaction) on `feature/self-355-db` after the `113` fix; P4's UI (Frontend) calls it; A10's own Backend half collapses to nothing beyond P5's actions, and SELF-366 closes with P5's PR unless Sec's review of `113`/`114` asks for an app-side layer. **Losing side:** an app-side endpoint for A10 (the AC's original shape) would give the on-demand path a place for rate limiting and a request log; under Gate A and R9 rider 1 the DB function is the one INSERT shape both cron and app share, and a second app layer would be a second place for the freeze to go wrong. A4's final head `d49cc15` (bare `node --test`) is with Sec for the full re-verify; Backend SIGNed P3's loader with no fixes.

### E40 — SELF-372's lane uses the Supabase CLI stack and an explicit file list; `pfin_etl` stays unprovisioned; A7's cron container half dispatched · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
DevOps (`meta/etl-ci-integration-lane` @ `dc6d30f`, PR #637) read the target tests before writing YAML: they `docker exec` against the Supabase CLI's own `supabase_db_<project>` container and need the `auth` schema, which a bare `postgres` service cannot produce — so the lane brings up the CLI stack as `db-tests.yml` does. Two deviations from my brief accepted: an explicit two-file list over a bare `-m integration` (which would sweep in two legacy tests needing a hosted `.env` + live external creds and reopen the vacuous-green hole), and `pfin_etl` (NOLOGIN by design at `055`) left unprovisioned because neither file needs it and provisioning needs a new `ci_only` secret — DevOps stopped at the manifest boundary as instructed, recording the candidate name `PFIN_DB_PASSWORD_TEST` without creating it. Two fail-closed layers: exit 5 on zero collected; a summary-grep guard failing on any skipped test (the files self-skip when the container is unreachable — correct locally, the finding itself in CI). 12/12 measured. A7's tests will join the list; they may be the first to need the secret. **Losing side:** a bare `postgres` service is lighter and matches `pgtest`; it cannot run these tests at all. A7's Coolify cron half dispatched to DevOps on `feature/self-351-devops` (schedule `0 6 1 * *` with the UTC residual stated per AC 9; env list matched to the existing nav-daily cron; Discord operator wiring; no invented RT-27 sentinel — cron containers have no admission endpoint).

### E41 — A4 merged at `bde35a7` on Sec GREEN; `111`'s audit helper is a `'cron'` forgery channel — routed into the live unit review; A5's PR assembles · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec's final A4 pass closed every condition and flag by measurement (F-11A's flag proven to suppress a scratch `postinstall`; F-11B as a key allowlist, stronger than Sec's own proposal; F-14's guard FATALs before the self-skip is reachable; N-3 over-delivered into the two per-directory CLAUDE.md files Sec's own enumeration had missed) and ruled R-7: JavaScript stays enabled and Backend's injection-point leg beats Sec's worker-side one — with a condition that a STRUCTURAL `{@html}` grep-fence over the template path accompanies it at P6/P10, and the residual recorded that WebSocket/WebRTC exfiltration is fenced by RT-27, not by the worker's code. Sec named two instrument errors of its own (a stale `origin/main` ref that made `107` look like part of a PDF-worker PR — the two-dot diff was the wrong instrument regardless; a paraphrased copy of `nameFromResolved` that would have red-lit the production lockfile). Merged at the refreshed head `77bfbd5`, content byte-identical to `d49cc15`. NEW-1–NEW-4 and NEW-6 → SELF-370 / SELF-358 / SELF-362. **`111` finding (Architect, measured on a clean chain while fixing `113`):** `fn_emit_audit_log` is DEFINER, EXECUTE-granted to `authenticated`, exposed, and takes `p_trigger_source` from its caller — an ordinary session minted an uncorrectable `trigger_source = 'cron'` row, inflating the V1.final month count R12 clause (2) reads. Ruled: routed into Sec's open #636 review with three candidate shapes; `111` is unmerged, so its signature is free to change on the branch; Architect prepares commit-ready fixes for shapes (i) and (ii) and pushes on Sec's ruling; `115` waits behind it. `113`'s fix at `c24a000` is accepted in all six judgment calls (the `app.` namespace on the exposed-INVOKER-writes-`pfin.*` ground; exact match defaulting to `on_demand` as the fail-closed direction; the chain derived alongside; transaction-locality as a perimeter obligation). A5 (`feature/self-349`) assembles with main now that A4's `auth.js` is there.

### E42 — P5 at `6fb0a0f`: five Frontend calls accepted; the P2/P3/P5 rebase chain goes now against `ddb62fb` / `c24a000`; P8 dispatched; A7 rebases with the GUC · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
P5 (SELF-357): `/reports/monthly` listing (one entry per month; `superseded` structurally excluded), the pending queue as "awaiting commentary" with no job-state vocabulary, the two-candidate target-month `<select>` (prior month default; current month "(in progress — as of today)"), Generate → `113`, Regenerate → `114` behind an inline two-step confirm (this codebase's `DeleteScheduleControl` precedent against `window.confirm()`), the pending-count badge in the shell nav from the same `locals.supabase` read (Sec F-8), no `data_as_of` anywhere (RT-25); 24 loader + 4 layout + 16 DOM cases. Rulings: `?/generate` structurally refuses any month outside the two server-recomputed candidates — accepted on AC3's "the user selects A target month" (two V1 options, not an open picker); `?/regenerate` needs only the format check because `114`'s contract is safe for any month; the microcopy (section heading, per-row link) stands, UX may reword; the seven fixture one-liners across unrelated tests are the correct mechanical fix for a root-layout data change. Frontend's predicate caveat was right — the DB branch had moved under it — and it did not rebase on its own. The chain rebases now in order P2 → unit `ddb62fb`, P3 → `c24a000` then onto P2 (one shared month module), P5 → P3 (the nav link exists once; P2's version wins). P8 / SELF-360 dispatched on top: the P2 slot wired from the live staleness read under the requesting tenant (RT-13), account-name strings from the frozen snapshot (SD-12), markers in the PDF path by construction (same component). A7 rebases onto `c24a000` and sets `app.report_generation_source = 'cron'` transaction-locally before calling `113`; QA's `113` battery carries Architect's six provenance legs plus a KNOWN-GAP watcher on the direct `111` call (the `(R0)` shape) that reds the day `111` is fixed.

### E43 — SELF-372 merged at `959608f`; Sec's RT-21 SECURITY row is on `feature/self-345-sec` awaiting the unit verdict · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
The etl-CI integration lane merged as a workflow-only change under the delegation (not Sec-classified: a test lane, not a fence; no manifest change) after its own run on the PR proved the two files execute (12/12) and both fail-closed guards exist. Sec's RT-21 row rewrite under R2 (C) is pushed at `7d73ad1` on `feature/self-345-sec` (SECURITY only, 2 insertions / 2 deletions, on the unit's `ddb62fb`); it merges into the unit together with Architect's `111` fix once Sec's #636 verdict rules the forgery channel's shape, so the unit re-verifies once rather than twice. Open on `main` now: `107` (A9), the A4 worker, the CI lane, the ADR amendments; open PRs: #636 (unit), #638 (A5).

### E44 — Unit RED on Sec's VETO-1: `111`'s DEFINER helper hands back the forge channel; repaired by trigger emission with EXECUTE revoked (Sec option A); five flags and three notes dispositioned · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec read `108`–`111`, ADR-068, the D3/D9 halves, both ARCH halves and all eight batteries at `ddb62fb` (source + read-only catalog queries; it did not clean-apply or run pg_prove — no `pg_prove` on its machine and a scratch DB is outside its fence, so Architect's clean-apply and QA's 86/86 stand unverified by Sec). **VETO-1:** `fn_emit_audit_log` is DEFINER, EXECUTE-granted to `authenticated`, PostgREST-exposed (`pfin` at `config.toml:18`), and validates shape never truth — `trigger_source` (incl. `'cron'`), the chain text, `data_as_of`, `subject_table`/`subject_id` all reach the row caller-chosen; QA's own legs 2/5b/8 do it from a plain session; UPDATE/DELETE/TRUNCATE are blocked for every role, so repair is a superuser runbook; V1.final's `'cron'` count is self-servable; `111`'s `comment on table` claim "a caller cannot POST a forged audit row through PostgREST" is true of the table and false of the function. Architect had measured the same channel independently while fixing `113` (E41). Ruled with Sec's recommendation, option A: an `AFTER INSERT OR UPDATE ON pfin.monthly_report` DEFINER trigger emits the row (the `031` `fn_reclass_history_insert` shape Decision 9 cites), EXECUTE revoked from `authenticated`, provenance derived from the transaction-local GUC (`113`'s fix is the model; the PostgREST perimeter sets nothing); `113`/`114` drop their explicit calls; `115` is designed against the trigger. **Losing side, recorded:** block AH narrows from a general helper to per-surface emission and S7's W-1 retrofit gets harder; option B (bind the arguments to a real privileged write in this transaction via `xmin`) keeps generality at the cost of a per-surface branch inside a "general" helper — Architect may argue B in one paragraph, not implement it. Option C alone (a GUC for `trigger_source`) closes only the attestation lie and is not taken alone. **Flags:** FLAG-1 five ARCH sites still assert the pull direction (two Mermaid) — Architect; FLAG-2 three sibling batteries say the allowlist was "widened" where D9 says REALIZED — QA, Sec's wording; FLAG-3 `108`'s DELETE legs observe only the ACL — QA adds two owner-role legs on the trigger branch; FLAG-4 `109`'s snapshot set is open on a `final` — one predicate in the BEFORE INSERT fence; FLAG-5 `rendered_payload` is a self-attestation under R4 (B) — say so on the column; FLAG-6 `110:1007` names `112` for `110`. **Notes recorded, not required:** NOTE-1 `draft → superseded` directly is permitted by clause (i); NOTE-2 `ON DELETE CASCADE` to `auth.users` is unreachable through immutability triggers on four-plus tables (`057`, `107`, `108`, `109`, `111`) — a standing account-deletion item for Architect, Linear; NOTE-3 aal2 arms observed on SELECT only, family-wide. **Sec's F-3 rider ruling** (R5's dormancy is product-path, not grant; the firing leg is REQUIRED and already shipped at `108` (12b)) amends an F/CTO rider and goes to F/CTO for ratify at the wave report — not a merge blocker. Sec's RT-21 row (`feature/self-345-sec` @ `7d73ad1`) merges with the fixes; it adds a build-time requirement on (d): a per-process nonce store in a stateless container is a replay window on deploy — single replica with the risk written, or an out-of-process store, Sec-consult at that choice. Booked: RT-27's SECURITY row does not reflect the N-4 compose-manifest wiring (DevOps proposes).

### E45 — `115` at `e482b81`: the finalize path's audit one-way door stays shut (the row is the record); A7's cron unit at `64e8b36`; a third worktree displacement recorded against the team-lead · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
`pfin.fn_finalize_monthly_report(p_target_month, p_commentary_disposition) → bigint`, INVOKER, one transaction: lock the live draft, write the disposition FIRST (`110` reads `commentary_disposition` off the row — composing first would freeze NULL into an immutable artifact beside a column saying `authored`; my brief had the order backwards and leg 7 is the watcher), snapshot `owner_header_at_generation` (my brief omitted it; finalization is the only moment there is something to freeze), compose via `110` with the row's own `data_as_of`, assert the composition is about the locked row, freeze payload + version + `generated_at` + `final`. The audit emission Architect was asked for needs a new `surface_name` member on an append-only table whose CHECK can never be narrowed back, and `111`'s `data_as_of` comment says the V1.final N reads `trigger_source = 'cron'` with no surface filter — a second surface would change N silently. Ruled: not taken; the row is the record, as `114` already reasons; under VETO-1's option A the trigger emits on generation INSERT only, never on the `114`/`115` UPDATEs, so the vocabulary stays at one member. Accepted: `generated_at` is the freeze instant (no `finalized_at`); the reconciliation array stays empty (populating it would silently activate #3's fence); `authored` never inspects the commentary columns (R12 rider 1); the 2000 ms budget is stated, not measured for `115` — a probe leg is booked. DevOps's cron unit: `0 6 1 * *` documented with the UTC/Pacific residual (AC 9); env list scoped to what the code path imports (accepted — least exposure; the existing nav-daily service's compose block is narrower than `.env.example` and is recorded, not fixed); the `secrets-manifest.yml` consumer-comment edit rides A7's mandatory Sec review. **Recorded against myself, third time:** I parked the DevOps worktree by `checkout --detach main` while merging A4, after logging the same act at E12 and E32. A memory note now forbids any checkout in a teammate's worktree.

### E46 — VETO-1 re-ruled: E44's option A (trigger emission) WITHDRAWN; Sec's clearance conditions C1–C4 adopted, general helper kept; A7 at `c79ffdf`; 112–114 batteries at `a048734`; the P2→P3→P5 chain rebased onto live tips; SELF-373/374 opened · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05
Sec's ruling on the three candidate shapes crossed the E44 dispatch. Shape (i) (GUC-derived `trigger_source`, parameter dropped) ACCEPTED as mechanism; (ii) REJECTED — identical strength against `'cron'` plus a redundant caller input, two sources of truth for one column; (iii) (revoke EXECUTE) unavailable on an INVOKER-called DEFINER helper because the on-demand path needs it. Sec does NOT require the trigger, and names what E44 missed: the trigger narrows block AH from a general helper to per-surface emission, **reversing F/CTO's R7 option (2) choice late in the wave** — not a reversal the delegation covers when a path exists that keeps it. E44 is therefore withdrawn and the trigger becomes the named fallback. Adopted verbatim: **(C1)** derive from `current_setting('app.report_generation_source', true)`, default `'on_demand'`, exact match for `'cron'`, `p_trigger_source` dropped, with an explicit `drop function if exists` of the old six-argument signature before the create — a changed parameter list does not replace, and a DB where `111` already applied would otherwise carry a second overload with the old forgeable grant, invisible to a clean apply and to a scratch-clone battery. **(C2)** for `monthly_report_generation` the subject must be a `pfin.monthly_report` row owned by the caller AND written in this transaction (`xmin`; `pg_current_xact_id_if_assigned()` NULL ⇒ refuse): closes the unbounded uncorrectable append and the locator forgery without any negative-space claim; cost is a per-surface branch inside the general helper — R7 rider 5 one notch stronger. **(C3)** a two-layer watcher: a pgTAP session can set the GUC itself, so no DB leg can prove unreachability, only derivation — QA's DB legs (a)–(e); DevOps's CI fence: no function in a Data-API-exposed schema with `authenticated` EXECUTE calls `set_config` with a non-literal first argument, fail-closed with a caught golden fixture; covers `054`/`107`/`111`, a pre-existing gap this PR makes the third instance of. **Sec's fork, recorded:** if the CI half cannot be built this wave, C1 cannot carry `'cron'` alone and the trigger replaces it. **(C4)** two now-false shipped sentences re-derived; `tenant_resolution_chain` marked caller-asserted (a mislabeled real event, not a fabricated one — the standard `subject_id` already accepts). Sec measured why the `054`/`107` precedent does not transfer: there `authenticated` holds SELECT only, so the GUC fences a path a user cannot enter; on `111` EXECUTE is granted by design. Sec also corrected its own leg-8 reading toward Architect's (no provenance watcher anywhere, the more durable half). One question opened, not ruled: whether `session_user` (the login role, unchangeable by SET ROLE or RPC) could replace the GUC and remove the negative-space property — Architect answers, Sec rules; C1 built as the GUC regardless. `113`/`114` are not in #636 and get their own frozen-sha read. **Also this entry:** A7 rebased onto `c24a000` at `c79ffdf` (transaction-local GUC set inside the impersonated block; positive `'cron'` leg + forgotten-GUC fail-closed leg) — accepted, unaffected by C1–C4 unless the `session_user` question lands. QA's `112`/`113`/`114` batteries at `a048734` (plan 21/21/27) — accepted with both disclosed judgment calls (concurrency as structural pins on the `101` SF-L precedent; `114` atomicity via temporary REVOKE because `108`'s CHECK forbids the pairing list's literal mechanism); its prerequisite `a4aa4a2` re-applies the four sibling DEFINER-count edits on a second lineage and MUST be dropped at the post-merge rebase. Frontend's chain rebased onto the live tips (`0c4b02f` → `b569b3a` → `980ea3f`, 2464 green) rather than the cited shas that had moved — the correct reading of a moved ref; shared `parseTargetMonth`/`CommentaryValues` module extracted; one nav link reconciled to P5's badge form. Liaison opened SELF-373 (account-deletion path vs immutability triggers, NOTE-2) and SELF-374 (RT-27 row N-4 compose-manifest coverage), both Backlog, Platform / Cross-cutting.

### E47 — VETO-1's code half CLEARED at `5ca2cd1` after four Sec/Architect rounds; `session_user` proposed, accepted, and reversed on a harness fact; a withdrawn commit stranded DevOps's branch; A5 AMBER at `0c5f54a` with the ⟨OPEN⟩ ruled KEEP; an unauthorized fork push on `feature/self-360` · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-05/06
**The unit.** My E46 redirect crossed Architect's push of option A (`8f2c143`, trigger emission, both branches). Architect disclosed before acting and unwound by history rewrite rather than forward revert, so no branch carries a commit asserting a withdrawn ruling — `git branch -r --contains 8f2c143` now names only `feature/self-345-devops` (`2579841`), which DevOps had fast-forwarded onto in the window; DevOps rebases `--onto 5ca2cd1 8f2c143`. C1+C2+C4 landed at `c573475` with two findings a clean chain cannot see: (1) the obvious C2 expression `xmin = pg_current_xact_id_if_assigned()` is WRONG here — `113` INSERTs inside a plpgsql exception block, a subtransaction with its own xid, so equality refuses the real product path while passing every bare-INSERT leg (measured: top 1664121, row 1664122); `not pg_visible_in_snapshot(xmin::text::xid8, pg_current_snapshot())` is correct across all four cases and closes the ordered-comparison hole; the epoch caveat fails CLOSED (availability, ~2^32 transactions out). (2) C1's `drop function if exists` on the six-argument signature was verified by applying old `111` then new — exactly one signature survives; PostgREST resolves RPC overloads by request-body keys, so without the drop the old forgeable function would still be reachable. Absent-row merged into the other-tenant refusal (existence oracle over a global id sequence on a DEFINER lookup) — Sec endorsed, its own enumeration was short. **`session_user`:** I asked whether the login role could replace the GUC; Sec accepted it (positive structural discriminator; `authenticator` is the only LOGIN API role; `SET SESSION AUTHORIZATION` is superuser-gated), then REVERSED on two facts Architect surfaced: the `PFIN_DB_*` role name differs across dev/CI/prod (silent `'on_demand'` in prod; an existence assertion would break the clean-apply gate), and the `'cron'` arm is unobservable in a one-connection pgTAP harness — Sec had scoped a watcher the harness cannot hold. Booked with Architect's gate verbatim: a migration-created `NOLOGIN` `pfin_cron` group role tested via `pg_has_role(session_user, …)` AND a multi-connection harness; either half missing ⇒ the GUC plus the C3 fence. C3's CI half returned to REQUIRED. Sec's own emphasis correction, now in `111`/ADR-068: C1 and C2 are not parallel — C2 is load-bearing, C1 bounds its residual, and a later C1-carrier discussion must not erode C2. Sec's "bounded per (user, month)" claim was WRONG at four sites (Architect caught it: the one-live-draft index caps concurrent drafts, and regenerate supersedes before delegating) — struck, not softened: C2 bounds fabrication, not row count. Two consequences routed to PM via Linear: define V1.final's cron measurement as distinct `(users_id, month)` where `trigger_source = 'cron'`, never a row count (removes the ADR-068 open dependency by construction); and `pfin.audit_log`'s user-drivable, uncorrectable append rate with no stated retention. New flag landed at `c765c4c`: C2's surface dispatch had no `else` — a procedural control (rider 5's grows-only-by-migration) on a structural default of unbound — now REFUSES unbound surfaces, and the `else` and the vocabulary CHECK are recorded as non-interchangeable (CHECK = storable; `else` = bound; both removals look like cleanup); QA gets a required owner-path leg on the CHECK because the `else` makes it unobservable through the granted path. Sec's FLAG-1 re-assertion was a stale-ref read (fixed at `c573475`, twelve sites not seven; Sec's finding list was line-anchored and self-invalidated) — WITHDRAWN by Sec with a three-defect diagnosis; Sec's RT-21 clause (f) had pre-decided the owed route naming — corrected at `480d548`, merged. The route-naming booking reaches ADR-015's RT-26 canonical audit-scope enumeration (`/internal/pdf-render` is renamed, not removed — deleting the entry would shrink an audit scope); annotated MUST-NOT-SIMPLY-BE-DELETED and pinned to P6 (SELF-358), the PR that builds the export route. Architect's own near-miss recorded: merged Sec's branch on the wrong branch, `git merge` echoing nothing about the target; caught before push. **Tree, verified:** `feature/self-345` = `5ca2cd1` (code half CLEAR), `feature/self-355-db` = `638bf3c` (`115` byte-identical to `e482b81`), `-sec` = `480d548`, `-qa` = `a46626c` (FLAG-2/3 landed; plan 36→39), `355-db-qa` = `9b4b109` (`115` battery plan(32), strike flips leg 7 as the migration predicted). Remaining to clear: QA's `111` legs (a)–(g), DevOps's rebased fence, Sec's `112`–`115` read at `638bf3c`. **A5 (#638, `0c5f54a`): AMBER, no veto**, second Sec instance. ⟨OPEN⟩ RULED KEEP `users_id` — zero security delta today (worker has no DB reach, reads the claim for presence only), the proposal's forensic rationale is DEAD (the rejection signal is a fixed enum by ADR-050 D4), the prospective hazard named (a tenant-keyed render cache in the worker, RT-10) and outweighed by a five-artifact swap mid-RT-21-rewrite; the cheaper future shape is deletion (correlate by nonce), booked as F/CTO's call. F-1 is real and adversary-free: the body read sits outside the abort window AND the error guard, so the timeout bounds headers only and a mid-body error throws from a function contracted never to throw — a divergence from the sibling it claims to mirror. F-2 (no watcher on SD-20's `data_as_of` prohibition), F-3 (three legs weaker than their labels incl. an `expect(true)`), F-4 (dead constant), F-5 (two false "A4 unmerged" claims), F-6 (R-6 wording), F-8 (no entropy floor on the signing key — code half to Backend, manifest note booked), F-9 (D1 false-composite) dispatched to Backend; F-7 is a merge-order condition — #636 lands before #638 or `main` carries a control whose catalog row states the opposite direction. Record placed at `self349-sec-review.md`; Sec-2's memory placed. **Incident:** a `fork` Frontend spawned for read-only research wrote, committed and pushed a full P8 implementation to `feature/self-360` @ `6e7e6e7` from the wrong AC source and reported "complete" under a non-roster id; Frontend caught it from the tree and refused to build on it. Ruled: not the deliverable; reset to `9c23844`, build from the Linear AC (extracted by the liaison), fork's diff usable as a cross-check only; standing rule — research delegation uses `Explore`, never `fork`. Two pre-rulings: Asset Allocation carries a section-header badge (payload has no per-row `account_id`); the banner is a live sentence and shows live account names over the 109-snapshot ∩ currently-stale membership. Also: A7 rebase stands; `pfin_etl` is a dedicated NOLOGIN-inheriting login role, direct-Postgres, no pooler.

### E48 — `112`/`114`/`115` GREEN, `113` AMBER on a branch-scope premise; the Lock 12 snapshot table has NO writer at any sha; QA's `111` legs and the C3 fence land for Sec's read; A5 flags fixed at `e037776`; P8 rebuilt from the Linear AC at `fe51000` · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Sec read `112`–`115` at `638bf3c` (verified: descends from `5ca2cd1`, every sibling-owned blob byte-identical). `112` GREEN — the two-branch refusal is not an existence oracle (both reads RLS-scoped), no second length bound by design (N-5 is an equality between exactly two layers). `114` GREEN — concurrent regenerates serialize on the `final` row's lock, the loser skips the supersede and finds the winner's draft; NOTE-1's `draft → superseded` clause has no writer anywhere in `112`–`115`. `115` GREEN, and Sec RULED on the routed no-audit-at-finalize question: ACCEPT — D1 clause (d) attaches to privileged-context writes, finalize runs under the user's own session, the immutable row is the stronger record — with the revival condition Architect had not named: the first worker/cron call to `fn_finalize_monthly_report` makes it privileged and the costed alternative becomes mandatory (written into `115`'s header). `113` AMBER because "nothing writes `app.report_generation_source` anywhere in the tree at this sha" — TRUE of `638bf3c` and FALSE of the tree: the writer is A7's `monthly_report_cron.py` on `feature/self-351` @ `c79ffdf` (`set_config(…, 'cron', true)` inside the impersonated block before the `113` call). Corrected as branch scope; Sec's catch criterion adopted for A7: same-transaction `is_local`, plus the leg nobody writes — a SECOND transaction on the SAME connection does not inherit `'cron'` (required even without a pooler; in-worker connection reuse is the same hazard). **The writer gap that is real:** nothing INSERTs into `pfin.monthly_report_account_snapshot` in `supabase/migrations`, `api`, or `workers` at any sha — the Lock 12 child ships with RLS, grants, D3 #4's fence and an immutability trigger and no legitimate writer, so its only exercise today is adversarial through the `authenticated` INSERT grant; FLAG-4 is moot only because the set is always empty; ADR-068 D7's queryable-index justification has nothing to filter; P8's banner takes final-report membership from it and would degrade to the live set forever. Ruled owed in V1.5, `115` at finalize the natural site; Architect rules the shape and implements. Three Sec findings converge on one undefined predicate — the V1.final measurement exists only as prose in `111`'s column comment: the row-vs-month inflation, the unfiltered-`surface_name` coupling at `115:99-100`, and the writer gap; one definition (`count(distinct (users_id, date_trunc('month', data_as_of))) where trigger_source = 'cron' and surface_name = 'monthly_report_generation'`) closes two, and the durable half is to COMMIT IT AS SQL — a view — not prose; appended to the PM Linear item. RT-25 is discharged by SIGNATURE in both write paths (no `as_of` argument exists); no D9 or D3 event; Lock 14's app-layer half (Zod `.strict()`, code-point mirror) is owed by Frontend/Backend and RT-11 is not treated as discharged. **QA:** `111` re-legged at `0558a5c` (plan 29; 4a → the `else` P0001; 4b retired, no premise; 4d owner-path CHECK watcher; strike on exact-match reds leg 12 only). Two measured findings disclosed in the file header: dblink opened mid-transaction poisons `pg_current_snapshot()` for the rest of it (legs 8-i/9 become structural pins); and — possibly product-relevant — a `throws_like()`-caught exception raised by a real BEFORE UPDATE/DELETE trigger poisons the same transaction's later `pg_visible_in_snapshot` attestation of a plain write (13-line repro). Routed to Architect to reproduce and answer whether any real caller interleaves a caught exception between the subject write and the emit; Sec rules whether C2's expression changes. `355-db-qa` at `ddfb269` on `638bf3c`, `113` gains the through-subtransaction C2 legs (the row carries a subtransaction xid; C2 accepts it; the naive-equality strike aborts the file from leg 3). **DevOps** C3 fence rebased off the withdrawn commit to `0338048` (one commit on `5ca2cd1`; header carries the bounded-residual framing scoped to `111`'s C2 and not extended to `054`/`107`; "MEASURED TODAY" corrected to say `111` reads the GUC name). **A5:** Backend fixed all nine flags at `e037776` (body read inside the guarded region, `redirect: 'error'`, exhaustive claim-set leg, entropy floor, ⟨OPEN⟩ rewritten as RULED with rationale (ii) struck); catch criterion (2) — hung body aborted by the timeout — NOT landed: `jose`'s WebCrypto signing hangs under vitest fake timers; named for sec-a5 to rule required-or-bookable. **P8:** rebuilt from the Linear AC at `fe51000` over the unauthorized `6e7e6e7` (six files, 21 new tests, RT-13 leg, negative AC 6 leg); the post-hoc diff against the fork's commit differed on exactly pre-ruling (ii) — the fork used frozen names in a live sentence. Walk-gated; blocked on the F/CTO dev-DB decision. **Liaison:** the two-issue create is still owed after one idle-before-deliverable; re-poked "not a re-fire".

### E49 — C2's predicate ruled UNSOUND under any concurrent commit (Sec, reproduced with a matched control pair); clearance re-opened on the expression, the requirement untouched; A5 GREEN-conditional at `e037776`; A7 leg at `38a9419`; SELF-375/376 opened · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
QA's finding 2 was the real thing. Sec reproduced it in one transaction on a scratch temp table: T0 plain top-level write, snapshot `1703270:1703270:`, attested-written-here TRUE; T1 caught exception around a NON-writing block, unchanged, TRUE; T2 caught exception around a WRITING block, snapshot `1703270:1703272:`, FALSE. The discriminator is whether the aborted subtransaction consumed an xid: aborting completes it, `latestCompletedXid` advances past our top-level xid, the next `pg_current_snapshot()` has `xmax` above our xid, and our own xid is never listed in our own snapshot's `xip` — so `pg_visible_in_snapshot` reports our own write as visible and `v_written_here` goes FALSE. **The generalization nobody had stated:** `latestCompletedXid` is CLUSTER-WIDE; an ordinary commit from any second session between `113`'s INSERT and the emit produces the same refusal, and because the emit is inside the generation transaction the whole monthly-report generation rolls back, non-deterministically, under ordinary concurrency. The batteries pass because a scratch database is idle. QA's dblink diagnosis is the same defect (the dblink'd transaction COMMITS) — legs 8-i/9 were the defect reproducing, not a harness limit to disclose, and are reclassified. Ruled: C2's REQUIREMENT stands and is load-bearing — a control is not demoted to make its implementation true; the PREDICATE is replaced. Property: row R's `xmin` belongs to the current top-level transaction or one of its subtransactions. Disqualifier: no dependence on `latestCompletedXid`, snapshot `xmax`, or any cluster-wide counter — `xmin = pg_current_xact_id()` is blind to subtransactions, visibility is sensitive to unrelated transactions, the two shapes fail in opposite directions. Sec deliberately names no expression (over-specifying cost a wrong requirement at E46); its input only: a commit-STATUS test (a visible row whose `xmin` is not yet committed must be ours). Architect picks and verifies on the matrix: plain; subxact; aborted-writing-subxact between write and emit (must succeed); non-writing abort (control); dblink commit between write and emit (must succeed); earlier-transaction (refused); other-tenant (refused). REQUIRED leg whatever lands (QA): advance `latestCompletedXid` between write and emit and assert the emit succeeds — the writing/non-writing abort pair is proxy and control; without it the next expression is validated on an idle database exactly as this one was. The exception-interleave question no longer decides anything and is dropped. Order: expression first on `feature/self-345`, then the snapshot writer and items (1)–(4) on `355-db`; `-devops` rebases once more after. Sec holds QA's legs and batteries until the new sha; reviews the fence now. **Sec's own error, logged at its request:** "the C1 GUC-writer is not anywhere in the tree" — body said "at this sha", headline dropped the qualifier; a branch-scope claim stated as a tree-scope fact, the same class as its FLAG-1 stale-ref read one turn earlier; the catch criterion survived because it described the property, not the location. **A5 at `e037776`: GREEN conditional on two comment-only fixes** (sec-a5 read both files from the ref): R-1 the F-6 header fix did not reach the call site (`renderClient.ts:213` still says "(g)'s app-side half"); R-2 the rewritten ⟨OPEN⟩ names churn — the third ground — as the surviving one and so installs the wrong revisit trigger (churn instead of capability). Criterion (2) ruled NOT required for GREEN and NOT redundant (moving `clearTimeout` above the body read leaves the 502 leg green while re-unbounding the download) — booked as a tracked QA issue with an accepted shape (in-test `setTimeout`/`clearTimeout` stub, never a production knob; a bare no-op `clearTimeout` stub is a false green) plus N-6's 31/32 boundary pair and N-5's deploy precondition (Coolify key length ≥ 32 before the worker's first deploy). Record placed at `self349-sec-reverify.md`; the earlier record verified byte-identical by its author. F-7 (merge order #636 → #638) stands. **A7:** `38a9419` adds the two-sequential-transactions-one-connection leg against the real mechanism; today's `NullPool`/fresh-engine-per-tenant path cannot reach the hazard, so the leg guards a future edit. **Linear:** SELF-375 (V1.final measurement as distinct (users_id, month), committed as SQL) and SELF-376 (`audit_log` user-drivable append rate, no retention) created.

### E50 — The C2 predicate replaced with `pg_xact_status` and the one-statement invariant watched at two layers (`72c3e5c`); the Lock 12 snapshot writer lands in `115` (`158a644`) and is finally dispatched to Sec; C3 fence GREEN with a NULL-`prosrc` flag; A5 GREEN confirmed on the tree; the base rate the joint review measured; P4/P6 Linear descriptions do not match their titles · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Architect had already moved C2 to `pg_xact_status(xmin::text::xid8) = 'in progress'` at `aab0396` before E49's redirect arrived — independently, from the same reproduction, in the family Sec named as input. It reads the commit log for that xid, consults no snapshot and no cluster-wide counter, answers `'in progress'` for our top-level transaction and every subtransaction (aborted or RELEASED, nested included) and `'committed'` for an earlier one; carries no ACL entry so it is posture-independent; a frozen `xmin` reads `'committed'` cleanly and the clog-retention raise is reachable only on the refusal side. Architect's finding was broader than Sec's ruling: a caught exception BEFORE the write poisons the snapshot form too, so the constraint E48 anticipated ("do not catch an exception between the subject write and the emit") would have shipped FALSE as well as unenforced — a documented rule that licenses the case it forbids. No real caller interleaves (`113` is the only production call site; its handler returns or re-raises on every branch); the fix was taken because the documented version would have been false, not because a live caller forced it. Sec confirmed all six cases plus a nested-savepoint case neither had listed, then named the invariant the predicate's soundness rests on: `'in progress'` is also true of another session's transaction, and what excludes it is that the row was resolved by THIS transaction's own MVCC read IN THE SAME STATEMENT — split it, cache it, or pass the row in and the coupling breaks silently while the predicate still looks right; no behavioural leg can see that, only a structural one. Architect reported adding a catalog watcher for it; Sec measured the tree and it did not exist — an ad-hoc shell query reported as a committed mechanism ("I could not have named the file and line it lived in" is the tell). Built at `50d17ee` as an apply-time `do $watch$` block plus QA leg 7g, inversion-proven on a matched pair; Sec found a one-character fail-open (case-sensitive regex; a split read written `FROM` passed) fixed at `37cf766`; 7g's inversion requirement written into the spec at `ca6d5cb` as THREE bodies (correct → silent; lowercase split → RED; `FROM`-cased split → RED); and at `72c3e5c` the standing rule that presence checks on `prosrc` are vacuous in this file BECAUSE the comments are good — `pg_visible_in_snapshot` raw 3 / stripped 0, `pg_xact_status` raw 5 / stripped 1 — so QA's existing structural legs go GREEN on a false property (Architect had predicted RED; Sec inverted it: a red gets investigated, a green gets trusted). Every `prosrc` assertion in the battery now requires comment-stripping. `115` writes the Lock 12 children at finalize from the composed payload's account set (a live-table writer would freeze a different set — `fn_nav_composition` excludes tax-authority ledger accounts; QA leg 14b discriminates), values read from `pfin.account` under the caller's RLS in the same transaction, children before the status flip (FLAG-4's predicate forces it, and the two changes prove each other), no `on conflict` with the failure mode named (the no-duplicates premise lives in another migration; `109`'s unique index then fails the finalize rather than dropping a row), each row permanent; Sec's worker-revival condition written as "watch the grant, not the prose." **Recorded against the team-lead:** Sec asked six times for `115` at a frozen sha and refused to grade it from Architect's description; I serialized it behind the C2 loop and dispatched it only at `158a644` — the byte-identical file had been stable since `4b6debe`, only its address moved, and the churn was the review loop's own. Both QA branches are stale against the predicate change (0558a5c has zero `pg_xact_status` references; ddfb269 predates the snapshot writer): VETO-1 clears on the `345-qa` re-leg, not on `115` — Architect's "345 has no work outstanding" was true of Architect's queue, not the branch. C3 fence GREEN at `0338048` (Sec ran the predicate over ten evasion cases; the workflow asserts exit 1 exactly plus the fixture token — three causes share rc=1); FLAG: NULL `prosrc` (PG14+ `BEGIN ATOMIC`) is a silent pass — scan `coalesce(prosrc, pg_get_functiondef(oid))`; the residual list UNDERSTATES the fence (dynamic SQL via `execute '… set_config(' || …` flags; only the split-token form evades); widen to `anon`; cite C2's requirement, not its expression. A5 GREEN at `5899494`, confirmed by me on the tree (zero "(g)'s app-side half"; the ⟨OPEN⟩'s trigger reads "capability, not churn"); #638 waits only on F-7. **Methodological record, the durable home Sec asked for:** five defects traded across Architect and Sec in one review — Sec's over-specified `xid` equality and branch-scope headline; Architect's snapshot predicate, the watcher that did not exist, the case-sensitive regex — zero self-caught in the moment on either side, roles swapped every round, one self-caught against a recorded prior measurement (Architect's vacuously-green harness). The operative property is not a second person but a check run against a STATED claim by something that is not the author's present belief; joint review manufactures that reliably, a recorded measurement manufactures it cheaply — which promotes "record the measurement" from documentation to control. Corollaries adopted: a filtered grep is a claim about the filter, and a file filter is a filter; a count is a claim about what counting can distinguish; any change to a fence or watcher predicate runs against the matched-body matrix unconditionally (a trigger conditioned on noticing inherits the invisibility of what it should catch). Sec's three memory candidates placed. **For F/CTO at the report:** promote `fence-set-config` into `required-contexts.tsv`; give the fence an RT catalog row (CI-fenced set, never the §10 ledger). **Linear defect:** the SELF-356 (P4) and SELF-358 (P6) descriptions carry other issues' ACs (P9's and P3's); the P4 brief is held until the liaison confirms what Linear holds.

### E51 — Worktrees moved under `.claude/worktrees/` on F/CTO's directive; the Linear "mismatch" was the liaison's own extraction error; `115` AMBER on FLAG-7 (silent cardinality); P4 and P6 dispatched from the pre-flight record; SELF-377 opened · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
F/CTO, mid-run: *"too many Bash approval requests"* → *"this is unworkable. the worktrees should be under .claude/worktrees/ … I've just had to approve about 30 requests. doesn't work if I have to sit by the terminal constantly."* Cause: every agent worktree sat at `~/Projects/mosko-fintech-worktrees/<agent>`, outside the session's working directory, and the read-block setting turns any command that touches such a path — a teammate's bare `grep`, my log appends — into an approval prompt, and refuses the Read tool outright. All eight worktrees moved with `git worktree move` to `<repo>/.claude/worktrees/<agent>` (branches and files intact, verified by `git worktree list`), the directory excluded via `.git/info/exclude` (a `.gitignore` line rides the agent-memory chore PR), and every live teammate told its new absolute path before its next command. Recorded as a standing rule in memory: paths inside the working directory, plain commands over heredocs, batched log commits, and a prompt class that appears twice gets fixed at its cause rather than clicked through. **Linear:** the liaison's re-check shows Linear is CORRECT — SELF-356 holds P4's AC and SELF-358 holds P6's; the liaison had written P9's and P3's bodies into the wrong files. P4 and P6 were dispatched from `docs/records/v15-preflight/rederived-acs.md` (which matches Linear): Frontend builds P4 (SELF-356) on `feature/self-356` from `feature/self-360` @ `fe51000` — complete-or-explicitly-skip gate over `115`, PM's pending-queue and skip-confirmation copy verbatim, the no-ledger prompt BEFORE finalize (load-bearing under R1 rider 6), the skip writing the skipped disposition and the editor's Finalize writing authored, in-page confirm never `window.confirm`; Sec routed as a light review (R13's conditional gate) after the walk. Backend builds P6's server half (SELF-358) on `feature/self-358` from `feature/self-360` with `feature/self-349` merged in — the user-session route that renders the shared template server-side, pushes finished HTML through `renderReportHtml`, streams bytes with `mosko-monthly-{YYYY-MM}-{generated_at}.pdf` and never the owner string, refuses drafts, persists nothing, reads staleness live via a shared server module, carries the INV-2 escaping proof leg, and must say how the LayerCake chart is sized inside a worker that aborts every non-`data:` request. SELF-377 (A5 follow-ups: the hung-body timeout leg with Sec's accepted shape, the 31/32 boundary pair, the signing-key deploy precondition) opened; liaison shut down. **`115` at `158a644`: AMBER, no veto, FLAG-7** — the child INSERT…SELECT walks a hardcoded payload path with no version guard and no count check; Sec measured four failure shapes: path absent → 0 rows SILENT; element without `account_id` → partial set SILENT; scalar path → raises; malformed id → raises. The silent first shape is the regression `payload_schema_version` exists to allow (ADR-068 D4), and it would re-create the P8 degradation this commit fixes with every fence green, permanently. Fix (Architect): assert the path RESOLVED (`#>` not null — distinguishes absent from a legitimately empty `'[]'`) and assert `ROW_COUNT` equals the payload's account-element count. Sec's `on conflict` ruling: the named failure mode covers the direction it names (duplicate → 23505, loud); the omission direction is silent and the cardinality assertion is its watcher — bound on the outcome this file can see, not on the grouping premise it cannot. FLAG-4 RESOLVED more completely than asked (NULL-safe `is distinct from 'draft'`; the table's first legitimate writer); D2 halves named (children under the immutable half; INSERT-new-version one grain up at the parent); D3 #4 live but not exercised on the product path; EXECUTE `authenticated`-only makes the finalize revival condition a catalog fact.

### E52 — `115` GREEN at `924085d` (FLAG-7 closed, Architect's traversal guard closed a gap in Sec's own spec); the C3 fence's NULL-`prosrc` fix corrected by measurement (`''`, not NULL) at `f788788`; agent memory on PR #639 and the runbook line on #640; Sec's vacuous confirming probe · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Architect landed FLAG-7 at `924085d`: the payload path must resolve to an ARRAY before the INSERT (type check riding with the null check — a scalar previously failed closed with a raw 22023 from inside the traversal; same failure class, now a named message), and the inserted row count must equal the payload's account-element count after it; plus one guard of Architect's own — a group carrying no `accounts` array — because the count query and the INSERT walk the SAME traversal, so both drop the group and the counts still match: Sec's (b) binds the JOIN, the extra guard binds the TRAVERSAL. Inversion-proven by replacing the composer on a scratch clone: four refusals (path renamed; path scalar; element without `account_id` — "wrote 0, payload names 2"; group without `accounts`) and two controls in the same run (full set for a tenant with accounts; ZERO children and no refusal for a zero-account tenant). Sec GREEN, both deviations CONFIRMED with the mechanism verified rather than accepted ("I specified one and it needed both") and the false-RED direction checked against today's composer (a group exists only with ≥1 leaf; the empty case is `coalesce(…, '[]')` one level up). Sec corrected its own framing: the subtree `115` traverses is produced wholesale by `fn_nav_composition` — `110:1161` is a single delegating line — so the party who can break `115` is one hop further away than "a `110` restructure"; one comment-only clause owed naming `fn_nav_composition` as the file to watch, and recording that the guards cover absent/mistyped/dropped but not "moved to an additional sibling key" — a payload version bump remains a Sec-review event, not a thing the DB self-checks. The `on conflict` question is closed both directions (under-count by the cardinality assertion; over-count by `109`'s unique index raising 23505). No rebase of the unit onto `main` (`959608f`): `gh pr update-branch` absorbs it at merge without rewriting the shas Sec and QA are reading. **C3 fence at `f788788`** (one commit on `72c3e5c`): DevOps measured the `BEGIN ATOMIC` fixture and found `prosrc` is an EMPTY STRING, not NULL as Sec's flag stated, so the plain `coalesce` would have shipped looking fixed while remaining blind — landed as `coalesce(nullif(p.prosrc, ''), pg_get_functiondef(p.oid))`, a second fixture (`__c3_fixture_guc_spoof_sqlbody`) with its OWN CI assertion (the first name is a prefix of the second; a shared grep could pass while the sqlbody case regressed), the residual list corrected to "only split-token forms evade", the predicate widened to `anon OR authenticated`, the severity note citing C2's requirement not its expression. Sec's verdict pending. **Memory:** five agents' accumulated memory (28 files) committed on `chore/agent-memory-v15-batch1` as PR #639; the main checkout's tree therefore reads as lacking it until the merge — Architect nearly reported it destroyed and caught itself by `git log --all --diff-filter=A` (the first pre-send self-catch of the session, by a fresh recorded measurement); Sec first confirmed my placement on a chained `test -f … && … || …` probe whose byte counts matched its own `temp/` files (it measured the source and reported it as the destination), then retracted as "ABSENT, only copy in temp/" — both half-right: absent from `main`'s working tree, present on the PR's ref (`ls-tree`, three files, three index lines). Three causes of "absent from disk" — deleted, moved, on an unmerged branch — each with its own resolving check; and a confirming probe deserves a better instrument than a challenging one, not a worse one. Ruling on where teammate memory lands now that worktrees are inside the repo: the MAIN checkout's `.claude/agent-memory/<agent>/`, swept into chore PRs by the team-lead; never a feature branch. **#640** (DevOps, doc-only): the SELF-377 deploy precondition in `docs/deployment-runbook.md` §7 — the Coolify `PDF_WORKER_SIGNING_KEY` must be ≥ 32 characters and identical on both containers before the worker's first deploy (the app fails closed under 32; the worker enforces no floor). Both PRs gated to merge on green in the background.

### E53 — C3 fence GREEN at `f788788` (Sec confirmed `''`-not-NULL against its own catalog query; its proposed fix would have been a no-op that closed the flag); #640 merged at `1ef8ec6`; DevOps shut down; the approval-prompt class traced to its second cause; ledger gap stated · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Sec on the fence at `f788788`: GREEN, all four items verified on the tree. Sec queried `pg_proc` for `prosqlbody is not null` (`lpad`, `rpad`, `substring`, `bit_length`) and confirmed `prosrc` is the EMPTY STRING for `BEGIN ATOMIC` bodies — the exclusion it found was real, its stated cause was wrong, and `coalesce(p.prosrc, …)` — the remedy it prescribed — would have passed `''` through untouched: a closed flag with nothing behind it, "worse than an open one, because nobody looks again." DevOps caught it by building the fixture and measuring. Sec's named class, second instance today: effect-verified is not cause-verified, and a remedy is written against the cause. The fixture pair (`__c3_fixture_guc_spoof` is a prefix of `…_sqlbody`; two separate assertions, one disambiguated by argument signature) is recorded as DevOps's catch. Sec also withdrew "not on any ref" as a claim about the three refs it chose — the unmerged-branch cause was the one it did not enumerate. DevOps's deliverables are all consumed (A7 compose unit, C3 fence, #640) → shut down; a fresh DevOps is spawned if A7's Sec review needs compose/manifest work. **#640 merged at `1ef8ec6`** (one-file doc change verified before the merge gate); `main` = `1ef8ec6`; the devops worktree still sits on the merged local branch — left alone, no checkout in a teammate's worktree. **#639** is BEHIND/UNKNOWN after `main` moved; it takes `update-branch` when its slow lane finishes. **Approval prompts, second cause:** with every worktree inside the repo, prompts continued — the sandbox also asks on any command it cannot statically analyze (heredocs, `$(...)`, loops), regardless of path, AND on files outside the working directory such as the session scratchpad under `/private/tmp` where I kept commit-message files and the PR-wait loops. Ruled and broadcast: commit messages via a Write-tool file in the worktree's gitignored `temp/` + `git commit -F`; no command substitution; no inline loops — committed scripts invoked by path; Read/Write/Edit for files. F/CTO's call, asked: relax the setting now that everything is inside the working directory, or rely on the re-brief. **Ledger gap, stated per the merge skill's step 6:** `MILESTONES.md` last names `3de6be7` (#628); `main` is nine merges past it (#629 A8, #630 A6, #631 probe, #632 P9, #633 A9, #634 A4, #635 ADR-054/050 doc, #637 etl-ci lane, #640 runbook). Recorded lag, cleared at the wave's close-out PR together with the execution log — not silently carried across a session boundary.

### E54 — The prompt storm: roster frozen, the setting identified and turned off by F/CTO, roster resumed; QA's two batteries landed (`57f3952`, `365a9b9`); #639 merged at `ea680ea`; P4 accepted; fresh Sec and Architect spawned · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
F/CTO: *"something is seriously wrong … maybe 100 requests and counting"*, then the diagnostic question — a `cd <worktree>/api && ls node_modules | wc -l` prompt, plainly inside the working directory. Cause, finally stated correctly: `permissions.blockReadsOutsideWorkingDirectories` enforces its block by proving from the command text that every readable path is inside the working directory, and fails CLOSED whenever the parser cannot finish the proof — multi-line commands, `cd` state carried across lines, pipes, heredocs, `$(...)`, loops — regardless of where the command runs. The worktree move fixed the path half only; the re-brief to plain commands could not cover a database or CI role; and every message I sent made idle agents run verification commands in exactly those shapes. Actions: Architect, Sec and Frontend stopped (deliverables all on refs or in records); QA and Backend held with a no-commands order; my background PR wait killed; no shell from me until the setting changed. F/CTO turned the setting off (user-level `~/.claude/settings.json`; the project's deny list on `supabase db reset`, the PreToolUse hook, and the allow lists are unaffected). Resumed: QA and Backend by RESUME message; `sec-b` and `architect-b` spawned fresh from brief files with their predecessors' memory now on `main`. **Landed during the freeze:** `feature/self-345-qa` @ `57f3952` — `111` re-legged for the `pg_xact_status` predicate (plan 34; 7d pair, 7e both shapes, 7f disclosure, 7g three-body inversion matching Sec's counts, 8-i behavioural via dblink with a synthetic tenant, 9 structural on its surviving reason, one strike reds 7d-T1); `feature/self-355-db-qa` @ `365a9b9` on `924085d` (QA verified the ref rather than the stale sha in the brief; 115 battery plan 53 with 14a–14h incl. the ON DELETE RESTRICT correction — a `final` parent never reaches the FK because `108`'s trigger blocks first, so the RESTRICT leg needs a draft parent with a directly-inserted child — and the four FLAG-7 shapes via savepoint-wrapped stub replacement; QA flags that this branch still carries a stale six-argument `111` battery from an older lineage, reconciled after #636 merges). Sec-b reads both; VETO-1 clears on the first. **P4 (SELF-356) @ `5c1b689` accepted** (16 files; the no-ledger prompt on both exit doors; an unsaved-changes guard on Finalize because `115` takes no text; ANY across jurisdictions; one composition per pending row booked as a measured scale item for the P10 probe; P5's pending section extended in place); walk-gated before the R13 light Sec review. **P6:** Backend halted with uncommitted work and a real finding — `render()` from `svelte/server` emits zero component-scoped CSS, so the self-contained HTML the worker needs has no code path beyond the two global stylesheets; LayerCake sizing solved by its own `ssr`/`width`/`height` props. Architect-b rules (options, one-way doors named), then Backend and Frontend build. **#639 merged at `ea680ea`** (the pull hit a conflict with DevOps's post-batch memory edit — resolved keeping both sides; batch 2 stays on the working tree). `main` = `ea680ea`; ledger gap now ten merges, recorded.

### E55 — VETO-1 CLEARS at `57f3952` (Sec-b); the unit goes AMBER on two doc defects; FLAG-3/FLAG-4 and two merge gates ruled; the P6 CSS ruling (build-time extraction; `css:'injected'` struck on a measured CSP defect); the drain before the settings restart · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Sec-b (fresh spawn, read-only from refs, executed nothing) graded `feature/self-345-qa` @ `57f3952` GREEN — the required set holds leg by leg (7d pair, 7e both shapes, 7f disclosure, 7g comment-stripped `'gi'` exact-count, 4a → the `else` P0001, 4d owner-path vocab watcher, both `prosrc` legs stripped and failing CLOSED on NULL; plan 34 = 34 assertions) — **VETO-1 CLEARS.** `feature/self-355-db-qa` @ `365a9b9` GREEN (plans 21/23/27/53 match; 113's 4c/4d prove C2 through the real subtransaction; 115's 14b discriminator, the 14e-c draft-parent construction that makes ON DELETE RESTRICT reachable, both FLAG-7 controls in one file), and the GREEN carries to `41eede8` (delta = the 14i sentence, +11/−1) and to `15434ce` (QA dropped its agent-memory commit from the branch by `rebase --onto`; `git diff 41eede8 15434ce -- supabase` empty, verified). **The unit `72c3e5c` is AMBER on two doc defects, Architect's pen, both must land before #636 merges:** FLAG-1 — ADR-011 D9's amendment (`DECISIONS.md` L5328) names the SIX-argument signature, the one C1's `drop function if exists` removes; one token. FLAG-2 — `111`'s `comment on function` (L969) still says "THE TRANSACTION TEST IS A SNAPSHOT-VISIBILITY TEST" — the predicate E49 ruled unsound, contradicted by the body's inline comment, by L779–803, and by ADR-068; C4's class, third instance, created by the E50 replacement after C4's two were fixed, sitting in the one place a maintainer consults before touching the predicate; no battery leg observes a function comment. FLAG-3 (QA, plan 34→36) — C2 guard (b) (`p_subject_table is distinct from 'pfin.monthly_report' or p_subject_id is null`) has NO observer; the tell is an unused `\set m_bad_subject`; delete the guard and the battery stays green; it is the locator-forgery half of VETO-1. Lands in this PR. **FLAG-4 RULED, option A:** the "payload version bump is a Sec-review event" convention had no carrier (no pin in any battery, no fence, no required context); one version-pin leg in the `110` battery, so a bump REDs a test in front of QA and Sec instead of freezing reports — preserves exactly the tradeoff Architect declined at 12ccd2f. **M-1 RULED:** the C3 CI half (`f788788`) is not in `main`; E46 built C3 as a two-layer watcher — #636 assembles `feature/self-345` + `feature/self-345-qa` + `feature/self-345-devops` and merges as one. **M-2 RULED, a measured gate:** `355-db-qa` carries a stale plan-19 `111` battery (zero `pg_xact_status`) that would silently overwrite the plan-34 clearance artifact; before that branch merges, `git diff origin/main <tip> -- supabase/tests/rls/111_audit_log_rls.sql` must be EMPTY. Sec-b's non-objections stated (no duplicate Lock 12 mods in 115; no cross-tenant child leg in 115; 7g's schema-qualified pattern is right because `search_path = ''` makes an unqualified read unresolvable; §10 = 3 read live; CI-fenced set = RT-05/22/26/27/30, different sets). Sec-b's own near-miss: nearly called an unqualified `from monthly_report` a 7g evasion before reading `set search_path = ''` two lines above. Record placed at `self345-sec-clearance.md`. **P6 CSS ruling (Architect-b, record at `self358-css-ruling.md`):** build-time extraction — a standalone Vite lib build over `MonthlyReportView.svelte` (`cssCodeSplit:false`) emits one committed `src/lib/generated/report.css`, a prebuild step, inlined by the PDF route with the `?raw` mechanism it already uses; no new dependency; not a one-way door. `css:'injected'` is settable per-file (`dynamicCompileOptions`, precedent in `vite.config.ts`) but not per-route (one server build; a query suffix does not reach child components) and is STRUCK on a measured CSP defect: Kit passes a component's `head` through verbatim (`render.js:302`) and nonces only the style it emits itself (325–327), so under `style-src: ['self']` the injected block is blocked on every report page load while the page still looks right — a permanent, silent violation stream on a Sec-reviewed surface. Runtime manifest read rejected (request-time filesystem read on a §4.1 surface, no precedent; a reversal routes to Sec). Architect-b's scratch build emitted `report.css` once and then broke — EVIDENCE, NOT PROOF; Backend's spike owes a reproducible emit. Carry-forward: QA owes a hash-coverage assertion (every `svelte-[a-z0-9]+` token in the rendered body matches ≥1 selector in the extracted CSS — the only thing between a build drift and a fully-unstyled PDF that passes every value assertion); DevOps owes a regenerate-and-diff fence (`052` shape); a terse `DECISIONS.md` entry recording the CSP finding rides SELF-358's PR. LayerCake: one enumerated render-context prop, sizing only, threaded route → view → chart. Architect-b also compressed the architect MEMORY.md (27.2 → 16.2 KB, all entries kept — it was over its read limit, tail entries invisible). **Drain:** QA final at `15434ce`; QA's memory copied into the shared checkout additively (main's copy had diverged) — main's QA index is itself un-deduped, open; Architect-b and Sec-b complete; Backend's drain report pending, then the restart.

### E56 — Drain complete; pinned state for the settings restart · TEAM-LEAD · 2026-09-06
Backend did not report but the tree says enough: `origin/feature/self-358` = `169f4c8` (P6 server half pushed as WIP: loader extraction, the PDF route with the two global stylesheets inlined and the scoped-CSS gap stated in its header, the INV-2 escaping leg); the A7 rebase onto `12ccd2f` is COMPLETE LOCALLY in the backend worktree (`feature/self-351` HEAD `65e8d35`, ahead 39 / behind 7 of origin, working tree clean) and not pushed — the branch ref survives a restart; the successor verifies two A7 commits on `12ccd2f` and pushes. Every other agent has drained: QA `57f3952` / `15434ce`; Sec-b complete (record placed); Architect-b complete (record placed); no uncommitted work anywhere except agent memory in the shared checkout (batch 2: DevOps ×2 files + index, Sec's temp-handoff correction, QA ×6 files + index) which is on disk and unaffected. Unit state: `feature/self-345` @ `72c3e5c` (AMBER on FLAG-1/2, Architect-c), `feature/self-345-qa` @ `57f3952` (VETO-1 CLEAR; FLAG-3 → QA-c), `feature/self-345-devops` @ `f788788` (GREEN; merges with #636 per M-1); `feature/self-355-db` @ `12ccd2f` (GREEN); `feature/self-355-db-qa` @ `15434ce` (GREEN; M-2 drop of the stale 111 copy → QA-c); `feature/self-349` @ `5899494` (A5 GREEN; #638 behind #636); `feature/self-354` `0c4b02f` → `-355` `b569b3a` → `-357` `9c23844` → `-360` `fe51000` → `-356` `5c1b689` (walk-gated); `main` = `ea680ea`. Successor briefs in the team-lead worktree's `temp/`: `architect-c-brief.md`, `qa-c-brief.md`, `backend-c-brief.md`; Sec-c and Frontend-c are written when their inputs exist. F/CTO told: safe to restart.

### E57 — Restarted; the setting is bound (proved by reading `~/.claude/settings.json`: `blockReadsOutsideWorkingDirectories: false`); every pinned tip intact; Architect-c, QA-c, Backend-c respawned from briefs · TEAM-LEAD · 2026-09-06
Post-restart checks: the Read tool now opens the user settings file it refused before the restart, so permission settings do bind only at session start — recorded in memory. Tips unchanged: `345` 72c3e5c · `345-qa` 57f3952 · `355-db` 12ccd2f · `355-db-qa` 15434ce · `358` 169f4c8 · `351` 38a9419 on origin with the local A7 rebase at 65e8d35 in the backend worktree. Respawned: `architect-c` (FLAG-1 six-arg signature in the D9 amendment; FLAG-2 the stale snapshot-test function comment; then rebase `355-db`; plus the terse DECISIONS text for the P6 CSP finding), `qa-c` (M-2 drop of the stale `111` copy first, then the `110` version-pin leg, then FLAG-3's two guard-(b) legs, rebasing onto Architect-c's tip), `backend-c` (verify and push the A7 rebase; the P6 build-time CSS extraction spike with a reproducible emit and the hash census; the LayerCake prop-thread named for Frontend's review). Sec-c follows at the fresh shas; #636 assembles after it; Frontend-c after Backend-c's sha and the walk gate.

### E58 — FLAG-1/2/3/4 and M-2 landed; the unit ASSEMBLED as one head (`feature/self-345` = `dc4310a`: 49a26f4 + `-qa` ee597b1 + `-devops` f788788 + `main`); `gh pr update-branch` refused on a workflow-touching PR, done by SSH push instead · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Architect-c landed FLAG-1 (the D9 amendment now names the five-argument signature; one occurrence tree-wide) and FLAG-2 (the `comment on function` paragraph replaced verbatim from the clearance record) at `49a26f4`, proving the `111` delta comment/string-only with a SQL normalizer that it inversion-proved non-vacuous against a control edit, clean-applying 001–111 and 001–115, and reading the comment back through `obj_description` on a clone — byte-identical to Sec's source after unescaping; `feature/self-355-db` rebased to `e85720a` (nine commits preserved; diff to 12ccd2f is exactly the two doc files); the P6 CSP DECISIONS entry drafted (6 lines, with an added clause stating no §10 effect) and routed to Sec-c before it lands in the SELF-358 PR. Architect-c's flags: the shared `pfin_tmpl` now stamps e85720a (a clone from another head is REFUSED until rebuilt — told to QA-c and Backend-c); `feature/self-345` alone still carried the pre-clearance plan-19 `111` battery, so assembly without `-qa` would land a battery that cannot pass — M-1's ruling made explicit. QA-c: M-2 drop at `879b03a` (`git rm` of the stale copy); FLAG-3 legs 4e/4f on guard (b) at `94e7a39` (plan 34→36); the FLAG-4 option-A version-pin leg in the `110` battery at `ee597b1` (plan 14→15), rebased onto 49a26f4 and pg_prove-verified on a fresh clone. QA-c caught a real bug on the way: Postgres's ARE regex treats `\b` as a literal backspace (`\y` is the word boundary) — its leg silently matched nothing until diagnosed; a sweep for `\b` in other `prosrc` legs is booked. Two items routed to Sec-c rather than decided: QA-c did not run a DB-level inversion strike on guard (b) (read the guard order instead) — VETO-1's locator-forgery half, Sec decides whether a strike is required before merge; and leg 8-i (dblink) fails on QA-c's local stack with "password or GSSAPI delegated credentials required" (PG16's dblink auth rule), identical on the unmodified control — CI decides, Sec names the fallback if it reds. **Assembly:** a scratch worktree `.claude/worktrees/assembly` on `origin/feature/self-345`; `-qa` fast-forwarded (49a26f4 → ee597b1, six battery files), `-devops` merged (three files, +490, the C3 fence job), each sibling's files proved carried intact by empty diffs; pushed over SSH to PR #636's head. `gh pr update-branch 636` FAILED — "refusing to allow an OAuth App to create or update workflow `.github/workflows/security-scan.yml` without `workflow` scope" (the same missing scope that blocks HTTPS pushes of workflow files) — so `main` was merged locally and pushed over the deploy key instead: head `dc4310a`, state BLOCKED on CI. Recorded in memory. Sec-c reads 49a26f4 / ee597b1 / 879b03a / e85720a and the CSP entry; #636 merges on CI green + Sec GREEN, with #638 next.

### E59 — PR #636 MERGED at `e3546ab` (the A1+A2+A3 unit, the R7 audit helper, the batteries, the C3 fence); Sec-c's re-verification; a ruling of mine withdrawn on QA-c's strike; #638 updated for its turn · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
CI on the assembled head (`dc4310a`) was CLEAN with zero non-passing checks — leg 8-i's dblink connection authenticates in the CI lane, so QA's local "password or GSSAPI delegated credentials required" was the local hba path only. Sec-c (fresh, read-only from refs): surface (1) `49a26f4` GREEN — FLAG-1 names `(text, text, date, text, bigint)` (my brief had transcribed the five-argument signature wrongly; Sec-c graded against the tree and the record and flagged the brief, correctly); the six-argument residue sweep returns exactly the `drop function if exists`; FLAG-2 byte-exact (1421 chars) against the record's replacement. Surface (2) FLAG-3 GREEN and **no DB-level inversion strike required** — legs 4e/4f are behavioural (`throws_like` against the real function), `requires p_subject_table` occurs once so no other raise can satisfy the pattern, the function is not STRICT so the null argument reaches the body, and both branches of the disjunction are asserted separately; FLAG-4 GREEN — the `110` pin asserts `count(*) = 1` against the installed `prosrc`, comment-stripped, `gi`, `\y` not `\b`, and REDs on NULL. Leg 8-i AMBER on Sec-c's reading (it expected CI to fail identically; CI passed) with the fallback ranking recorded at `self345-sec-8i-dblink-options.md`: re-point the inner connection's hba path; commit the fixture in a CI pre-step; a structural pin acceptable with the loss named; `dblink_connect_u` or a superuser battery VETOED if ever proposed. Booked for P10: the 8-i cleanup runs after the assertion, so a failure leaves tenant E behind and the battery is not re-runnable on a persistent DB. Surface (3) GREEN — M-2's stale battery is gone (`ls-tree`), `e85720a` minus `12ccd2f` is exactly the two doc files, and the C3 fence is in the assembled tip (M-1). Surface (4), the P6 CSP DECISIONS entry: AMBER on one token — it cites "SECURITY §4.1" (tenant-isolation posture) where the server-source surface is ARCH §4.1 (RT-26's anchor; the obligation is SECURITY §4.2), a right-content/wrong-pointer composite; Architect-c fixes it; everything else GREEN, the CSP claims verified against `vite.config.ts` and the `?raw` claim verified on `feature/self-358` after Sec-c caught itself measuring against `main` where the route does not exist. **Verdict: CLEAR TO MERGE**, conditions met by the lane and the assembly. Merged by `gh pr merge --merge --delete-branch`; `main` = `e3546ab`; sibling remote branches `-qa`/`-devops`/`-sec` deleted; the assembly worktree removed. **A ruling of mine withdrawn:** I told QA-c the four savepoint-wrapped FLAG-7 legs in the `115` battery "cannot fail" because `ROLLBACK TO SAVEPOINT` discards pgTAP's results table, and ordered a restructure. QA-c ran the strike first — guard 6a to `if false` on a scratch clone, the committed battery through pg_prove: `Failed tests: 50-51`, named — and three isolated probes. The TAP line is the statement's own return value, printed before the rollback; only `finish()`'s bookkeeping rolls back, which is the whole of "planned 53 ran 49". I inferred a cause from a known structure and prescribed against it without verifying the effect — the class Sec named twice this wave, now mine. Withdrawn; `115` keeps its legs and gains one header sentence (mechanism, expected drift = 4, and the real residual: the plan count no longer guards a leg accidentally never executed inside such a savepoint); my memory note rewritten to record the error. **Next:** `355-db` rebases onto `main` (Architect-c), then `355-db-qa` and A7 onto it; #638 (A5) updated with `main` and waiting on its checks, merge on green (F-7's order now satisfied); Linear Done flips for the merged issues via a fresh liaison. **Ledger:** eleven PR merges past `3de6be7`, recorded lag, cleared at close-out.

### E60 — `355-db` rebased onto merged `main` (`14af648`); the CSP entry's pointer fixed; the merged V1.5 issues were already Done in Linear (branch-name auto-close); Architect-c and the liaison shut down · TEAM-LEAD · 2026-09-06
Architect-c re-measured Sec-c's pointer finding against `origin/main` before editing (ARCH `sec-4-1` = "V1 server-source surface — RT-26 allowlist anchor"; SECURITY `sec-4-1` = isolation axes, `sec-4-2` = credential posture) — it held; one token changed (`a SECURITY §4.1 server surface` → `an ARCH §4.1 server-source surface`), md5 `f2724274…`; the entry is copied into the team-lead worktree's `temp/` so Backend can read it and land it verbatim in the SELF-358 PR. `feature/self-355-db` rebased onto `main` @ e3546ab → `14af648`, verified four ways: `origin/main..HEAD` is exactly the nine migration commits; `git diff e85720a HEAD -- supabase/migrations` empty; a fresh template build stamped content sha `cb1284d4…`, byte-identical to the pre-rebase build (a route that does not go through git); and `111`'s FLAG-2 paragraph still served from `pg_proc` on a clone. `pfin_tmpl` now stamps 115 migrations — anyone still on `main` (111) gets the staleness refusal. QA-c's `fdb8fd4` (the 8-i EXPECTED-DIFFERENT-LOCALLY note) landed on `feature/self-345-qa` after the merge — my amendment crossed its push; harmless, post-merge, rides the P10 pass; the remote branch it recreated is left for that. QA-c's counter-evidence confirmed a third time (the earlier confirmations crossed its check-ins). Linear: the liaison found SELF-345/346/347/348/350/352/353/361 already Done — the `self-NNN` branch names auto-closed them at merge, as the memory predicts; SELF-377 left open. Architect-c and liaison-b shut down (deliverables consumed). Next: QA-c rebases `355-db-qa` onto 14af648 with the `115` header sentence → the `112`–`115` PR assembles → Sec-c re-confirms by diff → CI → merge; Backend-c rebases A7 onto 14af648 and lands the CSP entry with the P6 spike; #638 merges on green.

### E61 — PR #638 (A5, the PDF render client) MERGED at `1afe807`; PR #641 opened for `112`–`115` under an id-free branch name; the M-2 hazard resolved structurally at rebase · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
#638 CLEAN after its `main` update (`gh pr update-branch` works there — no workflow files); merged at `1afe807` on A5's GREEN (sec-a5's conditional verdict plus my own confirmation of the two strings at 5899494) with F-7's merge order now satisfied — RT-21's catalog row and the shipped control state the same direction on `main`. QA-c's last rebase of `355-db-qa` onto 14af648 hit the expected modify/delete conflict on `111`'s battery: the pre-merge drop commit met a base that already carries `main`'s cleared plan-36 file; QA-c kept the base's content (confirmed byte-identical to `origin/main`), so the drop commit became a no-op and the M-2 gate is met by structure — `git diff origin/main HEAD -- …/111_audit_log_rls.sql` empty, `origin/feature/self-355-db..HEAD` only battery commits; head `3f65ce0`; `pfin_tmpl` content sha matched Architect-c's build. **Ruling on the PR's branch name:** the head was pushed as `feature/monthly-report-rpcs-112-115`, not `feature/self-355-db`, because a merged PR whose branch or body carries `self-355` would auto-close SELF-355 (the P3 UI issue) before P3's own PR — the memory's auto-close hazard applied deliberately; the body names no issue ids except the two follow-ups already Done-proof. PR #641 opened; updated with `main` after #638; Sec-c asked for a diff-only re-confirm (migrations byte-identical across 12ccd2f → e85720a → 14af648 → 3f65ce0; batteries' delta from the graded shas = the `115` header note; the 111 battery equal to `main`); merges on CI green plus that line. QA-c's deliverables are consumed; it holds for the walks and the P10 pass. Next on Backend-c: the A7 rebase onto 14af648 and the P6 spike with the CSP entry; then Frontend-c (P6 button, LayerCake review) and DevOps-c (the `report.css` regenerate-and-diff fence).

### E62 — The P6 CSS spike landed (`fbf2fd8`); A7's three-commit count ruled correct; Sec-c's stale surface-(3) verdict superseded by the tree; the disarmed plan/ran watcher named as a FLAG for P10; Frontend-c and DevOps-c spawned · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Backend-c stopped the A7 push on the brief's own guard — `12ccd2f..HEAD` on `feature/self-351` holds THREE commits (`e1a5e1b` cron worker, `47dc5f8` GUC fix, `65e8d35` the one-connection leg added under E49), not the "two" the brief said. Ruled: three is the expected state; the brief was written before the leg; run the ETL battery and push. **P6 spike at `fbf2fd8`:** Architect's build-time extraction reproduces — three byte-identical builds of `api/src/lib/generated/report.css` from `api/vite.report-css.config.mjs`, wired as `build:report-css` and a prebuild step; the census over a fixture render found every `svelte-[a-z0-9]+` token in the body matched by ≥1 selector (0 missing; 4 of 14 CSS-side tokens belong to conditional-branch components QA's real fixture must also cover); `renderContext` threaded route → `MonthlyReportView` → `HistoricalExpendituresChart` with first-pass print constants (1024×320, derived from the file's own rem constants — Frontend/Visual to revisit). Backend-c also fixed, without asking, a pre-existing build-breaking defect it surfaced: `composeReportDocument` was a named export directly off the `+server.ts` route, which the route-module export allowlist forbids and which made `npm run build` fail at SvelteKit's analyse step — moved to `$lib/server/pdf/`; judgment call ACCEPTED (it blocked the verification the brief demanded). Two things it surfaced and did not own: under the repo's vitest `node` project a `.css` `?raw`/`?inline` import resolves to an EMPTY STRING from a server-environment module (reproduces on `main`; the real `vite build` embeds the CSS — grepped in the built server chunk) — no vitest test can assert real CSS content today; and `api/node_modules` was missing `layercake`/`d3-scale`/`d3-shape` despite the lockfile pin (F/CTO approved the `npm install`; zero lockfile changes). **Sec-c's surface-(3) BLOCKING finding** — merging `2fe5092` conflicts modify/delete on the clearance battery and the drop commit's own subject invites the wrong resolution — is correct about 2fe5092 and already superseded: the PR head `3f65ce0` is the rebase in which that commit became a no-op, measured empty-diff against `main` before the PR opened; Sec-c is re-running its gate at 3f65ce0 in the same turn as its line. Sec-c named its own two errors (a two-way diff against a moving `main`, unbounded by the sha it measured at; the wrong prediction that CI would red 8-i — it merged byte-identical and green, `dblink_connect_u` zero times on `main`). Sec-c's sharpened residual adopted as a FLAG for P10, not a blocker: with plan(53) and 49 ran, `finish()`'s planned-vs-ran diagnostic is a knowingly DISARMED watcher for that file — a fifth silent drift would read as expected — and a header comment is documentation, not a watcher; P10 carries the wording and option (C), an explicit ran-count assertion, with the 8-i cleanup footgun and the `\b`/`\y` sweep. QA-c's in-place conflict resolution (no rm+restore pair in history) stands; the supersession is narrated here. Spawned: `frontend-c` (the "Download PDF" affordance on the final report page, disabled on drafts with the tooltip; review of Backend's additive `renderContext` thread and print constants in its two files) and `devops-c` (the `report.css` regenerate-and-diff CI fence with a corrupt-one-byte inversion, plus a ≤10-line triage of the vitest `?raw` defect against the `vite ^8` pins, no pin change). Walk gate still closed on the dev-DB decision: P7, P2, P3, P5, P8, P4 and now P6 wait on it.

### E63 — PR #641 MERGED at `85ddcca`: migrations `112`–`115` and their batteries on `main`; Sec-c GREEN at the true head `d5aeacc` with two of my check predicates corrected · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Sec-c graded the TRUE head (`d5aeacc` — `3f65ce0` plus the `main` merge after #638) against `main` @ `1afe807`, naming both shas. (a) migrations byte-identical across e85720a → 14af648 → d5aeacc, confirmed by git — and stated plainly: the `112`–`115` bodies were NOT re-reviewed, the predecessor's grade carries because the bytes do. (b) M-2 structurally resolved: the `111` battery equals `main`, the merge simulation clean. (c) my predicate "one file, comment-only" did not hold as stated (7 files, +795 — because 2fe5092 predates the `main` merge that brought #636's batteries onto the branch); correctly scoped, the `115` battery delta is +27 lines with ZERO executable lines changed, stated as a measured count, not "comment-only" (a removed `--` line reads as `---` and a naive filter swallows it). (d) my predicate "migrations + batteries only" did not hold and it mattered: the PR also carried `DECISIONS.md` (+6) — Architect's FLAG-7 close naming `fn_nav_composition`, a Sec-attributed block inside ADR-068 — which a `supabase/`-only diff would have merged unread; Sec-c graded it GREEN with both tree claims verified exactly (`110` has one non-comment `account_holdings` line at L1161; `fn_nav_composition` is created in exactly `051`/`102`/`105`, nothing after), and flagged the predicate rather than the outcome because the outcome was luck. The `115` header note meets Sec-c's wording requirement — "cosmetic" scoped to pass/fail detection, the disarmed plan-count residual stated in full with the inversion proof. Optional note, not required: the ADR block's "not something the database self-checks" predates FLAG-4's `110` version-pin leg; it errs in the safe direction. Verdict GREEN, no conditions, with the caveat to re-run the merge simulation in the same turn as the merge — done (`merge-tree` against `main` at the moment of merge: zero CONFLICT lines). Merged by `gh pr merge --merge --delete-branch`; `main` = `85ddcca`; the stacked source branches `feature/self-355-db` and `-qa` deleted. **Lesson recorded against me:** a re-confirm brief must name the check's SCOPE (which paths, which baseline sha) as a claim to be measured, not as a predicate the reviewer is asked to confirm — two of four were wrong and one hid an ADR edit. Next: A7 rebases onto `main` and opens as its own PR (D1-mandatory Sec review); the frontend chain rebases onto `main` after Frontend-c's P6 button lands; the walks stay gated on the dev-DB decision. Ledger: thirteen PR merges past `3de6be7`, recorded.

### E64 — A7 (SELF-351) assembled and opened as PR #642 at `87bb0a5`; Sec-c's D1 joint-review requested; ADR-069 (the P6 CSP finding) landed on `feature/self-358` · TEAM-LEAD RULING UNDER DELEGATION · 2026-09-06
Backend-c pushed `feature/self-351` at `f1d0f36` — the three A7 commits (`6cbe8bf` cron worker; `41578eb` the GUC fix per `113`; `f1d0f36` the one-connection leg) rebased onto 14af648, an ancestor of `main`; the three `pfin_etl`-NOLOGIN test failures recorded as EXPECTED-DIFFERENT-LOCALLY on my ruling that arming the role is a cluster-wide change on F/CTO's local instance and off-limits, with the CI etl lane as the consumer of record. Assembly by the team-lead in a scratch worktree, announced to Backend-c: DevOps's `feature/self-351-devops` @ `64e8b36` (the Coolify cron unit, `0 6 1 * *`, narrow env scope, the `secrets-manifest.yml` consumer comment) merged in, then `main` @ 85ddcca (the merge brought the `112`–`115` batteries and A5's client into the head — the earlier two-way diff's 2074 "deletions" were `main` content the branch lacked, the instrument's blindness, not a change); head `87bb0a5`, `git diff --stat origin/main` = 8 files, +1309/−1; PR #642 opened (branch name `feature/self-351` auto-closes SELF-351 at merge, correct). Sec-c's D1 joint-review requested at 87bb0a5 with `main` 85ddcca named — clauses (a)–(d), the C1 GUC-writer half deferred to this surface, the one-connection leg as the leg it claims, `RESET ROLE` discipline, no `service_role` composition path, the Discord payload under ADR-050 D4/D6, the compose env scope, the manifest edit. Sec-c's precision notes on #641 recorded: `3f65ce0` was the tip of `feature/self-355-db-qa`, the PR head `d5aeacc` was that plus a `main` merge — different objects, same lineage, gate run at both; its blocking finding was current when measured at 2fe5092 and CONSUMED by the rebase, not overtaken; `main` moved between two of its commands in one turn (#641's own merge), the third baseline shift of the day, so every `main`-relative claim from Sec now carries its measured sha. Backend-c landed ADR-069 (the CSP finding, md5 `f2724274…` verified before landing) on `feature/self-358` at `5c7d313` through a throwaway scratch worktree without touching Frontend-c's checkout; Frontend-c told to rebase before pushing. Backend-c's worktree hazard recorded in its memory: `workers/etl/.venv` in a worktree is a symlink to the main checkout with a stale editable-install pointer — `PYTHONPATH` override, never `pip install -e .` from a worktree.
