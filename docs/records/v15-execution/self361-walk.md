# SELF-361 live walk-through — §2.5.x staleness ramp (P9)

**Branch:** `feature/self-361-walk` (record-only; no source edited)
**Assembled branch under test:** `origin/feature/self-361` at `d81de5d`
**Migrations needed:** through `104` only (tax readers + `fn_aggregation_has_stale_constituent`) — already present on the gateway-bound dev DB. No migration applied; no `db reset`.
**Test user:** `qa-self268-walk@example.com` (`b078df6b-2ab0-4d94-8144-74aec564b486`) — the existing 2-account (designated) QA walkthrough fixture: `QA IRS Ledger` (4291, `tax_jurisdiction=irs`) + `QA FTB Ledger` (4292, `tax_jurisdiction=ftb`), plus an undesignated `QA Checking` (4290). Never F/CTO's account.
**Date:** 2026-09-05 (server `current_date`)

## Setup outcomes

1. **Local stack** — already running (`supabase status`): Studio/Mailpit/MCP up on 54321-54324; DB at migration `104` (confirmed live before starting — this surface needs nothing newer).
2. **App** — worktree `api/.env` was absent; copied from the main checkout. `api/node_modules` was also absent; symlinked from the main checkout rather than running `npm install` (mutating command) since `api/package.json`/`package-lock.json` are byte-identical between `main` and this branch (`git diff main -- api/package.json api/package-lock.json` empty). `npm run dev` on port 5173 (no other instance running this time).
3. **Login** — sanctioned magic-link + session-cookie-transplant method (Supabase local-dev admin API `generate_link` → `curl -sD -` the `Location` header directly for the tokens → `cookieStore.set(...)` on the real app origin, per the SELF-265 update to the method). No password ever created or entered. Verified identity via the nav bar's own `qa-self268-walk@example.com` label before trusting anything downstream.
4. **Browser** — Chrome MCP tools, fresh tab. 6 screenshots saved under `temp/self361-walk/` (gitignored, not part of this commit).
5. **Fixture note (disclosed judgment call):** this tenant had **no `pfin.linked_source` row at all** (all 3 accounts are manual) — the brief's "DB flag on the connection row under the walk tenant" method presumes one exists. I created one (`source_id 542`, provider `plaid`, institution `QA Bank (walk, SELF-361)`), linked `QA Checking` (4290) to it, then flipped `connection_status` to `login_required` for leg 2 — a DB-only fixture change, never a Plaid call. Fully reversed at leg 6 (see below).

## Legs 1–6

| Leg | Result | Evidence |
|---|---|---|
| 1 — baseline, no stale badge | **GREEN** | `/taxes/decomposition` and `/taxes/quarterly` both render with no stale badge and no re-auth banner; CA/Federal tables fully populated. `leg1_decomposition_baseline_no_badge.jpg`, `leg1_quarterly_baseline_no_badge.jpg` |
| 2 — credential-error → page-level badge on both | **GREEN** | Verified `fn_aggregation_has_stale_constituent()` reports `is_stale=true` for this tenant (impersonated read, rolled-back probe txn) BEFORE touching the UI. Reloaded both pages: top-of-page re-auth banner ("1 connected account needs you to re-authenticate...") + a page-level **"May be stale"** badge appear on each — `/taxes/decomposition` (top-right, beside the page title) and `/taxes/quarterly` (beside "Estimated Quarterly Taxes"). One badge per page, no per-row tint, matching the ramp's stated design (neither table has an account-level row to join). `leg2_decomposition_stale_badge.jpg`, `leg2_quarterly_stale_badge.jpg` |
| 3 — separation: badge vs. no-tax-authority-designated register | **GREEN** | Cleared `tax_jurisdiction` on `QA FTB Ledger` (4292) while the credential-error state from leg 2 was still active, reloaded `/taxes/quarterly`: the **"May be stale"** badge (top) and the CA panel's own **"No ledger designated for this jurisdiction."** / **"YTD Paid is unavailable."** envelope copy render together, independently — neither swallows the other, and the Federal panel (still designated) is untouched. Checked `/taxes/decomposition` too: the badge persists there with no jurisdiction-envelope collision (that page has no per-jurisdiction register to collide with — confirms the two registers are genuinely on separate surfaces, not merely non-overlapping by luck). `leg3_quarterly_separation_badge_plus_no_designation.jpg` |
| 4 — loader fail-soft (UNKNOWN badge, not a 500) | **NOT WALKED** | Attempted the cheapest sanctioned live-DB lever: `revoke execute on function pfin.fn_aggregation_has_stale_constituent() from public` (the function's only non-owner grantee, so this is what would force the RPC to throw for every request). **Blocked by the Claude Code auto-mode classifier** before execution — flagged as a sensitive security-grant mutation on a live, shared dev database rather than an isolated/rolled-back read. No other DB-level lever exists to force a throw without touching source (Frontend's own vitest suite covers this path — `staleness.error-degrade.test.ts` — per the brief's own escape hatch: "confirm by the cheapest sanctioned means, or state it as not walked if none exists — do not fake it"). Recording as not walked rather than faking a screenshot. |
| 5 — `/` NAV composition regression (unaffected) | **NOT WALKED — pre-existing environment gap, unrelated to SELF-361** | `/` 500s in this worktree with `Cannot find module 'layercake' imported from .../NavHistoryChart.svelte` (also `d3-scale`/`d3-shape` via `HistoricalExpendituresChart`/`Bars`). Confirmed these packages are **also absent from the MAIN checkout's own `api/node_modules`** (`ls .../mosko-fintech/api/node_modules/layercake` → not found) — this is not something the SELF-361 branch broke and not something my worktree's symlink-instead-of-install caused; it predates this walk and blocks `/` for ANY worktree sharing that node_modules tree until reinstalled. Flagged as a bubble-up rather than worked around (installing packages is a mutating `npm install`, out of my tool boundary without confirmation, and wouldn't be scoped to this walk anyway). |
| 6 — restore + confirm | **GREEN** | Restored `QA FTB Ledger`'s `tax_jurisdiction` to `ftb`; cleared `QA Checking`'s `linked_source_id`; deleted the fixture `linked_source` row (542) entirely (0 rows remaining for this tenant, matching the pre-walk state exactly). Reloaded `/taxes/quarterly`: byte-for-byte back to the leg-1 baseline appearance — no badge, no banner, both panels fully populated at their original figures. `leg6_quarterly_restored.jpg` |

## Defects

**None.** Every walked leg (1, 2, 3, 6) behaved exactly per the AC: the page-level badge appears/disappears correctly with the tenant's actual connection state, and the aal2-unrelated separation from the ADR-067 D5 register holds cleanly with both registers rendering their own independent copy.

## Not walked, and why (not a defect — recorded per the brief's own escape hatch)

- **Leg 4** (loader fail-soft): the only sanctioned DB-level way to force the RPC to throw live (`revoke execute ... from public`) was blocked by the harness's own safety classifier as a live security-grant mutation. Frontend's `staleness.error-degrade.test.ts` is the existing coverage for this path; nothing here contradicts it — I simply could not add a live-app confirmation without a mechanism the harness refused.
- **Leg 5** (`/` regression): blocked by a pre-existing, shared `node_modules` gap (`layercake`/`d3-scale`/`d3-shape` genuinely absent from the main checkout's own install, confirmed independently of anything this branch or my worktree setup did). Worth a DevOps/Backend `npm install` pass on the main checkout before the next walk needs `/`.

## Notes for the record (not defects)

- The fixture tenant `qa-self268-walk@example.com` had zero `linked_source` rows before this walk — every account is manual. I created a throwaway one for leg 2 and fully removed it afterward (see Setup note 5); a future walk needing this tenant's credential-error state will need to redo the same fixture step, since nothing here persists it.
- Worktree `api/node_modules` was a symlink to the main checkout's install for this session only (never committed — `node_modules` is gitignored) rather than a fresh `npm install`, since the two package manifests are identical on this branch. Removed after the walk.
