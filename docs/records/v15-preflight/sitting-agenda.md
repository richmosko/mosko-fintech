# V1.5 pre-flight recalibration — sitting agenda

**Status: NOTHING HERE IS RULED.** This file is the team-lead's consolidation of three findings files (two rounds each) into the order F/CTO rules them in. Rulings land in `sitting-log.md` (not yet written) as they are made, with provenance per [ADR-063](../../../DECISIONS.md#adr-063) Decision 3 — F/CTO ruling vs default-and-notify, never flattened. V1.5 is NOT under the V1.4 execution delegation; every ruling below is F/CTO's unless it is listed in §3's default-and-notify block.

**Baseline: `origin/main` @ `b90b846`** (2026-09-04, post V1.4→V1.5 rotation; migrations `001`–`105`). `origin/main` did not move during the pass (re-read by every author at every commit).

**Inputs, by ref** (each verified on `origin` at consolidation — sha and md5 of the committed blob):

| File | Owner | Branch tip | md5 | Scope |
|---|---|---|---|---|
| [`architect-findings.md`](architect-findings.md) | Architect | `7b12e33` | `1048f3f2…` | 5 seams (S-1…S-5), 14 findings, dispatch order, ruling-owed index, §8 round-2 cross-reference with positions |
| [`rederived-acs.md`](rederived-acs.md) | Architect (PM wording folded, credited) | `7b12e33` | `28dd87e8…` | landing-ready replacement AC text per issue, blocks self-carrying the baseline; `⟨RULING⟩` placeholders; A10 sketch proposed-not-promoted |
| [`pm-findings.md`](pm-findings.md) | PM | `77425b3` | `eb7bcb2c…` | 11 drift catches (D-1…D-11), PRD §2.6 slice (A-1…A-14), per-issue classification, product wording (§8), Gate A–F homes (§9), §12 round-2 reconciliation |
| [`sec-findings.md`](sec-findings.md) | Sec | `fe757d1` | `888f637f…` | joint-review map (15 MANDATORY / 2 light-loop / 1 conditional), F-1…F-9, M-1…M-6, D-1…D-5, R-1…R-6, catch criteria, round-2 positions + per-block pre-verdicts, N-1…N-6 |

Issue text: `temp/v15-preflight/issue-dump.md` (md5 `b6f9e76c…`, 18 V1.5 issues + context 365/363/364/326/208/233). Gitignored; the findings quote what they rely on. ⚠ Live `SELF-NNN` ids appear inside the records files only; none appear in this PR's title or body (a merged reference auto-closes the issue).

---

## 1. What the pass measured

- **Buildable as drafted: 2, 3 and 4 of 18 — over three different predicates, presented as three definitions, not one number.** PM 2 (A8, P7: identifiers resolve, no PRD/Lock contradiction, nothing already on the tree). Sec 3 (A9, P4, P9: would pass joint review without an AC-grade correction). Architect 4 (A8, A9, P7, P9: no unresolved seam before an engineer starts; Architect records his own A8 as inconsistent with his predicate by one). Intersection empty; union five. Nobody disagrees about the tree. Same result in kind as V1.3 (0 of 14) and V1.4 (0 of 9): drafted at Wave 6 against the pre-GL, pre-V1.4 substrate and promoted verbatim.
- **§2.6 predates V1.4 more than it predates the GL.** PM §1: the GL-era falsifiers are zero; what falsifies §2.6 is the tax flip, the ADR-067 envelopes, R1's UNAVAILABLE capital gains, and Locks 11/12/13 against φ-1.
- **One one-way door (Architect S-1 = PM A-5, Sec R2.1).** PRD §2.6.4 φ-1 promises a frozen rendered artifact; Lock 11 locked read-time composition. Four of six sections cannot recompose as-of because Lock 14 settings keep no edit history. Blocks A1/A2/A3/P2/P5/P10. All three lean the same way after round 2.
- **The PDF direction is inverted in three drafts** (Architect S-2, PM D-7, Sec F-5): ARCH §3.2 has the worker PULL rendered HTML; A4/A5/P6 draft an app→worker JSON push. The inversion creates the §7.32 item 6 escaping hazard and puts the worker credential's trust boundary in the browser.
- **One issue is already built** (A6 / SELF-350: the RT-22 fence shipped at `eada4b2` with an inversion fixture) and two of its AC clauses would weaken the live fence — **Sec veto D-1**. The same text sits in `BACKLOG.md` §7.1, so the defect is in the promotion source. The live RT-22 residual is the dependency manifest the fence is documented not to see (Sec F-6).
- **Sec map: 15 of 18 joint-review MANDATORY** (A1–A9, P2, P3, P5, P6, P7, P10 by trigger), P8 and P9 light-loop-eligible, P4 conditional. **Sec pre-verdicts on the round-1 re-derived blocks (`66288e0`): 12 GREEN / 5 AMBER / 1 RED** — a fourth count over a different object (re-derived text, not drafted text); not comparable to the three above. The RED (A7 item 5 — the D1(d) audit-log home does not exist) became R7; three AMBERs were already fixed at 2b (ref-skew), the other two are applied at 2c.
- **Already on the tree that a draft treats as unbuilt:** A6 (whole); A4's placeholder Dockerfile (deliberately a fence target — "extend", not "scaffold"); A7's tenant-binding module (`connection.py` = Sec's option α); the P8/P9 staleness framework; P9's NAV-composition leg.
- **After round 2b, exactly ONE cross-role disagreement is live: A8's milestone (R8).** R4 (supersession), R5 (the array column), R6 (A6's disposition), R9 (A10) and A8's review class all converged in writing, each side naming what moved it; they stay on the agenda because each edits a Lock, a D2/D3 reading, or a Linear issue's scope, and a converged lean is still not a ruling.
- **The milestone's substrate is not in the milestone** (liaison; PM D-1; Architect §5.3): the nine A-items carry no Linear milestone while P2–P10 carry V1.5 — the V1.4 R7 shape again.

## 2. Already ruled — recorded, not re-decided

- **Wave 6 Gates A–F** (F/CTO 2026-06-03, CHANGELOG v1.46). Gates C, D, E are realized or discharged. **Gates A, B, F have no ADR home** (PM §9): A amends Lock 11's helper shape, B changes Lock 11's locked column enumeration, F is a mechanism rider on Lock 13. Owed one consolidated ADR (see R14). A ruling is not owed here; the home is.
- **Lock 11 / 12 / 13 / 14 / 15** (ADR-011 D15–D19) govern. Where a draft contradicts them (A1's UNIQUE, A2's width, A5/P6's direction, P8's frozen markers, PRD "overwrite") the re-derived ACs follow the Lock; the PRD text amendments are R10.
- **ADR-011 Decision 3 labels #3 and #4 are already allocated** to `monthly_report.included_reconciliation_event_ids` and `monthly_report_account_snapshot.account_id` (Architect F-6, PM D-4, Sec F-1). The drafted "6th"/"7th" ordinals are struck by citation; no number is drafted anywhere in this pass. A1's `users_id` is the tenant anchor, not an instance (`007`/`015`).
- **ADR-067 D3** (Sec veto on backfilling `nav_daily`) transfers to S-1 on ground (ii) only — past tax/settings state is unrecoverable, so recomposition fabricates. Sec scoped the transfer explicitly (R2.1). ⚠ **N-1**: the sentence *"a fabrication with the shape of a measurement"* is not in `DECISIONS.md` (it lives in `105`'s comment and the V1.4 records); cite ADR-067 D3 for the veto and the V1.4 record for the rationale.
- **ADR-054** (A9) is Accepted and on the tree; A9's only defect is tense. Sec: "the best-drafted issue in the wave", no change required.
- **`secrets-manifest.yml` is clean for this wave** (Sec §5). No new secret is required by any of the 18.

## 3. Rulings owed, in proposed sitting order

Order: the one-way door first (everything in Track 1 re-derives from it), then the direction (Track 2), then the mechanisms that depend on those two, then scope/placement, then text. One per turn.

| # | Ruling | Options and leans | Blocks |
|---|---|---|---|
| **R1** | **S-1 / A-5 — the monthly report is a FROZEN RENDERED ARTIFACT (PRD φ-1) or a READ-TIME RECOMPOSITION (Lock 11).** ⚠ ONE-WAY DOOR. Lock 14 settings (targets, brackets, owner string) have no history; ADR-067 D3's ground (ii) transfers. | **(A) freeze the rendered payload** at finalization (`rendered_payload JSONB` + `payload_schema_version` on the header; Lock 12 child stays the per-account index; every read of a `final` report reads the stored artifact) — **Architect lean, PM conceded from (B), Sec backs A**. (B) narrow φ-1 to live-recompute-as-of — **Sec VETO** (renders unrecoverable past state as present fact; F/CTO sign-off to override). (C) hybrid: freeze the four unrecoverable sections, recompose Cash Flow + gross Holdings — Architect/Sec name it defensible if payload-compat outweighs render-path unity; Sec would then require the frozen/recomposed split as a schema column, not prose. **Riders under (A):** every `{status, reason}` envelope, `basis_year`, exclusion-line state and unclassified count are frozen IN (PM 12.3); staleness stays OUT (live read, §2.6.4 carve-out); the A2 widening is drafted as a Lock 12 amendment (Architect F-7), not an implementation detail; JSONB here does not touch Lock 14's forward-compat fence (`101` precedent). Losing side of (A): the payload shape becomes a permanent versioned compatibility surface, and a frozen report freezes its defects (remedy = regenerate/supersede, so the §8 copy must make that reachable). | A1, A2, A3, P2, P5, P10 |
| **R2** | **S-2 / D-7 — the PDF render direction.** ARCH §3.2 + Lock 13 + RT-21: worker mints JWT → GET `/internal/pdf-render` → app returns rendered HTML → Puppeteer. Drafts: browser → app POST JSON → worker composes HTML. | **(A) conform to ARCH §3.2** — no ADR amendment; the worker holds no listener; but the app must serve a page for a user identity to a machine caller ⇒ **S-3 goes live** (R3). **(C) app composes + escapes under the user's live session and pushes finished HTML; worker is a pure function** — **Architect + Sec lean**; PM neutral between A and C. Costs: Lock 13 amendment + RT-21 rewrite (JWT proves "this is our app", not "who is asking"). **Sec's two conditions on C** (R2.2): (1) `setContent` + request interception denying every scheme but `data:` — `<iframe src="file:///proc/self/environ">` otherwise exfiltrates the worker's only secret; two-abort catch criterion; (2) **N-4** a compose manifest for `workers/pdf-render` carrying the RT-27 private-bind sentinel — an intra-instance coverage expansion on the CI-fenced side only, **no catalogued-ledger effect, no new instance drafted** (RT-30 precedent cited in-file). (B) JSON push as drafted — dominated; not supported by anyone. **Consequences:** decides R3 (moot under C), the §7.32 item 6 home (discharged structurally under A/C, mandatory in the worker under B; the stored-`<script>` PROOF leg is owed under every outcome — INV-2 spans both engines), and A4's second Sec trigger (fence-boundary change under B/C). | A4, A5, P6; R3 |
| **R3** | **S-3 / Sec R-3 — tenant binding for a non-JWT caller of the SECURITY INVOKER helper**, plus **N-3**. (a) The cron half is already solved: `workers/etl/src/pfin_back_etl/connection.py` (`SET LOCAL ROLE authenticated` + `request.jwt.claims`, singular GUC nulled) = Sec's α; A7 reuses it, A3 drops `p_users_id` (`101`/`105` precedent). ⚠ Fail-open shape Sec F-4 names: claims WITHOUT the role leaves `rolbypassrls` in force — silent full-tenant read. (b) The app half exists only under R2 = (A). (c) **N-3**: ARCH :208 says the helper is *"user-session only — never invoked from a worker"* while A7's cron invokes it per tenant — PDF-scoped or general? | Ruling = (i) ratify α as the binding for every non-JWT path (Sec lean; β retracted by Sec — ARCH says the endpoint does not use `service_role`; γ session-minting rejected as a larger auth surface); (ii) ratify the ARCH :208 reading — **Architect's reading (2c): the clause constrains the SESSION CONTEXT, not the process identity, and is PDF-scoped**; the cron satisfies it by impersonating (at the DB layer that caller IS a user session); the general reading would void Lock 11 mod #4's ratified V1-SHIP-BLOCK cron tenant-binding discipline, so it cannot be right. Sec: no position, only that the answer must end up unambiguous ON THE TREE. Team-lead lean: Architect's reading; ARCH :208 narrowed in the consolidated ADR's doc PR (R14). **Standing leg regardless (Sec R2.5, Architect P10 item 2):** catalog assertion that no `rolbypassrls` role holds EXECUTE on A3 — closes F-4 at the DB layer whatever R2 decides; standing, not one-time. | A3, A5, A7 |
| **R4** | **Sec R-1 vs Architect §8.3 — the supersession mechanism on an immutable table**, nested: the mutable draft window (PM D-6) and A1's missing whole-row-on-final + `service_role` fence (Sec AMBER A1 item 6). A1 drafted a SECURITY DEFINER `fn_supersede_monthly_report` outside the D9 allowlist. | (A) supersession DERIVED from the partial-UNIQUE ordering — **Sec's round-2 lean, WITHDRAWN at R2.9 as internally incoherent** (the index permits exactly one `final` row, so there is never a set to order; both rescues either amend the locked index or leave it never firing). (B) **one monotone transition** — any column while `draft`; `generation_status` only on `final → superseded`; nothing else ever; `users_id`/`target_month` fenced in every state; no DEFINER — **Architect §8.3 + Sec R2.9 + team-lead: converged**. **Sec's four conditions on B** (additions, not objections): (a) DELETE stays blocked (D2 is two verbs; `authenticated` needs INSERT for the on-demand path, never DELETE — PRD §2.6.4 indefinite retention); (b) the trigger is NOT role-conditional — refusal proven under `authenticated` AND `service_role` (the cron does the `final → superseded` UPDATE; an early-return for `service_role` is the realistic later defect); (c) legal INSERT states constrained, not only transitions (a row written straight in as `final` would take the month's slot without the authoring gate); (d) `superseded` is terminal, plus the ADR-011 D4 runbook line (the trigger is the only layer for an RLS-exempt writer and goes inert under `session_replication_role = replica`). With (a)–(d) folded, Sec's A1 AMBER → GREEN. (C) DEFINER as drafted + D9 amendment — rejected by both. D2's text then says which half applies to which table (immutable half on the child and the frozen payload; INSERT-new-version half on the header's regeneration path). Converged; ruled for the record because it edits how D2 reads on a Lock 11 surface. | A1, P3, P4, P5, P10 |
| **R5** | **S-5 / Sec F-2 — `included_reconciliation_event_ids INTEGER[]`** (Lock 11 mod #9, V1-SHIP-BLOCK, D3 label #3) has no reachable writer at `b90b846`; ADR-035 names a V1.6 consumer. | **(a) ship the column + the array-element fence now, header naming the revival condition; the QA leg labelled construction-only** — Architect lean, **Sec moved to (a)** (fold-ins get forgotten; D3 #15/#16 record it twice). (b) retire/defer by D3 amendment — Sec's round-1 lean, retracted. Converged; ruled for the record because it realizes a V1-SHIP-BLOCK label. | A1 |
| **R6** | **A6 / SELF-350 disposition + Sec F-6.** Fence already shipped; Sec **VETO D-1** on the `SUPABASE_URL` carve-out and the stale "both catalogued instances" figure; the live gap is the dependency manifest (`COPY package*.json` + `RUN npm ci` admits `pg` with the fence green). The two sides crossed in round 2 and re-converged at 2b. | **(ii) re-scope SELF-350 in place** as the RT-22 manifest fence (DevOps + QA, `sec-joint-review`), first AC sentence stating the Dockerfile fence is already built and citing the shipped fence's own header (which documents manifest inspection as a deliberate non-catch, so a future reader does not close the successor as redundant) — **Architect (round 2) + PM + Sec (R2.9: disagreement withdrawn) + team-lead: converged**. (i) close-and-open-successor — nobody's lean now. **Sec's two conditions:** the ordering constraint on A4 becomes a blocking dependency edge, or better the fence audits `package.json` if present and passes if absent (deliberately unlike the Dockerfile fence's exit-2-on-missing); the catch criterion + golden fixture route through Sec before merge. The closing/re-scope comment carries both AC corrections so the `SUPABASE_URL` carve-out is never inherited. Doc fix owed: `BACKLOG.md` §7.1 A6 text. | A4, A6 |
| **R7** | **Sec N-2 — A7's ADR-011 D1 clause (d) audit-log home.** "Lock 13 mod #4 audit log" was dropped at `015`; its successor `linked_source_sync_audit` rejects a report row on its `source` and `provider` CHECKs; the general helper is reserved-unauthored (D9). Sec's A7 catch criterion withdrawn until homed. **RED** on A7 item 5. | (1) extend the successor's two CHECKs (cheap; widens an append-only audit-class table's domain — D2 change, joint-review); (2) author the reserved general audit helper (largest; discharges a standing reservation); (3) a report-scoped audit table (narrow; a fourth audit surface). **Architect leans (2)** — author the reserved general helper, discharging a standing D9 reservation; losing side: the largest option, on the critical path of A7. Sec has no lean. Team-lead lean: (2), because (1) is a D2 change on a shipped audit table reached for convenience and (3) adds a surface nothing else will use. | A7, P11 measurability (PM §6 needs the row) |
| **R8** | **Milestone placement.** Nine A-items carry no Linear milestone; V1.4 R7 ruled project ≠ milestone. | **A1–A7 → V1.5**: all three agree (V1-SHIP-BLOCK, upstream of the P-items, inside P10's gate). **A9 stays out**: all three agree (ADR-054 D6 orthogonality; dispatch early on its own clock). **A8 — split**: PM IN (P7 is in V1.5 and cannot ship without it; `owner_header_at_generation` is written from its row); Architect OUT (closes a Lock 14 family spanning V1.2–V1.5, incidental to §2.6). Team-lead lean: IN — the close-gate battery covers RT-12 either way, and an unmilestoned blocker of a milestoned issue is the V1.4 shape. Label/milestone changes are F/CTO-only; liaison executes. | P10's gate set |
| **R9** | **PM §7 item 2 — five PRD-required V1 surfaces no issue carries:** (i) report listing with target-month selection; (ii) the on-demand generation WRITE path (Lock 11 row, server-derived `data_as_of`, RT-25); (iii) in-app pending notification + queue; (iv) copy-from-prior-month + `$ ReAlloc` side-by-side in the editor; (v) report-level staleness banner. | (1) fold all five — (i)+(iii)→P5, (iv)→P3, (v)→P8, (ii)→P5 — PM lean. (2) **fold four, open A10 for (ii)** — a D2 write path with an RT-25 obligation inside a Frontend-reviewed issue has no Sec gate — **Architect lean; Sec R2.9 backs A10** (ADR-064 D5 cited as precedent for the reasoning, not jurisdiction: "the trigger is the surface, not the layer, and not the author's assessment of risk"; the trigger needs to sit where the router looks — a label); **team-lead lean**. Sec's stated fallback: a fold is acceptable if P5 carries `joint-review:sec` and its AC names the D2 + RT-25 obligations explicitly. Sketch block A10 in `rederived-acs.md`, proposed-not-promoted. (3) two new issues — rejected (splits P5's own subject). Losing side of (2): 19 issues, a new blocking edge into P5. | P5, P3, P8 |
| **R10** | **PRD §2.6 amendment batch** (PM A-1…A-14, class (a)/(c)) via one PRD PR, including Sec R-4's overwrite→supersede replacements (exact sentences at PM 12.3), the `generation_status` bridge (A-8, worded to hold under R4's outcome), founding-user "the F/CTO"→"the user" sweep (A-11), and under R1 (A) φ-1 stays as written (A-5's exception wording withdrawn). Sequenced BEFORE P9 (PM §4: P9's copy is written against the amended §2.5.3 from SELF-364 — same PR or adjacent). | Authorize the PR as drafted, or hold any passage. Not a design ruling; listed because a locked story's text moves. | P2, P4, P5, P9 |
| **R11** | **D-4 / Architect F-3 — the commentary column identifier.** PRD §2.6.2 names Marketable Securities (ADR-058 D7, F/CTO 2026-08-19); Gate B ratified `commentary_equity`. | (a) **rename to `commentary_marketable_securities`** now — Architect (a column on an immutable audit-class table is permanent). (b) keep `commentary_equity`, heading follows the PRD — PM (schema wording governs identifiers). Sec: no stake, either name. Either way the heading is Marketable Securities, and the decision edits Gate B's ratify text and **rides the consolidated ADR (R14), never a migration alone** (PM 12.3). Both `rederived-acs.md` blocks (A1, P3) carry `⟨RULING D-4⟩` rather than a pick (Architect 2c). Team-lead lean: (a). | A1, P3 |
| **R12** | **PM §6 — V1.final's "month of operation"** on a greenfield deploy (PM's six-clause definition: full month held; cron fired and created the pending report with an audit row; authored + finalized; PDF exported; SELF-362 green on that tree; F/CTO attests no sheet reconciliation) and **does an explicitly-skipped commentary month count toward N = 2?** | Ratify the definition (or amend a clause). Skip: **(A) no** — PM lean (§3.4(c) says *commentary authoring*); (B) count if ≥ 1 sub-section authored; (C) any finalized report. Either way a skip must be persisted distinguishably from four empty strings — a V1.5 AC on A1/P4, not V1.final's. Depends on R7 for clause (2). | SELF-365 AC |
| **R13** | **Ordering judgment.** First dispatch **A8 → P7** (unblocked; closes Lock 14 5/5 and the Settings ramp 4/4; PM's prerequisites decided: 120-char bound, §8 copy, forward-only line at the editor); **A9 second and soon** (its clock); **P9 after SELF-364's PRD PR**; **A1+A2+A3 as ONE design unit under one S-1 ruling and one Sec joint-review**; P10 last. Track 2 (A4 extend → A5 → P6) needs only R2. | Accept, or invert. | the order |
| **R14** | **The consolidated ADR** (ADR-067 shape) riding A1's implementing PR: Gate A (helper shape/signature, `p_users_id` dropped), Gate B (column enumeration + R11), Gate F (cron mechanism rider on Lock 13), the R1 ruling + Lock 12 widening, R4's D2 clarification, the `generation_status` bridge, S-5's dormant-fence header text. **Architect volunteered to author.** | Authorize Architect as author on A1's PR, or route elsewhere. Losing side of "on A1's PR": A1 becomes the wave's largest review. | A1 |

**Default-and-notify candidates** (team-lead takes these unless F/CTO objects; recorded in the log with reasoning; reversal window open until the amendment batch merges): the five canonical RT labels added to their blocks (RT-11→P3, RT-12→A8/P7, RT-19+RT-25→A3, RT-20→A2 replacing the RT-21 false composite, RT-25→A5/A7/P5) · aal2 step-up clause on A1/A2/A8 per the `090` standard (Sec F-9) · A5's RT-21 (a)–(g) restored verbatim, A5's inventions kept as labelled additions, (g) routed to Sec at build · A1's UNIQUE = Lock 11's partial index verbatim; three-regeneration leg · `owner_header_at_generation` on A1 (Lock 11 + PRD §2.6.4) · A3 drops `p_users_id` (`101`/`105`) · cax21 → the Coolify V1 target (ADR-021) on A4/A7 · A4 "extend", not "scaffold"; A7 names `connection.py` · P4: complete-or-explicitly-skip + in-app queue, not Discord (PRD locked; no ruling) · P8: markers read live at every render/export, never frozen (§2.6.4 carve-out) · P10: tri-axis only where tax_treatment exists, two-axis elsewhere, PRD condition quoted per leg (Sec M-3) · P6: no PDF of a pending report; filename `mosko-monthly-{YYYY-MM}-{generated_at}.pdf`, never the owner string · P5: pending = awaiting commentary, no job states; tenant-scoped listing/queue read asserted (Sec F-8) · P2: no inline commentary edit (§2.6.2 V2+; ADR-013 P5 citation struck) · owner header: 120-character bound; unset → in-app prompt, PDF carries no owner line (PM A-13) · commentary length: CHECK-enforced bound mirrored in Zod, PM picks the number against the parity artifact (Sec N-5) · **A8 is Sec joint-review MANDATORY** (Sec R-6 + PM; Sec's map governs classification; Architect's block updated) · S-4 per-section staleness attribution in the app layer reusing the V1.3 shape (Architect (a), PM concurs, Sec advisory) · A9 tense fix only · A2's `scope` column dropped (`pfin.scope` is not a type) · the A7 UTC year-boundary second consumer recorded (PM §10, owner still unnamed).

## 4. Doc fixes owed, no ruling needed

- `BACKLOG.md` §7.1 A6 entry: strike the `SUPABASE_URL` carve-out and the ledger figure (Sec D-1 — the defect is in the promotion source); §5.6 and SELF-355's "(Cash / Bonds / Equity / Alternatives)" → the PRD's four names (PM D-3).
- `BACKLOG.md` §7.32 item 6: becomes conditional on R2 (activates under (B) only); the proof leg (stored `<script>` inert in the PDF, naming commentary + owner string + `schedule_label`) is owed under every outcome and homes in P6 (PM 12.3, Sec R-5 retracted-in-part). Reduce to header + citation once homed.
- ARCH §3.2 sentence at :208 narrowed per R3(ii); ARCH §3.2 sequence + RT-21 body amended if R2 = (C) — inside the consolidated ADR's PR, Sec joint-review.
- N-1 citation form: cite ADR-067 D3 for the veto, the V1.4 record for the rationale sentence (Architect 2b applies it in S-1).
- `MILESTONES.md` Active Feature row: names the set A1–A9 + P2–P10 today; after R8 it names A1–A8 (+A9 unmilestoned) — folded into the close-out PR.
- Architect F-14: dated `comment on` tallies in `059` and `054` read as live state and are falsified by D3 read live — recorded; no comment-only migration in V1.5 (rider on the next migration that touches either object).

## 5. Proposed dispatch order (Architect §4 three tracks; PM concurs on A8→P7 first)

```
Step 0  the sitting (R1–R14)
Track 3 (unblocked today)
  1     A8 → P7   owner identification table + editor          ∥ 2
  2     A9        NAV component-checkpoint capture (own clock) ∥
  3     PRD §2.6 amendment PR (R10) + SELF-364 §2.5.3 PR       → then P9
  4     P9        §2.5 staleness ramp (two routes; NAV leg already shipped)
  5     A6        close per R6; successor manifest-fence issue opened
Track 1 (after R1, R4, R5, R7)
  6     A1 + A2 + A3 — one design unit, one Sec joint-review; consolidated ADR (R14) rides A1
  7     P2 ∥ P5 (+A10 if R9 = (2)) — the read surface and the listing/queue/on-demand path
  8     P3 → P4   commentary editor, then author-before-generate
  9     P8        §2.6 staleness markers (S-4)
Track 2 (after R2)
  10    A4 (extend; manifest fence lands FIRST) → A5 → P6
Close
  11    P10       §2.6.6 RLS battery — close-gate, LAST
```

Every issue except P8/P9 is Sec joint-review MANDATORY (P4 conditional); every user-facing one is walk-gated before the Sec spawn (ADR-063 Decision 4).

## 6. Bookings that outlive the sitting (candidates for a §7.34)

- A latency probe on the A3 composition (fn_nav_composition + every §2.1–§2.3 reader + `104` in one call, on two interactive paths) BEFORE A3's shape is fixed; A3's AC states a render budget or names the async shape (PM §10; carries the MILESTONES "latency probe" item).
- The pre-finalize prompt when no tax-authority ledger is designated — a report generated before designation freezes the high NAV with its reason forever (PM §10; P4/P5).
- `lot_match` / sale-writer / wash-sale items (§7.32 1/2/5) reach the report only as the frozen UNAVAILABLE capital-gains state.
- The M-4 UTC-pin year boundary now has a second consumer (`data_as_of` = last day of the prior month on the UTC clock); owner still unnamed.
- Sec M-6 standing condition: the first AC that persists a rendered PDF creates a storage-class surface and is joint-review-mandatory at that PR.
- Off-tree rulings living only in CHANGELOG/Linear — third pass in a row finds them (Gates A/B/F). Sec's V1.4 rule applied: for A/B/F the discharge would have been a Lock amendment, and none exists.
