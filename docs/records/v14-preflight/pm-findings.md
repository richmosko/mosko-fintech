# PM findings — V1.4 pre-flight recalibration (Estimated taxes, §2.5)

**Baseline:** `origin/main` @ `2cd94ae` (read 2026-09-03, PM worktree `meta/v14-preflight-pm`). Every claim about the tree below is against this sha. Issue dump: `temp/v14-preflight/issue-dump.md` (md5 `9d128f6d8d78c9a24620116da5912d69`; SELF-263/264/265/266/267/268/269/302/303, all Backlog; context appendix 211/225/226/245).
**Sibling findings cited, not restated:** Architect `docs/records/v14-preflight/architect-findings.md` @ `9774a7c` (Seams A–H) · Sec `docs/records/v14-preflight/sec-findings.md` @ `39bc549` (M-1..M-11, F-1..F-7). Where a product question is answered there, this file points.
**Verbatim sources read this pass:** PRD §2.5.1–§2.5.5 (all) · §2.1.1 / §2.1.2–§2.1.5 (NAV-definition passages) · §2.3.4 (`is_tax_payment` passage) · §2.4.2 / §2.4.3 (manual-account attributes; correction/removal; sale language) · App B §2.5 (a)–(q) · App C 2.5.1–2.5.5 · ADR-062 · ADR-031 + Amendments 1/2 · ADR-063 · ADR-064 · ADR-006 · ADR-005 · ADR-013 D7 (P5 + A4 wash-sale handoff) · ADR-011 D18 + family-size amendment · BACKLOG §7.28 (items 1–4), §5.5 (full), §7.29, §7.19 (incl. both "known pre-sweep findings"), §7.24 titles, §7.2 P9/A8 · MILESTONES V1.4 rows · CHANGELOG Wave-5 line · v13 sitting-log items 16/19/20 · migrations `003` `011` `036` `041` `049` `084` `087` `091` (targeted) · `docs/records/v13-preflight/pm-findings.md` (shape precedent).

Classification triad per BACKLOG §7.19 AC1 / ADR-063 D1: **(a)** text amendment · **(b)** design conflict → Architect options memo → F/CTO · **(c)** already-superseded → pointer edit. Every drafted AC below is PRD-traceable and self-carries the baseline sha per ADR-063 Consequences.

---

## 0. Brief-drift catches (canonical text read verbatim before answering)

- **D-1. Brief: "the ledger says V1.4 holds 8 issues, Linear holds 9 including 264."** Measured true: `MILESTONES.md:44` enumerates SELF-263/265/266/267/268/269 + 302/303 (eight, no 264); the dump's index table and the liaison's tail finding place SELF-264 in milestone "V1.4 — Estimated taxes full (§2.5)". Ruling at §3 below.
- **D-2. Brief: "§7.28 item 3 (the V1.4 tax-value inventory session F/CTO adopted)."** Verbatim BACKLOG §7.28 item 3 AC: *"The session happens before §2.5.1 implementation ships: every `posting_prototype_default` row's `tax_relevant`/`tax_character` confirmed or corrected against the V1.4 tax model; the Contribution flag resolved per account type; outcomes recorded on the V1.4 issue that consumes them."* Sitting-log item 19(1) verbatim: *"PM's mid-milestone tax inventory session BOOKED as V1.4 fuel."* Both match the brief. ⚠ **Scope drift inside the booking itself, not the brief:** the AC names `posting_prototype_default` rows only. The **asset-side** `taxonomy_default` rows — all seeded `tax_relevant = false / tax_character = NULL` at `041:270–310` — gate the entire capital-gains half of §2.5.1 (*"on each Sub-Cat in the §2.2.1 asset taxonomy that holds securities subject to capital-gain realization"*). As booked, the session cannot make the CG columns non-empty. Fixed in the §4 AC draft.
- **D-3. SELF-267's ratify citation: "F/CTO Gate B Option A locked 2026-06-03 (`tax_jurisdiction` enum column on `pfin.account`)."** Repo-durable record: **CHANGELOG Wave-5 line only** (*"F/CTO Gate B Option A (`tax_jurisdiction` enum column)"*). Zero hits for `tax_jurisdiction` / `tax_authority` in DECISIONS.md, PRD, ARCH, SECURITY, `supabase/migrations/`, `api/src`. Stronger than V1.3's D-3 class (which had no repo record at all) but still not the decision ledger and not DDL. ⚠ **Sec's F-1 asks the question Gate B already answered** — F-1's option (C) *is* Gate B Option A — and Sec's lean is (A). F/CTO re-confirms or re-rules at the sitting (§6 item 2); either outcome lands in an ADR this time.
- **D-4. SELF-268's ratify citation: "F/CTO Gate C ratify 2026-06-03 (planned placeholder-flip confirmed)."** CHANGELOG Wave-5 line: *"Gate C NAV composition placeholder-flip confirmed (SELF-268 closes §2.5↔§2.1.1/§2.1.5 cross-section contract)"* — matches. What Gate C did **not** rule is *where* the flip lands relative to `nav_daily` — that is Architect Seam E, a one-way door, open.
- **D-5. Mis-citations inside issue bodies (identifier-level, not paraphrase):** SELF-266 AC7 cites **SELF-261** as the "Settings editor PM Issue 4" — SELF-261 is the *transaction-annotation table for the wash-sale flag*; the Settings editor is **SELF-265**. SELF-268 AC1 calls **SELF-211** the "NAV backend" and AC4 calls **SELF-226** the "NAV trajectory" — per the context appendix 211 = §2.1.1 headline **UI**, 225 = §2.1.5 **backend** (`051`), 226 = §2.1.5 table **UI**; the trajectory is §2.1.2–§2.1.4 (`062`/`071`/`073` readers over `nav_daily`), not 226.
- **D-6. Verified NOT drifted (ratify can rely on them):** SELF-263's two §2.5.1 quotes ("V1 carries two attributes…" / "Both attributes are seeded at V1 bootstrap…") · SELF-264's two ("three-column Ordinary Income / ST CG / LT CG decomposition at Sub-Cat granularity for current calendar year" / "recomputes live as transactions land…") · SELF-265's ADR-013 P5 quote and D18 "schedule+rows replace-all SERIALIZABLE" · SELF-266's §2.5.3 layout quote, (ξ-1) quote, δ-2 captions, ν-1 "-$X overpaid through Q2" · SELF-267's §2.5.3 quote (elision honest) · SELF-268's §2.5.4 contract-closure quote and (ρ) quote · SELF-269's §2.5.5 title quote — all byte-match the PRD at `2cd94ae`.
- **D-7. PRD-side identifier drift (Architect Seam G, independently re-read by me — two-reader concurrence):** §2.5.1's worked example writes *"Marketable Securities → US-Index_Non_Sector"*; `041:294` seeds `US-Index-Non_Sector` (hyphen) under Cat `Equity`, renamed to `Marketable Securities` only at `082`. Class (a); §1 A-3.
- **D-8. Retired-vocabulary residue in the PRD (class (c)):** §2.5.1 *"Sub-Cats marked tax_relevant = false (e.g., transfers, AcctSetup events)"* — `AcctSetup` dissolved at ADR-031 Amendment 1 §1 into the `acct_setup` event type. §1 A-4.
- **D-9. Founding-user framing (the V1.3 A-1 class, three instances):** §2.5.1 *"any other ordinary-income Sub-Cats the F/CTO seed marks"* and *"seeded at V1 bootstrap from the F/CTO existing system"*; §2.5.2 (ι) *"applicable to the F/CTO's current filing status (fixed at V1 seed time)"*; SELF-267 AC2 *"F/CTO populates IRS + FTB accounts at creation."* V1 is general multi-user software with open signup (ADR-036/057 provisioning). §1 A-5 / A-6; SELF-267 AC rewrite.
- **D-10. A Phase-2 handoff that was never built and is now the tree's biggest §2.5 gap:** ADR-013 D7 **A4** verbatim — *"the §2.4.3 sell-transaction gains a user-marked wash-sale flag + user-entered disallowed-loss field; the Lock-10 immutability mechanism (mutable-annotation vs reverse-and-replace) is Architect/Sec's call."* That became SELF-261 (unbuilt; `pfin.transaction_annotation` has no DDL). Architect Seam G's widened search finds only `basis_adjust` `metadata.reason = 'wash_sale'` (`030`/`034`), routed to Suspense at `035` P7 *"P&L deferral not yet specified."* Class (b); §5 seam W.
- **D-11. Platform-lane dependency status is not in the dump.** SELF-259 (`tax_bracket_schedule`/`row` + replace-all endpoint), SELF-260 (bracket seed), SELF-261 (wash-sale annotation), SELF-262 (`fn_compute_tax_liability`) are upstream of every V1.4 issue and **none has DDL or a function body on the tree** (Architect Seam A; `git grep fn_compute_tax_liability` hits only forward-reference comments in `013`/`037`/`046`/`049`/`supabase/CLAUDE.md`). Same shape as V1.3's D-4 (245/246/247): the critical path runs through four unbuilt Platform-lane issues whose Linear status I have not read. **Team-lead: liaison read of 259–262 status before the sitting; F/CTO decides whether they promote INTO V1.4 as 245/246/247 did.**

---

## 1. PRD §2.5 slice — passage → falsifier → proposed wording

**A-1 [c] §2.5.1 "on each Sub-Cat in the §2.3.1 cash-flow taxonomy" — the carrier moved.** Post-ADR-058, cash-flow Sub-Cats are posting prototypes (`pfin.posting_prototype` / `_default`, `084`); the attributes live there (Architect Seam D). Product text stands; add pointer: *"(the cash-flow attributes ride the posting vocabulary per ADR-058; the asset attributes ride the storage-classification spine — two id spaces, composed by UNION, per the Architect determination at the V1.4 pre-flight)."*

**A-2 [a] §2.5.1 tax_character enum "with V1 values …" — enum is a registry, not a type.** `011` promoted the inline CHECK to `pfin.tax_character` (ADR-024 Option C); adding a value is a seed migration. Add: *"(the five values are the seeded rows of the `pfin.tax_character` registry; V2+ expansion is a seed migration, not a type change)."*

**A-3 [a] §2.5.1 worked example identifiers (D-7).** Replace *"Marketable Securities → US-Index_Non_Sector"* with *"Marketable Securities → US-Index-Non_Sector"*. (Cat label correct post-`082`; Sub-Cat spelling corrected to the seeded row.)

**A-4 [c] §2.5.1 "(e.g., transfers, AcctSetup events)" (D-8).** → *"(e.g., Transfer-class buckets; structural events — `acct_setup` / `corp_action` / `basis_adjust` — carry no cash-flow classification and enter no §2.5.1 column)."*

**A-5 [a] §2.5.1 founding-user seed framing (D-9).** *"…the F/CTO seed marks"* → *"…the default set marks"*; *"seeded at V1 bootstrap from the F/CTO existing system (parallel to ADR-004 Decision C taxonomy seeding)"* → *"seeded from the system default taxonomy at provisioning (ADR-036/ADR-057; the default set derives from the founding user's existing system, parallel to ADR-004 Decision C), with the V1.4 value inventory (BACKLOG §7.28 item 3) as the audit of those seeded values."* (Mirrors V1.3 A-1 verbatim pattern.)

**A-6 [a] §2.5.2 (ι) founding-user filing status (D-9).** *"applicable to the F/CTO's current filing status (fixed at V1 seed time)"* → *"applicable to the filing status the user's entered schedule set represents; the seeded default set states its filing-status assumption in its label and the user revises it (a seeded schedule is a template, not a determination)."* Product consequence carried into SELF-265 AC7′.

**A-7 [a] §2.5.1 wash-sale sentence — HELD, gates on seam W (§5).** Current: *"User-marked wash-sale flag on the underlying sale transaction (parity-matrix decision #11) excludes the disallowed-loss amount from the ST/LT column on that transaction; auto-detection is V2+."* Wording lands once F/CTO rules W (options drafted there). Deliberately not pre-worded.

**A-8 [a] §2.5.3 IRS/FTB identification — one sentence owed, gates on §6 item 2 (F-1 / Gate B).** PRD names *no* mechanism (Sec F-1, verified). Proposed shape under Gate B Option A: *"The user designates an account as the IRS or the FTB account with a tax-authority attribute set on the §2.4.2 manual-account form (one account per authority per household); §2.5.3 reads the designated ledgers and §2.1.5 excludes them from the composition buildup (see A-9)."* Companion §2.4.2 attribute-list edit: *"…tax-treatment (taxable / tax-deferred / tax-free), an optional tax-authority designation (IRS / FTB — §2.5.3), and an initial value…"* App B (j) closure-trace pointer in the same edit.

**A-9 [b→PRD] §2.1.5 × §2.5.3/§2.5.4 — the tax-authority ledgers double-count payments in NAV. NEW SEAM, PM-found; Architect to verify the arithmetic before the sitting.** PRD §2.5.3 makes the IRS/FTB accounts standard §2.4.2 manual accounts whose balance = YTD Paid; §2.1.5 puts every manual account in the asset half (or Debt if `liability`-typed); §2.5.4 makes Realized Tax Liability = obligation **net of payments**. Composed: a payment P leaves checking (Gross −P), lands in the IRS account (Gross +P), and reduces Realized Liab by P — NAV moves by **+P** for money that is gone. Under `liability` typing the credit reduces Debt instead; same +P. The incumbent's asset taxonomy carries `Liabilities / EstTax-Pending` (*"Estimated Taxes Due but not yet paid"*, `041:304`) — i.e. the incumbent books the **net** obligation and treats paid money as gone, which matches §2.5.4's net-of-payments form only if the authority ledgers are **outside** the composition. Options for F/CTO: **(A) exclude tax-authority-designated accounts from §2.1.5's buildup** (they are payment-tracking ledgers, not assets; the designation from A-8 is the exclusion hook; §2.5.4 stays net-of-payments as locked) — **PM rec**, zero PRD re-litigation of §2.5.4; **(B)** keep them in the buildup and make Realized = **gross** obligation (amends the §2.5.4 lock and ν-1's overpayment reading); **(C)** accept the double-count for V1 — rejected on its face, it overstates NAV by exactly YTD Paid. Under (A) an overpayment is a genuine receivable: Realized goes negative and NAV rises by the excess only — consistent with §2.5.4's *"smaller (or negative)"* sentence and Sec M-3. Wording under (A), §2.1.5: *"Accounts designated as a tax authority's ledger (§2.5.3) are payment-tracking ledgers and are excluded from the composition buildup; their effect on NAV arrives through the Realized Tax Liabilities line."*

**A-10 [b→PRD] §2.1.1 / §2.1.2–§2.1.4 — which surfaces carry the four-component NAV once the flip lands (Architect Seam E, PM refinement).** §2.1.1 defines NAV *with* both tax lines and the headline (SELF-211) renders `fn_compute_nav(today)`; §2.1.2–§2.1.4 read `nav_daily`, which `fn_compute_nav` writes with **no tax leg** (Seam E, verified). Seam E's Option A as Architect framed it ("the flip lands on §2.1.5 only") would leave the **§2.1.1 headline** pre-tax against its own definition. **PM refinement — Option A′:** the flip lands on the two *live* surfaces that share one composed value (§2.1.1 headline + §2.1.5 foot — the same number, as SELF-226's foot-to-headline reconciliation already asserts); the *checkpointed* series (`nav_daily` → §2.1.2 chart / §2.1.3 deltas / §2.1.4 reference dates) stays the pre-tax definition permanently and is labeled. PRD amendment under A′: §2.1.2 gains *"The trajectory plots the checkpointed gross NAV (Gross − Debt, before the §2.5.4 tax lines); the tax-adjusted NAV of §2.1.1 lives on the live surfaces only, so today's chart point and today's headline differ by exactly the two tax lines, and the chart names that basis."* Same sentence pattern at §2.1.3/§2.1.4 by pointer. Tradeoff stated honestly: two numbers called NAV, one basis line of copy — versus Option B's discriminator on an append-only audit-class table across four readers, or Option C's rejected backfill. **PM rec: A′.** Ruling is F/CTO's (one-way door).

**A-11 [a] §2.5.3 CA installment weighting — a known simplification, book it.** PRD (μ-2) locks *"expected annual liability divided by four"* for both jurisdictions and says CA *"differs on Q3."* The FTB's published weighting is 30/40/0/30 (no September installment). μ-2 is a ratified V1 simplification, not drift — no PRD change; add one bullet to BACKLOG §5.5 "Quarterly-installment-sizing refinements": *"CA FTB 30/40/0/30 installment weighting (V1 ships ÷4 for both jurisdictions per μ-2)."* Due dates themselves are data SELF-266 renders; the F/CTO supplies the current-year table at build.

**A-12 [a] §2.5.3 tax-year scope × the Jan-15 Q4 due date (Sec M-4b / F-4).** Product position: **F-4 (B)** — extend the §2.5.3 render window so the prior year's Q4 row stays visible until its due date passes; smallest honest fix; (A) records a known wrong $0 in the two weeks the user most needs the number; (C) is out of proportion. Wording lands with the ruling: *"Between Jan 1 and the Federal Q4 due date, the tables also show the prior tax year's Q4 row (obligation, YTD Paid, Funds Due) until it is paid or the date passes; the current year's table is otherwise unaffected."*

**A-13 [c] App B §2.5 resolved-pointer sweep.** (a) attributes — resolved at `009`/`011`/`041`/`084`/`091` (+ the value audit at §7.28 item 3); (d) holding period — Architect determination: computed from `lot_match` join, not materialized; (f) settings-store shape — D18 FIVE + Seam A ruling; (g) routing location — (g-1) hardcoded, recorded in `011`'s header; (j) IRS/FTB semantics — Gate B (D-3) once re-confirmed; (l) tax-advantaged exclusion — Seam F ruling; (k) aggregate G/L — `049`. Mark each per the App-B closure-trace convention after the sitting.

**Count: 13 passages — 8 (a), 3 (c), 2 (b→PRD) [A-9, A-10]; A-7/A-8/A-12 hold for rulings.**

---

## 2. Per-issue classification (all 9)

**Recurring family first — the §7.19 generator, 5th recurrence.** SELF-267 AC3 carries `p_users_id UUID` (ratified out at `059`/INVOKER + `auth.uid()`); SELF-264/266/268 cite `fn_compute_tax_liability()` and SELF-266 AC4 `applied_marginal_rate` — a function with **no body on the tree** (SELF-262 unbuilt): **SIGNATURE-PROVISIONAL** until Architect drafts it. SELF-263 AC1 names a PG `enum` that never existed (registry at `011`). SELF-269 AC8 names `pfin.transaction_annotation` (no DDL). Product content of every AC survives; identifiers re-derive from DDL per §7.19 AC3.

- **SELF-263 — (c) + RE-SCOPE (major; the ADR-063 D1 widen-the-search case).** The drafted deliverable is on `main` under other names: columns at `009` (asset spine) / `084` (posting pair), registry at `011`, cash-flow values at `041` (7 Revenue rows `true` with characters; STC/BTC `true`/NULL — coherent by design, Sec E-1), Equity pair at `091`. AC1 (PG enum) schema-false → registry pointer. AC2 (`user_taxonomy` cash-flow columns, `DEFAULT FALSE`) — asset side true at `009`; cash-flow side lives on the posting pair (ADR-058) — retarget. AC3/AC5 discharged at `041`/`091` **as columns and seed; the values are unaudited** — which is exactly §7.28 item 3. AC4 (asset-side `tax_relevant = TRUE` for CG-eligible Sub-Cats) is **live product work**: every asset row is `false/NULL` today, so the CG half of §2.5.1 is gated shut by the PRD's own boolean until the inventory marks them — and `taxonomy_default` reaches provisioned users only by backfill (ADR-057; the `077`/`091` shape). AC6 (disjoint from `is_tax_payment`) ✓ keep, add the Transfer-class note (Sec M-6 ⚠). AC7 ✓. **Re-title:** *"§2.5.1.a Tax-attribute value inventory + seed-delta migration (asset + cash-flow defaults) — carries BACKLOG §7.28 item 3."* Replacement AC set at §4. Dispatch **first**; it is the `role:migration` issue and 264/266/268 consume its values.
- **SELF-264 — (a) (major on count, product-clean on shape).** AC1 provisional signature. AC2/AC3/AC4 ✓ match §2.5.1 Layout (ε-1). AC5 enum badge ✓ within *"reads at a glance which buckets contribute to which tax-character class"* — rider: CG rows carry NULL `tax_character` by design; the badge on those rows shows the **holding-period class (ST/LT)**, not the enum. AC6 ✓. AC7 ✓ PRD-verbatim, **+ hard-gate AC** citing SELF-263 (the §7.28 item 3 precondition; ADR-062 D3 shape — a sequencing commitment must live in the consumer's ACs; Seam H, Sec M-6). AC8 empty-state is wrong for general software and for the seed: no user can "mark" (V2+ CRUD), and every provisioned user has seven `true` Revenue rows, so the reachable empty state is *"no tax-relevant activity this tax year yet"* — rewrite; second empty state for the CG section (§6 item 1). AC9 ✓. **+ACs:** unclassified-items loud exclusion (V1.3 S-2 ruling, one-source `in_queue` predicate — an unclassified cash item cannot be tax-relevant, so the banner is *"N items unclassified — any may be income — classify"*, copy MUST NOT claim they are income); unknown-holding-period behaviour per F-2 (Sec M-1; PM lean **(A) ST/ordinary**, fail-closed on tax, with the row footnoted *"holding period unresolved — treated as short-term"*); wash-sale per seam W; per-`lot_match`-row apportionment (M-1 second half) cited from Sec, not restated.
- **SELF-265 — (a) (minor; substrate unbuilt).** AC1/AC3/AC5/AC7 depend on SELF-259/260 (zero DDL — D-11). AC3 D18 quote ✓. AC4 ✓ Lock 14. AC5 monotonicity ✓ (+ Sec trap 1: deferrable-constraint shape — cite). AC6 ✓ P5. **AC7′:** the seeded default set is a *template labeled with its filing-status assumption* (A-6); the editor shows which **tax year** a schedule is for (D18 `tax_year` day-one). **+AC unset semantics (M-11):** a jurisdiction with no schedule for the current tax year renders §2.5.3 UNAVAILABLE-with-reason (*"No Federal schedule entered for 2026 — enter it in Settings"*), never $0; at Jan 1 rollover the prior year's schedule is **not** inherited (BACKLOG §5.5 keeps inheritance V2+) — the CTA is the mechanism. **+AC:** standard-deduction scalar per jurisdiction is nullable-until-set with the same UNAVAILABLE rule (never coalesce to 0 — Sec M-11). Sec joint-review mandatory ✓ (AC8).
- **SELF-266 — (a) (major on count; the money-correctness riders are Sec's).** AC1/AC4 provisional. AC2 ✓ PRD-verbatim row set. AC3 ✓. AC5 ✓. AC6 ✓. AC7 ✓ with the D-5 citation fix (→ SELF-265). AC8: the "zero IRS/FTB accounts" empty state is right in product terms; its CTA lands on the §2.4.2 form's tax-authority field once A-8 lands. **+ACs by citation:** YTD-Paid source per Seam B ruling (PM concurs with Architect lean **A — ledger balance**, PRD-verbatim, dissolves the Jan-15 flag problem; Option B would be a PRD amendment for no product gain); Q4 straddle per A-12/F-4; rounding + Q4 residual (M-8); clock (Seam C); negative-taxable clamp (M-9); no schedule → UNAVAILABLE (M-11). CA weighting: ÷4 stands (A-11). No as-of toggle on any §2.5 surface (Lock 15 — §2.3.3 is the only client-toggle surface) — state it so nobody adds one.
- **SELF-267 — (b) → Architect options memo → F/CTO.** Three rulings converge here: (i) the identification mechanism — Gate B Option A (D-3, CHANGELOG-only) vs Sec F-1 (lean A settings row) — **PM position: re-confirm Gate B Option A** (a nullable `tax_authority` attribute on `pfin.account`, `irs`/`ftb`, one per authority per user via partial unique index; it is what the PRD's *"standard accounts with tax-domain interpretation overlaid"* most literally means, it is set on the §2.4.2 form the user already fills, and it is the natural NAV-exclusion hook for A-9 — a settings-row pointer would need a second write surface and a D3 fence for a fact that belongs on the account); (ii) YTD-Paid source (Seam B); (iii) NAV exclusion (A-9). AC3 signature: `p_users_id` retired → INVOKER; `p_through_quarter` grammar is Seam B's; **PM adds the source predicate** Sec catch 2 asks for: *"YTD Paid = the designated ledger's cash balance as-of today (`056` shape); every `standard` cash row on that ledger counts — a refund recorded as a negative payment reduces it; there is no per-row 'is payment' flag."* AC2 *"F/CTO populates … at creation"* → *"the user sets the designation on the §2.4.2 form at creation or edit"* (D-9). `account_type` for a tax-authority ledger: `manual_other` (003 vocabulary has no tax member; A-9 (A) makes the type irrelevant to NAV). AC5 ✓ (ADR-002 §3.0). AC6 ✓.
- **SELF-268 — (b) one-way door (Seam E / A-10) + (a).** AC1 as written ("SELF-211 NAV backend updated to consume … rebroadcast as part of NAV decomposition") would put the tax leg **into `fn_compute_nav`** → `nav_daily` — the Seam E one-way door, and D-5's mis-citation; rewrite against the A′ ruling. AC4 ("historical NAV recompute back-fills correctly") is **Seam E Option C, rejected on the record** (append-only audit-class; the tax state for a past date is unrecoverable) — **strike**. AC2 ✓, AC3 ✓ (ρ), AC5 ✓ (minus the trajectory under A′), AC6 ✓ (+ Sec catch 4: `051`'s `V1.4 ramp` comment and the `comment on function` update in the same migration), AC7 ✓. **+ACs by citation:** Unrealized floor per F-3 (PM concurs **clamp at zero**: §2.5.4 defines Unrealized as *"the estimated tax that would be owed"* — a tax that would be *refunded* is not that, and the V1 boundary already calls the figure *"an LT-aware floor estimate"*; a negative value would be a NAV inflation the PRD never contemplated); sign-boundary pair (M-3); (π) filter placement per Seam F (PM: no product preference — Architect's B is fine provided the extract-on-second-consumer rule is in the AC); A-9 exclusion; the three-layer flip incl. `NavCompositionTable.svelte`'s `isTaxPlaceholder` branch (Seam E part 3 — the silent one).
- **SELF-269 — (a) (minor).** AC1's surface list includes SELF-261 (unbuilt) and "PM Issue 8 / PM Issue 10" (= 267 / 268 — name them). AC7 depends on Gate B re-confirm; AC8 on seam W (if W lands on the `basis_adjust` route, AC8 retargets to the `034`/`035` surfaces; if on an annotation, it stands). AC4 ✓ (π). AC6 ✓. AC9 ✓. AC10 ✓ SELF-257 precedent. **+** Sec §3's battery list by citation (chain-resolved child pair · DELETE-policy-alone · replace-all-path monotonicity · negative-taxable / negative-G/L legs). Close-gate LAST ✓ (MILESTONES:46).
- **SELF-302 / SELF-303 — see §5 below (keep/move ruling). Both have no AC sections and no labels;** neither is dispatchable as drafted regardless of placement.

**Counts:** 9 issues → **(a) 5** (264, 265, 266, 269 + 263's residual ACs) · **(b) 3** (267 — mechanism/source/exclusion; 268 — Seam E one-way door; 263/264 jointly — seam W wash-sale) · **(c) 1** (263 — the on-`main` deliverable, re-scoped) · **302/303 — placement question, not the triad.** 0 of 9 buildable as drafted (V1.3 precedent held). Every issue still traces to a PRD §2.5 story; no V2 creep found in the drafts; one V1 gap of scope-changing size (§6 item 1).

---

## 3. SELF-264 membership ruling

**SELF-264 is V1.4 scope on PRD grounds; the LEDGER is wrong, not the milestone.** §2.5.1 is the milestone's first primary story and its V1 boundary names *"three-column Ordinary Income / ST CG / LT CG decomposition at Sub-Cat granularity"* — the table **is** the story's only user-facing surface, and §2.5.3/§2.5.4 consume its decomposition. Corroboration from three independent repo records: CHANGELOG Wave-5 line (*"Estimated taxes V1.4 = 7 PM §2.5"* — 263–269 is seven, which includes 264); BACKLOG §7.2 P9 AC (*"3-col tax decomposition table at SELF-264"*); the liaison's live read (milestone = V1.4). `MILESTONES.md:44`'s eight-issue list is a transcription omission at the 2026-09-03 rotation (264's `updatedAt` matches 266/268/269's — the rotation touched it). **Fix:** correct the ledger line — and per the derivable-counts discipline, **drop the count**: *"V1.4 holds the §2.5 issue set (SELF-263–269) + GL follow-ups per §5 ruling; close-gate SELF-269."* A named set cannot go stale the way "8" did.

---

## 4. §7.28 item 3 placement — carrier + drafted AC

**Carrier: SELF-263** (re-scoped per §2). Rationale: it is the milestone's `role:migration` issue; its original deliverable is already on `main`, so its honest remaining work *is* the value audit plus the seed-delta migration the audit produces; and it must precede 264/266/268 — which is the §7.28 AC's own sequencing requirement. **SELF-264 carries the hard-gate dependency AC** (the consumer-side half ADR-062 D3 / Seam H / Sec M-6 require), the SELF-245→SELF-255 pattern.

**SELF-263 replacement AC set (baseline `2cd94ae`; identifiers tree-verified this pass):**
1. **Scope (carries BACKLOG §7.28 item 3, F/CTO-adopted 2026-08-22).** The tax-value inventory session runs before this issue's migration is authored and its outcomes are recorded on this issue. Its row set is **both default tables**: every `pfin.posting_prototype_default` row's `tax_relevant`/`tax_character` (cash-flow side, 27 + 2 rows at `041`/`091`) **and** every `pfin.taxonomy_default` row's (asset side, `041:275–310`, all `false`/NULL today) — the asset side gates §2.5.1's capital-gains half per PRD §2.5.1 *"on each Sub-Cat in the §2.2.1 asset taxonomy that holds securities subject to capital-gain realization."*
2. **Decisions the session must produce, each recorded with its reason:** (i) `Equity / Contribution` resolved per account type (ADR-062 D4's notes rider is the open item; `true` today is flag-for-review, not a determination); (ii) `Revenue / Bond Premium` = `ordinary` and `Revenue / Dividend` = `qualified_dividend` confirmed or corrected (largest §2.5.2 routing consequences — Architect Seam H); (iii) asset-side marking principle stated once (which Sub-Cats produce reportable capital gains on realization; `Volatility-60/40` keeps its §1256 role; Cash / Real Estate / Liabilities rows decided explicitly, not by default); (iv) `long_term_capital_gain_eligible` / `short_term_only` assignments on the asset side, since those two values exist for that side (PRD §2.5.2 forward-compat sentence).
3. **Migration = the seed delta + explicit backfill**, `077`/`091` shape: a value change on either default table reaches already-provisioned users only by backfill (ADR-057, generalized to the posting pair at ADR-062 D5); header states the reach decision once. No new column, no DEFAULT change on `tax_relevant` (Sec F-5 (A)/(C): keep the DEFAULT; land the ADR-062-style `comment on column` scoping what `false` means on each table).
4. **Struck as discharged (recorded so it is not re-litigated):** drafted AC1 (`pfin.tax_character_enum` — the registry at `011` is the shipped shape), AC2 (columns — `009` asset / `084` posting pair), AC3/AC5 (seed — `041`/`091`); the `user_taxonomy`-only carrier premise (ADR-058 split).
5. Disjointness from `is_tax_payment` stands (drafted AC6) with the rider: the seeded tax-payment buckets are **Transfer**-class (`041:341–342`) and outside `is_tax_payment`'s Expense-scoped meaning; §2.5.3's YTD Paid never reads that flag (Sec M-6 ⚠).
6. Read-only via migration in V1; CRUD UI V2+ (drafted AC7, PRD-verbatim) ✓.
7. QA legs: pre-migration-provisioned user carries the delta after it; fresh signup receives the full sets by **row count**; the asset-side `true` set is non-empty after the migration (the leg that proves the CG half is reachable at all).

**SELF-264 +AC (hard gate):** *"Precondition (BACKLOG §7.28 item 3; ADR-062 D3 shape): this surface does not ship until SELF-263's inventory migration is on `main`; `tax_relevant = false` is never read as an answered question (Sec M-5) — the page footer names the seed-delta migration it was built against."*

---

## 5. Seams (PM half) + SELF-302/303 placement

**Seam W — wash-sale mechanism (Architect Seam G, routed to PM + F/CTO).** PRD §2.5.1 / ADR-013 A4 specify a user-marked flag + disallowed-loss amount **on the sale transaction**; the tree has no flag and no sale (§7.19 known finding — no sale writer; `lot_match` write-dormant until M4-GL), only `basis_adjust reason = 'wash_sale'` parked in Suspense. Options for F/CTO:
- **(A) Defer wash-sale handling to the same milestone as the sale writer** — §2.5.1's sentence becomes *"user-marked wash-sale treatment lands with the manual-sale writer (BACKLOG §7.3 G3 dependency); V1.4 ships no wash-sale adjustment"*; SELF-261 stays unbuilt; SELF-302 moves out. Cheapest; honest: there is nothing to mark in V1.4 because there is no sale to mark it on. **PM lean**, conditional on §6 item 1's ruling.
- **(B) Adopt the `basis_adjust` route as V1's mechanism** — the user records a dated `basis_adjust` (`reason = 'wash_sale'`, `cost_basis` = disallowed loss) against the replacement lot; SELF-302 posts its P&L (disallowed loss → replacement-lot basis) and **becomes §2.5.1 fuel**; §2.5.1's sentence is rewritten to the basis-event mechanism; App B (j)-adjacent A4 handoff closed as superseded. Coherent with ADR-031 D4 (economic adjustments are new dated transactions); costs a `basis_adjust` writer (none exists) and a GL design Sec calls *"its own tax-complex mini-design."*
- **(C) Build SELF-261's annotation flag as handed off** — requires a sale row to annotate; does not exist in V1 as the tree stands; would be built against nothing.
**Rec:** (A) now, (B) as the recorded V1.x path; never (C).

**SELF-302 (wash_sale P&L posting) — MOVE, conditional.** It is Platform/GL work that consumes no §2.5 story unless seam W rules (B); under (A) it is a `037`-deferred GL completion item with no V1.4 consumer. Recommend: move to Platform / Cross-cutting V1.x (ADR-017 D2 keeps Platform in Linear at all times, so the move costs nothing), annotate *"returns to the tax milestone if seam W rules (B)"*; it has no AC section and no labels either way.
**SELF-303 (substantive corp_action GL: spin-off / cash-in-lieu) — MOVE.** No §2.5 story mentions spin-offs or cash-in-lieu; the PRD's corp-action surface is §2.4.3 (`corp_action` splits / renames, ADR-033) and its tax treatment is unspecified. It landed in V1.4 by the `037` deferral note, not by product trace. Move to Platform / Cross-cutting V1.x with SELF-302; carry its Sec test-durability nit unchanged.
**If F/CTO keeps either in V1.4:** neither has ACs; both are joint-review-mandatory money-flow migrations (Sec §1 decision rule) — they would need drafting before dispatch and would sit after 263 and before 269 in the order.

**Other seams are Architect's (A–H) — PM positions, one line each, for the sitting:** Seam A storage grain — no product preference; Lock 14 names two tables, Option C keeps that; **Seam B YTD-Paid source — Option A** (PRD-verbatim; the Jan-15 hazard dissolves); Seam C clock — determination, concur; Seam D UNION — determination, concur (A-1 pointer); **Seam E — A′** (A-10); Seam F — Architect's B, no product stake; Seam H — carried at §4.

---

## 6. Scope flags for F/CTO (options with tradeoffs; no unilateral calls)

1. **§2.5.1's capital-gains half has no V1 input path as the tree stands — a scope-size gap, not an AC defect.** ST CG / LT CG columns require a realized sale paired to a lot (`lot_match`). Measured: V1 has **no manual-sale writer** (`088` is BUY-only — `quantity > 0` enforced; §7.19 "known pre-sweep finding" verbatim: *"V1 has no way to record a sale of anything"*), and `lot_match` is write-dormant until M4-GL (ADR-064 D2). Plaid-sourced investment sells are the only conceivable source and the pairing substrate for them is the same dormant table. Options: **(A) ship §2.5.1 with the CG section rendering UNAVAILABLE-with-reason** (*"Capital gains appear once sales are recorded — the sale writer lands at V1.x"*; ADR-049 non-silence; Ordinary Income column fully live) — smallest, honest, keeps V1.4 on its clock; **(B) pull the manual-sale writer + `lot_match` activation (BACKLOG §7.3 G3's named dependency) INTO V1.4** — makes §2.5.1 whole but imports an M4-GL substrate project (FIFO/specific-lot ratified one-way door, Sec joint-review, QA per fact-kind) into a tax milestone; **(C) re-scope §2.5.1's V1 boundary to Ordinary Income only and stage the CG columns at §7** — a PRD amendment un-saying a locked V1 story. **PM rec: (A)**, with (B)'s item named as the milestone that closes the window, and seam W ruled (A) in the same breath (no sale → no wash-sale). Consequence under (A): §2.5.3's Federal LT-CG walk runs over `qualified_dividend` Ordinary contributions only (step 4) — still a live computation, not dead code.
2. **IRS/FTB identification — re-confirm Gate B Option A or take Sec F-1 (A).** PM position at §2 SELF-267; the ruling must ride an ADR this time (D-3) and must carry the A-9 NAV-exclusion rider whichever mechanism wins.
3. **The NAV double-count (A-9).** Options (A)/(B)/(C) stated there; (A) recommended; Architect verifies the arithmetic against `051` before the sitting.
4. **Seam E / A′ (one-way door).** Slow down: the reversible option (A/A′ — writes nothing to `nav_daily`) carries a standing advantage that survives being wrong (the ADR-064 asymmetry argument applies verbatim).
5. **F-2 unknown holding period** — PM lean (A) ST/ordinary; **F-3 Unrealized floor** — clamp; **F-4 Q4 straddle** — (B) extend the window; **F-5 `tax_relevant` DEFAULT** — (A)+(C) keep-and-comment. One-liners for the batch; each has its Sec options memo.
6. **Platform-lane promotion (D-11):** whether SELF-259/260/262 (and 261 per seam W) promote INTO V1.4 as 245/246/247 did — they are on the critical path of every V1.4 issue and their status is unread.
7. **PRD content no issue covers (gaps), minor:** (i) §2.5.2's *"one-line applied-rate caption"* is covered (266 AC4) but the caption's **basis when a schedule is absent** is not (falls under M-11 — UNAVAILABLE); (ii) §2.5.4's **user-facing disclaimer sentence** (*"treat the Unrealized Tax Liability as an LT-aware floor estimate"*, Sec-endorsed at App B (p)) has no rendering AC — add to SELF-268 as a tooltip/footnote on the §2.1.5 row; (iii) §2.5.5's *"single full-household tax view by default … spanning every ownership scope"* — no issue asserts the scope-aggregation posture on the §2.5 read surfaces beyond the battery (269 AC3 covers it) ✓ acceptable; (iv) §2.5.3's *"Tax Balance Prior Year"* informational row — sourcing (settings-stored vs recomputed; App B (c)) has no home in any issue: **PM rec: a nullable scalar on the Federal/CA schedule row (user-entered at rollover, same cadence as brackets), rendered `—` when unset** — Architect confirms placement under Seam A.
8. **Exceeds-PRD check:** SELF-264 AC5 (enum badge) and SELF-266 AC8's CTAs are within the stories; nothing in the nine drafts exceeds §2.5 V1. Nothing to strike for creep.

---

## 7. Routing (architectural / security-shaped; flagged, not ruled)

- **Architect:** A-9 arithmetic verification against `051` (before the sitting); the `tax_authority` column shape + partial unique index + `fn_create_manual_account` 8th parameter (Lock 14 widening) if Gate B re-confirms; Seam E A′ feasibility — whether the headline and the §2.1.5 foot read one composed value (a reader over `fn_compute_nav` + `fn_compute_tax_liability`) without touching `fn_compute_nav`'s body; `fn_compute_tax_liability` signature (SELF-262) so 264/266/268's provisional citations resolve; the Tax-Balance-Prior-Year scalar's home (§6 item 7(iv)); `p_through_quarter` grammar under Seam B (A); the CG-section UNAVAILABLE predicate under §6 item 1 (A) (`lot_match` empty for the year vs no sale rows — two different facts, one banner).
- **Sec:** the `tax_authority` attribute is a new user-writable column read by a money path and by the NAV exclusion (A-9) — joint-review at its migration (Sec F-1 already names the FK-shaped variant's #18 treatment; a plain enum column on `pfin.account` is not FK-shaped, stated so it is checked, not inherited); the §2.4.2 form widening is a Lock 14 mass-assignment surface (`.strict()` extends); SELF-263's seed-delta migration touches values a money path reads — joint-review per ADR-062's own precedent; seam W (B), if ever taken, is a `basis_adjust` writer (money-flow, ADR-064 D5 class).
- **QA:** the SELF-263 row-count and asset-side-non-empty legs (§4 AC7); SELF-268's boundary pair and clamp legs (Sec M-2/M-3) — cited, not restated.
- **F/CTO batch-ratify sitting (proposed order):** D-3 Gate B re-confirm (+ A-9 rider) → §6 item 1 (CG input path) + seam W → Seam E / A′ → Seam B → F-2/F-3/F-4/F-5 one-liners → Seam A/F (Architect) → PRD amendment wording A-1..A-13 → SELF-264 ledger fix + 302/303 placement → §7.28 item 3 carrier (SELF-263 re-scope) → Platform-lane promotion (D-11) → issue-AC re-derivation authorization.
