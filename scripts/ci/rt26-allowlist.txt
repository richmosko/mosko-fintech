# RT-26 allowlist registry — 3 V1 service_role surfaces per ADR-016 Decision 1.
#
# Each non-comment, non-blank line is a permitted file path (relative to repo root).
# References to SUPABASE_SERVICE_ROLE_KEY in any other file within audit scope
# (src/** + repo-root config files) trip the fence at PR-time (fail-closed).
#
# Lock anchors:
#   - ADR-016 Decision 1 (3 V1 surfaces; canonical enumeration).
#   - ADR-016 Decision 2 (webhook-allowlist annotation convention durably ratified).
#   - SECURITY §4.2 axis vi (V1-SHIP-BLOCK + HIGH severity posture obligation).
#   - ARCH §4.1 (RT-26 allowlist anchor narrative).
#   - ADR-011 Decision 4 (second catalogued §10 instance).
#
# ADDITION POLICY (per ADR-016 D2 + SECURITY §4.2 webhook-allowlist annotation convention):
#   Adding a 4th allowlist entry REQUIRES Sec-consult + ADR-016 amendment at
#   the surface-introducing lock. This file is NOT freely editable; updates land
#   alongside the corresponding ADR-016 amendment PR.
#
# OPEN AT PHASE 5 ROUTING PICK:
#   Entries 2 + 3 paths are "or Phase-5-routing-equivalent" per ADR-016 D1
#   verbatim. If Phase 5 lands different routes for credential-class admission
#   (/item/public_token/exchange) or retention-enforcement (/item/remove), update
#   this registry + flag the equivalence-vs-ADR-016 paths to Sec for verification.
#
# OPEN AT PHASE 5 IMPLEMENTATION (factory-file question; per team-lead γ-hybrid ratify 2026-06-08):
#   If Phase 5 detail design lands a Supabase admin client factory at
#   src/lib/server/supabase-admin.ts referencing SUPABASE_SERVICE_ROLE_KEY directly,
#   that introduces a 4th allowlist surface. Per ADR-016 D2 (webhook-allowlist
#   annotation convention durably ratified), this requires ADR-016 amendment +
#   Sec-consult BEFORE this registry adds the entry. This is an ADR-amendment
#   one-way-door — route to Architect at Phase 5 implementation; NOT a W1 inline
#   addition.

src/routes/api/plaid/webhook/+server.ts
src/routes/api/plaid/onboard/+server.ts
src/routes/api/plaid/revoke/+server.ts
