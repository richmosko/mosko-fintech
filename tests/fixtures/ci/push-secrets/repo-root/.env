# Fixture .env for scripts/ci/fence-push-secrets-strikes.sh -- NOT a real
# .env, values are fixture strings only, never anything resembling a real
# credential. Leg (v) greps fake-curl's own logged argv for the literal
# string below to prove it never crosses into curl's argv.
SUPABASE_SERVICE_ROLE_KEY=FIXTURE-DO-NOT-LEAK-app-secret-value
FMP_API_KEY=FIXTURE-DO-NOT-LEAK-etl-secret-value
