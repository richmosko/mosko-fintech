# Same fixture as secrets-manifest.yml (also PLAID_CLIENT_ID/PLAID_SECRET-
# free, same Sec preference), PLUS one name with no SECRET_RESOURCE_MAP
# entry (NOT in the script's exclusion sets either) -- exercises the "a
# new secret was added to the manifest since this script's mapping table
# was last updated" fail-closed guard (exit 2).
production_only:
  - SUPABASE_SERVICE_ROLE_KEY
  - PDF_WORKER_SIGNING_KEY
  - SIMPLEFIN_TOKEN
  - WORKER_ADMISSION_SHARED_SECRET
  - DISCORD_WEBHOOK_URL
  - PFIN_DB_PASSWORD
  - FMP_API_KEY
  - BLS_API_KEY
  - POSTGRES_PASSWORD
  - JWT_SECRET
  - SECRET_KEY_BASE
  - VAULT_ENC_KEY
  - SERVICE_ROLE_KEY
  - ANON_KEY
  - DASHBOARD_PASSWORD
  - PG_META_CRYPTO_KEY
  - SMTP_PASS
  - MIGRATOR_DB_PASSWORD
  - FAKE_UNMAPPED_SECRET_NAME
ci_only: []
